# 项目文档硬规则

本文件适用于本项目内的所有代码、配置、部署和产品功能修改。

## 修改前

1. 必须先阅读 `docs/INDEX.md`。
2. 根据索引阅读本次涉及的模块文档。
3. 阅读 `docs/CHANGELOG.md` 最近记录，确认历史决策和未完成事项。
4. 不得在未核对现有接口能力、指标口径和多平台规则时直接修改实现。

## 修改后

1. 更新涉及的模块文档，写清当前功能、交互、接口、边界和代码入口。
2. 更新 `docs/INDEX.md` 中对应模块的“最后修改”和“当前状态”。
3. 在 `docs/CHANGELOG.md` 顶部追加一条按时间排列的变更记录。
4. 变更记录必须包含：时间、模块、需求/问题、代码修改、验证结果、部署状态。
5. 新增模块时必须新增模块文档，并加入索引。
6. 删除或替换功能时不得直接删除历史记录；在时间线中标记“已废弃/已替换”。

## 文档要求

- 文档描述以当前代码和真实 API 能力为准，不把 Mock 能力写成正式能力。
- 接口必须同时记录内部 ID、真实路径、参数范围和已知限制。
- 多平台能力必须说明是全部汇总、单平台还是最多 8 平台对比。
- 不在文档中记录 Token、密码、Cookie、数据库密码或完整环境变量。
- 未完成、接口 500、字段待确认等状态必须明确标注。

## 完成标准

代码通过测试和构建，但文档未同步，任务仍视为未完成。
