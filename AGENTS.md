# YPBI 长期工程规则

本文件适用于 YPBI 内所有代码、配置、部署、产品实现与文档修改。目标是在保留现有有效成果的前提下渐进演进，避免功能遗漏、双口径、不可恢复写入和无边界重构。

## 1. 权威来源与适用边界

不同问题按各自权威源判断，不能简单用“代码优先”或“文档优先”覆盖全部情况：

- 用户最新明确确认是当前任务的最高决策；确认后必须写回相应权威源，聊天记录不长期充当规范。
- `docs/requirements/BI-产品需求文档.md` 是目标产品范围、页面行为、对象关系与验收规则的权威源；现有代码不能反向缩减已确认需求。
- `docs/requirements/BI-现有平台调整方案.md` 是当前架构事实、目标差异、数据准入、迁移和风险治理的权威源，兼作本项目架构与迁移文档，不另建内容重复的架构说明。
- 指标 ID、名称、分类、维度、口径与版本以外部《全站指标体系》为准；事件、属性与版本以埋点协作主文档为准。YPBI 只维护引用、能力映射、查询支持与验数状态。
- 版本化 API contract 是接口契约权威；数据库 Schema 与有序 migration 是持久化结构权威；代码、自动测试和运行态是当前可执行事实。模块文档是这些事实的可读镜像，冲突时核实实现并修正文档，不用旧实现改写产品或数据口径。
- `docs/UI-已确认问题与防回归规则.md` 只维护已复现 UI 根因与统一验收护栏；`docs/INDEX.md` 只维护入口、模块地图与状态；`docs/CHANGELOG.md` 只记录历史，不反向成为当前规则。

不得在多个位置维护可独立修改的重复业务规则、指标公式、事件契约、对象 Schema 或状态枚举。文档中不得记录 Token、密码、Cookie、数据库密码、完整环境变量或可还原的筛选敏感信息。

## 2. 开始任务前

1. 先读 `docs/INDEX.md` 当前摘要、本次涉及模块的对应段落和 `docs/CHANGELOG.md` 最新相关记录；产品实现或验收任务再读取总 PRD 与调整方案对应章节。版本未变的无关章节不重复全量加载。
2. 涉及 UI、交互、响应式、弹层或布局时，按 UI 防回归文档的“低成本自检入口”读取适用问题 ID、共享实现入口和验收规则；新工作区或大范围迁移首次通读，局部修改按影响面加载。
3. 使用 `rg`、调用链、Schema、状态定义和测试建立完整影响面，至少覆盖调用方、数据、接口、权限、缓存、异步任务、埋点、部署和同类功能。
   先按改动性质筛出实际受影响项；不相关项登记“不涉及”，不展开全仓审计。首个代表位置验证正确后，再复用到其余受影响调用方。
4. 新模块、跨模块、高风险变更先做范围化架构健康检查；纯文案和低风险展示调整只检查受影响链路，不机械进行全仓审计。
5. 找到唯一权威源并从源头修改；现有接口能力、指标口径、多平台规则和数据状态未核实时，不直接修改正式实现。

## 3. 架构与模块边界

### 产品与体验任务准入

- **仅数据接入任务**：原页面结构、区块顺序、图表类型、组件、布局、尺寸和既有功能保持不变。复用现有渲染入口，只替换数据来源与必要的来源状态；不得创建简化页面、扁平卡片列表或以新布局代替原成果。接入前固定原页面基线，交付时逐页对照真实/演示状态下的布局和交互。确需改变展示必须先明确提出，取得用户同意后单独实施。

- 新模块或核心任务开始前，必须在总 PRD 中确认目标角色、使用场景、要解决的问题、本期范围和非目标；小控件以可验证结果验收，不为满足模板逐按钮编造用户故事或数字指标。
- 用户提出新的产品体验、UI 或交互调整时，先在总 PRD 修正目标状态、交互规则和验收边界，再按该权威描述修改代码、测试与派生文档，保证 PRD 可逐项对应实现。纯缺陷修复不为还原已确认语义而虚构新需求或频繁改 PRD；若缺陷暴露了原规则缺口，则先补全权威规则再改代码。
- 同一类缺陷默认按系统性问题处理：先定位根因，再检索共享组件、同类页面、所有状态分支、响应式视口、接口适配、派生产物与旧兼容逻辑；在唯一权威源一次修正全部受影响位置，清理已失效的补丁，并增加能阻止同类问题复发的自动回归。除非用户明确要求局部修改，不等待用户逐页重复指出。
- 先确认信息架构、主次操作及返回、前进、刷新、直接深链的结果，再实现页面；新能力必须接入既有用户闭环，不创建重复或孤立入口。
- 不可逆动作必须明确权限、数据归属、影响说明、确认方式、失败和恢复；能安全撤销时优先撤销，不能撤销时不得暗示可恢复。
- UI 优先复用 `src/theme/tokens.css` 与现有基础组件；只有存在真实复用、独立复杂行为或稳定变体时才抽取组件，不创建一次性风格或大量条件分支的万能组件。
- 已确认的多维分析交互优先复用总 PRD 5.18：总体单结果，小规模分组默认全显示并可逐项隐藏 / 恢复；交叉表突出结果、重复计算依据按需查看。业务核对表的主数据、高基数搜索分页、固定留存和专用分布保持各自用途；不复制指标卡，不混淆查询筛选与图例显隐，不相加去重人数或平均比率。新区域先核对分析目的与真实数据支持；具体规则只在 PRD 维护，实施范围与状态见调整方案。
- 卡片主值与比较按总 PRD 4.3 统一：普通日卡末日值、明确日基准、合法周期统计；自然月与固定成熟批次保持专用时间语义。同类数值诊断优先复用轻量趋势和就地详情，既有完整图不重复建设；主卡投影不能改写专用周期图的数据语义。
- 只实现当前能力真实适用的默认、交互、加载、空、错误、无权限、超时、部分失败和恢复状态。在线 BI 不预建离线模式；断网按可恢复的服务不可用处理。
- 核心链路或行为语义变化时必须评估产品埋点影响；不按按钮数量机械埋点。正式事件、字段、触发和隐私边界仍按既有埋点权威源及授权流程处理，技术日志和安全审计不能冒充产品埋点。

- 默认采用同仓分层迁移的模块化单体。除非现有结构已经无法安全演进，否则不整体重写，也不在旧 `Workspace → Template → Card` 模型中继续堆目标功能。
- 前端依赖方向为 `app / routes → features → shared`；服务端依赖方向为 `route / plugin → application / domain → port`，外部接口、数据库和文件系统作为 adapter / repository 实现 port。`shared` 与 contract 不反向依赖业务或基础设施。
- 模块只通过公开入口、稳定 contract 或 port 协作，不跨模块导入内部文件、不直接访问其他模块的数据表，不形成循环依赖。
- 服务端对象、页面查询和表单 / 编辑草稿分别保留唯一状态来源；复杂流程使用明确状态模型，不用多个冲突布尔值拼接。UI 不承载跨模块业务规则，不绕过业务层直接修改其他模块数据，也不伪造服务端成功。
- 指标目录、事件目录、查询、已保存分析、看板、导出、身份权限等按业务能力划分；旧代码在迁移期保持隔离，不与新对象双写或共享可变状态。
- 上游 API、身份、存储、导出等外部能力必须通过 adapter 隔离；业务层不依赖原始响应字段、凭据或部署细节。
- 每项业务规则和状态只保留一个权威来源。禁止用前端固定数组、名称判断、缺失补 0、Mock 或旧近似字段填补正式能力。
- 只在已有第二个真实用例或已确认的近期变化需要时抽象；不为模糊未来预建微服务、事件总线、插件系统、CQRS、万能 Repository、万能 Schema 或配置 DSL。

## 4. 风险分级与渐进演进

- P0：安全、权限、数据错误或泄露、不可恢复丢失、静默覆盖、伪结果、正式链路不可回滚；阻断相关功能交付。
- P1：近期会阻碍闭环、扩展或形成双口径、双状态、跨模块耦合；随当前切片修复或明确登记负责人和进入时点。
- P2：一般重复、可读性、性能优化和长期整理；不阻断主目标，不借机扩大本轮范围。

发现风险时先定位根因和完整影响面，在唯一权威源修复并清理失效分支、临时补偿和重复逻辑。临时兼容必须明确适用边界、默认路径、关闭方式和退役条件；功能开关不能长期维持双写或两套口径。

## 5. 高风险变更确认门槛

以下变更实施前，必须按一个可独立回滚的批次列出影响范围、兼容方案、迁移 / 存量处理、回滚路径和验证计划，并取得用户明确确认：

1. 新增或改变供浏览器、第三方或跨模块消费者使用的公共 API，包括路径、请求 / 响应、错误码、鉴权和语义；
2. 数据库建表、改表、删除、回填、保留期、备份恢复或存储介质变化；
3. 身份、角色、Owner / 维护范围、PID 数据范围、审计与授权策略；
4. 持久化语义、双写、不可逆操作、安全边界或大范围架构重构；
5. 会改变指标、事件或维度语义的 adapter 映射；此类变更还必须先回到对应业务权威源确认。

在已确认批次内，保持契约与语义不变的私有重构、测试补充、索引更新、非敏感日志、低风险可逆整理和兼容性缺陷修复不重复索要授权，但仍须检查影响面并验证。总 PRD 已确认某项产品能力，不等于其物理 Schema、权限来源或迁移方案已经自动获批。

## 6. 数据、接口与写入可靠性

- 数据库变更使用有序、可追踪的版本化 migration，不修改可能已经执行的旧 migration。破坏性变化采用 `expand → backfill → switch → contract`，并说明前滚、回退和存量数据验证。
- 外部 API 默认向后兼容；响应变化只在 adapter 内归一化。每个正式能力应有稳定 ID、版本、能力声明、数据水位和完整性状态。
- 创建、复制和可重试任务使用明确幂等策略；更新使用 `revision` 或等价条件写入；跨对象原子动作使用事务。并发冲突不得自动覆盖，部分失败不得留下半成品或悬空引用。
- 外部调用必须有超时；只对可安全重试的操作做有界重试，并处理重复执行、进程中断和失败恢复。
- 真实 0、缺失、未产出、未成熟、失败、部分数据和不适用在接口、计算、UI 与导出中保持一致，不以 `0`、空数组或上次结果静默替代。

## 7. 安全、可观测性与发布保护

- 身份、对象权限和数据范围必须在服务端执行；前端隐藏按钮不算权限控制。身份或范围来源未就绪时失败关闭，不创建假用户或前端角色兜底。
- 关键查询、保存、复制、导出、迁移和权限拒绝记录结构化结果、耗时、对象 ID 与追踪 ID；禁止记录 Token、Cookie、用户明细、完整链接或筛选敏感明文。
- 关键链路至少具备成功率、错误率、延迟和依赖 readiness 的可定位信号；当前不为此预建复杂监控平台。
- 高风险新旧路径可使用 feature flag 或独立路由灰度。每个 flag 必须有默认值、服务端保护、关闭后行为、回滚步骤和退役时点。
- 正式发布使用最小权限进程、正式静态资源服务和 HTTPS；健康检查失败不接流量。

## 8. 修改、文档与清理

1. 只修改授权范围内的唯一权威源；派生产物从权威源重新生成，同类位置遵循同一逻辑。
2. 清理被替换的旧分支、重复定义、补偿逻辑、过时入口和无效 feature flag；无关用户改动保持原状。
3. 删除前列出明确删除对象、必须保留能力和相邻依赖；完成后双向验证“目标已消失、未授权内容仍存在”。
4. 更新受影响模块文档，写清当前功能、接口、边界与代码入口；同步 `docs/INDEX.md` 的状态 / 日期，并在 `docs/CHANGELOG.md` 顶部追加时间、模块、变更、验证和部署状态。
5. 产品确认写回总 PRD；当前能力、风险、迁移和准入变化写回调整方案；UI 根因规则写回 UI 防回归文档。不得为单个功能随意新增 Markdown。

## 9. 风险分级验证

- 同类 UI 反馈沿用原问题 ID，记录可复现输入、错误结果、目标结果及共享修复入口；原失败样例必须成为回归断言。验收结论同时引用业务可见结果和实际截图，结构存在、无报错或测试数量仅作为辅助证据。
- 日常展示修改先跑 `npm run test:ui:shared`，按影响面选择浏览器用例；命中图表、单位、主次层级或提示时运行 `npm run test:e2e:ui-shared`。测试日志优先保存本地，只读取失败项和结果摘要。首张看板收口、共享行为大范围变化或发布再执行完整回归；仍保留下方规定的类型检查、构建与实际渲染复查。
- 批量浏览器测试先确认实际可用的浏览器、运行地址和依赖，并通过代表页面及相关登录/读取路径的冒烟检查。批次运行期间冻结受测代码；开发热更新和生成构建分开执行。环境启动失败先修环境，不扩散重跑。
- 失败先区分产品逻辑、测试断言、数据样本和运行环境。断言变更须同时核对已确认规则、实际DOM与数据，保留原业务验证，禁止猜选择器或为通过测试削弱断言。同一原因无新证据时不重复盲跑；第二次仍失败即转最小复现。
- 在现有实施记录中按“验收点—受测版本/改动—命令—结果—日志路径”保存证据。修复后先复测失败项及受影响相邻项；与后续改动无关的通过结果可复用，共享逻辑变化则重测相关调用方。完整回归按上条门槛执行，不在每次局部调整后重跑全量。
- 收尾核对授权项、数据/权限/关键交互、必要文档和用户体验入口后交付。非阻断且不属本批的问题另记待办，不反复扩大本轮；数据准确性、真实缺失、权限与原功能回归不因节省Token而省略。效率改进由执行代理从当前任务起落实，每批检查重复读取、无效重试与用户再次纠正的遗漏；没有可靠用量记录时不宣称节省比例。
- 后续看板复用首张看板已确认的组件、Token 与交互规则，并对照相同类型的实际状态验收；新增组件变体需有现有组件无法表达的明确需求及对应回归，不复制页面样式建立第二套规则。

- 纯文档：检查链接、版本、状态、引用和 Markdown 差异，运行 `git diff --check`。
- 普通前后端代码：相关单元测试、`npm run typecheck`、`npm run build`。
- API / adapter：覆盖成功、空、缺失、错误、超时、字段漂移、鉴权和数据范围；在安全只读前提下抽样验证真实上游。
- 数据库 / 持久化：验证 migration up、旧数据兼容或回填、失败后的前滚 / 回退、revision、并发、幂等和进程重启恢复。
- 权限：覆盖角色 × 对象归属 × PID 范围的正反例，并验证服务端拒绝。
- UI：按受影响范围在真实浏览器覆盖主链路及至少一个关键失败恢复链路、1280 / 1024 / 390 视口、返回 / 前进 / 刷新 / 直接深链和键盘主链；使用空集合、单条、上限条数、长中英文、特殊字符、0、负数、超大值及缺图等真实适用边界数据，检查 default / hover / focus-visible / active / disabled 和 loading / empty / error / 无权限等适用状态，并保存关键页面截图证据。不得为了清单机械建设当前功能不存在的状态。
- 发布：验证 health / ready、关键 smoke、日志脱敏、flag 开关和回退路径。

当前项目没有 `lint` script，不得声称已运行 lint；以后正式配置后再纳入门槛。当前环境缺少 Bun 或真实依赖时，相关测试必须明确标为“未运行 / 被环境阻断”，不能声称通过。

## 10. 完成与交付

代码测试通过但权威文档、模块状态或 UI 防回归记录未同步，任务仍未完成。最终交付必须说明：

- 改了什么，以及为什么这样设计；
- 影响范围和明确未改的权威口径 / 功能；
- 实际验证命令与结果；
- 部署状态；
- 适用时的迁移、回滚方式和剩余风险。
