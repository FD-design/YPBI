# 配置驱动 BI 全方位优化评审

> **历史评审记录（非当前需求）**：本文只保留 2026-07 原型问题证据，不得作为功能清单、产品对象、指标口径或开发顺序。当前产品需求以 `docs/requirements/BI-产品需求文档.md` 为准，当前实现差异和迁移方案以 `docs/requirements/BI-现有平台调整方案.md` 为准。

更新时间：2026-07-10  
评审范围：模板中心、卡片库、卡片工厂、模板工厂、数据字典，以及多平台指标、事件、分析与图表体系。

## 一、核心结论

当前版本已经把“事件/指标/维度 -> 分析卡片 -> 模板 -> 日常看板”的产品骨架搭出来了，但仍属于高保真交互原型，还不是一套真正可信的 BI 演示。

最关键的问题不是图表数量不足，而是“配置”和“结果”没有真正连起来：日期、平台、指标、维度、转化窗口可以选择，但多数图表仍显示同一组硬编码数据。用户一旦切换平台、改变指标或修改漏斗步骤，就会发现分析结果没有对应变化。

下一版应围绕一个完整闭环重构：

> 进入看板发现问题 -> 切换平台/时间验证 -> 点击节点下钻定位 -> 在分析工作台进一步拆解 -> 保存为卡片 -> 组合或更新模板 -> 回到看板持续使用。

产品定位建议保持为“专业产品团队使用的轻量自助分析平台”，而不是一开始就做成 Tableau 式全能 BI。专业性来自预封装事件、统一指标口径、可配置分析模型和可信的下钻，而不是暴露 SQL、数据表 Join 和复杂权限。

## 二、当前版本的关键事实

| 领域 | 当前实现 | 实际影响 |
|---|---|---|
| 图表数据 | 趋势、排行、热力、KPI 等使用固定数据 | 选择指标、维度后图形不变，配置缺乏可信度 |
| 全局筛选 | 日期、平台只改变选中状态 | 看板数据、摘要、明细和下钻不联动 |
| 图表口径 | DAU、收入、付费率共用同一 Y 轴 | 不同单位直接比较，容易产生错误结论 |
| 下钻 | 搜索模型和其他模型共用两套固定明细 | 支付、留存、会员等卡片下钻会展示不相关字段 |
| 自动结论 | 所有节点使用通用诊断文案 | 结论与真实波动、贡献、平台无关 |
| 卡片保存 | 创建草稿含主图和统计表，保存时仅取第一张卡片 | 用户配置的明细统计表会被丢弃 |
| 卡片编辑 | 从卡片库进入编辑后再次保存会新增副本 | 缺少“更新原卡片”和版本语义 |
| 模板状态 | 页面显示“已自动保存” | 实际没有 localStorage、后端或草稿持久化 |
| 功能按钮 | 导出、保存筛选、预览模板、发布模板无处理逻辑 | 演示时会出现点击无反应 |
| 模型完整度 | 类型定义 7 个模型，Mock 仅实现 4 个 | 会员、留存缺失；平台对比被引用但没有模型定义 |
| 指标完整度 | 类型定义 11 个指标，字典仅 7 个 | 新增用户、观影率、完播率、ARPPU 等无法治理 |
| 事件完整度 | 仅 9 个事件 | 使用时长、观影时长、搜索承接、会员存量、退款等无法分析 |
| 响应式 | 页面整体最小宽度 1440px，内部仍有固定三列 | 实测 1366px 模板工厂横向溢出 156px，1440px 溢出 82px |
| 可访问性 | 无键盘拖拽、ARIA、焦点管理 | 拖拽是唯一操作时无法替代，抽屉焦点也不受控 |

对应代码证据主要位于：

- `src/main.tsx:104`：各类图表的固定 Mock option。
- `src/main.tsx:233`：卡片图表只按 `card.type` 重新计算，漏斗步骤等变化可能不刷新。
- `src/main.tsx:279`：平台筛选只有本地选中状态。
- `src/main.tsx:294`、`src/main.tsx:494`：无处理逻辑的按钮。
- `src/main.tsx:1043`：保存卡片只取 `draftTemplate.cards[0]`。
- `src/styles.css:88`、`src/styles.css:91`、`src/styles.css:723`：固定列宽和最小宽度导致横向溢出。

## 三、产品结构应如何调整

### 1. 建议的信息架构

| 顺序 | 工作区 | 用户心智 | 核心职责 |
|---|---|---|---|
| 1 | 看板中心 | 我今天要看什么 | 使用已发布模板、筛选、对比、下钻、保存视图 |
| 2 | 新建分析 | 我现在要分析一个问题 | 选择分析模型，配置事件/指标/维度，运行预览并保存卡片 |
| 3 | 卡片库 | 我们沉淀了哪些分析 | 搜索、筛选、预览、复制、编辑、查看依赖和版本 |
| 4 | 模板编辑器 | 哪些卡片组成一个看板 | 选择模板、拖入卡片、调整布局、映射筛选器、预览并发布 |
| 5 | 数据字典 | 这些数据到底是什么 | 管理事件、属性、指标、维度、口径、状态和覆盖范围 |

“卡片工厂”建议在界面上改名为“新建分析”或“分析工作台”。卡片是分析完成后的产物，不应成为用户开始任务时必须理解的概念。“模板工厂”可改为“模板编辑器”或“看板编辑器”，表达也更直接。

### 2. 两条主路径必须明确分开

日常查看路径：

1. 默认进入看板中心。
2. 选择一个模板，恢复上次使用的时间和平台范围。
3. 通过总汇、单平台、平台对比三种模式查看。
4. 点击异常指标或图表节点，在右侧抽屉验证原因。
5. 必要时“基于当前条件继续分析”，跳入新建分析并自动带入上下文。

创建路径：

1. 进入新建分析，先选择趋势、分布、留存、漏斗、路径、事件、统计表等分析类型。
2. 选择预封装事件或指标，配置属性条件、维度、时间和平台。
3. 点击“运行分析”生成真实预览，修改配置后增量刷新。
4. 验证无误后保存为卡片。
5. 在卡片库复用，或进入模板编辑器组装看板。

## 四、五个工作区的具体优化

### 看板中心

- 顶部只保留真正的全局条件：时间、平台模式、平台选择、时区/币种（必要时）。
- 平台模式明确分为“全部汇总”“单个平台”“对比平台”，对比最多 8 个。
- 全局条件必须驱动全部卡片、摘要、明细和下钻，并显示“当前数据范围”。
- 图表节点支持两种动作：仅筛选当前看板、打开右侧下钻。动作入口要区分，避免误触。
- 卡片右上角统一提供：查看配置、编辑卡片、复制、导出、加入模板、刷新。
- 看板要有加载、空数据、部分平台缺数、查询失败和数据延迟状态。
- “更新时间”使用真实模拟时间，并显示数据是否完整，不再所有卡片写同一固定时间。

### 新建分析

- 中央区域始终优先展示图表预览；事件库和配置面板应可折叠，不能同时长期占满左右空间。
- 推荐布局：左侧 240px 分析素材库，中间弹性预览与步骤配置，右侧 320px 当前分析属性；在 1440px 以下自动把右侧变成抽屉。
- 先配置、后运行。配置变化后显示“有未运行的更改”，点击运行才更新结果；简单条件可 300ms 防抖自动刷新。
- 漏斗至少支持：步骤事件、每步属性过滤、同一用户、严格顺序、转化窗口、步骤间最大间隔、排除事件、拆分维度。
- 漏斗步骤点击新增默认追加到末尾；拖入某个位置才按落点插入。步骤至少 2 个，重复事件给出提示但允许用户确认。
- 指标和维度必须真正改变图形、摘要、表格列和下钻内容。
- 统计表不是漏斗的附属预览，而是一种可独立保存的卡片类型，支持自定义字段、排序、Top N、分页和条件格式。
- 保存前要校验名称、至少一个指标、分析类型所需的维度/事件、图表兼容性。
- 编辑已有卡片时提供“保存修改”和“另存为新卡片”，不能默认复制。

### 卡片库

- 现有类型筛选保留，增加搜索、分析模型、业务域、平台适用范围、更新时间、状态筛选。
- 提供“网格/紧凑列表”切换。网格看图，列表适合管理大量卡片。
- 卡片摘要增加：核心指标、维度、平台范围、更新时间、被几个模板使用、状态。
- 删除前显示依赖模板，并二次确认；被模板引用时优先建议停用或复制替换。
- 支持复制卡片、批量加入模板、收藏、最近使用和版本历史。
- 当前固定卡片宽度造成大片空白，应改为 `repeat(auto-fit, minmax(320px, 1fr))` 或受控 12 栅格。

### 模板编辑器

- 顶部编辑区压缩成单行工具栏：模板名、草稿状态、预览、发布、更多设置。使用场景和模型放到可展开设置中。
- 左侧模板列表可折叠；右侧卡片库固定 300-320px，可折叠；中央画布永远优先获得空间。
- 使用 12 列栅格，卡片既能排序，也能调整宽度和高度。推荐使用成熟拖拽/网格库处理碰撞和占位。
- 拖动时显示卡片缩略图、真实插入占位和自动滚动；无效落点要明确提示。
- 模板全局筛选器不是一个单选框，而是可新增、删除、排序，并能映射到各卡片字段。
- 增加撤销/重做、未保存提示、预览模式、发布前校验。
- 发布应产生可恢复版本；这不是权限系统，只是保证模板编辑不破坏日常使用版本。

### 数据字典

- 从当前“事件列表 + 指标列表”升级为“事件 / 事件属性 / 指标 / 维度”四个页签。
- 增加搜索、业务域、状态、负责人、覆盖平台、数据可用时间筛选。
- 点击条目进入详情抽屉，展示定义、触发时机、字段、枚举、示例、使用该条目的分析模型和卡片。
- “拖到卡片工厂”在跨工作区时无法真实完成，应改为“用于新建分析”，点击后自动跳转并带入事件/指标。
- 状态不能只显示英文 `ready/partial/missing`，应展示中文状态、缺失平台、缺失字段、预计补齐条件。

## 五、多平台必须成为系统级能力

平台不能只作为普通维度按钮，而要成为整个 BI 的一级分析范围。

### 三种平台模式

| 模式 | 用途 | 规则 |
|---|---|---|
| 全部汇总 | 判断总体经营健康 | 数量类求和；比例类使用 `sum(分子) / sum(分母)` 加权计算 |
| 单个平台 | 定位一个平台自身问题 | 所有卡片和下钻继承该平台；允许与该平台历史基线比较 |
| 平台对比 | 找差异和最佳实践 | 最多 8 个；颜色稳定；可切换绝对值、占比、指数化表现 |

每张卡片需要声明：是否支持汇总、是否支持单平台、是否支持多平台对比、比例汇总公式、缺失平台处理、币种和时区。平台名称、ID、渠道别名要通过统一平台维表映射，避免同一平台在不同数据源出现多个名称。

## 六、字段和语义层需要补全

### 事件定义应增加

- 事件 ID、中文名、业务域、描述、准确触发时机。
- 主体类型与身份键：用户、设备、会话、内容、订单。
- 事件时间字段、客户端时间、服务端时间和时区。
- 属性清单：字段名、中文名、类型、是否必填、枚举、示例、说明。
- 覆盖平台、版本、负责人、状态、首个可用日期、数据延迟、质量规则。

### 指标定义应增加

- 原子指标/派生指标类型、单位、聚合方式、统计粒度。
- 分子、分母、去重主键、过滤条件、时间窗口。
- 空值处理、跨平台汇总方式、币种/时区、归因规则。
- 数据源、依赖事件/表、负责人、状态、版本和质量阈值。

### 卡片配置应拆成两部分

查询配置：分析模型、指标、维度、事件步骤、过滤条件、时间、平台、对比、排序、Top N、分页、计算字段。  
表现配置：图表类型、编码映射、颜色、单位、小数位、阈值、目标线、标签、图例、卡片尺寸、下钻路径。

这样才能做到同一份查询结果切换不同图表，同时避免换图表时丢失业务口径。

## 七、事件体系建议

当前 9 个事件只能支撑最基础的曝光、播放、搜索和支付漏斗。建议至少补齐以下业务事件组：

| 事件组 | 建议事件 | 支撑分析 |
|---|---|---|
| 会话与活跃 | `app_start`、`session_start`、`session_end`、`foreground_enter`、`background_enter` | DAU、会话数、人均使用时长、中断页面 |
| 页面与元素 | `page_view`、`page_leave`、`element_exposure`、`element_click` | 页面曝光/点击、页面路径、按钮转化 |
| 内容消费 | `video_exposure`、`video_click`、`play_start`、`play_progress`、`play_end`、`play_complete` | 观影率、人均观影时长、播放/完播、流失位置 |
| 内容互动 | `like`、`collect`、`share`、`comment` | 内容质量与深度互动 |
| 搜索 | `search_submit`、`search_result_exposure`、`search_result_click`、`search_no_result` | 搜索需求、供给缺口、搜索后转化 |
| 会员与支付 | `paywall_exposure`、`member_plan_click`、`order_create`、`payment_result`、`refund` | 付费漏斗、渠道成功率、退款 |
| 会员生命周期 | `member_activate`、`member_renew`、`member_cancel`、`member_expire` | 当前会员、历史会员、各档人数、续费和流失 |
| 身份链路 | `register_result`、`login_result`、`user_identity_merge` | 新老用户、跨设备归因、注册登录漏斗 |

关键属性必须统一：`platform_id`、`user_id`、`anonymous_id`、`session_id`、`page_id`、`position_id`、`position_rank`、`content_id`、`category_id`、`keyword`、`order_id`、`member_plan_id`、`payment_channel`、`result`、`fail_reason`、`duration_ms`。

历史付费会员和当前付费会员不应只靠点击事件推导，需要会员快照或会员生命周期事实表。页面点击/曝光需要定义是 PV 还是 UV；位置指标要同时保留 `position_id` 和 `position_rank`。

## 八、建议补齐的分析模型

1. 经营总览：活跃、使用、收入、付费、留存。
2. 平台对比：规模、效率、增长、贡献、质量。
3. 内容位置效率：平台 -> 页面 -> position -> rank -> 分类 -> 内容。
4. 内容消费漏斗：曝光 -> 点击 -> 播放 -> 有效播放 -> 完播 -> 付费。
5. 搜索需求：搜索词 -> 结果曝光 -> 结果点击 -> 播放 -> 付费。
6. 支付转化：付费墙 -> 套餐点击 -> 拉单 -> 支付结果 -> 会员生效。
7. 会员经营：历史会员、当前会员、套餐结构、收入、续费、到期、流失。
8. 留存质量：新增/活跃 cohort、D1/D3/D7/D14/D30、回流和沉默。
9. 使用深度：会话时长、观影时长、频次、页面路径、内容互动。
10. 自定义统计表：任意合法维度 + 指标 + 过滤 + 排序 + 分页。

每个模型都应预设可用事件、指标、维度、默认图表、下钻路径和兼容规则。用户仍可专业配置，但不需要从零理解底层表。

## 九、图表体系应围绕问题选择

| 要回答的问题 | 主图表 | 辅助表现 |
|---|---|---|
| 整体是否健康 | KPI + sparkline + 同比/环比 | 目标线、异常徽标、贡献摘要 |
| 什么发生了变化 | 同单位趋势、小倍图、控制带 | 同比区间、事件标注、变化表 |
| 哪个平台更好 | 排序条形、点图、子弹图 | 排名变化、指数化对比、小型趋势 |
| 哪个位置效率差 | 页面 × position 热力图 | 曝光-转化气泡图、明细表 |
| 漏斗掉在哪里 | 居中阶梯漏斗或对称条形漏斗 | 步间转化、流失人数、平台小倍图 |
| 收入由什么贡献 | 瀑布图、Treemap | 平台/套餐贡献排行 |
| 搜索机会在哪里 | 搜索量 × 无结果率气泡矩阵 | Pareto 排行、搜索转化 Sankey |
| 支付卡在哪一步 | 漏斗/Sankey、渠道成功率热力图 | 失败原因 Pareto、收入瀑布 |
| 留存是否改善 | Cohort 热力图、留存曲线 | cohort 规模、平台对比 |
| 数据分布如何 | 直方图、箱线图 | 分位数、异常点明细 |
| 自定义明细 | 透视统计表 | 条件格式、列内 sparkline、分页 |

图表设计规则：

- 不同单位不要强行共用一个 Y 轴；优先使用小倍图，必要时才使用清晰标注的双轴。
- 平台颜色全系统固定，状态色只表达好坏/告警，不能与平台色混用。
- 漏斗同时显示人数、相对首步转化率、相对上一步转化率和流失量。
- Tooltip 必须包含口径、时间、平台、分子分母和对比值。
- 图例可开关系列；缩放、框选和数据点点击应保持状态。
- 饼图只用于少量稳定构成；雷达图不作为主要精确比较图。

## 十、下钻不应只是一个通用结论弹窗

右侧下钻抽屉建议固定为六层结构：

1. 上下文：来自哪张卡片、点击了什么、时间和平台范围。
2. 面包屑：平台 -> 页面 -> position -> 分类，可返回任一级。
3. 当前值：指标值、环比/同比、总体基线、目标差距。
4. 原因拆解：贡献度排行、异常维度、趋势和对比。
5. 明细数据：与当前模型匹配的字段，支持筛选、排序和分页。
6. 后续动作：继续下钻、基于当前条件新建分析、保存视图、导出。

自动结论必须从当前数据计算，例如“TikTok 支付成功率较总体低 6.8pct，其中信用卡通道贡献 71% 的下降”，不能所有节点都输出“高曝光低转化”。

## 十一、交互必须补齐的真实状态

- 所有按钮必须有结果：成功反馈、失败提示、禁用原因或明确的演示状态。
- 删除卡片/模板前确认；保存后 Toast；编辑离开时提示未保存更改。
- 创建和编辑支持撤销/重做，至少保留最近 20 步。
- 拖拽支持插入线、占位、自动滚动、非法落点提示，以及“上移/下移”键盘替代。
- 右侧抽屉打开时锁定背景滚动、管理焦点，按 Esc 关闭。
- 查询过程显示 skeleton；无数据、部分缺数、错误、超时分别处理。
- 操作历史建议包含：最近浏览、最近分析、最近编辑、模板版本。暂时不需要角色和审批流。

## 十二、前端和样式优化

### 布局

- 移除整体 `min-width: 1440px`，以 1280/1440/1600 三档适配。
- 构建工作区进入“紧凑导航模式”，主导航从 210px 折叠为 64-72px，给画布让空间。
- 禁止页面横向滚动；模板画布和素材库各自有受控纵向滚动，避免三层滚动条。
- 右侧面板可折叠；当宽度不足时改为抽屉，不再把图表挤到视口之外。
- 卡片采用 12 栅格和稳定高度，避免内容变化导致跳动。

### 视觉

- 保留蓝色主色，但增加中性灰、绿色成功、橙色风险、红色异常，避免所有强调都用蓝色。
- 页面分区尽量使用留白和分隔线，不再层层嵌套卡片。
- 标题、辅助信息、指标值、结论建立固定字号层级；内部工作台保持专业、紧凑。
- 图表统一主题：网格线、轴、Tooltip、图例、空态、动画时长和平台色由主题控制。
- 控件高度、圆角、内边距统一；卡片圆角控制在 6-8px。

### 工程结构

- 当前 `main.tsx` 超过 1000 行，应按 `dashboard`、`analysis-builder`、`card-library`、`template-editor`、`dictionary` 拆分。
- 将卡片配置状态放入 reducer 或轻量 store，提供撤销/重做和明确 action，避免多个局部状态互相覆盖。
- 增加统一 `executeQuery(cardConfig, globalFilters)` Mock 查询引擎，所有图表、摘要、表格和下钻使用同一结果。
- 使用固定随机种子生成多平台、多日期、事件级 Mock 数据，保证每次演示一致且能形成真实结论。
- 用图表注册表把“查询结果 -> ECharts option”解耦，切换图表不修改业务查询。
- 使用 `ResizeObserver` 监听卡片和抽屉尺寸，避免只监听 window resize。
- 演示状态使用 localStorage/IndexedDB 持久化，并带 schema version 和“恢复演示数据”。
- 使用成熟拖拽/网格方案处理模板排序和缩放；手写逻辑只保留业务规则。
- 增加单元测试（指标公式/聚合）、集成测试（保存卡片/发布模板）和 E2E（看板筛选/下钻/创建漏斗）。

## 十三、建议的完整交付优先级

### P0：让演示可信

1. 建立配置驱动 Mock 查询引擎，平台、时间、指标、维度、窗口期真实生效。
2. 补齐 8-10 个分析模型、事件/指标/维度字典和留存/会员数据。
3. 修复卡片保存丢失、编辑变新增、按钮无响应、模板假自动保存等逻辑。
4. 重做卡片工厂和模板工厂布局，保证 1366px 以上无横向溢出、图表始终可见。
5. 下钻按模型返回匹配字段和计算结论，明细支持分页。
6. 增加本地持久化、撤销/重做、空态/错误态/成功反馈。

### P1：让它像专业 BI

1. 跨卡片联动筛选、平台 2-8 对比、同环比和贡献拆解。
2. 模板 12 栅格缩放、全局筛选器映射、草稿/发布版本。
3. 卡片库依赖、复制、搜索、列表模式和版本历史。
4. 漏斗属性过滤、顺序、间隔、排除事件和分群对比。
5. Cohort、瀑布、Sankey、箱线、气泡矩阵等高价值图表。

### P2：让分析更有结论

1. 基于阈值、同比、贡献和异常检测生成数据化结论。
2. 保存分析视图、异常追踪、订阅/分享等后续能力。
3. 计算字段和更自由的统计表配置，但仍受语义层约束。

演示版可以一次性呈现完整闭环，P0/P1/P2 只是实现顺序，不代表要把产品拆成互不连贯的几个版本。

## 十四、验收标准

- 切换任意平台、时间或对比模式，所有相关图、摘要、表和下钻同步变化。
- 修改漏斗步骤、窗口、指标或维度后，预览结果和转化数据发生合理变化。
- 保存卡片后可在卡片库找到；编辑可更新或另存；刷新页面后数据仍在。
- 卡片可从库拖入模板指定位置，可缩放、排序、移除、撤销和重做。
- 模板发布后在看板中心可直接使用，草稿修改不影响已发布版本。
- 会员、留存、搜索、支付、内容位置、使用时长均有匹配的事件、指标、图表和下钻字段。
- 1366px、1440px、1600px、1920px 桌面宽度均无页面级横向滚动，核心图表不被侧栏遮挡。
- 所有可见按钮均有有效交互，所有拖拽均有非拖拽替代操作。
- 每张图都能回答一个明确问题，并能从图上继续验证原因，而不是只展示数值。
