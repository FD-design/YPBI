# 多平台 BI 正式版代码级实施规格

> 项目目录：`D:\CodexArtifacts\config-driven-bi-demo`  
> 部署目录：`/opt/config-driven-bi-demo`  
> 产品定位：基于现有后台 API 的受控自助分析平台  
> 交付方式：按本文完整实现、联调、测试并一次上线，不拆分业务版本，不以 Mock 功能作为完成标准

## 1. 最终交付范围

正式版必须同时交付以下能力：

1. 看板中心：查看产品配置并发布的多平台 BI 看板。
2. 分析工作台：使用已有 API 能力创建指标、趋势、排行、明细、漏斗和留存卡片。
3. 卡片库：统一查看、筛选、预览、编辑、复制和删除所有分析卡片。
4. 模板编辑器：选择已有卡片组成模板，拖动布局，保存草稿、预览和发布。
5. 数据字典：查看指标、维度、事件、来源接口、计算公式、粒度和能力限制。
6. 多平台分析：支持全部平台汇总、单平台查看、自选最多 8 个平台对比。
7. 服务端持久化：卡片、模板、布局、版本和查询审计均保存到 PostgreSQL。
8. 生产数据链路：浏览器不直接请求原后台，统一由 BI 服务代理、校验、聚合和缓存。
9. 完整状态：加载、空数据、无权限、部分平台失败、超时、重试、配置失效均有明确表现。
10. 部署运维：Docker Compose、Nginx、HTTPS、健康检查、日志、备份和回滚可用。

不实现任意 SQL、跨接口自由 Join、用户级任意事件序列、任意计算表达式。界面只能开放能力注册表明确支持的组合。

## 2. 最终代码结构

保留 React、TypeScript、Vite、ECharts，拆除 `main.tsx` 中的业务实现。最终仓库结构如下：

```text
config-driven-bi-demo/
  apps/
    web/
      src/
        app/
          App.tsx
          router.tsx
          providers.tsx
          queryClient.ts
        api/
          httpClient.ts
          cards.ts
          templates.ts
          analytics.ts
          dictionary.ts
        components/
          chart/ChartRenderer.tsx
          chart/ChartState.tsx
          chart/chartTheme.ts
          filters/DateRangeFilter.tsx
          filters/PlatformFilter.tsx
          layout/CardGrid.tsx
          layout/DraggableCard.tsx
          feedback/ErrorState.tsx
          feedback/EmptyState.tsx
          feedback/PartialFailureBanner.tsx
        features/
          dashboards/
          analysis-builder/
          card-library/
          template-editor/
          dictionary/
          drilldown/
        hooks/
          useAnalyticsQuery.ts
          useCardMutation.ts
          useTemplateMutation.ts
          useDebouncedValue.ts
        model/
          card.ts
          template.ts
          analytics.ts
          capability.ts
        stores/
          analysisDraftStore.ts
          dashboardFilterStore.ts
        styles/
          tokens.css
          globals.css
      tests/e2e/
    server/
      src/
        app.ts
        config/env.ts
        plugins/auth.ts
        plugins/db.ts
        plugins/cache.ts
        modules/
          analytics/
            analytics.routes.ts
            analytics.service.ts
            query-orchestrator.ts
            aggregate.ts
            capability-validator.ts
            metric-registry.ts
            dimension-registry.ts
            event-registry.ts
          upstream/
            upstream.client.ts
            upstream.errors.ts
            adapters/
              overview.adapter.ts
              video.adapter.ts
              user.adapter.ts
              event.adapter.ts
              search.adapter.ts
              retention.adapter.ts
              channel.adapter.ts
          cards/
          templates/
          dictionary/
          health/
        db/
          migrations/
          repositories/
        shared/
          schemas/
          errors/
          logging/
      tests/
  packages/
    contracts/
      src/
        api.ts
        card.ts
        template.ts
        analytics.ts
        capability.ts
  deploy/
    docker-compose.yml
    nginx.conf
    Dockerfile.web
    Dockerfile.server
  package.json
  bun.lock
```

约束：页面不得引用原后台响应类型；页面只依赖 `packages/contracts` 中的标准模型。每个业务文件建议不超过 400 行，硬上限 800 行。

## 3. 核心配置协议

### 3.1 卡片配置

```ts
type AnalysisType =
  | 'metric'
  | 'trend'
  | 'ranking'
  | 'table'
  | 'funnel'
  | 'retention';

interface CardConfig {
  id: string;
  schemaVersion: 1;
  name: string;
  description?: string;
  analysisType: AnalysisType;
  metricIds: string[];
  dimensionIds: string[];
  eventIds?: string[];
  filters: FilterConfig[];
  chart: ChartConfig;
  query: {
    dateMode: 'global' | 'fixed';
    fixedDateRange?: [string, string];
    platformMode: 'global' | 'fixed';
    fixedPlatformIds?: string[];
    sort?: { field: string; direction: 'asc' | 'desc' };
    limit?: number;
  };
  capabilitySnapshot: {
    sourceApiIds: string[];
    metricVersions: Record<string, number>;
  };
}
```

保存前，前端用于即时提示，服务端再次执行 `validateCardConfig(config)`。任何不支持的指标、维度、事件、图表或接口组合必须返回结构化错误，不允许静默降级。

### 3.2 指标注册表

```ts
interface MetricDefinition {
  id: string;
  name: string;
  unit: 'count' | 'person' | 'currency' | 'duration' | 'percent';
  valueType: 'integer' | 'decimal';
  aggregation: 'sum' | 'avg' | 'latest' | 'weightedRatio' | 'derived';
  numeratorMetricId?: string;
  denominatorMetricId?: string;
  formulaLabel?: string;
  sourceApiIds: string[];
  supportedDimensions: string[];
  supportedAnalysisTypes: AnalysisType[];
  grain: 'realtime' | 'day' | 'record' | 'cohort';
  status: 'available' | 'restricted' | 'unavailable';
  restrictionReason?: string;
  version: number;
}
```

首批必须注册：DAU、新增用户、观看人数、观看次数、总观看时长、收入、付费人数、视频数、搜索次数、VIP 点击、预支付、支付成功、D1/D3/D7 留存，以及接口能明确提供的渠道和圈子指标。

派生指标必须保留依赖关系：

```ts
viewRate = sum(viewerCount) / sum(dau)
avgWatchDuration = sum(watchDuration) / sum(viewerCount)
payRate = sum(payerCount) / sum(dau)
arppu = sum(revenue) / sum(payerCount)
```

分母为 0 时返回 `null`，展示为 `--`，不可返回 0%。跨日 UV 若只能按日累加，元数据中必须标记“按日 UV 累加，可能重复”。

### 3.3 维度、事件和能力注册

`DimensionDefinition` 至少包含 `id/name/sourceApiIds/filterType/multiSelect/maxSelections`。首批维度为日期、平台、视频、视频分类、搜索词、渠道，以及接口真实支持的用户或圈子字段。

`EventDefinition` 只注册已有事件接口可查询的事件。支付漏斗固定为 `vip_click -> pre_pay -> success_pay`；漏斗属于事件汇总值比较，不宣称用户级有序转化。

`CapabilityRule` 描述可保存组合：

```ts
interface CapabilityRule {
  analysisType: AnalysisType;
  requiredMetricCount?: [number, number];
  requiredEventCount?: [number, number];
  allowedDimensionIds: string[];
  allowedChartTypes: string[];
  maxPlatforms: number;
  sourceApiPolicy: 'single-api' | 'registered-composition';
}
```

## 4. 后端功能与具体实现

### 4.1 统一上游请求层

实现 `UpstreamClient.request<T>(definition, params, context)`：

- 从服务端环境变量读取后台 API Base URL，不在前端保存。
- 从用户请求上下文提取登录凭证，按原后台鉴权方式转发。
- 默认超时 10 秒；仅 GET 和明确幂等请求最多重试 2 次。
- 写入 `x-request-id`，日志记录接口 ID、平台、耗时、状态码，不记录 Cookie、Token 和完整个人数据。
- 使用 Zod 校验每个接口响应；字段类型漂移返回 `UPSTREAM_SCHEMA_MISMATCH`。
- 将 401、403、429、超时、空响应和上游 5xx 映射为统一错误码。
- 相同用户权限、接口、参数的并发请求合并；成功结果按接口配置缓存 30 秒至 5 分钟。

### 4.2 API Adapter

30 个后台 API 每个都必须有定义和响应 Schema，按领域集中在 7 个 Adapter 中，禁止在 React 组件里做字段兼容。

每个 Adapter 输出统一结构：

```ts
interface NormalizedDataset {
  grain: 'realtime' | 'day' | 'record' | 'cohort';
  rows: Array<Record<string, string | number | null>>;
  fields: Array<{ id: string; type: string; sourceField: string }>;
  source: {
    apiId: string;
    fetchedAt: string;
    timezone: 'Asia/Shanghai';
    warnings: string[];
  };
  pagination?: { page: number; pageSize: number; total: number };
}
```

必须完成的映射：

| Adapter | 主要 API | 标准用途 |
|---|---|---|
| `overview.adapter.ts` | `pDaySum`、`pRealDayLine`、`getAllByRole` | 日汇总、实时趋势、角色/平台汇总 |
| `video.adapter.ts` | `todayVideos/getMany`、`getVideoStatsByCategories`、`videoWatchRanking` | 视频明细、分类贡献、观看排行 |
| `user.adapter.ts` | `todayUsers/getMany` | 用户观看明细和观看深度 |
| `event.adapter.ts` | `trackEventsReport/getEventStats` | 事件汇总、事件趋势、固定支付漏斗 |
| `search.adapter.ts` | `hotSearchWords/getMany` | 搜索词排行和表格 |
| `retention.adapter.ts` | 留存关系接口 | Cohort 与平均留存 |
| `channel.adapter.ts` | 渠道统计接口 | 渠道汇总、明细和 A/B 数据 |

搜索接口不得产生“无结果率”“搜索后点击率”；视频接口没有 `page/position/positionRank` 时不得开放位置分析；支付接口没有支付渠道和会员档位时不得显示对应筛选。

### 4.3 查询编排器

`QueryOrchestrator.execute(query, userContext)` 的执行顺序固定为：

1. Zod 校验查询结构。
2. `CapabilityValidator` 校验分析类型、字段和来源组合。
3. 将“全部平台”展开为平台字典中启用的平台 ID。
4. 最多并发请求 4 个平台，最多选择 8 个平台对比。
5. 调用对应 Adapter，使用 `Promise.allSettled` 保留部分成功结果。
6. 按指标注册表执行聚合和派生公式。
7. 生成图表无关的标准结果，不在服务端拼 ECharts option。
8. 返回数据、元数据、警告、失败平台和查询耗时。

统一返回：

```ts
interface AnalyticsResponse {
  data: {
    columns: AnalyticsColumn[];
    rows: AnalyticsRow[];
    summary?: Record<string, number | null>;
  };
  meta: {
    queryId: string;
    grain: string;
    sourceApiIds: string[];
    fetchedAt: string;
    cacheHit: boolean;
    partial: boolean;
    failedPlatforms: Array<{ platformId: string; errorCode: string }>;
    warnings: string[];
  };
}
```

### 4.4 BI 自有接口

```text
GET    /api/bi/capabilities
GET    /api/bi/dictionary/metrics
GET    /api/bi/dictionary/dimensions
GET    /api/bi/dictionary/events
POST   /api/bi/analytics/query
POST   /api/bi/analytics/validate
GET    /api/bi/cards
POST   /api/bi/cards
GET    /api/bi/cards/:id
PUT    /api/bi/cards/:id
POST   /api/bi/cards/:id/clone
DELETE /api/bi/cards/:id
GET    /api/bi/templates
POST   /api/bi/templates
GET    /api/bi/templates/:id
PUT    /api/bi/templates/:id
POST   /api/bi/templates/:id/publish
POST   /api/bi/templates/:id/unpublish
DELETE /api/bi/templates/:id
GET    /api/bi/health
```

列表接口统一支持 `page/pageSize/keyword/type/sort`。写接口必须验证名称长度、字段白名单、卡片归属和 `version` 乐观锁。

## 5. 数据库实现

### 5.1 表结构

```sql
CREATE TABLE bi_cards (
  id uuid PRIMARY KEY,
  name varchar(100) NOT NULL,
  description varchar(500),
  analysis_type varchar(30) NOT NULL,
  config jsonb NOT NULL,
  schema_version integer NOT NULL,
  version integer NOT NULL DEFAULT 1,
  created_by varchar(64) NOT NULL,
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now(),
  deleted_at timestamptz
);

CREATE TABLE bi_templates (
  id uuid PRIMARY KEY,
  name varchar(100) NOT NULL,
  description varchar(500),
  status varchar(20) NOT NULL DEFAULT 'draft',
  global_filters jsonb NOT NULL,
  version integer NOT NULL DEFAULT 1,
  published_version integer,
  created_by varchar(64) NOT NULL,
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now(),
  deleted_at timestamptz
);

CREATE TABLE bi_template_versions (
  id uuid PRIMARY KEY,
  template_id uuid NOT NULL REFERENCES bi_templates(id),
  version integer NOT NULL,
  snapshot jsonb NOT NULL,
  published_by varchar(64),
  published_at timestamptz,
  UNIQUE(template_id, version)
);

CREATE TABLE bi_template_cards (
  template_id uuid NOT NULL REFERENCES bi_templates(id),
  card_id uuid NOT NULL REFERENCES bi_cards(id),
  position integer NOT NULL,
  layout jsonb NOT NULL,
  overrides jsonb NOT NULL DEFAULT '{}',
  PRIMARY KEY(template_id, card_id)
);

CREATE TABLE bi_query_audit (
  id uuid PRIMARY KEY,
  user_id varchar(64) NOT NULL,
  query_hash varchar(64) NOT NULL,
  source_api_ids jsonb NOT NULL,
  platform_ids jsonb NOT NULL,
  duration_ms integer NOT NULL,
  status varchar(20) NOT NULL,
  error_code varchar(50),
  created_at timestamptz NOT NULL DEFAULT now()
);
```

卡片删除使用软删除；被模板引用时先提示影响范围。模板发布时把完整布局、卡片配置快照写入 `bi_template_versions`，避免卡片后续编辑直接改变已发布看板。

## 6. 前端页面与功能代码

### 6.1 看板中心

路由：`/dashboards`、`/dashboards/:templateId`

组件：

- `DashboardListPage`：模板搜索、状态筛选、更新时间排序、进入看板。
- `DashboardPage`：加载发布版本、顶部固定日期和平台筛选、渲染卡片网格。
- `DashboardToolbar`：日期区间、全部/单平台/多平台、刷新、数据更新时间。
- `CardGrid`：按模板布局渲染，不因图例、Loading 或空数据改变卡片尺寸。
- `DashboardCard`：标题、口径提示、图表、来源、刷新状态和下钻入口。

全局筛选只覆盖 `dateMode/platformMode = global` 的卡片。固定筛选卡片在标题区域展示锁定条件。多个卡片请求应去重并限制并发，切换筛选时取消旧请求。

### 6.2 分析工作台

路由：`/analysis/new`、`/analysis/:cardId/edit`

采用三段式布局，但中间预览区必须占主要宽度：

- 左侧 `AnalysisTypePanel`：指标、趋势、排行、明细、漏斗、留存。
- 中间 `AnalysisCanvas`：实时图表预览、结果摘要、数据表切换。
- 右侧 `ConfigurationPanel`：字段槽位、筛选、排序、图表样式。

创建流程：

1. 选择分析类型。
2. 从字段区拖入指标、维度或事件槽位。
3. 根据能力规则即时隐藏或禁用不兼容字段，并显示原因。
4. 配置平台、日期、排序、Top N 和图表类型。
5. 400ms 防抖后调用 `/analytics/validate`，通过后再请求预览。
6. 预览成功后填写卡片名称并保存；新卡片插入卡片库顶部。

各分析类型规则：

| 类型 | 配置要求 | 可用图表 | 下钻 |
|---|---|---|---|
| 指标 | 1-6 个指标，无维度或仅平台 | 指标组、子弹图 | 指标构成与平台明细 |
| 趋势 | 1-4 个指标，日期必选 | 折线、面积、柱线组合 | 点击日期查看平台/明细 |
| 排行 | 1 个指标 + 1 个排行维度 | 横向条形、棒棒糖 | 点击条目查看来源记录 |
| 明细 | 同一 API 内多个字段 | 表格 | 分页、排序、导出当前结果 |
| 漏斗 | 2-8 个已注册事件 | 阶梯漏斗、转化路径 | 点击步骤查看该事件趋势 |
| 留存 | 留存指标 + cohort 日期 | 留存热力矩阵、曲线 | 点击单元格查看分子分母 |

### 6.3 卡片库

路由：`/cards`

- 顶部突出“创建卡片”，点击进入分析工作台，不在弹窗中塞完整创建器。
- 按类型分组或列表展示，可按关键词、分析类型、指标、更新时间筛选。
- 每张卡片显示真实缩略图；缩略图复用 `ChartRenderer` 的 compact 模式。
- 支持预览、编辑、复制、删除和加入模板。
- 拖动排序只改变当前视图的个人展示顺序；拖到某位置就落在该位置，不追加到底部。
- 删除前查询模板引用，列出受影响模板；确认后软删除。

### 6.4 模板编辑器

路由：`/templates`、`/templates/new`、`/templates/:id/edit`

- 模板中心只负责列表、筛选、新建、复制、删除和进入编辑。
- 编辑模式左侧为可折叠卡片库，中间为完整看板画布，顶部为模板名称、预览、保存、发布。
- 卡片从库拖入画布时按鼠标落点计算目标索引，禁止默认追加到底部。
- 已加入卡片支持拖动排序、宽度切换、局部筛选覆盖、移除。
- 新增卡片默认插到画布第一位；画布内拖动必须保持当前位置语义。
- 保存草稿不影响已发布版本；发布创建不可变版本快照。
- 编辑旧模板时可替换或删除卡片，并提示发布后影响。

### 6.5 数据字典

路由：`/dictionary`

四个 Tab：指标、维度、事件、数据源。每项展示中文名、代码 ID、状态、定义、单位、公式、来源 API、粒度、支持平台、支持分析类型和限制。支持搜索与状态筛选；“待建设”项可见但不能拖入分析工作台。

### 6.6 下钻抽屉

点击任何支持下钻的图表节点后从右侧打开 `DrilldownDrawer`，不得只输出一句自动分析结论。

抽屉内容：当前筛选上下文、节点值、环比/占比、分子分母、平台拆分、时间趋势、关联明细、来源 API 和更新时间。抽屉中的图表节点可继续切换维度，但不做无限层级递归；关闭后保持主看板筛选和滚动位置。

## 7. 图表渲染规范

`ChartRenderer` 仅接收 `CardConfig + AnalyticsResponse`，按 `chart.type` 分派到独立 option builder：

```text
charts/options/buildMetricOption.ts
charts/options/buildTrendOption.ts
charts/options/buildRankingOption.ts
charts/options/buildFunnelOption.ts
charts/options/buildRetentionOption.ts
```

要求：

- 使用统一蓝色主色，但平台对比采用可区分的多色序列，不能全是同一蓝色深浅。
- 图例过多时滚动；最多同时展示 8 个平台。
- Tooltip 同时显示原值、单位、占比/转化率和日期/平台上下文。
- 漏斗使用横向阶梯路径或规整梯形，不使用细小悬浮色块。
- 留存使用带数值的热力矩阵，颜色阈值固定且有图例。
- 排行图至少显示 Top 10 Mock/真实项，不产生大片空白。
- ResizeObserver 驱动 ECharts resize；组件卸载时 dispose。
- ECharts 按需引入图表和组件，解决当前 500 KB 以上主包警告。
- 图表点击统一触发 `onDrilldown({ cardId, dimension, value, filters })`。

## 8. 状态、错误和交互

每张卡片独立处理状态，不能因一张失败阻塞整页：

- 首次加载：骨架屏保持最终高度。
- 后台刷新：保留旧数据，标题区显示刷新状态。
- 空数据：说明当前日期/平台无数据，并提供重置筛选。
- 部分成功：显示成功平台结果，顶部列出失败平台和重试按钮。
- 配置失效：显示具体失效字段，提供进入编辑器修复。
- 登录失效：统一跳转原后台登录或触发既有刷新流程。
- 快速切换：使用 AbortController 取消过时请求，旧请求不得覆盖新结果。
- 保存冲突：版本不一致时提示重新加载，不静默覆盖。

所有按钮、拖动项和图表节点支持键盘焦点；图标按钮提供中文 tooltip；抽屉和对话框支持 Esc 关闭并正确恢复焦点。

## 9. 真实数据功能映射

| 功能 | 数据来源 | 输出 | 必须明确的限制 |
|---|---|---|---|
| 经营总览 | `pDaySum`、`pRealDayLine`、`getAllByRole` | 核心指标、趋势、平台贡献 | 跨接口计算前校验日期和平台粒度 |
| 平台对比 | `pDaySum`、`getAllByRole` | 最多 8 平台规模、收入、观看、付费 | 比率用分子分母加权 |
| 视频分析 | `todayVideos/*`、`videoWatchRanking` | 分类贡献、视频排行、明细 | 不支持位置分析 |
| 搜索分析 | `hotSearchWords/getMany` | 热词排行、搜索词表 | 不推断搜索转化 |
| 支付分析 | `getEventStats`、`pDaySum` | 三步汇总漏斗、支付趋势、收入 | 非用户级有序漏斗，无渠道/套餐拆分 |
| 观看深度 | `pRealDayLine`、`todayUsers/getMany` | 观影率、人均观影时长、用户明细 | 人均使用时长需总使用时长字段 |
| 留存分析 | 留存关系 API | Cohort 热力图、D1/D3/D7 | 必须用 cohort 分子分母加权 |
| 渠道分析 | 渠道统计 API | 渠道贡献、趋势、A/B | 渠道与内容平台维度分开 |

接口没有提供的历史会员总数、当前会员总数、各档会员人数/收入、页面曝光/点击、视频位置曝光/点击统一标记“待建设”，禁止使用 Mock 冒充生产数据。

## 10. 测试代码要求

测试不是后续阶段，而是每项功能的交付组成。使用 Bun 执行单元/集成测试，Playwright 执行 E2E。

### 10.1 单元测试

- 每个 Adapter：正常、空数组、字符串数字、缺字段、错误响应、分页。
- 聚合器：sum、latest、weightedRatio、分母为 0、多平台、跨日期。
- 能力校验：合法组合与所有禁止组合。
- 配置迁移：旧 Demo localStorage 配置导入或明确拒绝。
- 图表 option builder：列映射、单位格式、空值、8 平台图例。
- 覆盖率：核心服务和纯函数行覆盖率不低于 90%，项目整体不低于 80%。

### 10.2 集成测试

- 使用 Mock Server 模拟全部 30 个上游接口，而不是在业务代码中返回 Mock。
- 验证鉴权透传、超时、重试、Schema 漂移、缓存、请求合并和部分平台失败。
- Cards/Templates CRUD、乐观锁、软删除、发布快照和数据库事务。
- `/analytics/query` 的参数白名单、错误码和审计记录。

### 10.3 E2E

必须自动化覆盖：

1. 打开看板，切换日期、全部平台、单平台和 8 平台对比。
2. 创建趋势卡片，拖入指标和维度，看到预览，保存后出现在卡片库首位。
3. 创建固定三步支付漏斗并验证不支持的事件不可保存。
4. 从卡片库拖入模板指定位置，排序、保存、刷新后位置不变。
5. 发布模板，打开看板看到发布快照而非未发布修改。
6. 点击图表节点打开右侧下钻抽屉并显示对应上下文。
7. 上游一个平台失败时，其余平台仍显示。
8. 1440x900、1920x1080、1366x768 三种桌面视口无横向溢出、遮挡和大块异常空白。

## 11. 环境与部署代码

服务端环境变量：

```dotenv
NODE_ENV=production
PORT=3000
UPSTREAM_API_BASE_URL=
DATABASE_URL=
REDIS_URL=
LOG_LEVEL=info
REQUEST_TIMEOUT_MS=10000
MAX_PLATFORM_CONCURRENCY=4
```

`.env.example` 只保留键名，不放真实凭证。Docker Compose 包含 `web/server/postgres/redis/nginx`，各服务设置健康检查。Nginx 仅暴露 80/443，将 `/api/bi` 代理到 server，其余路径返回前端 SPA。

上线脚本必须完成：构建镜像、数据库迁移、健康检查、冒烟测试、切换容器；失败则恢复上一镜像。PostgreSQL 每日备份，至少保留 7 天。日志按天轮转，不记录凭证和完整响应体。

## 12. 一次性交付验收清单

以下项目全部满足才算完成，不以某个页面可演示作为中间交付：

- [ ] 五个工作区全部连接真实 BI 服务，生产构建不引用 `mockEngine.ts`。
- [ ] 已有 30 个 API 均有定义、Schema、Adapter 或明确“不进入 BI”的代码标记。
- [ ] 所有可用指标、维度、事件均来自注册表，禁用能力无法绕过前端保存。
- [ ] 全部平台、单平台、最多 8 平台对比的聚合结果核对正确。
- [ ] 指标、趋势、排行、明细、漏斗、留存六类卡片均可创建、预览、保存和编辑。
- [ ] 卡片库支持搜索、分类筛选、真实图表缩略图、复制和删除。
- [ ] 模板支持指定位置拖入、排序、移除、保存草稿、预览、发布和版本快照。
- [ ] 看板全局筛选、卡片独立筛选、刷新、部分失败和下钻抽屉可用。
- [ ] 指标口径、来源、粒度、限制和更新时间均可追溯。
- [ ] 数据库迁移、缓存、日志、审计、备份、健康检查和回滚可用。
- [ ] 单元、集成、E2E、类型检查和生产构建全部通过，整体覆盖率不低于 80%。
- [ ] VPS `/opt/config-driven-bi-demo` 部署完成，HTTPS 地址可访问。

## 13. 开发时直接执行的工作清单

这不是阶段划分，而是一次实现中必须全部提交的代码集合：

1. 将当前工程改造成 `apps/web + apps/server + packages/contracts` 工作区。
2. 拆分现有 `main.tsx`，迁移现有视觉、交互、图表和配置，不从零重画 UI。
3. 增加 Fastify BI 服务、Zod 合同、PostgreSQL migration 和 Redis 缓存。
4. 为 30 个后台 API 编写定义、Schema、Adapter 和 fixture 测试。
5. 实现指标/维度/事件/能力注册表与查询编排器。
6. 将五个工作区全部切换为真实 BI 接口，并保留可控的开发 Mock Server。
7. 完成卡片和模板持久化、发布快照、拖动布局和乐观锁。
8. 完成图表统一渲染、下钻抽屉、状态处理和无障碍交互。
9. 完成全部单元、集成和 E2E 测试及覆盖率门槛。
10. 完成 Docker、Nginx、数据库迁移、备份、监控、部署和回滚脚本。

实际编码前只需要补齐无法从代码或接口文档推断的外部参数：测试/生产 API 地址、鉴权获取方式、完整平台 `pid` 字典、生产域名。它们只影响环境配置和真实联调，不改变本文功能结构。
