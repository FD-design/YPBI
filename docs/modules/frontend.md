# 前端功能模块

## 团队账号管理（2026-09-16）

入口：顶部“账号管理”，仅 maintainer 可见；普通账号直接访问 `/admin/accounts` 显示无权访问。列表支持账号/姓名搜索及20项分页，显示角色、状态、首次改密提示和全部平台范围。管理员使用独立原生modal弹窗创建账号、调整角色、启停或重置临时密码；取消不会保存，提交中禁止重复提交，错误保留操作上下文但清空密码。小屏表格横向滚动，弹窗限制视口尺寸。旧账号和登录库不迁移。

## 2026-09-16 提交版回归

`extendedDailyMetricModel` 先读取统一模型状态，仅对可用的主题卡转换日值；M097 演示失败不再被主题分支覆盖。失败可重试，固定批次未成熟、末日缺值及历史趋势保持各自状态，正式接口与指标口径不变。模型回归覆盖三种日期范围及相邻指标。

浏览器用例按已确认的末日日值、D−1 / D−7、独立数据表和轻量诊断组件核对日期、数值、涨跌颜色、空值、计算依据和导出。`qa-v2-connected-dashboards.spec.ts` 从 PATH 使用 Bun，不依赖私有本地安装目录。提交验收和日志见[部署模块](deployment.md#原多平台-bi-代码审核交接2026-09-16)。

## 2026-09-14 全局导航使用范围精简

全局导航按各 Tab 的整体表现展示点击人数、点击次数、渗透率和使用者人均点击，保留日期、两期比较、单 Tab 趋势、逐日核对及导出。导航移除客户端与新老用户选择、交叉结构及对应导出页；旧链接保留 Tab 与指标并归一总体。单功能使用分析继续保留原有维度。下方历史版本中的导航分组说明以本节当前范围为准。

## 2026-09-14 内容连续分区与共享滚动边界（2.33）

`ContentDiscoveryBoard` 复用现有卡片、来源分析与表格，按视频发现与互动、搜索需求与承接、作者与社区连续排列。`ExtendedBoardPreview` 只装配该专用分区，移除旧循环里的散落插入与末尾重复追加；全部 13 指标和原全量导出保留。引导文案仍由 PRD 5.16 生成。

共享 `ui-search` 建立定位上下文，约束内部读屏标签的绝对定位，修复内层看板滚动正常但外层页面被撑大、继续滚动出现灰白区域的问题。版本、指标目录及样例库搜索共同生效，不改读屏或输入能力。真实金额展示由响应中的元/元每人传递到卡片、计算依据、结构图和导出，隔离演示 USD 保留。实施与实测见调整方案 9.27。

5.12 播放阶段的 M101 摘要、详情及导出读取真实候选，M097/M031 保留原演示；混合表单独保留逐指标来源说明。M098 的可读时长支持分钟/人，转换后保留 `/人`。`CalculationEvidence` 只展示来源公式和实际输入，不再拼接忽略单位换算的重复数字等式；经营表头、完整表和下载使用响应的候选定义及 `sourceNote`。

## 2026-09-14 版本表现客户端维度（2.32）

版本分布、核心表现、变化趋势及导出仅保留版本与客户端，总体独立计算；同名版本单行展示，Android / iOS 控制显隐。复用 AnalysisScopeControls、PivotTable、CalculationEvidence、PaginatedTable；核心格聚焦摘要与趋势，搜索和完整表保留全部版本，固定注册和存量批次不变。详情和导出复用同源工作表投影，旧人群链接归一总体、精确实体旧链接保留。分布投影集中在 versionDistributionByName，图表组件 VersionDistributionPanel 控制 Top12 与主表宽高；结果缺失不补零。其他看板及正式数据接口不变。验证见调整方案 9.26。

## 2026-09-14 摘要、计算表与信息层级（2.31）

共享日日期显示采用 `MetricReadingContext`；完整日期仍保留在范围、提示和导出。`live-period-statistics` 只投影确切查询范围的服务端统计，不在浏览器聚合。`CalculationColumns` 和导出共用稳定输入身份判定，单指标显示实名分子 / 分母列，混合输入保留逐行身份。

分组趋势提示按当前日期与总体、分组值与占比、对比期三个层次组织；复杂结构分析独占一行，不拉伸普通卡片。有效观影留存按摘要、批次明细、用户去向顺序阅读。具体实施及实测记录见调整方案 9.25。

## 2026-09-13 日值阅读、轻量诊断与分组显隐（2.30）

普通单指标卡复用 `DashboardMetricCard` / `MetricSummary`，由 `daily-reading-model` 统一末日值、两项日基准及合法周期辅助统计。`daily-reference-plan` 在已启用比较且原查询未覆盖基准日时，复用既有逐日接口补读所需日期；`LiveDashboardContext` 校验平台、指标、日期和请求状态。四类演示主卡使用对应日值投影，原周期结构图、经营核对与结算模型保留。共享导出按主值、日比较、周期辅助统计、逐日结果分别标注，末日缺失仍可查看历史，不回退主值。

`CompactMetricReading` 为落地页原六项阶段/关系提供小趋势、两日变化与就地详情；日期轴紧凑显示，完整日期及区间比较在提示/详情核对。播放阶段与支付日报使用同一组件的纯链接变体，不重复铺设趋势图；支付保持所选支付方式及日值、周期核对的独立语义。紧凑关系不重复上方卡片的周期统计；缺失、刷新失败和孤立有效点保持可辨。

`AnalysisScopeControls` 集中管理总体/分组与客户端、人群显隐，精确分组集合与查询条件分离；轴开关批量切换，图例单组切换，不把剩余组重新扩成笛卡尔积。版本、活跃、消费、单功能和单导航复用；交叉表突出结果，计算依据按需打开，业务核对输入列和完整导出保留。

真实周期汇总和交叉数据缺口保持待支持，版本事实继续为隔离演示；正式指标、API、数据库、账号与Token不变。实施验收、真实能力边界和体验路径统一见调整方案9.24。本地构建继续执行生产演示隔离校验，本批未部署生产。

## 2026-09-13 集中多维分析与全局视频点击来源（2.28）

共享 `AnalysisScopeControls` 统一客户端与用户人群、依赖版本及恢复总体；`PivotTable` 保持纯展示，增加受控单元格点选和键盘入口，不计算总值。版本、单项功能、全局导航、活跃结构和消费结构复用；摘要、计算输入和趋势使用同一范围，完整对照表与导出不被单格点选裁剪。原获客类型、渠道、50项功能目录、最多5项功能趋势与固定留存人群保持。

`TopicScopeAnalysis` 按已连接指标选择独立总体或既有单维系列；两维缺少独立结果时显示待接入，真实失败或结束日缺值保留状态与已有历史，不回填演示。`horizontal-bar-reading` 统一当前横条端点值，时间趋势不铺满标签。`ExtendedBoardPreview` 用连续区块流统一表格和卡片间距。原M109区扩展全局视频点击来源，保留稳定页面/列表/Tab上下文、全局日趋势及原视频首页分类细分。

`content-discovery-analysis-model` 为全局来源与首页分类提供同一演示点击事实；分类保留独立 ID，摘要、逐日、图表和导出从对应范围聚合。正式分类可加总性与来源映射仍按 PRD 5.13 / 5.18 的准入边界核对，不将演示分类归属当作接口能力。

本批没有修改正式指标、事件、API、数据库、账号或Token。测试、截图复查与正常体验入口统一记录在调整方案9.22；新增版本、导航、功能和全局来源事实仍为隔离演示，开发缺口见数据与平台模块。

## 2026-09-13 版本人群与诊断阅读（2.27，本地回归通过，待用户验收）

沿用原工作台、共享表格、卡片、趋势和导出组件，本批不改变正式指标、API、数据库、账号或 Token。

- `VersionPerformancePreview` 保留版本分布、核心表现、变化趋势；核心精确表并列总体、新用户和老用户，摘要与趋势支持人群选择。Android / iOS 分开，比较版本的拆解绑定实际选中版本；M020 / M024 维持各自固定人群，未知身份不分配为新老用户。
- `ExtendedBoardPreview` 的启动诊断突出 M080，M091 使用紧凑辅助摘要并保留趋势、详情和导出；启动与播放两板移除网络筛选和网络结果，底层事件分析与连接错误处理保持不变。
- `FunctionUsagePreview` 用常驻表格和行内条形呈现原 50 项功能，保留原所选功能趋势与结构；`GlobalNavigationUsage` 独立展示导航点击用户、点击次数、兼容活跃分母、渗透与使用者人均，支持客户端 / 新老拆解、完整逐日表和导出。
- `ContentDiscoveryAnalysis` 把 M044 总体曝光、匹配点击、转化与位置精确表放在同一区域；`SearchDemandAnalysis` 同表展示搜索规模与承接，图形只负责比较，不替代全量表。播放与玩法诊断表就近展示合法计算输入，公式集中说明。
- 注册 D3 / D30 使用紧凑摘要，`DashboardMetricCard` 显式保留紧凑详情入口；支付方式新增成功次数、拉单次数与成功率核对表，真实结果使用同一查询的基数与实际日期，缺失不回填演示。

新增版本人群、导航与内容位置事实继续隔离演示；真实接入范围仍引用数据模块。逐项回归结果、待支持项与体验入口统一记录于调整方案 9.21，业务验数和生产部署独立验收。

## 2026-09-13 获客、观影时长与启动版本阅读结构（已实现并通过本地验收）

本批按总 PRD 2.26 在原 11 板内调整三个阅读位置，不新增看板或改变公共查询：

- 获客与新增的原“增长效果 / 渠道结算”保留。增长效果内将来源渠道、下载目标、获客类型作为三个纵向同时可见的独立区块，各自管理状态、排序、分页和导出；来源渠道和渠道结算保留搜索，下载目标 / 获客类型仅在结果超过 8 项或旧链接恢复非空搜索时显示本区搜索，少量项不占搜索行。来源渠道保留原四指标组，下载目标用表格核对量与已登记转化，获客类型只做紧凑数值比较，重叠人群不画构成。
- 视频消费表现保留首屏总时长、人均时长两卡及原趋势；在原时长位置先显示总体摘要，再用紧凑行比较长视频、短视频、其他 / 未分类。类型数据不可信时只显示未接状态，不画占比、不用总体相减补类。
- 产品体验与启动质量把启动诊断和版本表现上下顺排并同时呈现。启动诊断标题下直接显示原两卡，不重复“启动核心”标题；原结构诊断、导航及版本三段标题保留。版本分布在宽屏同时展示图和精简表，窄屏上下排列；精简表、原 8 列完整表与导出使用同一完整行集。

三处均按分析问题选用表格、紧凑数值或图表，不为丰富页面重复原趋势。正式指标、API、数据库、Token、账号权限、真实数据准入和生产部署不变。

最终验证按批次保留而不重复相加：37 项独立 Chromium 目标回归及额外 4 项最新版版本专项通过；另有 38 项已登录原板结构 / 相邻质量和 23 项获客 Chrome 分别通过。五个相关模型文件共 41 项、775 次断言和 45 项共享 UI 通过。`npm run typecheck`、前端构建、生产演示隔离和唯一引导投影检查通过。获客、观影时长、启动首屏及版本图表的 1280 / 1024 / 390 截图已实际复查；系统 Chrome 启动环境异常通过独立临时 Chromium 测试浏览器完成回归，产品代码没有加入兼容补丁。真实上游业务值尚未验数，生产未部署。

## 2026-09-12 11 板区块引导与四项分析增强（已实现并通过回归）

`DashboardSectionHeading` / `DashboardPanel` 保留各页既有区块标题，以稳定文案键从 `src/v2/generated/dashboard-guidance.json` 读取总 PRD 5.16 生成的一句分析说明。标题保持 16px，说明为 13px 中灰；文案键不对用户显示，窄屏换行不缩字。页面不维护可独立修改的说明副本，父子区块和逐指标不重复说明。

- 获客与新增：保留原首屏 6 张指标卡和首次激活 3 卡；原下方重复趋势收敛为紧凑阶段关系，阶段在上、关系就近在下且全部位于正常文档流，不增加 Tab、绝对定位或压边。`ChannelQuality` 在原增长效果的来源渠道表内提供四指标组，保留搜索、排序、分页、完整明细、导出和原渠道结算。渠道 19 项当前是明确的隔离演示，不改写已有真实切片。
- 活跃与留存：在原板内增加整体活跃次日回访的独立待支持说明，本批不显示数字、不拼 M020+M024，M024 注册满 2 天限制不变。
- 视频消费表现：`WatchingRetentionDiagnosis` 保留原有效观影留存，增加次日 / 第3日 / 第7日的观影后三去向、明细和导出。三去向从同一成熟基数派生的模型仅用于交互验证，正式结果等待服务端用户跨日加工与验数。
- 产品体验与启动质量：`VersionPerformancePreview` 已具备版本分布、核心表现、变化趋势及 M091 启动次数 / M016 登录活跃用户分布切换。仅 Android / iOS，同版本字符不跨平台合并；M016 按行为发生版本分组，用户当日升级可进多组，占比用独立同端 DAU 分母且不强制归一。仅 M020 和 M024 / 待登记整体回访执行起点版本冻结。版本 9 项当前均为隔离演示；启动诊断与版本表现的当前页面编排以上一节 2.26 规则为准。

该实现不增加新看板路由、不删原区块或操作，也不改指标、事件、API、数据库、账号或生产隔离。57 项引导投影和 2 项生成器测试通过；174 个独立 Chrome 用例全部通过，覆盖基础工作台、专题、获客、已登录读取、生产隔离、区块引导、观影去向、相邻能力、版本表现及 1280 / 1024 / 390 三档视口。最终 `verify:release` 以 0 退出，539 项 `src` / `server` 测试共 4524 次断言、类型检查、前后端构建和生产演示隔离全部通过。已登录读取路径自动回归使用受控服务与接口夹具，不代表线上业务值验数。

## 2026-09-12 原版看板真实数据接入

已登录本地`/dashboards/public`由`DailyDashboardPage`装配原工作台，`ConnectedBoard`复用原核心/获客/活跃/视频/商业/功能看板渲染器；不使用替代卡片列表。保留原区块、图表类型、网格、尺寸、工具栏和个人工作区操作，查询控件沿用原位置与响应式样式。

`LiveDashboardContext`只向精确匹配的指标与范围注入真实快照；已连接的经营列、注册留存、端别人群结构、获客来源、广告切片及支付组成复用原渲染器；剩余单元与维度明确隔离为演示。真实字段为空或请求失败不改用演示。复合输入、主值、趋势点、对比、明细和导出同源；结束日缺值仍可读其他日期和有值的对比期，长日期缩放保留。当前覆盖以[数据支持清单](./data-platform.md#看板数据支持清单2026-09-12)为准。

真实查询每期独立重试、校验PID/日期/映射及迟到响应；刷新失败标注旧真实结果。原“下载演示XLSX”保留，另按现有导出权限提供“下载真实XLSX”；混合支付导出按工作表分开标记。公共/我的概览在同一工作台切换，本地内存对象与真实账号身份分离，不新增服务端对象写入或持久化。

回归入口为`qa-v2-connected-dashboards.spec.ts`和共享阅读/查询测试：逐页比较原设计入口与真实入口的区块、卡片顺序和宽度；验证按钮实际可点、当前/对比快照、失败恢复、真实0、空结束日、权限、收藏、个人对象及三档视口。生产构建仍排除DEV装配；本批只在独立本地运行。

## 2026-09-12 看板工作台与连接流程

公共/我的概览共用`DashboardDirectoryPreview`，同一目录容器按范围切换内容，当前对象、目录宽度和浏览器历史保持一致；个人对象仍只存本次体验内存。`ReviewTools`默认不渲染正文说明，账号菜单可打开完整图表体验和页内正常/组合异常状态；入口及样式仅在DEV加载。经典版顶栏入口已移除，遗留实现保留。

`RetentionMatrix`按36px行高呈现完整日期，表内滚动、固定头部/日期列；同口径表上方集中公式及周期定义，逐行输入/结果/窗口保留。`MenuSelect`在8项以内无搜索，诊断维度统一完整菜单；功能图提示不显示内部页面编码。回归覆盖39/90/180日及1280/1024/390。

本地5173已代理独立3000后端；从看板进入维护必须切换真实身份，登录、首次改密和二次认证后显示两站表单。全部已登录角色有共享Token维护能力；未完成改密、匿名和失效账号仍拒绝。旧后台404显示具体接口缺失原因及DEV返回入口，不能用演示身份绕过。真实账号流程见`qa-v2-local-connection.spec.ts`；目标、权限与运行边界引用总PRD11、调整方案9.17及部署模块。

## 2026-09-11 共享样式与阅读路径修复（本地待验收）
目标与状态引用总PRD4.3/5.6及调整方案9.16。同排卡片按最大内容高度对齐，计算入口收为一行；留存工具分组、图例位置及放大状态一致。共享搜索基础样式和阅读容器网格由唯一组件样式维护，直接访问与刷新不依赖其他页面。

DEV标题用`DataOrigin`明确演示来源；支付卡、方式比较、计算表及原始精度下载共用交易/尝试事实。指标速览增加有标识的完整示例、双列日周变化、配置顺序、权威定义和带指标/平台/日期的内存分析草稿。预览顶栏“连接真实数据”指向受保护路由，不保留演示身份。独立本地账号与维护流程已可用，两站Token已连通；本地看板部分真实查询已接入，逐指标验数仍待完成。

## 2026-09-11 经营阅读与数据源维护（本地实现，待视觉及真实数据验收）

目标规则见总PRD4.3、5.2.1、5.7.7、5.8～5.13、11、15.4；实施与数据缺口见调整方案9.15。DEV看板接入以下共享能力，正式业务查询未开放；受保护的数据源维护复用现有真实API。

| 位置 | 当前实现与入口 |
|---|---|
| 公共概览、我的概览查看态 | `DashboardCardMode`与`PreviewBoardPresentation`按看板记录本机纯数值/数值＋趋势偏好；普通单指标卡保留完整表，固定摘要和专用图保持原状；分析页、速览及编辑态不扩展 |
| 活跃与留存、视频消费、完整体验 | `RetentionMatrix`提供全日期热力概览、趋势、原矩阵表、完整计算明细和放大；率/人数各自统一色阶，未成熟断线；7/30日与自定义共用查询范围 |
| 少量单维分组 | `GroupedTrend`接收独立总体、分组、原总体占比及双期计算输入；总体/分组对比显式切换，分组至少一项，图/表联动；不合计人数、比例或人均 |
| 会员与付费经营 | `PaymentBreakdowns`宽屏构成与摘要并排，表格增加总体；金额/人数/ARPPU趋势与展示范围分别控制，ARPPU表直接展示双期实名输入；额外通用付费维度收起保留 |
| 支付链路 | `PaymentOrderAnalysis`按日报语义展示九项业务指标和总体/支付宝/微信；DEV显示有标识的同源演示数据；正式查询缺口保持待接入，同批转化不由日报拼接，辅助指标保留共享卡 |
| 核心经营总览 | `AdvertisingOverview`新增广告四项单日摘要、总体/新用户和共用日趋势，不扩展首屏九项契约，不继承经营明细局部PID |
| 获客与新增 | `AcquisitionVisuals`提供下载目标次数/占比、日趋势、阶段关系、计算依据及原明细定位；已登记来源只作比较，未确认的买量/裂变不纳入正式构成 |
| 共用计算与导出 | `CalculationEvidence`以紧凑入口及完整详情承载实名输入和公式，`CalculationColumns`保留完整表；当前/对比各取自身输入，缺失不反算。支付演示导出保留原始精度及逐日计算输入，正式下载准入不变 |
| 视频、播放、内容发现与功能 | 总/长/短时长表保留类型状态，M098沿用现有定义；播放质量加入同源M036，指标与完整维度分别控制；搜索/互动保留横向排行和精确表，功能趋势提升图例及提示可读性 |
| 数据源维护 | `DataSourceMaintenancePage`增加三步接入说明及指标中心入口；现有双站候选验证、服务器保存、失败保旧不变，保存确认取消保留输入，不在浏览器存储Token |
| 共用布局与说明 | 纯数值卡移除趋势高度、混合网格重排；区块标题和内层标题从同一CSS定义层级；`DashboardDescription`读取当前指标清单，`useDialogBackdrop`统一只读遮罩关闭与拖动保护 |

经营明细保留73业务列及完整导出；共享单位处理与表格定位修复继续生效。全局阅读容器下的页面也铺满工作区，不恢复固定最大宽度。完整图表体验复用业务组件，不另建不同口径的演示实现。

看板保留原结构；已连接区域显示真实日值及“待验数”，其余区域显示演示数据。指标/事件权威源、业务对象持久化及生产部署未变；独立本地身份和维护权限按9.17生效。两站Token已连接，日汇总真实抽查见数据与平台模块；本地部分真实读取及逐模块覆盖见本页首节和数据支持清单。导航广告IP身份、类型时长基数、位置上下文和支付查询映射继续逐项验数。回归入口为`qa-v2-review-feedback.spec.js`、`qa-v2-operations-redesign.spec.js`、`qa-v2-data-sources.spec.js`及既有专题、功能、共享查询与导航用例。

## 2026-09-10 个人工作区本地交互

`PersonalWorkspacePreview`接入我的概览、指标速览、指标分析、事件分析、漏斗分析和已保存分析，使用`?design=personal-workspace`精确DEV白名单。公共概览与顶栏共用入口，正式身份和技术查询路径保持原状。`personal-workspace-model.ts`只管理当前页面生命周期内的配置、稳定引用与修订，刷新后对象失效，不写本机存储、旧workspace或账号库。

`AnalysisQueryBuilder`统一顶部折叠配置、搜索定义选择、统计方式、别名、局部/全局条件、共同分组与时间粒度；漏斗配置由`FunnelQueryBuilder`按独立语义提供。`PersonalWorkspacePreview`管理草稿、已应用快照、过期保护、主分析项、日期展开、继续拆解和返回。图形切换不改变结果；完整聚合表与导出不受Top10和缩放裁剪。共享摘要、图表、图例、日期缩放、分页与页头继续复用。桌面编辑，平板/手机阅读查询。

`personal-query-preview.ts`为DEV查询分发入口：指标复用9项核心样例；`event-analysis-preview.ts`在合成事件事实上过滤、分组、统计属性及独立去重；`analysis-presentation.ts`限制构成图和二维展示。`funnel-analysis-preview.ts`使用固定独立事实流计算有序序列，`FunnelAnalysisPreview`展示转化、流失、耗时和导出。正式定义不在UI重写，未接入能力明确保留状态。完整实施与剩余边界以调整方案9.11～9.13为准。

回归入口：`qa-v2-analysis-rebuild.spec.js`、`qa-v2-personal-workspace.spec.js`、`event-analysis-preview.test.ts`、`personal-workspace-model.test.ts`，以及目录、专题、经营宽表和共享UI套件。截图与合成XLSX位于`/private/tmp/ypbi-analysis-*`、`/private/tmp/ypbi-funnel-rebuild*`、`/private/tmp/ypbi-personal-*`。生产构建校验继续隔离全部DEV组件。

## 2026-09-11 经营明细完整字段与纯数值卡

经营明细阅读与滚动的补充目标已确认，尚未实施。目标见总PRD15.4，复现事实、实施入口及回归补项见调整方案9.14与UI-036/041；下述已有实现不包含本次补充优化。

DashboardMetricCard的value变体复用名称、数值、单位、说明及状态模型，不生成趋势区域。经营明细默认6张、展开10张卡；完整体验复用10项并保留独立零值样例，原首屏9张趋势卡不变。局部平台默认全部，只联动本区摘要、宽表和导出；单平台卡表同源，大盘不相加PID人数。

operating-detail-columns.ts为唯一展示清单：日报58业务字段及15个既有额外列，区分基础指标、切片和周期；表头、搜索定位、详情、摘要与导出字段说明同源。模式/推广状态是元数据；自然月和注册留存目标日独立保留，缺失不补值。定位只滚动并突出列头，摘要收起不裁剪导出。详情复用MetricReadingDialog的只读关闭和焦点规则。

operating-breakdown-preview.ts按PRD15.4限定拆分，活跃、观影及注册次留遵循原规则；新增付费/广告摘要只提供已登记的新用户视图，M061新用户引用M064。无数据不画假条，未成熟优先；完整体验中的数值仅为明确标识的独立合成样例。正式接口、适配及验数不变，独立日期、多日明细及自由显隐仍待实施。回归入口：operating-detail-snapshot.test.ts、qa-v2-operating-detail.spec.js及共享UI/全局条件/原九卡套件。

目录页面的布局由`ProductApp`按路由归属启用：仅看板工作台使用贴边独立滚动；指标、事件目录和完整图表页保留标准页边距与文档滚动。1100px及以下分类以选择器收纳，结果宽表在内部滚动。`qa-v2-catalog-centers.spec.js`包含从看板站内进入两个目录、返回/前进/刷新及390～1920px边界检查，不只验证直接加载。

## 2026-09-10 全局查询、收藏分组与目录宽度

`preview-query.ts`校验并保存本地会话已应用日期、比较和受支持范围；`usePreviewQuery`接入全部11张看板，显式链接/历史优先，默认不对比。草稿、局部留存锚点、搜索、排序和图例选择不写入公共条件。会话存储受限时保留本页历史与内存；超出活跃/视频看板180日范围时由工作台提示修正，不静默截断或回退日期。

`DashboardDirectory`在公共概览目录顶部展示我的收藏分组；收藏复用原localStorage键，搜索对同一对象去重，取消只移除快捷关系，业务分类中的原看板保留。顶部导航不再列独立收藏入口，旧正式地址重定向到公共概览。`catalog-layout.css`同时适配指标与事件目录，填满主区可用宽度，长表只在表内横向滚动。

回归入口为`qa-v2-global-query.spec.js`、`preview-query.test.ts`及原导航/日期/目录回归组。正式接口、身份、数据库与数据定义未修改；新增个人对象和事件查询尚待调整方案9.11所列实施批次确认。

## 2026-09-10 全部官方看板与权威目录本地体验

本地工作台 `?design=dashboard-center` 已装配 11 张官方看板。`ExtendedBoardPreview` 覆盖 5.10–5.15；`FunctionUsagePreview` 覆盖 5.5，并由 `tools/generate-event-preview.mjs` 从权威页面字典生成 50 项功能目录。功能使用复用模块通用渗透率模板，只使用 DEV 引用，不新增正式指标 ID；主比较保留全集，最多 5 项限制只作用于主动选择的近 7 / 30 日趋势。单选提供平台、新老用户、获客类型及真实表形的二维交叉阅读，0 与不可用状态分开。

`CatalogCenterPreview` 在 `/data/metrics?design=catalog-center` 与 `/data/events?design=catalog-center` 展示 101 项指标和 101 项事件的权威只读投影；共用分类、搜索、分页与详情样式。事件的实施、采集、查询、验数和使用位置未接入时分别说明，不从定义登记推断上线或无引用。两个生成器和 DEV 白名单由构建检查，演示代码不能进入正式包。

所有看板通过 `DashboardHeader` 统一面包屑、标题、说明按钮与工具栏换行；说明使用无边框图标及同一持续阅读层。结构 / 排行默认图形，完整表通过 `ChartDataTable` 打开；原生二维结果由 `PivotTable` 展示，留存保持矩阵。只读窗口保留标题栏一个关闭入口，导出窗口底部仅保留下载动作；Esc、焦点恢复及正文键盘滚动走共享实现。详细卡元信息保持一行并提供完整文本，卡内图表对齐；共同查询条件在表头，分页内边距和区域滚动由 `PaginatedTable` 负责。

核心、获客及新增六张专题支持合法范围内的单日、整月和最多 366 日演示查询；活跃留存 / 视频消费维持 180 日上限并分别保留业务日期与用户起始日期。`TrendRange` 提供真实拖动端点、整体平移、键盘调整与复位，只改变绘图窗口，不改变完整表、导出或查询摘要。核心收藏、视图复制、刷新 / 重试和演示导出已使用实际本地状态；未支持的业务范围与比较方式明确禁用。正式业务数据、水位、接口、权限、存储和旧 BI 未修改。

正式账号下的个人看板写入、指标速览持久化、跨设备收藏及正式事件状态接口尚未实现。其API、权限、持久化和迁移按工程门槛分批确认；本地内存配置、收藏、合成查询和工作簿不是正式替代方案。个人工作区的已实施本地交互见本文件首节，完整目标与剩余范围见调整方案9.11。

## 2026-09-10 首批专题本地预览与共享日期反馈

`board=5.8` / `board=5.9` 分别装配活跃与留存、视频消费表现，入口限 DEV 工作台。`TopicPreviews` 复用指标卡、日期、同口径表、说明窗口、分页和导出；`topic-preview-fixtures` 只提供明确标注的合成布局观察值，`topic-preview-export` 投影相同状态与完整行。业务日、注册 / 有效观影 D0、失败 D0 分开保存；内容 Top10 不裁剪 126 行完整排行。多日 M098 / M035 / M043 保持待准入，不生成时间去重公式。正式 PID 适用范围、注册留存局部分组与消费二维结果、内容分析映射仍待数据侧接入，不以演示样例证明可用。

`DateRangePicker` 的最小宽度由内容决定，展开态绑定 `aria-expanded`，首端点清除旧悬停日期。核心工具栏列宽同步新顺序，移除移动端旧行号规则。自检入口：`qa-v2-date-control.spec.js`、`qa-v2-topics.spec.js`、`topic-preview.test.ts`；共享目录、阅读与周期继续使用原回归组。生产隔离检查增加专题标记；无正式 API、数据库、指标字典或部署变更。

## 2026-09-09 完整两级 Product Shell、指标目录、核心总览基线、M016 样板与数据源维护切片（本地代码完成，待生产接入）

本节只镜像当前代码已经实现的 Product Shell、YPBI 自有账号、完整指标定义目录、M016 纵向样板和受保护的数据源维护页，不代表生产验收通过，也不把下方旧界面能力列为目标。身份链路已形成可部署的本地实现，但仍受真实 PostgreSQL、HTTPS / 可信代理和生产黑盒未验证限制；M016 仍待验数。

### 已实现范围

目录收藏星标靠右，指标标题移除装饰箭头但保持链接行为；获客表标题、页签、筛选共用无重复基线的工具区。同口径表要求提供导出动作，获客单指标与整板逐日结果共用 `metricTrendRows`，保留实际日期、精度、单位和状态，拒绝未覆盖条件；正式结果表提供禁用下载及准确服务状态。嵌套导出窗口同步关闭并恢复焦点，Esc 不穿透到底层。日期布局由 `qa-v2-periods.spec.js` 用真实共享组件测试 1 / 7 / 30 / 90 / 366 天、1440 / 390 宽度，合成数据仅用于浏览器测试；长区间正式查询能力未新增。轻量动效已接入共享 V2 样式；时间趋势默认折线、类别比较默认柱状，由 `contracts/chart-presentation.ts` 提供；详细卡和体验页复用趋势 / 表格切换、固定阅读高度、独立滚动和共享分页。独立“对比”页签已移除，原周期比较保持。

桌面目录控制固定在侧栏底部，展开为图标加文字，收起为 32px 窄轨道内的展开图标；目录内容隐藏而不卸载，搜索、分类、焦点和目录滚动保持。正文不再增加“展开目录”首行；移动端继续使用模态目录。目录宽度一次切换到最终尺寸，仅内容进入使用共享轻量动效，不逐帧重绘图表。

看板正文铺满目录右侧宽度，标题右侧查询根据可用宽度收纳次要筛选。`ChartDataTable` 统一承载获客详细卡和完整体验页的同口径表，打开不撑高邻卡；`ui-chart-surface` 统一管理悬停细边框，`ui-search` 管理组合搜索焦点，`ComparisonDetails` 保留分行和弱分隔。响应式、嵌套详情关闭恢复、控件次序及悬停几何回归集中在 `qa-v2-acquisition.spec.js`。

获客与新增通过 DEV 工作台 `board=5.7` 查看：九张详细指标卡、来源渠道 / 下载目标 / 获客类型明细与独立渠道结算；详细趋势和体验页复用 `detailed-trend-option`，精确提示统一从 `src/components/chart-tooltip-content.ts` 生成。总览紧凑图的日期带连续覆盖绘图区，鼠标不必命中柱体或节点，键盘仍可逐期查看。获客收藏保存于当前浏览器，复制链接还原已应用筛选与明细状态；刷新重新载入演示快照，导出提供明确标注的演示 XLSX。未支持条件清空旧结果并说明边界，正式查询、账号收藏和正式导出仍未接入。新增切片回归见 `qa-v2-acquisition.spec.js`，不增加正式 API、业务事件或指标口径。

核心总览和 M016 日期输入复用 `DateRangePicker`，快捷计算在 `date-range-model`；桌面并列双月，窄屏纵排，面板草稿、外层条件与已应用结果分离。指标说明、数值比较、经营表头 / 单元格和迷你趋势通过 `FloatingHint` 统一测量定位并进入顶层，比较内容由 `ComparisonDetails` 按当前期、基准和变化分行。日期为辅助字号、当前值突出；鼠标 / 键盘切换只保留一份提示，Esc 关闭，聚焦自动滚动不丢失说明。

迷你柱的交互区覆盖柱身与相应日期，全零柱保持零高度；紧凑与完整纵轴通过 `chart-axis` 统一生成 1、2、5 乘以 10 的整数次幂的等距刻度，向外覆盖数据范围；完整数字与网格对齐，省去已有单位上下文时的图上单位行。真实 ECharts 提示沿用安全净化，只放行共用方向色标记和换行，其余 HTML 继续转义；V2 读取同一主题 Token。经营表按数据类型对齐，名称已表达的计数单位省略，必要单位同排小号括注；表格右上角与看板操作区的导出入口区分对象范围；正式下载只展示范围及服务未接入状态并保持禁用；获客 DEV 切片可另行下载明确标注的演示快照。未改变指标计算或真实数据准入。

独立数值摘要由 `MetricSummary` 与 `metric-summary.css` 统一承载，复用在链路阶段 / 转化 / 诊断、关键质量及紧凑 / 详细 KPI 体验。名称 14px、主数值 30px、辅助数值 26px 的唯一取值位于 Token；比较通过 `ChangeValue` 着色，常驻行使用 `ui-metric-comparison` 及统一 11px Token、400 / 500 字重；关闭后不保留占位，详情正文保持原层级。详细卡上部并列摘要、下方全宽趋势，不额外计算用户合计。经营明细默认当前值，统一“变化参考”选择一种变化；列头单位按 `metricHeadingUnit` 去重并就近括注，完整基准与差值通过视口内提示及数据详情抽屉查看。抽屉复用原滚动锁、键盘关闭和焦点恢复，真实明细与导出仍待接入。顶栏移除 Logo 竖隔线，一级文字使用清晰正文层级与整栏轻阴影。

本轮用户已确认核心总览与完整图表体验的其余展示交互。所有涨跌差异现共用 `src/components/ui/ChangeValue.tsx` 与 `change-presentation.ts`，颜色仅从 `tokens.css` 读取；覆盖紧凑 / 详细卡、质量区、经营表、同口径表和图表提示，深色提示使用同源可读变体。页面局部涨跌色已删除；零基准无法计算的增长率保持中性，真实 0 与不对比分支保持原语义。全局产品规则见总 PRD 4.3，专项护栏见 UI-031。

服务器联调后置期间，看板工作台开发切片使用“左侧常驻目录＋右侧当前看板”：`DashboardDirectory` 只消费目录模型，承载搜索、分类展开与选择；`DashboardDirectoryPreview` 负责 DEV 快照、地址和历史状态、窄屏目录以及内容装配，默认直接复用核心总览样板。当前已装配全部 11 项本地阅读页；正式查询仍按各项能力独立准入。生成器从总 PRD 5.1 唯一目标表提取 11 个规划看板，按权威指标分类校验顺序；`npm run dashboards:preview:generate` 更新 JSON，构建强制检查快照漂移。DEV 地址中的章节引用只标识预览项，不是正式看板 ID；搜索、展开及目录收起状态按当前历史项保存。原核心入口与工作台入口装配同一组件，不新增目录 API、对象模型或埋点契约。

| 入口 | 当前实现 |
|---|---|
| `/login` | YPBI 自有账号登录；登录成功后只允许回到同源安全产品路径，首次登录或管理员重置密码后强制进入改密页 |
| `/dashboards/public`、`/dashboards/mine`、`/dashboards/metric-overviews`、`/dashboards/favorites` | 看板中心四个稳定二级路径；正式路径当前均为真实“待接入”状态。开发环境精确地址 `/dashboards/public?design=dashboard-center` 与原 `?design=core-overview` 均进入同一看板工作台；已登记专题通过 `&board=章节引用` 直接定位，默认核心总览。`?design=v1-chart-states` 保持独立图表与状态体验。所有样例只供开发评审，不代表正式数据 |
| `/analysis/metrics`、`/analysis/events`、`/analysis/funnels`、`/analysis/saved` | 分析中心四个稳定二级路径；当前入口页均为真实“待接入”状态。已经存在的 `/analysis/metrics/:metricId` 技术纵向切片不等于完整指标分析入口已经开放 |
| `/data/metrics` | 从 V2 服务端读取权威指标快照的 98 个标准指标和 3 个周期派生指标；展示指标权威版本与接入登记版本，按动态一级/二级分类浏览，支持名称/ID/定义搜索、接入状态筛选、目录刷新和右侧详情，并覆盖加载/失败/空目录/无搜索结果状态；手机端用保留层级的分组选择器承载完整分类 |
| `/data/events` | 事件中心稳定二级路径；当前为真实“待接入”状态，不使用埋点文档静态内容冒充运行时目录 |
| `/analysis/metrics/M016?pid=...&start=...&end=...` | 先校验指标当前映射、验数准入与可信数据水位；M016 当前显示未就绪且不发起正式查询，验数通过但水位仍为空时服务端同样失败关闭，不展示上下文、折线图、逐日表或追溯信息 |
| `/admin/data-sources` | 先校验普通账号及`bi:data-source-maintenance:enter`权限，全部已完成首次改密的角色均可获得；再用独立维护密码建立绑定当前账号和客户端IP的二次会话，展示两站脱敏配置，支持当前测试、候选仅验证、验证后保存及全站影响确认 |
| 其他未知路径 | 显示页面不存在状态，不自动落入旧功能 |

- `src/main.tsx` 在 production 只加载 V2；经典版仅允许开发环境显式使用 `?ui=v13`，不进入生产构建或公网回退路径。`/dashboards`、`/analysis`、`/data`、`/admin` 及其子路径均由 Product Shell 承载。
- 全局 `AuthProvider` 在渲染产品内容前恢复服务端会话；未登录只显示登录门禁，首次改密用户只显示改密页，普通 401 回到登录页并保留安全回跳路径，403 保留当前身份并显示越权。前台复核若发现账号、角色、权限或 PID 范围变化，会以不含 Token 的内部会话范围 key 重挂载整个已认证页面树，立即卸载旧结果、草稿与请求并按新范围重新加载。
- 登录密码、改密字段和候选 Token 都不写入浏览器存储；登录或改密失败后清空全部密码输入。退出、切换账号、会话失效和首次改密成功都会清理敏感状态；窗口重新获得焦点时复核会话，12 小时绝对过期不由前端续期。
- Product Shell 全局导航由 `src/v2/app/navigation.ts` 唯一派生。`ProductTopNavigation` 在桌面常驻三类任务入口，悬停 200ms 展开二级菜单、离开 250ms 关闭，支持点击、方向键、Tab 和 Esc；悬停不抢焦点，菜单一次只展开一个。900px 及以下使用原有模态导航、焦点约束和 44px 触控目标。两端共用名称、路径、选中态与权限过滤，管理入口仅向具备权限的维护者显示。全局侧栏退出桌面布局，左侧只承载当前工作区的对象目录或分类；正式未就绪页面继续显示待接入状态。
- 新 Product Shell 顶栏及登录、认证不可用和首次改密页面统一复用 `BrandLogo` 与透明 `src/assets/ypinbi-logo.png`；原有内联 Y 形 SVG 和独立 `YPBI` 文字已从 V2 运行时入口移除。桌面使用完整字标，窄屏只按比例缩小、不裁切文字；登录文案、页面标题和“YPBI 接入”等产品术语未改。遗留经典版品牌继续作为迁移隔离事实保留，不被本次 V2 视觉修改穿透。
- 核心经营总览样板复用真实 Product Shell，只以 `import.meta.env.DEV` 条件动态装配，不进入导航、不调用指标目录或查询 API、不执行保存。样板已覆盖并通过 1280 / 1024 / 390、正常态，以及加载、真实 0、无记录、未成熟、部分数据、刷新失败保留旧值和单卡失败的组合评审。用户已确认整页及共用交互基线：一个日期范围入口连续选择起止日，筛选与 icon 操作组成紧凑工具栏，对比可关闭且关闭后没有残留字段；内部维护编号不进入界面，业务名称与 PID 仍可识别。内容消费、获客转化、支付链路使用轻量 Tab 并默认展示内容消费，选中面板按“阶段在上、关系 / 诊断在下”的卡内正常流排布；边界分别固定为 `M016 / M026 / M035 / M081`、`M001 / M003 / M008 / M005 / M006 / M007`、`M084 / M086 / M090 / M070 / M071 / M072`，不展示无指标 ID 的临时值。链路阶段、关系 / 诊断、质量指标及经营明细表头等全部可见指标名称均从指标权威投影提供 hover、focus 和 click 释义；经营表头的释义入口与排序按钮分离，继续使用“较昨日 / 较上周同日”比较。紧凑指标卡、共用数值摘要和核心总览专属只读页面模型位于正式功能目录，页头、筛选、链路、质量区和经营表仍为样板内实现，正式公共概览仍保持待接入。样板的 9 卡数据先通过 `coreOverviewQuerySuccessSchema`，再走与正式页面相同的合同→页面模型适配，不维护第二套自由形状的演示卡片数据。
- V1 图表与状态体验页同样只在开发环境按精确 URL 动态加载，不进入导航、不请求登录 / 目录 / 业务查询、不保存也不进入生产包。它集中展示紧凑 / 详细 KPI、单系列 / 两期 / 多系列折线、时间 / 并列 / 横向排行 / 合法堆叠柱、饼图 / 环图、聚合表 / 二维交叉表、真实漏斗、Cohort 矩阵和首帧耗时分布，并可独立切换 11 类数据状态。每张图都有同口径表和可访问名称；两期保留各自实际日期，堆叠展示分段及合计，Cohort 未成熟保留灰格且不当作 0。所有文案均将数值限定为视觉 fixture；漏斗、Cohort 和 M032 分位结果的正式能力仍受总 PRD 的真实计算与 MD-06 准入门槛约束。演示身份由独立 DEV 动态边界提供，正式入口不会引用该身份；生产构建结束后自动扫描产物，发现演示身份或任何 fixture 标记即失败。
- `src/v2/api/client.ts` 已增加核心经营总览严格只读客户端：请求先按固定 9 项和范围契约本地校验，再调用 `POST /api/bi/v2/queries/dashboards/core-overview`，响应必须通过完整运行时 Schema 并与原请求的指标、范围和日期语义一致才返回页面。多 PID 可以由服务端按目录重排，但不得缺少、增加或合并范围；当前期和非等长对比期使用两条独立趋势。正式 `/dashboards/public` 尚未调用该客户端，开发样板也继续只使用醒目标记的固定 fixture。
- `src/v2/features/dashboards/core-overview-page-model.ts` 只把已校验合同值转换为卡片展示模型，不在浏览器计算日均、累计、去重、比率、跨 PID 合计或水位。它从指标目录接收名称与定义，缺失权威定义时失败关闭；保留真实 0、全部不可用原因、部分结果和来源水位。当前期与对比期是两条可非等长序列；等长时用一组交互点读取双方实际日期和值，非等长时各自交互且不按序位虚构差值，缺失点保留日期/状态并断线。整板刷新失败只在普通技术错误时保留同查询键旧值，身份或权限失败清除旧结果；单卡重试固定旧结果实际日期范围且只覆盖目标卡。整板和单卡均以唯一请求/尝试标识拒绝同条件晚到旧响应，请求切换和页面离开同时取消旧请求；不可重试失败不提供无效重试入口。
- 指标中心只消费服务端完整定义目录，不以旧前端数组补指标；详情独立展示权威源建设声明、YPBI 映射和当前验数。M016 标记为首个接入样板，但在验数通过前“暂不可分析”；其余未接入项仍可查看定义和阻断原因。旧 M016 技术查询接口只用于兼容与受控验数，不等于正式分析页已经开放，也不读取旧分析注册表或工作区。
- 浏览器端对指标目录、平台目录、查询成功和错误响应执行严格运行时校验；指标查询还必须逐项核对服务端回显的 `metricId`、`pid`、日期范围和粒度，任何未知字段、缺失日期、重复 / 越界日期、非法状态组合或回显不一致均拒绝展示，不把畸形响应解释成空数据。
- M016 查询条件写入 URL 的 `pid/start/end`；直接打开、刷新以及浏览器前进/后退都会先按相同条件重新校验身份、完整指标目录与准入状态。URL 未指定 PID 时使用服务端平台目录的第一项，不对 PH 做特殊优先。当前待验数时停止在未就绪状态且不查询；表单候选日期暂为上海时区下截至昨日的近 7 日，不能作为正式水位。正式开放前必须先通过只读水位 / readiness 预检得到可查询结束日，或由服务端 `latest_complete` 解析并回显实际范围；查询响应里的水位只能验证本次已提交范围，不能反过来为第一次请求选择日期。即使后续验数状态改为 `passed`，只要 source 仍未提供可信水位，服务端就在构造响应前返回 `422 METRIC_NOT_READY`，页面不展示业务结果。单次范围由 V2 contract 限制为最多 366 日。
- 页面区分首次加载、身份源不可用、未登录、越权、一般失败、目录为空、查询无记录、部分日期不可用和查询成功。无记录/无值在折线中断开并在结果表显示状态，不补成 0；服务端返回的真实 0 正常展示。
- 同一查询条件手动刷新时保留上次成功结果并显示“刷新中”；刷新失败显示旧结果与错误提示。用户只修改 PID 或日期草稿时，页面显示“查询条件尚未应用”，旧结果及 URL 继续对应上一次已应用条件；点击“应用并查询”后才共同更新。直接通过历史记录切换到另一组已应用 URL 条件时重新加载，不把旧条件结果当成新结果。
- 验数通过后的正式查询追溯区展示 query ID、请求完成时间、数据水位、来源接口和映射版本。公共契约现接受 `watermark=null` 或严格结构化水位对象；对象必须说明完整业务日、Asia/Shanghai、PID、来源接口、来源类型和取证时间。浏览器会再次核对 `passed`、当前映射版本、PID、日期覆盖和来源，不合法即拒绝整份结果且不渲染业务值。当前 `pDaySum` 尚无经确认的水位字段，因此生产 source 仍返回 `null`，M016 正式页继续关闭；维护者技术查询可以返回待验数状态与空水位，但不会进入普通用户正式结果页。
- 断网只转换为可重试的服务不可用状态；V2 没有 offline 模式、结果离线缓存、旧工作区读写或跨会话草稿恢复。
- 数据源维护首次进入先检查会话，不闪现登录表单；配置状态与本次会话的凭证探针结果分开，`configured` 不冒充“连接正常”。候选 Token 只保留在当前组件内存且不写入本站浏览器存储；仅验证后保留以便直接继续保存，保存成功、发起退出、刷新、离开或会话失效后清空。保存使用原生模态确认并明确影响站点，服务端仍会重新验证。
- 维护客户端单独校验遗留管理 API 响应；响应一旦包含未声明字段（包括原始 Token）、非固定掩码或配置状态矛盾即拒绝展示。保存只有收到明确成功结果才显示已生效；断网、超时或畸形响应显示“结果未确认”并要求核对。退出失败不伪装成已退出。PID 路由具体范围和跨会话检测历史尚无接口支持，页面如实显示由服务器管理，不硬编码平台清单。
- 除受保护的数据源 Token 更新外，正式路径没有新增看板对象、个人保存、事件或漏斗查询写入；本地开发入口的收藏与演示导出只用于体验核对。

### 共用阅读入口

`DashboardQueryFields` / `DashboardActions` 固定顶部语义顺序。详细卡复用 `CardViewSwitch`、`DetailedMetricTrend`、`ChartDataTable`；`TrendRange` 仅裁剪绘图日期，完整表与摘要不变。`Pagination` / `PaginatedTable` 负责固定表头、独立滚动、范围总数、页码及完整表条数 / 跳页；页脚内边距由共享组件提供。获客的共同数据区间位于表头，逐日日期仍保留为行数据。少量分类趋势复用 `Chart.axisSeriesTooltip` 的安全同日分行格式，实际图例颜色与顺序保持。多 PID 体验以 `PaginatedLegend` 提供图下方单行分页，搜索位于图上方，翻页只改变图例窗口；曲线与选中 / 突出状态由图表持有，完整数据表保持全部 20 项。搜索、宽度与结果条件变化从第一页开始，条件切回不恢复过期页码或日期窗口。

`FloatingHint` 支持标题 / 整条比较命中，原生数据表初始聚焦关闭，恢复焦点不自动打开帮助。`Toast` 承载短暂操作反馈，暂停与重新计时由组件管理；指标定义和比较用 `MetricReadingDialog` 持续阅读。专项测试入口 `test:e2e:reading` 与 `test:e2e:periods`。

### 轻量动效与加载

`src/v2/motion.css` 统一面板淡入及移动目录短距离进入；`ProductApp` 只监听页面可见性以暂停后台循环，卸载后清理监听。日期 Portal 使用 V2 作用域标记；经典版不受影响。`LoadingIndicator` 复用于状态面板与指标卡加载；时长曲线只读 Token，不动画图表、数值或容器宽高。关闭、请求与焦点恢复不等待动画。支持减少动态效果，无新依赖。专项入口为 `test:e2e:motion`，断言实际几何、嵌套关闭、连续重开、状态完成与可见性事件暂停。

### 验证状态

- 2026-09-12 本地数据接入：完整发布校验及520项Bun测试通过；180个Chrome不同场景经全量执行与定点复测通过，覆盖原11板布局、26项连接专项及原功能。已复查1440/1280/1024/390实际截图和导出；来源、未接原因、口径差异与技术验收边界见[完整验数清单](./data-platform.md#看板数据支持清单2026-09-12)。真实值仍为待验数，未部署生产。

- 2026-09-10 全看板与目录：完整 Bun 411 / 411、共享快速逻辑 31 / 31、类型、生产构建和派生源检查通过；33 个不同浏览器定向场景分批复验，最后功能与目录 5 / 5 通过。实际查看桌面 / 手机截图并修复标题挤压、短表头竖排与说明图标差异；零值在主比较、趋势、结构和导出保持一致。生产包排除全部演示组件与样式；未部署，正式数据和对象写入仍未开放。详见最新变更记录。

- 2026-09-10 共享阅读与分页图例：Bun 402/402、类型检查、生产构建、权威快照与开发体验隔离通过。真实浏览器验证标题 / 比较行命中、数据表初始焦点、Toast 自动关闭与暂停、跨看板工具顺序、表级共同条件、同日多分类提示以及 20 PID 底部分页。图例仅翻页时 Canvas 保持、跨页选择及完整表保持；搜索清空和条件切回不恢复过期页码 / 窗口。1 / 7 / 30 / 90 / 366 天使用明确标注的合成布局，不代表正式长周期查询已接入。专项证据见[变更时间线](../CHANGELOG.md)。

- 2026-09-10 动效与目录收纳：最终目录 / 动效 Chrome 12/12、共享逻辑 27/27；本轮另有获客 9/9 与共享 UI 9/9。类型、生产构建和开发体验隔离通过。覆盖窄轨道、宽屏 / 手机、连续开关、目录状态恢复、嵌套焦点、减少动态效果及加载完成。后台暂停用可见性事件模拟验证，非后台性能测试。

- 2026-09-10 核心总览整体验证：统一发布检查、Bun 385/385 和系统 Chrome V2 61/61 通过；共享快速检查 26/26，额外表格显隐比较复验 1/1。覆盖同数据 ECharts 坐标对照、单位去重、辅助字号、日期、表格、完整柱身、实际 Canvas 提示、导航及桌面 / 窄屏 / 异常态。专项与边界详见[变更时间线](../CHANGELOG.md)。当前等待用户体验验收，其他看板尚未开始。
- 2026-09-09 看板工作台复核：`npm run verify:release` 全链通过，Bun 370/370、目录生成 / 历史状态 / DEV 地址 Node 回归 12/12；系统 Chrome V2 套件 52/52，最后补充的目录定向回归 8/8。覆盖默认核心结果、持续切板、搜索不清空内容、展开恢复、短名称不重复提示 / 截断名称 hover 与 focus、独立滚动、历史 / 深链、手机模态、工作区入口及正式身份边界。实际复查 1280 / 1024 / 390 截图；生产隔离同时检查演示身份、组件和规划快照。无 lint script，保留既有 ECharts 大包提示；不以 fixture 代替真实联调。
- 上一轮完整复核：系统 Chrome V2 套件 44/44，通过两个开发体验页、认证失败恢复、维护流程、导航与 1280 / 1024 / 390 视口；两个体验入口 HTTP 均为 200。该回归使用受控 fixture，不证明真实登录 / 上游已接入。

- 2026-09-09 涨跌统一专项：主题 / 对比度 4/4、卡片 / 页面模型 / tooltip 25/25、两体验页实际颜色与状态 / 响应式定向浏览器 3/3 通过，覆盖 1280 / 1024 / 390。类型检查、服务端及带预检和隔离扫描的生产构建通过。下方 43/43 为上一批完整 V2 回归记录，不代表本轮重新执行了完整套件。

- 上述条目是当前源码和受控本地浏览器中的实现事实，不等于真实 PostgreSQL 身份联调、M016 验数、生产配置或部署通过。
- 2026-09-09 当前复核确认：指标快照 / 验数证据检查、生成器 Node 测试 33/33、M016 离线验数工具 20/20、运行时发布准入 4/4、导航登记源 Node 测试 3/3、主题 Token 测试 3/3、紧凑指标卡与核心总览页面模型 Node 测试 16/16、核心总览查询控制器 Node 测试 10/10、核心经营总览独立合同测试 6/6、`npm run typecheck`、`npm run build:server`、带强制预检的 `npm run build` 和 `git diff --check` 均通过；构建仅报告 ECharts vendor chunk 超过 500 kB 的非阻断警告。本轮系统 Chrome 的 V2 Playwright 全套 43/43 通过，并确认合同驱动样板正常态 9 卡、组合异常态包含真实 0 / 部分 / 加载 / 无记录 / 未成熟 / 旧值 / 单卡失败共 9 卡，三条链路 Tab、卡内正常流、全量权威指标释义及表头释义 / 排序分离均可用，页面无当前运行错误及横向溢出。核心总览与 V1 图表体验定向回归覆盖单一日期范围、无对比、内部编号隐藏、业务名与 PID、明确比较表头、类型数量、两期实际日期 hover、键盘打开同口径表、不改变 URL 的页内定位、状态 Tab 和 1280 / 1024 / 390。两开发入口均不请求登录、目录或业务查询后端。生产构建检索确认不包含经典版，也不包含代表页 / 图表体验样板、样板 CSS、开发评审身份或固定演示文案；经典版 2/2 仅保留为开发迁移期既有回归事实。核心总览 Plugin 与浏览器客户端已纳入当前完整 Bun 回归，最新结论见本节。
- 浏览器回归曾发现分析页在 `popstate` 后卸载前把默认查询参数写入指标目录 URL；现已在规范化 URL 前严格校验仍位于同一指标分析路径，并用“进入分析 → 刷新 → 返回目录 → 前进回分析”用例设防。四张关键截图只作为本地验收证据，不是最终产品视觉稿。
- 本地测试运行时已就绪，Bun 1.4.2 当前完整套件 370/370 与统一发布检查通过。真实数据库、上游、生产代理和部署验证仍独立待完成。
- 自有账号 IdentityProvider、三档角色和统一 `pidScope=all` 已在本地实现；真实身份数据库、首个维护者、生产 Cookie / Origin / CSRF、限流、日志脱敏和反向代理仍未部署或黑盒验证。任一身份依赖不可用时服务端失败关闭，不降级为匿名访问。
- 真实上游 M016 抽样验数、生产日志、网关隔离、生产配置和部署仍未验证；不得用浏览器 fixture、类型检查或构建通过代替这些结果。

### 当前主要代码

- `src/main.tsx`、`src/v2/app/experience.ts`：V2 与遗留应用隔离加载。
- `src/v2/app/AuthProvider.tsx`、`src/v2/pages/AuthenticationPages.tsx`、`src/v2/api/auth.ts`：会话恢复、登录、强制改密、退出和严格认证响应校验；只有开发环境精确命中的目录、核心总览、图表三个体验地址使用固定评审身份，避免纯体验页面依赖本地账号后端；额外查询参数、尾斜线、hash、SPA 离开和所有生产路径都会立即恢复真实会话校验。
- `src/v2/app/ProductApp.tsx`、`ProductShell.tsx`、`router.ts`、`navigation.ts`、`src/v2/components/BrandLogo.tsx`、`src/assets/ypinbi-logo.png`：路由、Product Shell、角色可见导航、目标导航骨架与统一品牌资源。
- `src/v2/design/CoreOverviewDesignFixture.tsx`、`core-overview-contract-fixture.ts`、`core-overview-design-fixture.css`：只在开发环境动态加载的核心经营总览样板、严格合同 fixture 与三档响应式实现。
- `src/v2/design/V1ChartStatesFixture.tsx`、`v1-chart-states-fixture.css`：只在开发环境动态加载的 V1 图表、表格与数据状态体验页；不是正式业务页或通用图表组件库。
- `src/v2/features/dashboards/DashboardMetricCard.tsx`、`dashboard-metric-card-model.ts`、`dashboard-metric-card.css`、`dashboard-metric-card-model.test.ts`：紧凑指标卡、互斥视图状态、复用样式及模型回归；只接收归一化展示模型，不自行查询、推断水位或决定指标口径。
- `src/v2/features/dashboards/core-overview-page-model.ts`：核心经营总览专属的合同→卡片适配、整板/单卡只读状态、刷新互斥与请求取消模型；不是通用看板框架。
- `src/v2/features/dashboards/core-overview-query-controller.ts`、`useCoreOverviewQuery.ts`：把严格客户端与页面状态串联的专属只读查询控制器和 React Hook；管理规范查询键、整板刷新、单卡子集重试、跨条件晚到隔离、真实取消和失败恢复。正式公共概览尚未调用。
- `src/v2/pages/MetricCatalogPage.tsx`、`MetricAnalysisPage.tsx`：指标中心与 M016 只读分析。
- `src/v2/pages/DataSourceMaintenancePage.tsx`、`src/v2/api/adminDataSources.ts`：受保护的数据源维护页面、状态机与严格管理 API 客户端。
- `src/v2/api`、`src/v2/components/StatePanel.tsx`：V2 请求、刷新保留和可区分状态；包含尚未接入正式页面的核心经营总览严格客户端。
- `src/v2/v2.css`、`src/components/ui/primitives.css`、`src/theme/tokens.css`：当前 V2 页面样式、共享基础组件与唯一视觉 Token；`src/theme/tokens.ts` 只保留受测试保护的 Canvas 图表运行时投影。

### 统一业务释义与展示组件

- `metric-presentation.ts` 统一转换业务名称、协议条件和统计表达；输入仍来自权威指标快照。UI 投影生成器、指标中心、核心总览与图表体验共用同一转换逻辑。技术快照、公式计算和公共契约不变；未知协议在运行时显示说明待补充，在投影生成检查中阻断。有效观看规则保留首帧门槛、15 秒阈值及短视频完整播放例外；注册转化保留 IP·天分母。数据来源、时间粒度和平台范围也展示业务名称。
- 核心链路卡使用卡内文字 Tab，阶段在上、转化在下、名称与数值就近纵向排列。取消重复标题及大灰底，保留必要统计边界；全部说明支持悬停、聚焦和点击，较长公式分行显示并检查视口边界。详细 KPI 改为摘要在上、全宽趋势在下，两页继续复用既有图表与涨跌规范。
- 本批只修改展示、导航及派生文案，没有新建埋点事件或字段；现有导航与查询触发语义保持。未来采集方案继续使用总 PRD 的埋点权威定义，不能把菜单悬停当成页面访问。回退仅涉及前端组件、派生 UI 快照与相关检查，无数据迁移、服务器或旧 BI 变更。

当前验收顺序为核心经营总览全流程与共用图表体验核对，确认首张看板稳定后再推进其他看板。日期、表格提示、实际 Canvas 提示和整段柱身命中纳入浏览器用例；最近执行结论统一见变更时间线。生产数据库、上游验数及导出仍单独验收，不以演示结果代替。

## 遗留前端当前事实（仅迁移与回退核对）

本节及其全部子章节是带日期的旧实现记录，仅用于识别迁移影响、回退风险和待删除代码，不是 V2 目标能力或当前验收结论。目标范围以[总 PRD](../requirements/BI-产品需求文档.md)为准，当前差距与迁移顺序以[现有平台调整方案](../requirements/BI-现有平台调整方案.md)为准；旧页面即使仍能运行，也不得作为新页面的默认数据、权限、状态、保存或交互契约。

### 2026-08-28 全平台视觉与交互完整收口

#### 页面层级与品牌

- 顶栏品牌改为连续的 `YPBI` 整合式字标：文字统一使用近黑色，仅在 `Y` 交汇处保留单一品牌蓝识别点；移除独立图标、蓝色 `BI` 和小副标题造成的拼装感。
- 模板管理采用“左侧模板资源列表 + 右侧单一编辑画布”。模板信息和卡片画布属于同一主表面，内部用分隔线和背景层级组织，不再叠加外卡、内卡和描边状态。
- 分析创建器的模型导航改为无边框横向选择，当前模型使用底部品牌指示；页面标题不再套卡，模型说明采用柔和底色和单一左侧强调线，右侧设置区用字段分隔替代卡片嵌套。
- 状态类信息使用语义实底标签，数量使用辅助文本；输入、可点击控件、数据卡片和拖放目标之外不增加边框。

#### 选择器、分类和覆盖层

- UI v1.5 业务单选统一由 `MenuSelect` 承担，多平台选择由 `MultiMenuSelect` 承担；分析筛选、模板配置、卡片编辑、创建器、完整预览、下钻和标签平台不再调用浏览器原生菜单。
- 分析中心类型分类在桌面自动换行并完整展示，手机端才启用单行横向滑动；分类数量使用独立柔和徽标，不继承经典版边距。
- `FieldExplanationDialog` 通过 `platform-portal-layer` 挂载到 `body`，现代样式作用域和层级保持完整，遮罩统一覆盖顶栏、侧栏和内容区。
- 新版在 `html/body/#root/.app` 四层清理经典版 `overflow-x:hidden`，使用 `clip + visible` 保持横向安全且不制造隐式滚动容器；产品顶栏和模板页头在手机滚动时保持正确 sticky。

#### 验证边界

- 1280px：分析分类无裁切，模型菜单右对齐且不越出视口，日期筛选右边缘与触发器对齐，字段说明完整覆盖产品 Shell。
- 1024px：侧栏为 56px Icon Rail，分类完整换行，页面无横向溢出。
- 390px：产品顶栏持续固定，模板页头宽度等于内容视口，分析分类横向滑动，筛选 Sheet 全宽且底层内容不位移。
- `src/main.tsx` 不再包含原生 `<select>`；指标、接口、查询算法、模板契约、日期口径和权限未修改。

### 2026-08-28 全平台筛选交互与品牌骨架重构

#### 筛选结构

- 桌面平台与日期筛选由居中遮罩弹窗改为触发器锚定面板，保留看板上下文；平台面板使用 560px 紧凑宽度，日期面板使用 720px 并在低高度视口内部滚动。移动端继续使用顶栏以下的全屏 Sheet。
- 日期筛选明确拆成“分析周期”和“对比周期”两个语义区块。今日、近 7 日、近 30 日只改变分析周期；等长周期、同期进度、上月完整月只改变对比生成方式，各组保持一个选中态。
- 快捷周期不再点击后立即关闭并刷新；用户完成当前周期和对比方式配置后点击“应用筛选”，当前看板全部卡片统一刷新。日期输入仍支持自定义起止日，既有三种对比算法和查询口径未变。

#### 全平台视觉与稳定性

- 顶栏品牌改为原创 Y 形图形与 YPBI 字标，使用黑色主体和品牌蓝强调；同一顶栏、侧栏和选中导航规则覆盖看板、分析中心、创建分析、模板管理、数据字典和数据源维护。
- 调整全平台页面底色、边框和静态表面阴影 Token；侧栏当前工作区增加稳定左侧指示条，不使用位移式悬停。
- 修复经典版样式泄漏导致 Toast 残留 `left:50%` 的问题；现代 Toast 恢复桌面右下角、手机底部安全边距。工作区接口返回空内容或非 JSON 时改为稳定中文状态，并在远端不可用时停止无意义的自动保存重试。
- 筛选打开时焦点进入弹层容器，不再默认让关闭按钮出现突兀焦点环；键盘 Tab、Esc、焦点返回和共享滚动锁保持原能力。

#### 验证边界

- 应用内浏览器逐页复查看板中心、分析中心、创建分析、模板管理、数据字典和数据源维护；桌面平台/日期锚定面板、日期两组选中态、统一应用、390px 全屏 Sheet 和 Toast 定位通过。
- 1280px 桌面和 390px 手机均无横向溢出；筛选打开前后主内容坐标不变。`qa-ui-shell.spec.js` 新增日期双分区、选中态、锚定位置和区块不重叠断言。
- 本次未修改指标、接口路径、查询算法、平台范围规则、三种对比口径、模板契约或权限。

### 2026-08-28 全平台可点击反馈与覆盖层稳定性

#### 统一交互状态

- UI v1.5 内所有可用 `button`、链接、`summary` 和语义按钮统一显示可点击光标；禁用控件统一显示禁止状态，不再依赖各页面零散声明。
- 主操作使用品牌实心蓝；次级、筛选、图标和关闭操作悬停时统一使用浅蓝背景、品牌文字和品牌边框。反馈只改变颜色、边框和阴影，不改变控件尺寸或位置。
- 分析中心卡片缩略图隐藏重复的对比周期说明，仅保留方向和变化值，避免紧凑卡片内的徽标横向溢出；正式看板和完整预览仍展示完整比较信息。
- 内部滚动区域统一使用轻量滚动条，分析模型横向列表、卡片库和抽屉不再出现突兀的系统粗滚动条。

#### 页面表面层级

- 页面背景由单一浅灰改为顶部浅亮、内容区浅灰的低对比背景；工具区使用轻量抬升阴影，阅读重点、看板卡片和各工作区容器使用同一静态表面阴影。
- 新增 `surface-subtle`、`shadow-surface` 和 `shadow-surface-raised` Token；卡片不使用位移式悬停，保持 BI 长页面阅读稳定。

#### 无位移覆盖层

- `src/components/layout/useBodyScrollLock.ts` 是筛选弹层、完整预览、字段说明、下钻、移动导航、模板卡片库和卡片编辑抽屉的唯一滚动锁定入口。
- 锁定同时处理根节点和页面滚动，并按实际释放宽度补偿滚动条；多个覆盖层可叠加使用，最后一个关闭后才恢复页面状态。
- 桌面保留稳定滚动槽；手机端关闭固定滚动槽并由共享锁定补偿，确保全屏 Sheet 填满视口，同时底层画布不改变宽度。

#### 验证边界

- 应用内浏览器逐项测量桌面筛选、完整预览、下钻、字段说明、模板卡片库和卡片编辑抽屉：打开前后主内容 `x / width / right` 完全一致。
- 1280px、1024px 和 390px 均无横向溢出；390px 筛选 Sheet 填满 390px 视口，移动导航 Drawer 和全部可见按钮的点击光标通过。
- 前后端 TypeScript 检查和 Vite 生产构建通过；本次未修改指标、接口、查询算法、筛选口径、模板契约或权限。

### 2026-08-28 核心看板阅读页首轮试点优化

#### 当前范围

- 普通系统/自定义看板统一按“页面标题 → 模板与全局筛选 → 阅读重点 → 卡片内容”展示；分类、渠道、专项三个专用看板仍沿用现有专用页面组合，未在本轮改写业务结构。
- 模板选择由 `MenuSelect` 接管，不再使用浏览器原生下拉；系统模板和我的模板分组展示，并在选项内显示卡片数量或专用接口属性。
- 本轮只调整阅读层级、模板切换、卡片头与状态反馈；指标、接口、查询算法、筛选口径、模板配置和权限契约均未变化。

#### 卡片与状态

- 卡片头拆分为标题、模型、平台范围和查询状态两层；趋势/变化/表格使用同一分段切换，查看明细和完整预览收敛为 Icon 操作。
- 首次查询失败只显示一份带重试入口的错误状态；已有成功结果刷新失败时保留旧结果并显示轻量警告，不再同时重复错误条和错误卡。
- API 返回空响应或非 JSON 时统一转换为中文“数据服务暂时不可用/返回格式异常”，不再把浏览器解析异常直接展示给用户。
- 不支持的字段组合使用单一空态说明并指向分析中心，不重复平铺警告。

#### 稳定性与代码入口

- 隐藏的下钻抽屉同时使用位移和 `visibility` 隔离，不再在关闭时撑大页面横向滚动宽度。
- 1280–1439px 隐藏全局筛选说明块并使用紧凑控件宽度；1024px 工具区改为单列，390px 继续使用全宽模板选择和单一筛选入口。
- `src/components/ui/MenuSelect.tsx`：模板分组选择、上下循环、`Home / End`、前缀键入查找、Esc、外部点击关闭和焦点返回。
- `src/features/dashboard/DashboardReadFrame.tsx`、`dashboard-v13.css`：页面标题/工具区顺序与卡片头。
- `src/components/DataState.tsx`、`src/analytics/realEngine.ts`：单一错误反馈、重试和响应解析。

#### 验证边界

- 101 项 Bun 测试、前后端 TypeScript 检查和 Vite 生产构建通过。
- 应用内浏览器实际验证 1280px、1024px、390px：模板切换、工作区切换、移动筛选、导航开合、焦点返回及横向溢出通过。
- 本机 Playwright 调用系统无头 Chrome 时在启动阶段 `SIGABRT`，未进入测试断言；对应核心路径已完成浏览器人工回归，自动化用例已同步新增模板选择器和重复错误防回归断言。
- 未部署 VPS。

### 2026-08-28 UI v1.5 稳定骨架与核心交互收敛

#### 当前范围

- UI v1.5 已覆盖看板中心全部系统/自定义看板、分析中心、分析创建器、模板管理、数据字典、数据源维护，以及筛选、字段说明、完整预览、卡片编辑和下钻等覆盖层。
- v1.5 沿用 `v13` 运行入口并替换其唯一正式实现，不新增并行皮肤；`VITE_UI_VERSION=classic` 可项目级回退，`?ui=classic` / `?ui=v13` 只用于 QA 和回滚演练。
- 新旧界面共享 `DashboardCardConfig`、真实查询、模板持久化和权限契约。卡片展示字段 `mobileOrder`、`mobileVisibility` 只控制移动端编排，不改变请求与指标口径。

#### 前端结构

```text
src/app/                     UI 版本解析与项目级开关
src/theme/                   唯一视觉 Token、可访问图表色板和图表主题
src/components/ui/           Button、IconButton、StatusBadge、MenuSelect 基础组件
src/components/layout/       全平台顶栏、侧栏和响应式产品骨架
src/features/dashboard/      全部普通看板的阅读页组合和卡片样式
src/features/filters/        全局平台/周期筛选及弹层样式
src/features/workspaces/     分析、模板、字典、数据源和专用看板样式
src/features/overlays/       字段说明、完整预览、编辑和下钻覆盖层
src/main.tsx                 工作区装配与既有查询、保存和业务交互
```

依赖方向为 `app → features → layout/ui → theme`。新版不向 `src/styles.css` 追加页面规则；该文件只承担经典版回退兼容，后续在经典版下线时按引用整体清理。

#### 视觉与交互

- 使用 56px 顶栏、220px 展开侧栏、56px 收起侧栏、1600px 内容最大宽度和 12 栏看板网格。
- 颜色、字号、间距、圆角、层级和动效只来自 `src/theme/tokens.css`；图表使用 6 色可访问色板，多系列折线同时使用颜色、线型和点型编码。`tokens.ts` 只投影 Canvas 无法直接读取的值，并由主题测试阻止漂移。
- 通用 Icon 统一使用 `lucide-react`；IconButton 桌面热区 32px、移动端 44px，关闭和字段说明不再使用字符图标。
- 工作区导航改为同一 React 应用内切换，普通左键不会整页重载；浏览器前进/后退同步工作区，侧栏收起状态在工作区间保留。
- 桌面筛选区与模板选择器合并为看板页内工具区，不再插在产品顶栏与页面骨架之间；移动端收敛为单一“筛选”入口，打开全屏 Sheet 后再选择平台或日期。
- 模板管理继续使用同一产品侧栏；编辑器顶部操作栏固定，画布、模板列表、按需卡片抽屉和卡片编辑抽屉职责分开。
- 分析创建器使用“条件配置 + 右侧实时预览”双栏；漏斗步骤统一为编号、说明和线性 Icon 操作，不再使用字符箭头。
- 明细表的数据区独立横向滚动，分页固定在容器底部；数字右对齐、文本左对齐，首列冻结。
- 移动端主导航使用覆盖式 Drawer；筛选 Sheet 和导航 Drawer 均支持焦点进入、Tab 循环、Esc 关闭、焦点返回和背景滚动锁定。
- 普通看板复用统一阅读页，专用内容/渠道/专项看板复用同一页面头、状态、KPI、图表和表格规范；分析卡片库、创建器、模板编辑器、字典和维护页分别使用稳定工作区布局。
- 卡片继续复用真实查询、单卡加载/失败/重试、完整预览、变化、表格和下钻能力；本地没有 BI API 时继续显示真实失败，不用 Mock 填充。

#### 代码入口

- `src/app/uiVersion.ts`：UI 版本解析、QA 覆盖和导航参数保持。
- `src/components/ui/MenuSelect.tsx`：分组选项、上下循环、`Home / End`、前缀键入查找、键盘关闭、外部点击和焦点返回。
- `src/components/layout/PlatformTopbar.tsx`、`PlatformSidebar.tsx`：全平台产品骨架。
- `src/features/dashboard/DashboardReadFrame.tsx`：看板标题、阅读重点、页面操作和卡片区域。
- `src/features/filters/global-filter.css`：筛选工具条及平台、日期弹层。
- `src/features/workspaces/workspaces-v13.css`：所有工作区的页面编排。
- `src/features/overlays/overlays-v13.css`：全局覆盖层。
- `src/theme/tokens.css`：唯一视觉 Token；`tokens.ts`、`chartTheme.ts`：受一致性测试保护的 ECharts Canvas 投影与展示主题。
- `src/main.tsx`：装配全部工作区；查询、卡片、模板、权限和接口业务逻辑仍使用原实现。

#### 已验证边界

- `1440×900`、`1280×800`、`1024×768`、`390×844` 均无页面横向溢出；1024px 侧栏收起，390px 使用移动端导航和全屏筛选。
- 浏览器逐页通过：看板中心、分析中心、分析创建器、筛选弹层、模板管理、数据字典，以及桌面、1024px Icon Rail、390px 导航与筛选 Sheet；工作区切换状态标记证明应用未重载。
- 筛选 Sheet、导航 Drawer 的焦点循环、Esc 关闭、焦点返回和背景锁定通过；平台与日期桌面弹层在 1440px 内完整可见。
- `qa-ui-shell.spec.js` 固化侧栏/内容坐标、SPA 导航、侧栏收起、筛选 Esc、模板编辑器、平板和移动端横向溢出回归；`?ui=classic` 仍可恢复旧界面，且不迁移模板或数据。

### 2026-08-03 三种周期对比模式

- 日期筛选新增自然月、等长周期、同期进度三种自动对比模式。
- 自然月：当前周期结束日所在月份，对比上一个完整自然月；允许 31 天与 30/28/29 天比较，日均指标直接可比。
- 等长周期：使用当前周期之前紧邻的相同天数，适合收入、新增、次数等累计指标严格比较。
- 同期进度：对比上月 1 日至相同日序，适合月中经营进度比较。
- 对比周期由系统根据当前周期自动生成并展示天数，避免手工选错；旧筛选配置默认兼容为等长周期。

### 2026-08-03 日均与累计指标

- DAU、观影人数、付费人数、端别活跃和广告点击人数在多日范围统一显示日均值，名称同步增加“日均”。
- 分析中心新增累计活跃、观影、付费、端别活跃和广告点击人天字段，均带中文解释、公式、算法和接口来源。
- 多平台经营总览自动补入“周期累计规模”卡片，展示累计活跃人天、新增用户、累计付费人天和累计广告点击人天。
- 周期去重人数不使用日 UV 累加冒充；现有 API 无法支持时在字段说明中明确限制。

### 2026-07-27 卡片编辑返回

- 卡片创建和编辑页左上角新增“返回卡片库”。
- 存在未保存修改时，返回前进行二次确认；保存后仍自动回到卡片库。

### 2026-07-27 响应式主导航修复

- 统一侧栏最终断点，避免历史样式对 `<a>` 导航失效或互相覆盖。
- 所有屏幕下侧栏导航只显示单行标题，说明文案不再参与侧栏排版，避免高分辨率缩放场景绕过断点。
- 761-1500px 隐藏导航说明，只显示单行标题；761-1280px 使用 96px 紧凑侧栏。
- 760px 以下改为顶部横向滚动导航，隐藏品牌和分组标题，避免小屏文字堆叠。

### 2026-07-27 24小时走势卡片

- “留存与使用深度”系统模板新增登录用户、新增用户、观影用户三张真实 24 小时曲线卡片。
- 默认使用 Pornhub 单平台，可在完整预览或卡片配置中切换单平台及最多 4 平台对比。
- 桌面端按卡片网格展示，小屏自动纵向排列；当前日和对比日共用五分钟时间轴。

### 2026-07-23 多指标实时预览

- 指标卡支持同时选择并预览最多 16 个指标。
- 指标选择区显示当前数量和上限；点击、拖拽均执行同一上限校验。
- 已选择 7 至 16 项时不再因后端旧的 6 项限制加载失败。

### 2026-07-23 卡片加载失败修复

- “付费规模与效率”恢复真实经营总览查询。
- “内容消费汇总漏斗”及支付漏斗不再发送无关指标字段，完整预览和视图切换使用同一合法查询。

### 2026-07-23 模板防丢失

- 模板和卡片每次变化时同步保存到浏览器本地，作为服务器工作区之外的第二份恢复来源。
- 页面加载远端工作区时按模板 ID 和更新时间合并，不直接覆盖本地较新配置。
- 远端不存在的本地自定义模板和卡片会保留，并在工作区初始化后回写服务器。

### 2026-07-23 获客与付费看板

- 新增系统模板“获客转化分析”，包含核心同期对比、访问下载注册趋势、平台转化对比和明细表。
- 经营总览新增“付费规模与效率”卡片。
- 分析中心新增“获客转化”模型及“获客”指标分组。
- 新指标均提供中文解释、公式、聚合算法、接口路径和口径限制。
- 小于 1% 的比例显示 3 位小数；无当前期记录时显示“暂无记录”，不计算涨跌。

### 工作区导航

| 工作区 | 功能 |
|---|---|
| 看板中心 | 统一查看系统模板和我的模板；平台、日期筛选；图表切换；下钻；导出；保存布局 |
| 分析中心 | 统一管理已有分析卡片，并进入创建流程配置指标、维度、漏斗步骤和实时预览 |
| 模板管理 | 进入独立编辑模式，从卡片库组装模板；拖拽插入和排序；编辑、移除、预览、保存和发布 |
| 数据字典 | 查看 API 字段、指标、维度、分类与标签；跳转创建分析 |

一级导航不再分别暴露“卡片库”和“分析工作台”，避免同一任务出现两个入口。创建分析结束后返回分析中心的卡片列表。

### 系统模板

1. 多平台经营总览
2. 视频内容表现
3. 热搜词分析
4. 支付汇总分析
5. 留存与使用深度
6. 分类内容分析（原独立内容分析页）
7. 渠道经营分析（原独立渠道分析页）
8. 专项运营分析（原独立专项分析页）

多平台经营总览新增四组默认卡片：终端活跃与新增结构、新增来源规模与收入、广告点击覆盖与频次、新增用户付费质量。旧浏览器模板会按卡片 ID 自动补入新系统卡片，同时保留用户自建模板和原有卡片顺序。

看板中心的选择器按“系统模板”和“我的模板”分组。后三个系统模板继续调用原有专用真实接口，不伪装成普通可配置卡片；用户自行创建的模板仍通过卡片库和模板编辑器组装。

### 核心交互

- 全局筛选：看板页工具区统一控制全部 20 平台、单平台、最多 8 平台对比，以及今日、近 7 日、近 30 日和自定义起止日期。
- 平台筛选：在弹层内先选择汇总、单平台或 2–8 平台对比，再统一应用；取消不会触发查询或修改当前范围。
- 日期筛选：快捷范围可直接应用；自定义范围按自然日选择开始和结束日期，包含首尾两天，并校验开始日期不晚于结束日期。
- 时间对比：每次查询同时包含当前周期和对比周期；快捷范围自动使用等长前置周期，自定义时可分别选择两段日期，且要求天数一致。
- 对比展示：指标卡显示对比期数值和变化率；趋势图追加“对比期”系列；分析摘要明确列出两段日期。
- 分类、渠道和专项系统模板已改为继承完整平台范围：全部模式查询 20 个平台，对比模式查询所选 2–8 个平台，单平台模式只查询当前平台。
- 字段解释：指标卡、统计表头和分析配置字段均带 `?` 提示；悬停或键盘聚焦可查看字段来源、公式或口径。
- 图表视图：趋势、变化分析、数据表格。
- 下钻：点击图表节点打开右侧抽屉，查询真实用户或圈子明细。
- 表格：分页，不在主看板堆叠长明细。
- 卡片创建：指标、维度和图表选项由 API 能力表约束。
- 分析条件：查询筛选器、拆分维度和统计指标均可直接点选；非漏斗模型统一修改右侧正在预览的主卡片，选择后实时刷新图表。
- 选择反馈：已选字段显示勾选和高亮；指标、拆分维度至少保留一项，查询筛选器允许按需启用或关闭。
- 漏斗创建：点击或拖动已开放事件，支持排序和删除。
- 模板创建：卡片拖至当前位置插入；新增卡片默认进入顶部。
- 模板编辑器默认只保留模板列表和全宽画布；卡片库通过“添加卡片”按需从右侧打开，不再作为常驻第三栏挤压图表。
- 卡片库抽屉内支持类型筛选、结构预览和新建卡片；卡片可拖到画布插入线指定的位置，也可直接添加到模板顶部，关闭抽屉不影响未保存草稿。
- 卡片库抽屉按动态视口高度占满可用区域，卡片列表独立占据剩余空间并滚动；低高度屏幕自动隐藏示例预览，保留筛选和卡片操作空间。
- 窄屏抽屉宽度使用响应式范围，滚动条提供更大的可视与拖动区域，并增加底部安全留白，避免被浏览器或会议工具栏遮挡。
- 模板管理进入编辑状态，不显示业务平台和日期筛选，但继续保留可收起的产品侧栏；返回后恢复日常看板模式。
- 预览、取消、保存、保存并发布和添加卡片统一放在模板顶部工具栏；模板属性区只配置名称、场景、模型和公共筛选器。
- 卡片编辑、上移、下移和移除工具默认隐藏，悬停、键盘聚焦或选中卡片后才显示，减少画布噪音。
- 模板编辑：所有修改先保留在编辑器草稿中，点击“保存模板”或“保存并发布”后才写入工作区；“取消修改”恢复最近保存版本。
- 模板预览：预览保持在模板编辑器内，不切换到看板中心；工具栏提供返回编辑、取消修改、保存模板和保存并发布。
- 卡片资产：支持复制；分析工作台支持最多 20 步撤销，保存前继续执行 API 能力校验。
- 字段配置：卡片编辑抽屉、分析工作台和模板内配置统一读取真实 API 能力白名单，不再从 Mock 模型元数据暴露越界字段。
- 卡片宽度：看板中心和模板编辑器支持拖动卡片右侧边缘，宽度按 12 列网格吸附为半宽、2/3 宽或整行。
- 布局保存：看板中心先本地预览，点击“保存布局”后写入当前模板；模板编辑器调整后同样通过“保存布局”确认，随后由工作区 API 持久化。
- 键盘操作：宽度手柄聚焦后可使用左右方向键逐档调整；移动端保持整行展示，不显示拖动手柄。

### 看板阅读模式

- 看板中心只保留一条主导航和一块全宽内容区，不再长期占用二级模板侧栏。
- 看板模板通过标题上方的紧凑选择器切换。
- 卡片悬停时仅在右侧边缘显示宽度手柄，调整期间显示蓝色焦点框，未修改时保存按钮禁用。
- 卡片默认只展示标题、分析模型、查询状态和关键指标；日期、平台范围及数据粒度放入查询状态提示。
- 桌面端优先展示趋势与平台差异；移动端核心 KPI 使用 2×2 布局，图表卡片依次纵向排列。
- 经营、内容、搜索、支付和留存看板分别展示三项阅读重点，内容是分析方向提示，不冒充真实结论。
- 平台排行按核心指标降序，并始终展示当前筛选范围内的全部平台；超过 10 行时自动增加图表高度，不用 Top N 截断数据。
- 平台气泡矩阵保留全部平台点位，半宽卡片常显 DAU 前 3、较宽卡片常显前 5；其余平台通过悬浮查看，避免标签遮挡。
- 全平台趋势使用可滚动图例容纳 20 个平台系列；瀑布图、热力图、Cohort 和贡献矩形图同样不得省略当前范围内的平台。
- ECharts 悬浮详情挂载到页面 `body`，不受卡片圆角和 `overflow` 裁切；20 平台 Tooltip 限制在视口 70% 高度内并支持滚动查看全部平台。
- 顶部标题、说明和筛选控件使用自适应高度与明确行高，文字不得被固定顶栏裁切。
- KPI 在没有对比周期时只标记“当前区间”，不显示“较上期”。

### 工作台精简规则

- 工作台只保留一条全局主导航，不再并排显示第二条纵向模型侧栏。
- 分析模型改为内容区顶部横向选择条，集中显示名称和接口状态；当前模型说明只展示一次。
- 当前模型说明在中间区域展示。
- 接口路径与能力限制默认折叠。
- 右侧仅保留卡片名称、分析说明、漏斗字段和一张主预览。
- 保存按钮只保留一个，避免重复动作。
- 卡片库按图表类型折叠并支持类型筛选；卡片库和模板编辑器中的缩略图明确标注为“示例数据结构预览”，不冒充实时结果。
- 卡片库保留 12 种系统图表类型，并按能力标记“真实直接统计”“真实聚合计算”或“汇总近似”。旧浏览器缓存会保留用户卡片，同时自动补入新增系统卡片。
- 平台日期热力图、平台贡献瀑布图、平台贡献矩形图及内容/支付汇总桑基图作为可选卡片资产提供，不强行加入默认看板，避免首屏增加重复的全平台查询。

### 卡片平台范围与完整预览

- 顶部平台筛选是看板默认范围；卡片可保存独立的全部平台、单平台或 2–8 平台对比范围。
- 卡片标记必须展示实际范围来源，独立配置优先于顶部看板筛选。
- “完整预览”放大当前卡片，保留趋势、变化和表格视图，并允许临时切换平台范围；临时范围不自动覆盖已保存配置。
- 卡片编辑抽屉中的平台配置随模板工作区持久化，旧卡片默认跟随看板。
- 所有卡片标题栏均直接显示实际平台范围与完整预览入口；完整预览展示指标/维度中文口径、接口路径、聚合算法、公式、数据粒度和能力边界。
- 当前周期与对比周期统一显示 `↑/↓/→`、变化百分比和对比期数值；颜色依据指标正负含义，而箭头仅表示数值方向。
- 所有字段 `?` 为可点击说明按钮，统一弹窗显示中文定义、算法/公式、接口、粒度和能力边界；不再仅使用原生悬停提示。
- 指标说明使用独立的指标级口径字典，禁止以技术公式代替中文定义，也禁止直接展示模型的全部接口作为单个指标来源。
- “变化”只展示周期分析结论；“查看明细”使用独立按钮打开右侧下钻抽屉，图表节点点击仍可直接定位节点明细。
- 创建和编辑卡片时，指标按规模、终端、来源、广告、内容与观看、付费、留存等分组，并支持中文名称和字段 ID 搜索。
- 系统模板读取旧工作区时同步内置卡片的最新指标和图表配置，同时保留用户已保存的卡片宽度和独立平台范围；用户自建模板不自动改写。

### 数据状态

- 加载：显示明确的卡片级加载状态。
- 卡片查询：卡片顶部显示平滑进度条，状态文字同步显示模拟进度百分比；请求完成后补满至 100% 并淡出。该百分比表示等待进度，不冒充服务端真实处理比例。
- 页面查询：渠道、分类和专项页面的加载区域显示同风格流动进度轨道；减少动态效果模式下关闭循环动画。
- 顶部查询健康状态汇总加载中数量、异常数量、最近成功时间和单卡查询耗时。
- 查询失败：保留该卡片上次成功结果，并提供卡片级重试，避免短时异常清空整张图。
- 空数据：显示真实空态，不生成 Mock。
- 留存看板展示 D1、D3、D7、D30；未达到对应观察日的 cohort 显示“—”，不按 `0` 参与汇总。
- 全部平台模式下，接口未返回记录的平台以占位 `0` 补齐，并在卡片标题区显示覆盖数量；“暂无记录”与接口明确返回的真实 `0` 分开解释。
- 统计表的维度和指标列统一显示中文名称，内部字段 ID 只用于查询与计算。
- 配置不支持：阻止查询并提示进入卡片工厂调整。
- 接口错误：显示真实错误，不吞掉 401、403、429、超时和上游 500。

### 遗留主要代码

- `src/main.tsx`：工作区、看板、卡片、模板、下钻和专用分析页。
- `src/styles.css`：桌面与移动端布局。
- `src/components/Chart.tsx`：ECharts 渲染。
- `src/components/DataState.tsx`：加载、空态和错误。
- `src/components/AppErrorBoundary.tsx`：全局前端异常边界。
- `src/data/apiCapabilities.ts`：前端可配置能力和旧配置归一化。
- `src/analytics/realEngine.ts`：真实查询、结果转换和多平台图表系列。

### 遗留已知边界

- 正式数据不使用 Mock 兜底。
- 通用卡片和专项业务页均支持顶部完整平台范围；通用卡片还可保存独立平台范围。
- 图表点击下钻依赖对应明细 API；没有明细能力的指标只能展示汇总结果。
- 汇总桑基图仅连接固定漏斗各阶段的汇总量，界面必须显示“非用户级路径”；页面和坑位热力图仍因缺少 `page/position/rank` 字段而不开放。
### 2026-07-24 导航、筛选器与小屏预览

- 主工作区导航改为独立 URL，并在新浏览器标签页打开；支持 `templates`、`cards`、`cardFactory`、`templateFactory`、`dictionary`、`dataSources`。
- 左上平台与时间筛选器强化为主筛选块，展示平台模式、平台数量和双周期状态。
- 760px 以下完整预览使用 `100dvh` 全屏布局，工具栏横向滚动，图表、卡片和表格在内容区独立适配。
- 指标选择器将用户来源与充值彻底分组，充值指标只在“付费”分组出现。
