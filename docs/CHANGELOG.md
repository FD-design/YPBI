# 项目变更时间线

## 2026-09-16 原多平台 BI 内测上线

- 合并 PR #1，部署代码 d2d7ae8；Linux/Bun 1.4.2 发布校验 781 项通过，内测构建来源标记检查通过。
- Caddy HTTPS 静态服务、loopback Nginx/API、专用 ypbi 系统账号、独立 PostgreSQL 账号库上线；旧模板和运行配置已备份，NewAV 未修改。
- 更新并验证两站凭据，开启真实日查询，核心正式查询保持关闭。
- 八个 reader 交互创建并强制首次改密；PH 真实数据、TT 平台切换/刷新、登录及改密验证通过。真实指标仍待正式验数，缺失项保留演示/待接入标签。

## 2026-09-16 内测看板构建与接口决定

- 增加独立`build:internal-preview`：首期内部登录环境可保留已确认的看板演示区域，同时强制显示页面级来源说明；真实、待验数、演示继续按区块分层，查询失败或缺失不得由演示值替代。普通生产构建仍严格排除演示资源。
- 记录 M102、M058、M081 及全平台总体的接口补充决定：时长输入暂按秒且不额外剔除，界面自适应秒/分/小时；充值金额为全部支付成功人民币元，退款后续独立；周期观影率按人天合计重算；全平台总体按统一账号跨端去重。
- 本次只准备 PR 与内测构建方式，未合并、未部署、未创建线上账号，也未修改服务端正式指标准入。

## 2026-09-16 PR 上线阻断修正

- production 新增独立的`BI_DAILY_DASHBOARD_QUERY_ENABLED`，标准启动链可创建真实`DailyDashboardService`；登录、`bi:read`、PID、HTTPS反代、上游凭据和响应结构校验保持不变。旧本地开关仍仅限非生产loopback。
- 核心总览由标准启动链创建正式编排器；只有当前映射、passed验数、正式provider和可信完整日水位同时具备时才返回结果。当前9项未准入时返回`METRIC_NOT_READY`，不再因缺少手工注入而启动失败，也不伪造水位。
- 普通构建和发布校验改为验证仓库内固定指标、事件、功能投影及哈希证明；权威源再生成检查保留为显式维护命令，新环境不再依赖仓库外三份文档即可复现构建。

## 2026-09-16 首批账号密码限制调整

- 普通 BI 账号密码最短长度由 12 位调整为 6 位，使首批用户名加 `123` 的临时密码可以创建和登录；前端、接口契约、哈希校验及账号维护 CLI 使用同一常量。最长 256 位、scrypt 哈希、登录锁定、首次改密和会话撤销规则不变。
- 数据源维护密码继续保持至少 12 位，本次不降低维护入口权限。未创建账号、未修改数据库、未部署线上。

## 2026-09-16 原多平台 BI 代码审核交接准备

- 原多平台 BI 个人分支已上传并创建 [PR #1](https://github.com/FD-design/YPBI/pull/1)，目标为 main。源码验证提交为 `3c3436122d7b24bafe347dc9bbf937a111ed9dc0`，后续只同步交接状态；PR 未合并，服务器未部署。

- 建立个人审核分支并获取远端状态，保留原有提交及工作区改动，独立 NewAV 分支不合入。
- 类型检查、777 项单元测试、13 项合同测试、67 项生成与离线工具测试、前后端构建、生成快照和生产演示隔离检查通过。日卡演示投影保留失败状态并新增回归；浏览器更新旧展示断言，保留数值、日期、颜色、缺失、计算依据及导出验证。提交版 V2 浏览器分批复验完成；接口模拟专项 55 项分批通过；初跑长日期 1024 视口等待超时，同版本、同阈值下三档串行复验 3/3 通过，保留初跑记录。部署模块记录日志、正式截图、仓库外权威源依赖和内测上线分工；正式上线验收尚未执行。
- 接受目标仓库写入邀请并核实 `WRITE` 权限。按用户确认的 BI 工程范围，从最新 main 建立独立提交分支，迁入已审查的 BI 源码、配置模板、测试、必要文档及最终接口清单；第三方导出样例、竞品评审改动和临时产物不进入提交历史，原分支与文件保留。交付限定个人分支与 PR，未合并、未部署；本地 BI 账号、Token、数据与生产配置不变。

## 2026-09-15 本地看板后端恢复

- 确认前端和数据库仍在运行，后端 3000 无监听，登录会话代理返回 500。按用户授权使用现有本地 Bun 1.4.2 及私有配置启动后端，前端及数据库不重启。
- 后端健康检查、看板入口返回 200，匿名会话代理返回预期 401。未执行真实账号登录或指标验数；退出原因尚未确认，未新增自动恢复机制。
- 无业务代码、配置、账号、Token 或正式口径变更，未部署生产。启动命令与检查证据见部署模块。

## 2026-09-14 接口交接责任与全局导航精简

- Excel 主表区分开发补数据与 BI 复用计算，末列列出按对应范围和时间可得到的结果。完整日输入的日均、可加合计及日对比不再作为专用接口需求；区间真去重、同批输入、缺失状态及尚未确认的注册口径继续保留。12 项纯计算输入归入所属指标的公式、来源、范围及边界，独立主表为 159 行、定义表为 113 行，保留全部 125 个原引用。
- 全局导航仅保留各 Tab 总体。移除客户端/新老用户控件、交叉结构及对应导航导出页，保留点击人数、次数、分母、渗透率、人均点击、两期趋势、明细与完整导出；旧链接保留 Tab/指标并归一总体。功能使用及其他看板的维度不变。Excel 同步移除 252 项导航切片，剩余 2,706 项能力的状态与范围逐项回归。
- 验证通过：导航/相邻功能单元 8 项、共享 UI 单元 48 项、导航专项浏览器 9 项、共享浏览器 24 项、typecheck、build、生成与生产隔离检查；1280/1024/390 截图复查正常。日志位于 `/private/tmp/ypbi-navigation-{unit,shared,typecheck,build,shared-browser,overall-browser}.log`。Excel 通过覆盖/输入归属校验、两表渲染、计数重算、错误值、行高、颜色及冻结检查。
- 未改变正式指标口径、埋点契约、API、权限、账号或真实数据，未部署生产。导航真实采集及聚合查询仍待开发补齐，演示结果继续隔离。

## 2026-09-14 指标接口 Excel 按状态合并

- 主表按指标分组，同一指标的支持、部分支持、待确认、未支持各最多一行；编号和名称纵向合并，2,958 条能力收拢为 173 行。只有时间集合相同的范围归并，交叉维度只合并已完整存在的组合。
- 定义、公式、现有接口与字段集中于定义表；同名辅助引用合并为 124 行，保留全部 125 个编号及适用范围。主表为合并分组阅读视图，定义表保留筛选；颜色、横线、冻结定位继续保留。
- 自动反向展开与独立复核确认 2,958 条能力、状态、接口字段及关联信息无新增或遗漏；两表渲染、合并单元格、计数重算和导出错误检查通过。未改指标定义、接口契约、产品代码或线上数据。

## 2026-09-14 指标接口 Excel 逐项清单

- 工作簿收敛为「接口能力清单」「指标定义」两表。818 项精确维度范围展开为 2,958 条查询能力，逐项核对单日、趋势、区间、日均及适用对比；指标定义按 125 个编号去重，所属看板保留为字段。
- 每行列出支持状态、开发补齐项、筛选条件、返回数据及现有接口字段。支持使用绿色，部分支持和待确认使用黄色，未支持使用红色；数据行保留横线，表头和指标列冻结，支持筛选。
- 商品金额与每日 IP 输入明确复用现有字段；M095 按已登记的日/下载目标去重后累计，M114 业务定义与旧日报算法分列。月活与存量次日复访保留待确认状态。两表渲染、行分隔线、颜色、唯一编号、计数公式和错误值检查通过。未修改正式指标、埋点或接口契约。

## 2026-09-14 看板指标接口支持盘点

- 导出开发核对 Excel：11 板、193 条看板关联、91 个去重标准指标、2 个登记周期派生指标和 32 项辅助输入/读数。按 818 项精确范围能力分别核对单日、序列、合法周期结果、日均、比较和计算输入。
- 保留 80 条本地候选字段映射、30 项已登记接口及已记录目录外来源；区分已接待验数、近似源、基础输入可复用和未发现目标查询。旧 M114 日报与正式同批订单转化分开，月活保持自然月语义，人天复用既有日输入。
- 检查指标与看板引用去重、代表口径、公式重算和无错误值；7 张工作表完成渲染，6 个数据表支持筛选与冻结定位。盘点未调用上游，未修改正式指标、接口契约、权限、账号或 Token，未部署生产。入口见项目索引。

## 2026-09-14 内容分区、滚动修复及真实观影金额接入（2.33）

- 内容发现按视频、搜索、作者与社区连续分区，保留全部 13 项指标和原查询、趋势、明细及导出。共享搜索容器建立定位上下文，修复读屏标签撑大外层文档导致的滚动空白；保留键盘与读屏能力。
- 用户确认人民币金额直接展示元、元/人；`watchCount` 读取完整业务日 288 点作为播放发起次数，记录时长暂按秒换算小时及分钟/人。摘要、详情、经营表和导出同源；合法次数、总时长及金额补齐周期合计/均值。真实来源仍待验数，演示保持隔离。
- 用户批准本地日看板来源列表容量 4→5，旧 v1/v2、URL、权限兼容。PH 2026-09-12 实际页面核对发起次数 17,427,762、时长 135,104.97 小时、人均 10 分 17 秒/人、充值 11,603 元。整体活跃次日回访仍缺正式指标及同批查询，未生成估算值。
- 本批单元、浏览器、三档截图、类型检查、构建及生产演示隔离检查通过，逐项证据见调整方案 9.27；原 ECharts 分包体积提示保留。仅重载本地 3000 后端使映射生效，未修改账号、Token、数据库或线上旧 BI，未部署生产。

## 2026-09-14 版本表现仅保留客户端维度（2.32）

- 版本分布、核心表现、趋势及导出移除新老拆分；保留九项指标、两端显隐、总体、日期比较、版本聚焦与完整计算证据。注册 D1 和存量复访各自批次不变，其他看板的人群维度不变。
- 同名版本一行，总体独立去重或加权；人数分端并列，启动次数可堆叠。精简主表约束宽高，缺端与缺失状态保留，旧精确链接继续可用。
- PRD 2.32、调整方案 9.26、UI 规则 1.40 同步记录。实际测试与构建结果见 9.26。本地版本统计仍为隔离演示，未修改真实 API、权限、账号、Token 或线上旧 BI。

## 2026-09-14 日卡统计、计算表与提示分层（2.31）

- 普通日卡日期使用月-日（星期），移除常驻趋势对比说明；本地 `day-dashboard/v2` 返回明确能力范围的合计、均值，保留严格旧 v1 兼容。人数不按日相加，比率和未知币种金额不直接聚合；缺失、权限与验数状态保持。
- 同指标计算表与导出采用实名分子、分母列；保留两期、单位、公式和变量输入身份。分组悬浮提示分层对齐，共享定位按视口边界计算，窄屏不出界。
- 活跃结构独占一行，有效观影留存按摘要、批次明细、用户去向顺排；内容发现的曝光、点击名称精简并保留定义、公式、键盘和点击入口。小额人均金额与微小涨跌保留更多小数，不改变原始值或币种。
- 类型检查、构建、目录/引导/指标/事件投影校验及生产演示隔离检查通过；ECharts 原有分包体积提示保留。相关模型、服务及浏览器执行和定点复测结果统一见调整方案 9.25，不把重复测试累计为新增场景。
- 实际上游只读核对确认三个付费比率的计算输入一致；统计币种与全站观影聚合仍有待确认项，未新增未经批准的字段映射或换算。仅本地更新，未部署生产，未修改账号、权限、Token 或线上旧 BI。
- 内容发现的大区精简、版本分布按版本收敛及活跃人数堆叠尚未实施；整体活跃次日回访仍只有待支持说明，不计为分析功能完成。

## 2026-09-13 协作效率与长期规则固化

- 工作区规则增加确认事项持久化、按对象读取、精简并行交接和简明交付；产品规则仍只维护在原权威源。
- 项目规则明确影响面筛选、浏览器预检、受测代码冻结、失败分类、证据复用和定点复测。完整回归及数据、权限、原功能等质量门槛保持。
- 执行代理从本次起负责落实，每批交付核对重复读取、无效重试与遗漏；缺少可靠用量记录时不报告Token节省比例。本次仅修改协作规则与文档入口，不改产品代码、数据、权限或部署。

## 2026-09-13 末日卡片、轻量诊断与多维显隐（2.30）

- 普通日卡统一展示所选末日值；开启比较后显示较前一天、较上周同日及准确日期，曲线区间对比独立标注。周期合计、日均和去重结果只使用有来源的合法统计，自然月与成熟批次保留专用规则。
- 落地页原 3 个量与 3 个关系值补齐低高度趋势和就地详情；播放阶段、支付 9 项摘要补齐同范围日值、日比较和完整趋势/表格/导出入口，不重复大型图表。
- 版本、活跃、消费、功能和导航复用独立总体与精确分组显隐；支持单组关闭、两维批量开关和恢复。交叉表正常格突出结果，异常状态仍可见，计算依据与完整明细保留。
- 修正版本周期汇总将已产出零分母日误判为缺失的问题；真实缺失不补齐，全部零分母、未来日期和未成熟批次保持明确状态。固定注册日、D0 版本归属与 D1 实际回访样本设有回归断言。
- 总 PRD / 调整方案 2.30、UI 规范 1.38、长期规则、模块及入口同步；本批逐项体验和验证记录统一见调整方案 9.24。732 项单元测试通过，日卡、原阅读与原板/连接/多维等浏览器场景经执行及定点复测通过；重复批次不累计为新增场景。
- 最终类型、前端构建、各目录与引导投影校验及 8 文件生产演示隔离检查通过；原 ECharts 分包体积提示保留。
- 原 11 板工作台、专用分布、经营核对、平台、日期、搜索分页、明细与导出保留；未修改正式指标、API、数据库、账号或 Token。真实数据仍待独立验数，未部署生产。

## 2026-09-13 总体与分组显隐规则确认（2.29，文档同步）

- PRD 5.18 确认总体单结果、默认分组显隐、双维组合开关与简洁交叉表；版本表现目标与长期复用规则同步。
- 调整方案 9.23 区分已确认的版本交互、当前代码差异及其他区域复用建议。曝光 / 点击 / 支付等业务核对数据、高基数明细、固定留存和专用分布保留。
- PRD 与调整方案 2.29、UI 1.37、需求入口及索引同步；本次未改页面代码、接口、数据或部署，不继承上一批测试作为新交互完成证明。

## 2026-09-13 多维范围与读数联动（2.28）

- 新增共享分析范围控件与可点选交叉表；版本提供总体入口，多版本按客户端联合键和人群比较；功能、导航、活跃和观影复用同一交互。
- 单项读数补齐周期摘要和合法输入，横向排行固定显示当前值，连续区块统一间距。原M109区域扩展全局视频点击来源，并保留原视频首页一级分类明细。
- 确认方案写入AGENTS长期规则、PRD5.18和UI1.36；实现、回归与体验路径见调整方案9.22。正式指标、真实映射、账号、Token和生产准入不变。
- 本地回归完成：567项单元测试通过，真实读取与二维联动完整复测35项、版本6项及来源修正后Extended6项通过；首轮及重复覆盖详情保留在调整方案9.22。类型、构建及生产演示隔离检查通过；未部署生产，未执行新的真实业务验数。

## 2026-09-13 本地维护密码重置

- 按用户授权重置独立维护密码，仅替换本地私有配置中的密码校验值并重启本地 3000 后端；普通 BI 账号、权限、数据库、前端和两站 Token 保持不变，旧维护会话失效。
- 校验其余配置与凭据文件未变化、配置权限为 0600；维护鉴权 8 项测试通过，前后端健康读取均返回 200。明文密码与校验值不写入项目文档。

## 2026-09-13 版本人群、全局导航与诊断阅读（2.27）

- 版本核心表并列总体、新用户和老用户，摘要与趋势支持人群选择；Android / iOS 独立，固定留存人群保持原定义，对比版本拆解绑定实际版本。
- 启动成功率作为主结果，启动次数作为紧凑辅助；启动与播放看板去除网络展示，事件分析底层维度与连接错误不变。
- 功能概览采用全量表与行内条形，保留原选择趋势与结构；全局导航移入本页，展示点击人数、次数、活跃分母、渗透与使用者人均，支持客户端 / 新老和完整逐日核对。
- 内容发现整合总体曝光、匹配点击、转化和位置表；搜索规模与承接同表，播放与玩法就近列出计算输入。注册 D3 / D30 精简且保留原详情，支付方式增加成功次数、拉单次数和率核对表。
- 总 PRD 与调整方案更新至 2.27，UI 护栏 1.35，模块、入口和索引同步。正式指标、接口、账号、Token、数据库、既有真实映射及生产部署未改变；新增未接数据继续隔离演示。逐项实测与验收入口统一见调整方案 9.21。
- 本地模型、共享组件、浏览器专项与相邻功能回归通过；桌面与窄屏实图复查完成。紧凑指标保留完整折线、柱状、表格与下载，主辅宽度及工具按钮实际点击已验证。当前待用户界面验收，真实业务验数与生产部署独立进行。

## 2026-09-13 获客、观影时长与启动版本阅读结构实现与本地验收

- 获客与新增：保留原“增长效果 / 渠道结算”、首屏六卡及趋势。增长效果内将来源渠道、下载目标、获客类型改为三个纵向同时可见的独立区块，各自具备状态、排序、分页和导出；来源渠道和渠道结算保留搜索，下载目标 / 获客类型仅在结果超过 8 项或旧链接恢复非空搜索时显示本区搜索。来源渠道四指标组和渠道结算不变。下载目标用表格核对量与已登记转化，获客类型用紧凑数值比较，不重复首屏趋势、不把重叠人群画成构成。
- 视频消费表现：原首屏总时长、人均时长两卡及趋势保留；在原位置增加总体摘要与长 / 短 / 其他未分类类型比较。类型数据没有可信来源时不画占比、不用总体相减生成类别。
- 产品体验与启动质量：启动诊断与版本表现上下顺排且同时可见，不再使用外层互斥 Tab；启动诊断标题下直接接原两卡，移除重复“启动核心”标题，结构诊断、导航及版本三个问题标题保留。版本分布宽屏图与精简表同显、窄屏上下排，精简表与原 8 列完整表、导出共享全量行。
- 文档与数据边界：总 PRD、调整方案、需求入口、前端模块、索引及 UI 护栏同步至 2.26 / 1.34；一句引导仍从总 PRD 5.16 唯一派生。正式指标、API、数据库、Token、账号权限、真实数据准入、线上旧 BI 和生产部署不变。
- 验证状态：37 项独立 Chromium 目标回归及额外 4 项最新版版本专项通过；38 项已登录原板结构 / 相邻质量与 23 项获客 Chrome 分别通过，批次有覆盖重叠不相加为独立总数。45 项共享 UI 与五个相关模型文件 41 项、775 次断言通过；`npm run typecheck`、前端构建、生产演示隔离和唯一引导投影检查通过。获客、观影时长、启动首屏与版本图表三档截图已实际复查；未完成线上真实业务值验数或生产部署。

## 2026-09-12 本地登录超时运行恢复

- 现象核实：旧本地 `3000` 进程 PID `55216` 使用 Bun `1.4.2`；健康检查、匿名会话和空请求体 POST 均为毫秒级响应，但合成且格式合法的、不存在的测试会话值触发账号库查询超过 5 秒，既有 3 秒请求期限未按时返回。同期独立 PostgreSQL `select 1` 为 64ms，账号未锁定且无登录失败记录。
- 恢复与验证：仅重启本地 `3000` 后端，未重启 `55432` 账号库或 `5173` 前端；新进程 PID `6840` 仍使用 Bun `1.4.2`。重启后无效会话 39ms 返回 401，现有账号真实登录 439ms 返回 200，平台目录返回 200，PH 真实日看板读取成功。
- 补充实测：Chrome 实际登录后 20 个平台均可选择；PH→TikTok 应用后读取对应真实日值，刷新后账号会话、TikTok 平台、日期和真实结果均保留。最终健康检查 200 为 3.5ms，查库会话 401 为 5.1ms。Bun `1.4.2` 下认证插件、数据库与 HTTP 响应生命周期 21 项定向测试共 95 次断言通过，覆盖超时取消和中文响应生命周期。
- 结论与边界：当前定位为旧运行实例的认证查库或请求期限执行异常，底层触发原因未定，不宣称永久根治，也不归因为进程休眠或特定数据库客户端缺陷。本次未修改代码、权限、数据库、Token、布局或产品规则，因此未重复执行类型检查和完整构建；生产仍未部署。日常入口为 `/dashboards/public`，`design` 参数仅用于开发评审。

## 2026-09-12 11板区块引导与原看板内分析增强

- 共享引导：保留 11 张正式看板的既有区块标题，以总 PRD 5.16 的 57 个稳定文案键绑定对应区块；一句分析说明由生成投影统一提供，窄屏换行不缩字，不在父子区块或逐指标重复。
- 获客：保留原首屏 6 张指标卡、首次激活 3 卡和各自趋势；下方重复趋势收敛为正常文档流中的紧凑阶段关系。原增长效果的来源渠道表增加规模与转化、首次体验、注册留存、付费价值四组列切换，搜索、排序、分页、完整明细、导出及原渠道结算保留。
- 活跃与视频：活跃页增加整体活跃次日回访的待支持说明，不拼 M020 与 M024、不生成数字；视频页在原 D1 / D3 / D7 卡与成熟矩阵之间增加同源三去向，沿用原顺排结构且不增加 Tab。
- 版本：产品体验与启动质量保留原启动诊断和导航，以页内“版本表现”视图顺排版本分布、核心表现和变化趋势。启动次数与登录活跃人数分布分开切换，Android / iOS 不混合；行为版本归属、留存冻结及跨端回访边界按总 PRD 5.15 执行。
- 数据边界：渠道 19 项、观影三去向和版本 9 项仍为隔离演示；整体活跃次日回访仅显示待支持说明。原 7 板部分真实与 4 板演示状态不变；未新增指标、事件、公共 API 或数据库变化，未将演示值写入正式导出。
- 验证：57 项引导投影和 2 项生成器测试通过；覆盖基础工作台、专题、获客、已登录读取、生产隔离、区块引导、观影去向、相邻能力和版本表现的 174 个独立 Chrome 用例全部通过，并逐张复查 1280 / 1024 / 390 三档截图。最终 `verify:release` 以 0 退出，539 项 `src` / `server` 测试共 4524 次断言、类型检查、前后端构建和生产演示隔离全部通过。已登录读取路径仅使用受控服务与接口夹具验证，不代表线上业务值验数。

## 2026-09-12 原版看板真实数据接入与逐项验收清单

- 数据接入复用原11张看板的工作台、区块、卡片、图表与操作。7板部分真实、4板仍演示；候选映射从日汇总、注册留存增强、渠道V2、支付通道、签到概览5类来源派生77投影，引用31个既有指标ID。经营明细73列中58列接入，支持原日期、平台、昨日／上周同日、筛选、排序、拆解和导出。
- 原位置补接：注册留存率／人数与矩阵、客户端及新老观影切片、自然／内部导量新增和获客明细、导航广告与新用户广告、支付总体及微信／支付宝次数和成功率、VIP／金币收入构成、签到人数。未映射维度继续独立演示，不用总体替代切片；蓝色“待验数”和橙色“演示数据”按模块／单元区分。
- 准确性护栏：分来源独立失败、已回显平台／日期冲突拒绝、分页和重复行校验、严格数值、缺值与0分离、成熟观察窗口、比例计算输入及真实原币单位。独立注册日期单独读取；旧日期资源不冒充新日；详细图按当前／对比系列语义定位；导出保留两期、平台、时间、状态与计算输入。演示卡不显示正式“已验数”文案。
- 下载：经营明细增加逐期计算输入Sheet，真实／演示／混合来源分别命名。共享下载层处理Excel工作表名称限制，附完整名称索引，修复整板多指标导出失败；原演示下载保持可用。
- 真实验证：20平台同日共1,540个候选结果点，另对两站代表连续7日检查。补充核对发现PH日汇总与渠道统计的访问量／注册量不相等；两站新用户VIP＋金币金额与新用户总充值金额不等。FBI最后两次渠道核对超时，按来源失败处理。技术成功不替代独立业务验收，未修改正式指标定义。
- 清单：`docs/modules/data-platform.md`按11板逐模块、73经营列、77投影列出来源字段、已接／演示、未接原因和15项验收动作；区分来源存在未适配、口径未核定、接口失败与尚未发现来源，保留大范围性能及独立对账缺口。
- 验证：`verify:release`通过，最终520项Bun测试、类型检查、前后端构建和8文件DEV隔离通过；180个Chrome不同场景经全量执行及定点复测通过，最后连接专项＋支付／质量39项全通过。实际复查1440/1280/1024/390截图及下载结果；保留已有ECharts体积提示。文档与差异检查通过。
- 边界：新增本地受保护只读路径，原正式指标准入不变；只允许非生产loopback启用。未改变既有布局、图表类型、账号／Token、权限规则、数据库或线上旧BI，未部署生产。关闭本地候选开关可停止本批读取。

## 2026-09-12 两站真实日汇总抽查与分页修复

- 两站Token由用户在维护页保存后，只读抽查20个启用PID的2026-09-05至2026-09-11日汇总：每个平台7行，M016候选值均存在，无重复业务日或越界平台/日期；未输出凭据和用户明细。该检查不构成独立参考验数。
- 实际接口返回`msg.totalCount`，M016 source补充识别并校验其与既有`total`一致；截断、反向行数、损坏类型和冲突均拒绝，真实0及未知水位保持原语义。
- 验证：M016 source 9项测试、类型检查、生产构建及8文件DEV隔离通过，差异检查通过；保留既有ECharts体积提示。公共API、账号库、权限、权威指标定义及正式查询开关未修改，无数据迁移、未部署。真实看板读取仍未交付。

## 2026-09-12 本地登录账号更新

- 按用户指定更新独立本地BI账号的登录名、显示名和登录密码；保持用户ID、角色、数据范围、独立维护密码及两站配置不变，撤销旧会话。
- 通过本地前端代理实际验证新凭据登录、原身份和维护权限，验证会话已撤销；未修改认证策略、Schema或线上环境，未在文档保存凭据。

## 2026-09-12 共用概览工作台、长日期阅读与独立本地维护

- 工作台：公共/我的概览复用目录、筛选、内容区与手机选板入口，站内切换保留对象与历史；评审横幅、正常/组合异常态和完整图表入口集中到账号菜单的体验工具。正式包不包含评审界面与样式；移除返回经典版入口，保留底层旧版。
- 阅读：留存热力矩阵使用固定行高、冻结日期与表头、区域滚动，覆盖39/90/180日；完整表保留逐日计算输入，公式按留存周期集中说明。少量选项不显示搜索，比较维度采用完整菜单，功能提示使用中文业务名称；共享搜索框统一高度，弹窗下载在当前非惰性容器内触发。
- 维护：经授权启用独立5173/3000/55432本地链路、自有账号库与首次改密身份；两站HTTPS地址及请求用户名按用户提供配置。所有已登录角色拥有共享Token维护能力，继续执行维护二次认证、CSRF、候选验证、失败保旧及敏感输入清理；技术验数查询仍只对维护者开放。
- 本地认证：实际PostgreSQL发现的账号状态参数类型冲突修正为一致varchar，停用/重新启用撤销旧会话；集成回归账号测试后停用。首次初始化与日常启动分离，不覆盖既有配置和账号。
- 数据边界：两站等待用户在页面填写Token，未取得真实业务数据；既有指标/事件口径、正式指标准入和线上旧BI保持原状。连接成功与逐指标验数分别验收，未接入模块继续标注演示数据。
- 验证：完整发布校验通过，含454项Bun测试、类型检查、前后端构建及生产DEV隔离；154项Chrome用例经全量执行与定点复测通过，另有1项真实PostgreSQL登录/改密/维护/账号状态集成流程通过。复查1280/1024/390及宽屏真实渲染、下载文件与输入保护；10份文档53条链接和差异检查通过。保留既有ECharts分包体积提示，未执行生产部署或真实Token查询。
- 文档：总PRD2.23、调整方案2.22、UI护栏1.31及模块文档、需求入口、索引同步。执行与接入边界集中于调整方案9.17。

## 2026-09-11 共享样式、阅读路径与维护入口修复

- 共享展示：搜索容器补全基础样式；个人阅读Provider显式维护网格和间距。同排同类卡片对齐，计算依据常驻入口收为一行；趋势和表格共用阅读高度，完整表内部滚动，不推动邻卡。留存工具、图例与色阶按视图呈现，放大继承当前选择。
- 专题与速览：指标、比较维度和排序分别成组，玩法Tab保留区块间距；支付辅助指标使用共享卡。速览支持平台/大盘日周对照、7/30日趋势、指标排序、权威定义及带当前指标/平台/日期的分析草稿，未保存配置保持退出保护；移除日报页面无上下文的空白漏斗链接，独立漏斗分析保留。
- 数据：DEV标题常驻演示标识。支付九项、微信/支付宝、去重基数、图表和数值导出来自同一交易与尝试事实，下载保留原始精度及逐日实名公式输入；正式缺失、失败和权限状态不降级为演示。指标/事件权威定义、API、权限、数据库和正式开关未变。
- 维护：预览顶栏“连接真实数据”进入正式受保护路由并卸载演示身份。隔离用例覆盖从看板进入维护、候选验证、保存、失败和会话清理；本机真实后端、账号库及上游配置未启用，未创建维护身份、未接收真实Token。所有登录用户更新共享Token的权限批次待确认，未部署。
- 验证：449项Bun单元测试通过；61项Chrome组合回归通过，覆盖11张看板、个人工作区、速览、共享查询及11项隔离维护用例。实拍复查1440/1280/1024/390卡片及冷启动搜索、诊断工具和手机支付；类型检查、生产构建、DEV隔离和`git diff --check`通过。7份文档51条文件链接有效；保留既有ECharts分包体积提示。测试响应不代表真实上游联调。
- 文档：总PRD2.22、调整方案2.21、UI护栏1.30及前端模块、需求入口和索引同步。真实接入缺口集中记录于调整方案9.16。

## 2026-09-11 经营阅读反馈收口与Token接入指引

- 共享展示：复合指标卡用实名输入和公式，详细表分别列出当前期、对比期基数；总体与分组独立切换，占比保持原总体。纯数值卡移除趋势占位，混合网格重排，标题覆盖根因在共享CSS修正，宽屏新老付费构成与摘要并排；未采用页面缩放。
- 专题：留存提供全日期热力概览、折线、矩阵表、完整计算表及放大；播放质量加入同源M036。支付主视图采用九项日报业务指标和总体/支付宝/微信，移除虚构支付方式和订单数；真实查询未接入时不补值。观影总/长/短时长与导航广告分别保留类型和身份映射状态；搜索、互动保留横向排行与完整表。
- 交互：看板说明展示问题和当前指标清单；只读表格、说明、导出、目录与维护确认复用遮罩关闭及拖动保护，编辑草稿保留保护。功能趋势增大图例、节点与提示。
- 数据源：复用已有双站维护入口、候选验证、验证保存、失败保旧及按PID选择凭据；新增三步接入说明和指标接入状态入口。Token只经既有受保护后端处理；取消保存确认保留输入。未录入真实Token、未验证真实连接，连接成功不等于全部指标可用。
- 文档：总PRD2.21、调整方案2.20、UI护栏1.29及前端模块、索引同步；逐项实现与剩余数据缺口见调整方案9.15。指标/事件权威口径、公共API、身份、存储和生产开关未改变，未部署。
- 验证：40项Chrome回归通过，包含11项Token维护及1280/1024/390视口；另补拍维护页完成态，下载总体与实名占比基数补充后复跑7项组合用例通过。类型检查、生产构建及DEV隔离通过；25项看板模型和6项视觉Token测试通过；外部埋点文档38181项校验通过；7份文档51条文件链接有效。共享Bun套件因环境缺少Bun未运行，构建保留既有ECharts分包体积提示。

## 2026-09-11 运营组合展示与共享阅读本地试做

- 共享阅读：普通指标卡按看板切换纯数值/数值＋趋势，固定经营摘要与专用图保持原状；本机逐板偏好与查询分离，个人看板仅查看态适用。比例和ARPPU按当前结果展示公式、分子、分母及状态。少量分组支持默认全部、至少一组和局部摘要/图/表联动，完整导出不裁剪。
- 专题：留存矩阵接入率/人数色阶、固定日期/基数、近7日/近30日/自定义统一应用；会员付费接入新老付费、商品/端别收入构成、日趋势和交叉明细；支付页接入同批成熟订单漏斗及支付方式比较，原阶段关系保留；核心总览增加广告四项摘要共用趋势；获客增加下载构成、阶段转化依据、已登记来源比较和原明细定位。完整图表体验复用上述组件。
- 数据边界：组合内部从同一合成事实产生摘要、日序列和工作簿；收入与订单是独立演示场景，24小时是演示观察窗口。来源归因MD-14、真实订单关联/窗口及其他接入事项继续独立确认；未新增正式指标、未修改API、身份、存储或部署，未把待接入项补成0或闭合构成。
- 适配根因：工作台宽度覆盖新增阅读容器；展厅网格使用最小宽度为0的主轨；留存列保留可读宽度并局部滚动。结果树仅在真实导航时重建，本地视图写入URL不重置阅读状态。漏斗文字显式使用高对比色，表格单位只显示一次。
- 文档：总PRD2.20、调整方案2.19、UI护栏1.28，以及前端现状、需求入口和索引同步；本地已实现、用户视觉验收与正式数据准入分别记录。
- 验证：50项Chrome浏览器用例通过，覆盖新增组合、原经营明细、获客、六张专题、个人工作区、全局查询与导航；4项新Node单元测试通过。实际复查1280/1024/390截图，类型检查、构建、三类权威投影检查、生产预览隔离、7份文档51条文件链接与差异检查通过。共享Bun测试因环境缺少Bun未运行；构建保留既有ECharts分包体积提示。视觉定稿待用户体验，尚未上线。

## 2026-09-11 经营明细根因修复与确认状态复核

- 共享表头单位去重，移除端别比值单位的60px约束。经营表和完整图表体验为隐藏caption建立局部定位上下文，修复外层多余纵向高度；未通过隐藏body滚动补偿。
- 总PRD2.19、调整方案2.18、UI护栏1.27同步。12项展示方向均可试做；数据核对、正式对象接口与存储批次、视觉验收分别记录，其余组合未宣称完成。
- 验证：9项相关Node单元测试、6项经营明细Chrome回归、1280/1024/390截图复查、类型检查、构建和生产预览隔离通过。共享Bun测试因环境无Bun未运行；未部署，指标、API、身份和存储不变。

## 2026-09-11 支付与下载展示确认，进入统一试做

- 总PRD2.19、调整方案2.18：支付订单漏斗、支付方式比较、分端下载和落地页转化四项确认，目标分别落在5.11.5/5.7.7，与此前确认项统一实施。
- 数据边界：订单事实与通用人数漏斗分离，访问/下载次数与注册用户阶段关系不包装成真实漏斗；业务定义、正式接口、存储和权限准入保持独立。
- 本条记录产品确认，本地代码与回归结果另按实际完成情况登记。

## 2026-09-11 广告点击表现确认与剩余项收拢（待统一试做）

- 文档：总PRD2.18、调整方案2.17；确认核心经营总览经营明细之前的广告点击表现，四项单日摘要选择、总体/新用户、同源日趋势与计算依据。首屏九项及其批量契约不扩展。
- 待确认：支付漏斗/支付方式与分端下载/落地页转化共两组四点仍为候选；新老付费、商品及端别收入等已确认项保持原状态，数据目录与正式准入单独核对。
- 引用修正：来源归因使用MD-14，业务漏斗保留MD-12；同步当前及历史记录中的来源引用，消除同号异义，业务内容未更改。
- 验证：检查六份文档的版本、决策状态、唯一编号、引用、链接、Markdown与差异；本轮未修改代码、指标/事件、API、身份、存储、正式数据映射或部署，未执行代码及浏览器回归。

## 2026-09-11 来源结构展示方向确认（待统一试做）

- 文档：总PRD2.17、调整方案2.16；获客页新增来源构成、每日人数/占比及原明细衔接的展示方向确认，保留既有查询日期、渠道与结算边界。
- 准入：MD-14单独承接买量/裂变分类、归因窗口、去重/冲突优先级和内部导量交叉关系，正式指标源尚未修改；构成不完备时保留可比结果和状态，不相减补来源、不混入事件次数。
- 广告复查：调整方案OPT-07登记现有4项广告点击指标及新用户视图、经营明细8列和摘要入口；区分点击渗透率与曝光-点击转化率，广告具体展示方案保持待确认。
- 验证：文档版本、状态、引用、链接、Markdown及差异检查；代码、指标/事件、API、数据接入、身份、存储和部署未改，既有页面读取不作为新方案验收。

## 2026-09-11 新老用户付费表现与切换可发现性确认（待统一试做）

- 文档：总PRD2.16、调整方案2.15；新老付费以金额构成、两行摘要、金额/人数/ARPPU逐日趋势试做，补充指标与其他结构入口保留。原OPT-04拆出已确认OPT-04A，订单级链路/支付方式补充继续待确认。
- 交互：少量高频短选项显式分段，复杂长列表和低频配置保留菜单；原短指标规则集中至PRD4.3，分组多选继续复用图例。环图聚焦保留原构成及分母，区间去重、同快照计算依据和完整导出有独立边界。
- 验证：文档版本、状态、引用、链接、Markdown及差异检查；现有浏览器阅读仅作对照，不作为新方案验收。代码、指标/事件权威源、API、正式数据映射、身份、存储与部署未修改，视觉效果等待统一试做。

## 2026-09-11 整板卡片模式与商品分端收入确认（待统一试做）

- 文档：总PRD2.15、调整方案2.14；确认当前整板普通指标卡纯数值/数值＋趋势切换、固定单值及专用分析图例外、个人逐看板偏好和查询结果不变。各专题默认趋势描述统一受阅读模式约束。
- 收入结构：会员与付费经营复用现有VIP/金币及总充值金额维度，集中展示构成、每日趋势和精确明细；环图固定同口径总额、类别占比与精确提示，保留小占比、键盘和触控阅读。AI收入、单次均价及未验证端别映射独立治理。
- 核对：只读复查本地付费/支付页及运营充值商业化，结合共享组件与专题配置建立11张官方看板的展示复查范围；具体待确认专题不因整体重组原则获批。支付订单漏斗及新老充值细节仍待讨论。
- 验证：仅进行文档版本、状态、引用、链接、Markdown及差异检查；浏览器只读对照不作为新方案验收。页面、代码、指标/事件定义、API、存储、身份和部署未修改，视觉效果待统一试做。

## 2026-09-11 分组多选展示与按任务选图试做记录

- 文档：总PRD2.14、调整方案2.13；通用分组多选同意试做，默认全部、按需聚焦和同区块视图联动进入试做目标，逐指标落点与视觉定稿待体验确认。经营明细、留存及比例阅读继续等待统一批次。
- 规则：在既有选图规则中补充同一数据/范围/容器的合法形式对照，强调分析任务与实际可读性；分组明细可聚焦但保留完整结果和导出，不改变指标、分母或全局查询。候选展开、汇总及交叉提示布局不直接视为全部获批。
- 验证：仅检查文档版本、状态、引用、链接、Markdown与差异；未执行代码或浏览器回归。UI沿用现有护栏，未新增未复现的根因条目。
- 边界：本轮未修改代码、测试、指标/事件权威源、公共API、真实数据映射、权限、存储或部署；视觉效果在后续试做中验收。

## 2026-09-11 注册日期快捷确认与分端观影核对

- 文档：总PRD2.13、调整方案2.12。注册日期近7日/近30日/自定义、默认7日与统一应用确认落档；沿用同一日期状态，明确卡片、趋势、矩阵、导出及成熟范围边界。UI-025既有规则继续适用，UI版本不变。
- 核对：只读体验本地视频消费看板的消费结构、明细，以及运营看板的分端观影；当前区间结构与分端逐日阅读缺口记录于调整方案9.14。分端观影方案未进入正式PRD。
- 验证：文档版本、引用、状态、链接与Markdown差异检查；页面体验只作现状证据，不作为修复或上线验收。
- 边界：未修改代码、测试、指标/事件定义、API、数据接入、权限、存储或部署，已确认项等待统一实施。


## 2026-09-11 留存矩阵与比例计算依据确认落档（待统一实施）

- 文档：总PRD2.12、调整方案2.11、UI规则1.26。确认留存矩阵的率/人数分段、统一色阶和图例、状态及定位阅读；比例结果补齐权威公式、同快照分子/分母名称和数值，经营卡片拆解同步引用。
- 范围：留存保留D1/D3/D7/D30与M115周期引用；新增的7/30注册日期快捷呈现仅记录为待确认建议。共享有效观影矩阵保留其独立人群及窗口。
- 验证：仅检查文档链接、版本、确认/实施状态与Markdown差异；未运行代码或浏览器回归。当前实现差异写入调整方案，未标为已修复。
- 边界：未修改代码、测试、指标/事件权威源、公共API、真实数据映射、权限、存储或部署，等待统一批量实施。


## 2026-09-11 经营明细阅读优化确认落档（待统一实施）

- 文档：总PRD2.11、调整方案2.10、UI规则1.25。经营宽表的表头、定位、时间说明和单一纵向滚动规则写入15.4；9.14登记确认状态与待讨论议题，补齐调整方案2.09既有版本记录。
- 复查：浏览器确认两类Android:iOS表头单位溢出、工作台之外的外层空白；后者具体根因待定位。UI-036/041保留既有回归并增加待执行检查，未标记为已修复。
- 验证：文档版本、章节引用、确认与实施状态及Markdown差异检查；本轮不运行代码回归，不将既有测试作为本轮修复证据。
- 边界：仅更新既有文档；代码、测试、指标/事件权威定义、API、数据映射、身份、存储与部署未变。各议题确认后统一实施。

> 时间倒序排列，只追加新记录，不删除历史。时间均为 Asia/Shanghai。

## 2026-09-11 经营明细完整日报字段与紧凑摘要

- 产品：总PRD2.10、调整方案2.09、UI规则1.24。经营明细保持单日、逐业务平台的完整宽表；访问、下载和访问到下载转化引用次数口径。权威指标目录同步v0.24-draft，共98个标准指标和3个周期派生指标，未新增或修改正式指标定义。
- 字段：覆盖原日报62个字段，其中4项基础信息、58项业务字段；保留15项既有额外业务列，共73项业务列。补齐广告、ARPU/ARPPU、新用户付费、自然月MAU、订单获取及支付宝/微信切片、D1/D3/D7留存人数与比率。模式、推广状态及未接入指标保留空值和状态；订单获取与提交、支付成功与到账、广告人均与百分率分别引用已登记定义。
- 阅读：原6项纯数值卡默认展示，可展开到10项，新增付费人数、付费率、ARPPU、广告点击渗透率；卡面提示适用维度，详情沿用同一列映射与只读关闭。完整图表体验复用10项并保留零值样例。定位器支持名称与日报别名搜索，定位不裁剪表格或导出；同快照单日大盘付费率使用对应大盘付费人数与活跃人数，平台人数不相加。
- 导出：实际下载包含说明、明细、数值摘要、字段说明和数据状态5个Sheet；明细307列包含业务值、状态、两个基准及月活/留存日期。用表格工具只读核验真实下载文件，62个日报字段均可追溯、数值保持数值类型、缺失保持空值，原Excel未改动。
- 接入：旧M016映射仍绑定v0.23-draft，在当前目录标为stale；维护者也不能执行过期映射。查询服务的测试依赖与实际发布登记分离，生产默认门禁保持，未自动升级或批准映射。
- 验证：Bun1.4.2运行`test src server tools/generate-metric-definitions.test.mjs`，462项全部通过；经营明细、导航、全局筛选和目录中心23项浏览器回归通过，最终付费率修正后经营明细6项再次通过。实际查看1280/1024/390布局及下载文件；`npm run typecheck`、`npm run build`、三类派生检查与生产预览隔离通过，埋点文档38181项检查通过。既有ECharts分包体积提示保留。
- 边界：本地开发体验，尚未部署；正式API契约、上游adapter语义、指标/事件权威定义、身份、数据库、原工作簿和生产配置保持原状。字段覆盖不等于真实数据可用，待接入字段不补0、不倒算人数、不拼接交叉维度。

## 2026-09-10 分析配置、受控图形与数值卡本地交付

- 产品：总PRD2.09、调整方案2.08、UI规则1.23。事件/指标采用顶部配置与同快照完整表；受控饼/环及漏斗进入本地交互验收，完整正式目标保持独立准入。
- 实现：事件选择支持搜索、分类、能力说明，条件按元数据约束；兼容条件换事件保留，数值属性统计、时间粒度、真实二维合成事实、独立UV、日期展开和轻量继续拆解已接入。已应用结果与草稿分离，图形Top10/缩放不裁剪表格和导出，继续拆解收窄已有多选条件；提示中的用户别名按文本渲染。
- 图形：详细时间卡统一折线/柱状/表格；单维可加总事件构成允许饼/环，人数去重、平均值和比例不进入占比。分类点击三项使用分段，更多选项可搜索，搜索排行支持前100的阅读上限且不补造不足项。独立漏斗使用固定合成事件流计算有序转化、流失、中位耗时和成熟状态，接入保存/重开/加看板及五Sheet样例导出；视频与支付专题、完整图表体验提供入口。正式有效观看、到账终点、身份关联与执行器仍待接入。
- 数值卡：直接展示维度与待接入提示，整卡与定义独立命中；详情使用紧凑横条/精确值及折叠数据说明。共享只读遮罩关闭、内部拖动保护、焦点恢复与编辑草稿保护统一实施；经营宽表和原首屏九卡保留。
- 日报：只读核对原Excel表头及保存值，未改工作簿。调整方案9.13列明58个日报业务列中的16个候选对应、42个未展示/待确定，以及15个BI额外列；模式/推广状态独立列为元数据决策，不新增正式指标。
- 验证：最终436项前后端测试通过；20项相关浏览器回归全部通过，最终拆解修正后6项分析专项复跑全部通过；另16项分析/目录/专题回归通过。已实际复查1440/1280桌面、1024平板及390手机截图，包含卡片高度、弹层和内部滚动；样例XLSX确认数值保留数值类型。类型检查、构建、权威投影检查与生产包隔离通过；埋点文档38180项检查通过。180天三步漏斗合成查询在本机单次检查约695ms，不作为正式性能承诺。
- 边界：仅本地展示、隔离合成查询及配置内存，刷新清空。正式指标/事件契约、API、身份、对象数据库、上游映射和生产部署未改变。剩余目标集中在调整方案9.11，完整日报列按用户决策与权威准入继续实施。

## 2026-09-10 分析外壳与数值卡阅读规则确认

- 目标：总PRD2.08确认指标/事件顶部配置、完整表按日期展开、图表同快照与轻量继续拆解；明确作用域、返回、过期保护和不支持组合。数值卡直接提示适用维度、整卡操作，详情采用紧凑比较行并支持只读遮罩关闭。
- 现状：调整方案2.07记录现有侧栏配置、受限事件样例、拆解重复说明和缺少遮罩关闭的代码事实；9.12逐项区分新版业务图形、体验样例与尚未实现的候选。UI规则1.22增加待实施的复现和回归入口。
- 边界：本次仅修改既有需求、差异、UI护栏与入口记录，未实施页面、未改指标/事件权威定义、API、身份、存储或生产部署；经营宽表、既有首屏趋势和其他分析能力保留。图形候选的数据准入与正式接口批次不因产品确认自动获批。
- 验证：版本指针、唯一章节、被替换规则清理、代码围栏及待实施状态检查通过，`git diff --check`通过；浏览器与业务代码回归留待实施批次执行。

## 2026-09-10 个人工作区页面与内存配置闭环

- 产品：总PRD2.07、调整方案2.06、UI规则1.21。我的概览、指标速览、指标分析、事件分析和已保存分析从已确认需求继续实施；正式API/数据库审批不作为本地页面前置条件。
- 实现：精确DEV路由接入公共概览“我的概览”及顶栏入口；定义选择只读权威投影。配置仅存在本次体验内存，保存/另存/重开、加入看板及复制/移除组件保持稳定来源引用，刷新清空，不写本机存储或账号库。查询草稿与结果分离；不同单位分图，复用数值摘要、图例分页、日期缩放、结果表和唯一关闭详情。速览展示实际日、最近成熟注册日与同口径样例比较，单PID缺口不以大盘替代。
- 统一：共享页头负责按钮间距与换行；现有看板、两个目录与新页面按同一响应式边界检查。纯指标定义、事件契约、API、权限、数据库和生产部署保持原状。
- 视觉复查：修复结果tabpanel内层最小宽度造成的窄屏裁切，以及普通输入样式覆盖range输入造成的缩放缩略图遮挡；配置在平板/手机默认收起，数值列与单位列不重复单位。新增内层几何和滑块透明背景断言，最终6项个人链路重新执行全部通过，截图以最终结果为准。埋点文档38180项校验通过。
- 验证：8项目录/个人页面首次完整回归通过；随后6项个人核心场景及14项既有全局/共享交互合计20项浏览器回归通过。最终9项配置/路由/数值格式单测、40项共享UI测试通过；类型检查、构建与生产包预览隔离通过。图表复用规整坐标域，线图按可见波动范围、柱图包含零基线，最终6项页面场景再次通过。截图、实际样例XLSX及日志位于本机`/private/tmp/ypbi-personal-*`。原生Node测试入口受现有无扩展名导入限制，使用项目固定Bun1.4.2执行相同路由测试通过。
- 剩余：这是本地交互切片，不是完整四模块或正式业务交付。真实查询、持久化、权限与完整配置编辑的具体未覆盖项集中在调整方案9.11；接口批次仍待单独确认。未发布。

## 2026-09-10 16:28 纯数值卡维度详情与两个目录的路由适配

- 产品：总PRD2.06、调整方案2.05、UI规范1.20。活跃、观影人数和观影率按新老用户/客户端拆解；新增与注册次留按客户端/获客类型拆解。六张纯数值卡保留整体值，说明与拆解操作独立，完整宽表与导出不隐藏维度。
- 实现：`MetricBreakdown`共用右侧详情、滚动锁和焦点恢复；`operating-breakdown-preview.ts`从一个适用维度映射派生卡片详情与宽表。现为11项基础指标和20个维度列，仍不是完整日报覆盖。固定视觉样例仅在完整图表体验使用；经营数据缺口保持待支持，注册日次日观察尚未结束时卡片、详情与宽表统一未成熟。
- 根因修复：`ProductApp`仅对看板工作台启用上下文布局，目录与完整图表体验不继承零边距/固定高度/隐藏溢出。指标、事件目录共用结果表面，1100px及以下分类收纳；修复范围覆盖站内导航后CSS已加载的情况，不依赖页面刷新恢复样式。
- 验证：40项共享UI测试通过；22个不同浏览器场景中的导航与全局条件14项通过，最终受影响目录和经营卡片8项重新执行全部通过，含390/768/772/1024/1101/1280/1920宽度、实际点击和键盘、返回/前进/刷新、缺失/未成熟、真实XLSX下载与完整图表样例。类型检查、构建及生产预览隔离通过；埋点文档38180项校验通过。截图使用动效结束后的稳定帧，证据位于本机`/private/tmp/ypbi-breakdown-*`、`/private/tmp/ypbi-catalog-route-*`及`/private/tmp/ypbi-operating-detail-*`。
- 边界：本轮未修改指标/事件权威定义、真实接口映射、API契约、账号权限、数据库或生产部署。原日报不修改；其未确认字段和未覆盖数据仍按PRD15.1逐列准入。
- 后续：用户已确认本轮收口后继续我的概览、指标速览、指标分析和事件分析；个人配置保存、已保存分析及加入看板依赖调整方案9.11的独立API/持久化批次，已列明本地实施范围待确认，尚未实施或声明完成。

## 2026-09-10 经营明细纯数值卡、局部平台与紧凑宽表

- 产品：总PRD2.05、调整方案2.04、UI规范1.19。首屏九张数值＋趋势卡、关键链路及质量区保留；经营明细内增加6张纯数值卡，局部业务平台默认全部，只联动本区卡片、表格与导出。
- 组件：`DashboardMetricCard`增加纯数值变体，与完整图表体验共用，清除被替代的独立KPI样式。纯数值状态不继承趋势网格跨行和最小高度。数值列最小宽度由174px收拢到108px，长列名完整换行，客户端后缀独立成行，表内滚动及固定列保持。
- 快照与导出：`operating-detail-snapshot.ts`统一单日演示快照、摘要和宽表投影。单平台卡片与单元格取相同值；独立大盘不从PID求和。导出主体每个平台一行，21个指标/维度展示列及两组实际基准、状态保存为数值/空值；下载只覆盖所选局部平台，不受横向可视区域裁剪。切片列待接口支持，注册日次日观察未结束时标为未成熟，不补0。
- 数据边界：只核对旧接口映射并登记端别日活、观影率、新老用户及注册留存缺口；未修改指标/事件权威定义、API、正式adapter、数据库、原Excel或生产部署。当前明细仍为全局结束日单日快照；正式独立日期、多日经营宽表和列配置未在本轮实施。
- 验证：421项Bun单元测试、39项共享UI检查、类型检查、构建和生产预览隔离通过；指标/事件/看板目录派生检查通过，埋点文档38180项检查通过。浏览器覆盖经营明细、完整图表体验、原九卡、全局条件和目录，含1280/1024/390视口、键盘弹窗关闭及实际演示XLSX下载；最终结果与截图见本机`/private/tmp/ypbi-value-browser-*`及`/private/tmp/ypbi-operating-detail-*`。现有构建大包提示不阻断本次构建，未发布。

## 2026-09-10 经营宽表与纯数值卡组合评估

- 产品：总PRD2.04、调整方案2.03；经营明细采用单张宽表，业务分类仅组织列顺序，注册留存只针对新用户并支持注册端别。短指标选项采用分段切换；逐日逐PID行粒度、核心总览纯数值卡组合和图形扩展明确标为建议。
- 核对：纯数值摘要已存在于`MetricSummary`及图表体验页，经营主卡尚无纯数值变体。对照神策与GrowingIO官方说明，登记排行、词云、矩阵、散点、矩形树图及其他图形的适用条件和数据缺口，不以截图内容新增指标。
- 边界：只更新现有权威需求及状态镜像，不修改指标 / 事件定义、页面运行代码、接口、数据库、原工作簿或部署。新增真实数据列及个人对象批次仍分别准入。
- 验证：目录派生检查与5项生成器测试通过，48个本地文档链接及产品状态检查通过，`git diff --check`通过；埋点文档38180项校验通过。未运行新的页面渲染回归，本次未变更运行代码。

## 2026-09-10 全局阅读条件、收藏目录与日报核对

- 文档：总PRD 2.03、调整方案2.02、UI规范1.18。经营明细六组、端别/新老用户、留存日期、搜索排行与业务漏斗的已有项和缺口集中登记于PRD第15节；我的概览、指标速览、事件分析和已保存分析纳入目标，其新增接口、PostgreSQL对象与权限批次在调整方案9.11待确认。
- 页面：指标与事件目录共用全宽布局，1920/1024/390只在表内产生必要横向滚动；公共概览增加我的收藏分组，复用原本机收藏记录，搜索同一对象只出现一次，取消不影响原看板。顶部独立收藏菜单移除，旧正式地址只重定向。
- 查询：11张本地看板首次不对比；已应用日期与比较按有效深链、历史、会话、默认顺序恢复，未应用草稿和卡片局部条件不传播。超出目标看板180日范围时保留条件并提供调整入口，不静默换日期。不对比时专题共享明细及导出同步移除比较字段。
- 数据边界：日报工作簿仅只读分析，没有导入、重算或写回；95项指标与101项事件仍由原权威源投影，定义同步不代表真实查询与验数完成。正式指标、事件、上游映射、公共API默认语义、账号、数据库、旧BI和部署均未修改。
- 验证：Bun单元413/413、共享UI31/31；浏览器19个独立用例通过（全局条件5、目录2、日期2、图表/导航9、正式菜单与旧地址1），收藏字体与空态补充复测通过。类型检查、构建、目录生成检查及生产样板隔离通过；埋点文档校验38180项通过。截图保存在本机`/private/tmp/ypbi-query-*`和`/private/tmp/ypbi-catalog-fluid-*`；当前仅本地，未部署，真实事件查询与个人对象保存未完成。

## 2026-09-10 全部官方看板与指标 / 事件目录本地体验

- 范围：11 张官方看板均已装配到本地工作台，包含功能使用概览；指标与事件中心提供权威目录只读体验。总 PRD 2.02、调整方案 2.01、UI 规范 1.17；分析中心本批不扩展。
- 功能使用：从权威页面字典派生 50 项功能目录，通用渗透率模板不注册新指标 ID。主比较和导出保留全集，最多 5 项限制只作用于主动选择的近 7 / 30 日趋势；单选提供分组结构和二维交叉，单日、整月及 366 日范围使用同口径合成样本。搜索不裁剪导出，0 与无记录 / 未产出 / 未启用 / 失败分开。
- 共享修复：标题、面包屑、说明入口和工具顺序收敛到共享页头；标题 / 日期不被挤断。结构和排行通过同口径窗口查看完整表，矩阵和二维交叉直接展示。只读窗口仅保留标题栏关闭，导出底部只保留下载；补齐正文键盘滚动、焦点恢复、页脚内边距及手机短表头宽度。详细卡元信息与图表位置统一，涨跌方向明确。
- 日期与结果：核心、获客及六张新增专题支持合法范围内最多 366 日演示查询；活跃留存 / 视频消费保持 180 日边界。日期概览条支持真实端点缩放、整体平移、键盘与复位，完整表及导出不随绘图窗口裁剪。核心主卡和重复链路指标共用同一快照，关闭对比同步清理；核心与明细导出按各自实际对象范围生成，渠道和支付诊断保留完整结果。
- 目录：95 项指标与 101 项事件从已有权威定义投影，实施、数据、查询、验数和使用位置分别呈现未知或未就绪状态；生成器检查源文件漂移，构建隔离全部 DEV 样例。 生产隔离曾检出目录演示提示样式进入共享文件，现移回 DEV 专属样式，保留隔离断言；最终 8 个构建产物扫描通过。既有 ECharts 分包体积提示保留。
- 验证：完整 Bun 411 / 411、共享快速逻辑 31 / 31、类型检查、生产构建和三类派生快照校验通过。浏览器分批覆盖 33 个不同定向场景：一致性 4、功能 3、目录 2、日期控件 2、扩展看板 3、共享交互 9、周期 5、专题 5；最后功能与目录 5 / 5 复跑通过。长串行运行曾超时，最终以各专项结果核对，不计作一次完整套件通过。日志：`/private/tmp/ypbi-function-final-browser.log`、`ypbi-navigation-final.log`、`ypbi-period-topic-final.log`、`ypbi-final-five.log`；扩展看板与日期用例通过记录在 `ypbi-final-remainder-browser.log`。
- 未完成边界：个人看板创建 / 编辑、指标速览保存、跨设备收藏和正式目录运行状态接口尚未实现，需要后续 API、权限与持久化批次确认。未修改正式指标、事件、接口、账号权限、数据库或旧 BI，未部署；服务器配置和真实上游联调继续后置。


## 2026-09-10 首批专题本地验收与日期控件修复

- 产品：总 PRD 2.01、调整方案 2.00、UI 规范 1.16；核心与获客作为确认基线，首批实现活跃留存、视频消费的 DEV 阅读页面。
- 展示：独立业务日与 Cohort D0、成熟窗口、矩阵冻结列、区域内滚动、完整分页与内容 Top10 / 全量表；演示导出保留完整结果与状态。多日去重和人均口径待准入项保持明确不可用，不做前端推算。
- 修复：核心日期从旧窄列改为内容宽度；日期展开、首端点、范围、快捷选择和关闭恢复统一到共享控件。全部已有范围控件继承；规则并入原 UI-006 / UI-013，未增加同义清单。
- 验证：408 项逻辑测试通过；27 项目录、日期、阅读、周期与专题浏览器回归通过；最终 7 项日期与专题定向复跑通过，包含新增手机 180 天 Cohort 冻结列与视图重开，共覆盖 28 项独立浏览器用例。类型检查、生产构建、目录快照及生产隔离检查通过；既有 ECharts 分包体积提示保留。
- 边界：仅本地合成演示样例；正式数据、指标权威源、接口、权限、数据库及发布保持原状。

## 2026-09-10 共享阅读视图、分页图例与看板交互规范

- 产品：总 PRD 2.00、调整方案 1.99、UI 规范 1.15。时间趋势默认折线，分类比较默认柱状，由阅读任务决定；现有核心与获客时间卡以及未来看板定义统一遵循。详细卡保留趋势 / 表格，周期比较继续通过筛选、虚线、涨跌、提示和对比列呈现。
- 阅读：表格固定表头、区域内滚动，底部分页包含范围总数、页码与省略号，完整表按需提供条数和跳页。页脚内边距由共享组件控制。表级共同日期在工具区展示，实际逐日与对比日期保留。超过 31 日可调整绘图窗口，原摘要、逐日数据与完整表保持；未扩展正式查询或前端聚合。
- 多系列：少量同单位分类按日期逐行显示名称、色标和精确值；受控布局与外部字段转义分离。20 PID 体验使用共用 `PaginatedLegend`，图下方单行、右侧上下箭头和页码，空间不足按宽度分页；搜索位于图上方。分页不改变曲线、选择、颜色、表格或导出，搜索清空回到第一页；分页和日期窗口在条件切回时重新初始化。
- 交互：核心总览与获客通过同一有序槽位装配查询及附属动作，视觉和键盘顺序一致。指标标题和整条比较行提供帮助，数据表初始焦点位于关闭按钮，恢复焦点不自动显示提示。普通操作 Toast 3 秒消失，悬停 / 聚焦 / 隐藏暂停；定义与比较采用持续阅读窗口。目录控制固定底部，收起保留 32px 窄轨道；轻量动效和移动模态保持。
- 防回归：已有 UI 规则按原 ID 补充真实命中、跨条件返回、图例搜索 / 翻页、固定阅读高度及同源入口，不建立第二套规范。连续日期命中区的装饰点不再抢占相邻区域鼠标；同一日期区域内移动不重复通知帮助层。定向入口为 `test:e2e:reading`、`test:e2e:periods` 和 `test:ui:shared`。
- 验证：最终 Bun 402/402、类型检查、指标与目录快照、生产构建及开发体验隔离通过。实际截图复查覆盖 1280 / 1024 / 390 的图例、提示、分页与工具条，日期合成布局覆盖 1 / 7 / 30 / 90 / 366 天；浏览器专项包含仅图例翻页时 Canvas 不变、全部 20 项可达、完整表保留全集和条件 A → B → A 重置。构建保留既有 ECharts 大包提示。
- 浏览器覆盖：本批累计覆盖 86 个不同 V2 场景，受影响的 Shell / 阅读 / 日期 31 项均完成复验，其中最后 2 项定向回归通过。完整套件与定向结果合并记录，不作为一次全量运行结果；日志为 `/private/tmp/ypbi-reading-full-browser.log`、`ypbi-reading-final-browser.log`、`ypbi-last-two-regression.log`。
- 边界：本批只调整展示、共享组件、测试和文档，正式指标、业务 API、权限、数据库、服务器及旧 BI 保持原状；未部署。分组与长日期合成布局不代表真实查询已准入，正式数据及导出仍按原闸门接入。

## 2026-09-10 统一轻量动效与目录边缘收纳

- 产品：总 PRD 1.99、调整方案 1.98、UI 规范 1.14。菜单 / 面板 / 模态采用短暂淡入，侧栏短距离进入；关闭、焦点恢复、查询与结果不等待动画。加载复用稳定占位与环形反馈，无新动画库、数值滚动、虚假进度或图表逐帧缩放。
- 目录：桌面以同一个边缘把手替换底部收起和正文展开按钮，收起保留窄轨道；目录隐藏而不卸载，搜索、分类及内部滚动保持。命中区域与提示宿主按控件宽度布局，不受窄轨道压缩；手机沿用模态目录。宽度切换到最终值，内容进入复用共享轻量动效。
- 根因防回归：减少动态效果改为真正关闭动画与过渡，移除为全部元素设置非零时长的旧规则，避免默认 transition-property 使栅格尺寸也产生中间帧。无业务逻辑依赖动画事件。UI-038 更新目录护栏，UI-042 记录统一动效与加载入口，新增按需回归命令；不要求每次全仓遍历。
- 验证：最终目录 / 动效 12 项 Chrome 场景通过，共享逻辑 27 项通过；本轮获客 9 项及共享 UI 9 项也已通过。覆盖 768～2560 桌面窄轨道、390 手机目录、连续开关、键盘 / 焦点、嵌套导出、布局不抖动、搜索及滚动恢复、减少动态效果与加载完成。页面可见性暂停通过 Headless 事件模拟验证，不作为后台性能基准。实际复查展开 / 收起和手机截图，产物在 `/private/tmp/ypbi-directory-rail-*.png`、`ypbi-motion-directory-*.png`、`ypbi-loading-static.png`。
- 构建：类型检查、指标与目录投影、生产构建及开发体验隔离通过，保留既有 ECharts 大包提示。无后端、公共 API、指标口径、权限、数据库或部署变更，经典版保持隔离。
- 待确认：卡片阅读视图与表格分页呈现、按任务选择新增用户默认图形，以及长日期范围的缩放 / 粒度方案；本轮只分析这些方向，未实施。正式获客查询仍受固定一周演示快照边界约束。

## 2026-09-10 数据表导出、目录操作与日期范围布局验收

- 产品：总 PRD 1.98、调整方案 1.97、UI 规范 1.13。收藏星标靠目录行右侧，指标标题移除装饰箭头并保留点击与键盘能力；获客表标题、页签、筛选共同构成无重复基线的工具区。
- 导出：共用同口径表在类型层要求导出动作，获客单指标与整板逐日工作簿复用同一数据投影，保留精度、实际日期、单位、零值与缺失状态。实际下载并检查 XLSX 结构完整。完整体验及正式分析结果表提供导出范围和未接入原因，不生成虚假正式文件。未支持的条件在生成层拒绝导出。嵌套导出 Esc 只关闭顶层，关闭和下载后同步恢复触发点焦点。
- 日期：实际操作日期选择器验证单天、跨月 30 / 90 天的草稿、应用、未覆盖状态及禁用导出。独立浏览器测试以合成数据挂载真实共享紧凑 / 详细组件，覆盖 1 / 7 / 30 / 90 / 366 天、折线 / 柱状、1440 / 390 宽度及完整逐日表。单天点居中，柱宽随密度收缩，日期标签按宽度避让且保留首尾，长折线减少常驻点而保留全部数据和悬停。获客真实查询仍只支持固定一周演示快照；布局验收不等于查询准入。
- 回归：32 项共享 / 获客单测、20 项获客 / 共享 UI / 日期浏览器场景通过；正式 M016 结果表导出状态另行定向通过。类型检查、构建、权威投影和生产体验代码隔离通过，保留既有 ECharts 大包提示。截图包括 `/private/tmp/ypbi-period-*.png` 与 `ypbi-metric-table-export-mobile.png`；日期回归加入专用命令及 V2 完整回归入口。
- 边界：业务口径、API、服务器、数据库、旧 BI 与部署未改动。一年逐日数据在小卡上仍有较高视觉密度，布局验收只证明无溢出、数据及交互可达；放大 / 粒度方案待确认，不在前端擅自汇总。新增用户默认折线图和轻量动效均为待用户确认的建议，本轮未实施。

## 2026-09-10 看板宽度、独立数据表与共享交互收口

- 产品：总 PRD 1.97、调整方案 1.96、UI 规范 1.12。目录右侧正文铺满可用宽度；桌面查询放在标题右侧，按实际空间收纳次要筛选。控件按连续筛选组与搜索分组，组合搜索统一单层焦点。
- 图表：获客详细卡与完整体验页共用 `ChartDataTable`，查看精确值时邻卡、趋势和滚动位置保持不变。弹层关闭同步归还焦点，逐日详情和原导出入口保持可达。自动纵轴统一采用 1、2、5 乘以 10 的整数次幂的规整间隔，显式范围、堆叠和专用图表契约不变。卡片共用不位移的悬停细描边；对比提示保留分行与数值主次，仅一条低对比度分隔线。
- 实际验收：检查 390～2560 八档宽度、收起目录、全屏、搜索焦点层数、筛选可达性、柱形与折线空白区连续悬停、表格打开前后的邻卡几何和滚动位置、手机表格、嵌套详情、Esc 与焦点返回。截图留在 `/private/tmp/ypbi-review-layout-*.png`、`ypbi-review-data-dialog.png`、`ypbi-review-comparison.png`。
- 回归：共享逻辑 26 项、全量单测 390 项通过；全量浏览器 68 个场景首轮 67 项通过，关闭弹层后的焦点场景修复后连同数据表与完整体验页共 3 项定向通过。类型检查、构建、指标及目录投影校验、生产体验代码隔离通过。构建保留既有 ECharts 大包提示。
- 边界：未修改正式指标、事件、公式、API、权限、数据准入、数据库或服务器配置；未发布。正式数据与正式导出仍待接入。

## 2026-09-10 获客与新增本地体验、附属动作与连续日期悬停

- 产品：总 PRD 1.96、调整方案 1.95、UI 规范 1.11。核心总览基线确认后，按 5.7 提供九张详细卡、增长效果完整维度明细、首次激活质量与独立渠道结算。未覆盖的筛选不复用旧结果，计数真实 0、缺失与单卡失败分别呈现。
- 共享实现：详细卡与完整体验页共用 `detailed-trend-option`，提示内容移至 `src/components/chart-tooltip-content.ts`。总览折线和柱状的对应日期区连续覆盖绘图区，透明命中区不显示空心框；键盘保持精确日期与轻量焦点引导。帮助层仅在打开时通知其他提示关闭，不在同一区域的每次鼠标移动中重复通知。明细日期保持单行。
- 附属动作：获客页支持当前浏览器收藏、还原筛选和明细阅读状态的链接、演示快照刷新、全屏及真实下载的演示 XLSX。导出保留全部维度结果，不受搜索、分页或变化显隐影响；扣后结算独立成表，文件名与说明明确演示性质。
- 验证：共享逻辑 26/26、完整 Bun 390/390、完整 V2 浏览器 65/65，最终提示与日期调整后定向浏览器 13/13，明细实际右对齐坐标复验 1/1；类型检查、生产构建、指标/目录漂移检查及生产演示隔离通过。获客在 1800/1280/1024/390 实际截图复查，核心连续悬停覆盖首尾日期、柱体外空白区及键盘；下载的 XLSX 压缩包完整性校验通过。新增单元与浏览器回归复用原 UI 问题 ID。
- 边界：本轮未修改正式指标口径、公共 API、权限、数据库、后端服务配置或旧 BI；服务器联调、真实验数、账号收藏与正式业务导出继续后置。

## 2026-09-10 统一规整刻度、单位去重与辅助比较层级

- 产品：总 PRD 1.95、调整方案 1.94、UI 规范 1.10。纵轴按完整图表同源规则向外规整，必要单位在主值、提示或列名旁呈现；常驻涨跌属于辅助层级，详情正文保持原层级。首张看板继续优先验收。
- 根因与实现：紧凑图曾直接使用极值及中点作为轴标，现通过 `chart-axis` 复用 ECharts 的规整步长，按高度生成等距刻度并与真实 ECharts 结果对照。移除图上独立单位行和固定三档定位；计数表头通过 `metricHeadingUnit` 去重，时长和币种同排小号括注，已有分析页沿用相同规则。常驻比较通过 `ui-metric-comparison` 共用 11px 字号、周期常规字重与变化中等字重。
- 防回归：原 UI 问题 ID 增加精确错误样例、可见结果及实际样式断言；执行规则改为按需加载相关章节、优先定向测试及失败日志。新增 `test:ui:shared` 快速入口与 `test:e2e:ui-shared` 实际浏览器入口，后续看板复用已确认共享基线。
- 验证：统一发布检查通过，Bun 385/385；共享快速检查 26/26，耗时约 0.5 秒；系统 Chrome 完整 V2 61/61，额外表格比较显隐分支复验 1/1。实际复查两页及 1280 / 1024 / 390 截图，覆盖规整刻度、必要单位、涨跌字号、全高度柱身和真实 Canvas 提示。最终类型检查、生产构建、快照及生产隔离、diff 检查通过，两条体验链接 HTTP 200；保留既有 ECharts 大包提示，无 lint script。
- 边界：只修改展示、共享组件、测试和既有规范；指标公式、数据值、公共接口、权限、数据库、服务器、代理与旧 BI 未变。未部署、无迁移；正式数据和导出服务仍待接入，其他看板未开始。

## 2026-09-10 收口核心总览与共用图表交互

- 产品：总 PRD 1.94、调整方案 1.93、UI 规范 1.9 统一双月日期选择、提示层级与定位、数值列对齐、完整纵轴刻度、整段柱身悬停及导出入口规则。验收顺序为先完成核心经营总览，再制作其他看板。
- 实现：核心总览与 M016 复用日期组件；指标释义、比较摘要、表格和迷你趋势共用顶层提示，当前值、基准日期与变化分行显示。迷你柱按整段日期区域命中，真实 0 保持零高度；纵轴显示完整数字及明确单位，刻度与网格对齐。实际 ECharts 提示只放行受控方向色和换行，其余 HTML 继续安全转义。统一修正提示互斥、聚焦自动滚动、滚动遗留提示、日期控件高度与跨页导航再次悬停。
- 验证：统一发布检查通过，Bun 381/381、系统 Chrome 完整 V2 浏览器回归 60/60；图表与日期专项 8/8，导航及核心总览连续三轮复测 6/6。实际检查桌面、1024 和 390 视口、正常与异常态、柱身各高度及四种 Canvas 图表的真实悬停。类型、前后端构建、派生快照、生产演示隔离检查通过；两个体验入口 HTTP 200。保留既有 ECharts 大包提示，无 lint script。
- 边界：导出入口展示实际范围和服务未接入状态，正式下载保持禁用。本轮未修改指标口径、统计计算、公共 API、权限、数据库、服务器、代理或旧 BI，未开始其他看板；真实数据与导出服务仍独立待接入，无数据迁移。

## 2026-09-10 统一指标数值层级与按需比较

- 产品：总 PRD 1.93、调整方案 1.92、UI 规范 1.8 固定名称、数值、单位和变化的层级。详细 KPI 使用上方结果及比较摘要、下方完整趋势；合计与均值按指标统计规则及真实结果准入。经营明细默认只展示当前值，整表可选择一种变化参考，完整基准按需查看。
- 实现：链路阶段、关系指标、关键质量及图表体验页复用数值摘要组件；字号由共用 Token 管理，主数值 30、辅助数值 26、名称 14。比较沿用统一涨跌组件和关闭对比行为。相同指标复用已有演示卡模型；新增比较快照仅供开发体验。表格单位上提到表头，两组基准日期、数值和差异保留在悬停、聚焦及点击详情中。顶栏 Logo 与导航采用留白，一级文字调大，整栏轻阴影；菜单处理跨页延迟失焦，确保再次悬停正常展开。
- 验证：系统 Chrome 完整 V2 56/56；菜单与涨跌定向连续复测 6/6；新增明细展示纯函数 Node 3/3。统一发布检查通过，Bun 377/377；类型、前后端构建、生成快照及生产演示隔离检查通过。实际复查 1280、1024、390 下的链路、表格、详细 KPI 和导航，两个体验入口 HTTP 200；保留既有 ECharts 大包提示，无 lint script。
- 边界：仅改展示、交互与开发体验快照，未修改权威指标定义、统计公式、正式业务计算、公共 API、权限、数据库、凭据、服务器、代理或旧 BI。日活摘要使用对比日与增加人数，不将每日去重人数相加为周期人数。正式数据准入及服务器联调继续后置，无数据迁移。

## 2026-09-10 统一顶部工作区导航、业务释义与链路卡布局

- 产品：总 PRD 1.92、调整方案 1.91、UI 规范 1.7 同步顶部一级导航与二级展开、窄屏分层导航、详细 KPI 上下结构、紧凑链路卡及业务释义规则。保留已确认的目录、稳定路径、指标集合、公式和分母。
- 实现：桌面退出全局侧栏，顶部菜单与窄屏导航共用名称、路径和权限。指标中心、核心总览与图表体验复用业务释义层，95 项指标的技术条件转换成业务说明并明确公式；未覆盖协议在投影生成时阻断。链路卡移除重复标题和大灰块，转化名称和值纵向就近排列，保留 IP·天等必要边界；详细 KPI 使用上方摘要、下方全宽趋势。
- 验证：统一发布检查通过，Bun 373/373；系统 Chrome 完整 V2 55/55，最终定向补验 3/3；指标释义 Node 6/6，涵盖全部指标及详情字段、未知协议、有效观看门槛与比率分母。实查顶部菜单、链路和详细 KPI 截图，覆盖桌面、1024 和 390；两个体验链接 HTTP 200。类型、前后端构建、生成快照、生产演示隔离和 diff 空白检查通过；无 lint script，保留既有 ECharts 大包提示。
- 边界：本轮没有修改技术指标快照内容、公共 API、业务计算、权限、数据库、凭据、服务器或旧 BI；没有新增埋点契约。正式数据、明细、导出及部署仍待后续准入。回退仅涉及本批前端呈现、派生 UI 快照与检查，无数据迁移。

## 2026-09-09 看板工作台统一为常驻目录与直接阅读

- 产品：总 PRD 1.91 固定单列分类目录、默认直接阅读核心总览、一次点击切板、目录与正文独立滚动、搜索与展开恢复、历史 / 深链和手机模态规则。全局工作区导航在看板阅读态由顶栏进入，沿用原名称、路径与权限过滤；其他工作区布局不变。未建设专题如实呈现未接入状态，范围说明默认收起。
- 实现：目录组件只消费模型；DEV 工作台装配 PRD 派生快照、地址 / 历史状态和唯一核心总览组件。原核心链接进入同一工作台；删除旧目录介绍卡片、规划弹窗和独立返回目录入口。导航覆盖层样式由桌面工作区入口与手机共用。短名称不重复提示，截断名称可悬停 / 聚焦查看，Esc 关闭。
- 验证：统一发布检查通过，Bun 370/370、目录生成 / 状态 / 地址 Node 12/12；系统 Chrome V2 52/52，目录最终补验 8/8，已查看 1280 / 1024 / 390 实际截图。类型、前后端构建、快照一致性、生产演示身份 / 组件 / 数据隔离均通过；无 lint script，ECharts 既有大包提示保留。
- 边界：未改指标定义、真实业务查询、公共 API、数据库、权限、凭据、服务器或旧 BI，未部署。详细 KPI 上下结构仍待用户确认，本轮未实施。UI 呈现及 DEV 工作台可独立回退，无数据迁移；正式看板依旧等待真实数据准入。

## 2026-09-09 完成看板目录开发体验切片

- 产品：在总 PRD 5.1 补齐独立目录体验边界。按权威一级分类浏览 11 个规划看板，支持名称搜索、无结果清除、规划说明及进入核心总览样板；浏览器返回和刷新保留本历史项条件，新开入口使用默认条件。其余看板不生成空业务页。
- 架构：目录展示层接收模型和动作，DEV 容器负责规划说明及体验入口。清单从 PRD 唯一目标表生成，分类顺序校验权威指标快照，构建前强制检查漂移；章节引用仅标识预览项，不作为正式对象 ID。没有新增公共 API、数据库、权限、指标口径、埋点或持久化业务对象。
- 验证：`npm run verify:release` 全链通过（末端 Bun 367/367），目录生成和筛选 Node 回归 9/9，系统 Chrome V2 回归 50/50；实际复查 1280 / 1024 / 390 的目录和抽屉截图。生产构建隔离扫描包含新增目录标记，保留既有 ECharts 大包提示；无 lint script。测试网络桩限定真实 API 路径，不拦截前端模块加载。
- 未改与回滚：核心总览仅增加目录入口，已确认指标图表效果不变。旧 BI、代理、服务器、账号库、真实上游和正式数据准入均保持原状。撤下本次 DEV 入口及其独立组件、生成器和检查即可回退，无数据迁移；正式目录仍待对象、真实结果和整板导出达到准入门槛。

## 2026-09-09 22:13 +08:00 后置服务器联调并完成完整本地回归

- 执行顺序：用户先确认独立本机联调方案，后明确将服务器 / 连接配置及真实联调后置；已同步总 PRD、调整方案、索引及部署模块。当前只安装本地运行时，未初始化数据库、创建账号、保存 Token、修改前端代理、启动新后端或改动旧 BI。
- 根因与修正：Bun 1.3.14 的 Fastify 注入在中文响应结束后仍继续处理，最小例子复现同样行为；本地改用经反例验证的 1.4.2 并在 `.bun-version` 固定验证版本。新增 onRequest / preHandler 响应生命周期回归；维护 Cookie 测试按名称读取，兼容合法单值 / 多值响应头。未修改认证、Cookie 或权限的业务实现，也未升级生产运行时。
- 验证：`npm run verify:release` 全链通过，末端 Bun 测试 363/363；系统 Chrome 的 V2 浏览器回归 44/44，两个体验入口 HTTP 200；类型检查、前后端构建、权威快照及生产 fixture 隔离扫描通过。浏览器首次运行被沙箱禁止启动，获准后重新完整执行通过。无 lint script；构建仅保留既有 ECharts 大包提示。
- 部署与回滚：未部署、无数据迁移。新增测试和本地运行时标记可独立回退；已安装的 PostgreSQL 17.10 未初始化默认集群或启用自启动，无需进行数据回滚。已确认页面效果不变；真实数据库、上游接口、指标验数与可信水位仍待后续联调。

## 2026-09-09 确认展示基线并预检真实接口联调环境

- 用户确认核心总览与完整图表体验进入本阶段基线，后续按具体问题迭代；总 PRD 的设计实施闸门同步记录，下一阶段转入真实数据链路。
- 只读预检：本地仅配置远端 BI 代理，没有上游与账号库配置、默认运行凭据文件或 3000 后端进程；当前 shell 未找到 Bun / PostgreSQL CLI / Docker。通过本地代理及直接访问已配置目标，健康接口均为 200、新版会话接口均为 404。
- 下一批范围：建议使用现有技术栈建立仅本机可访问的隔离联调环境，初始化独立账号库、核对可复用的旧连接配置，并先做 M016 只读查询与候选采集。已将影响、兼容、回退和验收边界写入调整方案及部署文档，初始化与安全配置接入等待用户确认。
- 本次未安装运行时、初始化数据库、修改配置或业务代码，也未改动旧服务器、Token、公共契约、权限、正式准入或业务值。验证为脱敏配置存在性检查、监听 / 可执行文件检查及四次不带凭据的 HTTP 状态探针；不把健康检查成功记为接口验数或新版部署完成。

## 2026-09-09 统一全局涨跌并推进接口验数准备

- 产品确认：总 PRD 1.90 将涨跌定义为数值方向色，业务好坏另有权威依据；其余核心总览与完整图表展示交互已确认，调整方案 1.89 与 UI-031 同步。
- 共享实现：卡片、质量、经营表、同口径表和图表提示统一使用 `ChangeValue` / `change-presentation` 与明暗背景 Token，删除各页独立涨跌色。无法计算的零基准增长率保持中性；箭头、精确差值与真实 0 保留，未开启对比仍不渲染比较内容。
- 接口验数准备：增加 M016 受控技术响应到候选数据集的离线转换，复用现有公共 Schema 和权威映射 / 平台目录。核对版本、来源、范围与采集时间，保留 0 / null / 缺行差异；只写显式指定的新文件且权限 0600，终端不输出逐行业务值。产物只供离线比对，不是独立参考、正式业务源或完整水位证明。
- 验证：主题与可读性 4/4、卡片 / 页面模型 / tooltip 25/25、M016 验数及候选转换 27/27、两体验页定向 Playwright 3/3 通过；类型检查、服务端构建、指标快照预检、生产构建和 fixture 隔离扫描通过。生产构建保留既有 ECharts 大包提示；本机无 Bun，未运行完整 Bun 套件。
- 边界与回滚：仅本地代码与体验入口生效，未部署；没有修改公共 API、指标公式、数据库、权限、凭据或正式准入。可单独回退共享呈现及候选转换工具，不需要数据迁移。真实 API 采集、独立参考比对和完整日来源确认仍待完成。

## 2026-09-09 实现 V1.89 链路布局与全量指标释义

- 链路展示：核心总览的内容消费、获客转化和支付链路改为轻量 Tab，默认内容消费；选中链路在同一卡片内按“阶段在上、关系 / 诊断在下”的正常文档流排布，禁止关系指标悬空、压边或跨卡错位。
- 指标边界：内容仅使用 `M016 / M026 / M035 / M081`，获客仅使用 `M001 / M003 / M008 / M005 / M006 / M007`，支付仅使用 `M084 / M086 / M090 / M070 / M071 / M072`；不展示无指标 ID 的临时比率，不把付费用户或自算“到账订单 / 用户”接入支付链路。
- 释义一致性：链路阶段、关系 / 诊断、质量指标和经营明细表头等全部用户可见指标名称统一从指标权威投影提供 hover、focus 与 click 释义；说明保持简洁，不重复“单位：人”等显然信息。
- 交互完整性：阶段和关系指标都留在卡片正常流内；经营明细表头将指标释义入口与排序按钮分离，悬停、键盘聚焦和点击均可读取同一权威定义。
- 验证与边界：系统 Chrome 的 V2 Playwright 43/43、`npm run typecheck`、`npm run build:server` 和带强制预检的 `npm run build` 均通过；不改变指标口径、公共 API、数据库、验数状态或正式业务数据。

## 2026-09-09 收口图表悬浮提示与代表页一致性

- 用户体验：图表提示只增加理解当前点所必需的信息；单位跟随精确值，不再独立显示“单位：人 / 次 / 元”，也不重复卡片标题、图例或提示标题已经表达的指标名、系列名、数据日和“当前值”。
- 状态边界：真实 0 按正常数值展示；只有缺失、未成熟、部分或失败才追加短状态原因。对比图保留双方真实日期和值及合法差值，漏斗、Cohort 等专用视图保留其必要业务字段。
- 代表页一致性：核心经营总览桌面端把查询和看板操作收进标题右侧；正常卡把聚合语义、验数状态与截至时间合并到标题辅助行，不再保留底部“完整 / 已验数”状态条。迷你折线和柱状图沿用完整体验页的颜色、网格、轴、日期、图例与 tooltip 语法；7 日展示完整日期，当前 / 对比点独立，删除柱顶胶囊标记，更长区间才按密度抽稀。
- 影响范围：V1 图表体验页的折线、柱状、排行、堆叠、结构图、漏斗、Cohort 和耗时分布，以及核心经营总览的页头、九张指标卡与迷你趋势；不改变指标口径、API、数据库或数据准入状态。
- 验证：共用 formatter 单测、V2 定向浏览器回归、类型检查、构建和两个开发体验入口的错误日志核对以本轮最终结果为准。

## 2026-09-09 收口核心总览共用展示交互与文档分层

- 已确认交互：核心总览使用单一日期范围入口连续选择起止日；业务范围、日期、可选对比和应用组成紧凑查询组，收藏、复制当前视图链接、导出和刷新收敛为带 hover / focus 名称的 icon 操作。“不对比”会同步移除比较值、差值、比较线、图例与提示字段。
- 信息表达：用户页面不展示分区号、决策号或指标内部编号；业务名称与 PID 仍可见、可检索。指标名称提升为主要文字层级且无下划线，说明入口在 hover、键盘聚焦和点击时读取指标中心权威定义。经营明细改为“当前值 / 较昨日 / 较上周同日”，基准日期和值可核对，表头本身负责排序，不再设置“排序说明”。
- 图表与状态：折线和柱状图统一使用真实日期、数值轴及正确基线，tooltip 只保留当前、对比、差值和异常状态；多系列完整保留数据但限制视觉高亮。完整开发体验页补齐饼图、环形图、真实漏斗、Cohort、耗时分布、聚合 / 交叉表和动态多 PID 压力样例，不把高级图形塞进核心总览。
- 文档治理：用户可感知、研发必须实现且测试可验收的结果规则写入总 PRD；复现现象、根因、组件约束与专项回归写入 UI 防回归文档。再次出现同类反馈更新原规则和原回归，不建立同义条目或要求用户逐页重复说明。
- 边界与验证：本批没有开放正式看板、改数据库、变更公共 API 或接入真实业务结果。指标定义生成测试 33/33、主题 3/3、看板模型 16/16、导航 3/3、查询控制器 10/10、核心合同 6/6、类型检查和生产构建通过；系统 Chrome 的 V2 全套 40/40 及核心总览 / 图表体验定向 2/2 通过，覆盖 1280 / 1024 / 390。生产构建隔离检查确认不包含开发 fixture。

## 2026-09-09 实现 V1 图表与状态开发体验页

- 产品范围：依总 PRD 1.85 新增独立开发体验页，集中校准紧凑 / 详细 KPI、单期 / 两期 / 多系列折线、时间 / 并列 / 横向排行 / 合法堆叠柱、饼 / 环、聚合 / 二维交叉表、真实漏斗、Cohort 矩阵、首帧耗时分布及 11 类状态。核心经营总览未因此增加无关图形。
- 信息与交互：每张图都有明确的标题、对象 / 范围、实际日期、对比基准、单位、精确 tooltip、可访问名称和同口径表；两期保留各自日期，缺失不补 0，堆叠显示各段及合计，Cohort 未成熟显示灰格而非 0。图形 TopN 不裁掉表格全集。
- 隔离与根因回归：体验页仅在精确开发地址 `/dashboards/public?design=v1-chart-states` 可达，不进导航、不请求登录 / 目录 / 业务查询、不保存且不进入生产包。实际浏览器发现快速定位的 hash 会让严格预览地址失效，已在唯一源头改为不改 URL 的按钮滚动，并增加无 hash、无受保护 API 请求及离开后恢复真实会话的回归。演示身份进一步移入仅开发环境动态加载的边界；`npm run build` 现在自动扫描正式产物，发现演示身份、样板标记、样板样式或固定演示文案即阻断构建。
- 真实性边界：页级和卡级均明确标记“固定视觉样例”，不使用“已验数”等误导文案。真实漏斗仍要求身份、顺序与窗口证明；M032 的 P50 / P90 / P95 仍受 MD-06 阻断。体验页不证明业务数据已接入，也不作为抽象万能图表平台的第二个正式用例。
- 验证：类型检查、生产构建、生产产物隔离检索、代码格式检查及 2/2 定向 Playwright 通过；回归覆盖精确 / 非精确 URL、hover 两期日期与差值、键盘打开同口径表、Tab、页内定位及 1280 / 1024 / 390，390 只在表格容器内横向滚动。独立复核未发现 P0 / P1。

## 2026-09-09 收紧核心总览公共成功与零水位失败边界

- 公共契约：核心总览成功响应现在至少需要一张带合法可信水位的卡片，并直接计算全部卡片水位最小值，要求与 `commonCompleteThrough` 精确一致；可信无记录 / 无值 / 未产出 / 未成熟仍可合法证明共同周期。
- HTTP 边界：正确执行器在零可信水位时抛出周期解析错误，路由稳定返回 `422 METRIC_NOT_READY`；执行器若错误返回“200＋全 `not_ready` / `failed`”，完整出站 Schema 将其拒绝为 `502 CORE_OVERVIEW_SOURCE_CONFLICT`，路由不根据错误载荷猜测或补造日期。
- 同类同步：合同、Plugin、浏览器客户端、页面模型和查询控制器测试 fixture 均改为合法可信部分响应；单卡失败子查询改走请求失败路径，不再使用无水位假成功。
- 独立评审：开发环境精确地址 `/dashboards/public?design=core-overview` 不再依赖本地登录后端，仍不请求目录或业务查询；生产构建及其他路径的身份保护不放宽。
- 边界与验证：未改数据库、生产开关、provider、真实执行器、正式页面或业务数据。Node 合同 / 底座 / 控制器 / 页面模型 46/46、类型检查、服务端构建、定向浏览器 1/1 和代码格式检查通过；完整验证以本轮最终结果为准，环境仍无 Bun。

## 2026-09-09 增加核心总览准入与公共周期失败关闭底座

- 准入事实：新增纯内部指标准入计划，直接采用指标定义快照已派生的 `analysis` 状态，再检查日粒度和 PID / 正式整体能力，不新建第二套准入状态。当前快照九项全部不可执行：M016 未验数，其余八项未配置映射。
- 周期解析：新增仅接受已校验可信水位的公共周期解析器；共同完整日取所有传入指标×范围水位的最小值，支持最近完整周期、上一等长期及明确非等长双周期。数据无记录 / 无值与数据水位未知保持分离；零水位和明确日期越界必定失败。
- 边界：未收紧公共 Schema，未修改 HTTP 路由、Plugin、数据库、UI 或生产开关，也未创建伪 provider / executor。未来全部卡片无可信水位时必须返回现有 `422 METRIC_NOT_READY`，不能用请求日期或测试日期伪造 `commonCompleteThrough`。
- 验证：准入与周期 Node 测试 14/14、类型检查、服务端构建及代码格式检查通过；发布统一入口已纳入该测试。

## 2026-09-09 接通核心总览专属查询控制器并关闭跨通道竞态

- 查询编排：新增核心经营总览专属只读控制器与 React Hook，将严格查询客户端、页面状态和卡片模型串联；统一生成规范查询键，管理整板加载 / 刷新、单卡子集重试、取消和错误归一化，尚未挂入正式公共概览。
- 竞态边界：整板刷新与单卡重试互斥；整板刷新取消已在进行的单卡请求并清空尝试，刷新期间页面模型和控制器双层拒绝新单卡。跨查询条件、同条件、同一卡片及两个通道的晚到成功 / 失败均不能覆盖当前结果。
- 恢复语义：身份服务临时 503 清除受保护旧结果但可由用户重新检查；401 登录失效与 403 无权限继续保持独立语义。不可重试卡片不能绕过界面直接调用。
- 边界与验证：未改公共 API、数据库、权限、正式路由、真实数据或生产开关。控制器 Node 测试 9/9、紧凑指标卡与页面模型 16/16、类型检查通过；其余验证见本次交付。

## 2026-09-09 收口核心总览请求竞态与趋势真实语义

- 请求归属：整板刷新与单卡重试分别增加唯一请求/尝试标识；同条件连续触发时，较早请求的成功或失败即使更晚返回也不能覆盖最新状态。取消旧请求继续用于节省资源，但不再承担唯一正确性保障。
- 趋势语义：当前期和对比期只有等长且按相对周期位置对齐时才合并交互点；非等长序列分别保留真实日期、值和状态，不按数组位置制造差值。缺失点断线但保留可聚焦状态，不补 0；比率趋势使用真实有效值跨度，不再被固定最小跨度压平。
- 重试边界：首次失败和已有值刷新失败继续分离；只有 `retryable=true` 才提供单卡重试，已有值的单卡失败保留旧值并明确来源。
- 边界与验证：未改公共 API、数据库、权限、正式路由、真实数据或生产开关。紧凑指标卡与页面模型 Node 测试 16/16、系统 Chrome 的 V2 全量回归 37/37 通过；其余结果见本次交付。

## 2026-09-09 收口可信数据水位来源登记与查询出站校验

- 水位登记：新增服务端唯一可信水位来源登记表，必须同时匹配 `metricId + sourceKind + sourceApiId` 才能被正式查询采用；当前生产登记保持为空，测试来源不会被生产自动放行。
- 查询门禁：M016 查询在返回业务结果前复核指标目录验数状态、结构化水位 Schema、PID、登记三元组、取证时间和所选结束日；验数通过但水位缺失或未覆盖范围时返回 422，来源冲突返回 502，不截短周期、不推断完整日，也不返回半可信结果。
- 出站边界：V2 单指标查询对执行器成功体再次执行完整响应 Schema 与 `metricId / pid / grain / dateRange` 回显一致性校验；畸形或错范围结果安全映射为 502，内部查询 ID 不回传浏览器。
- 边界与回滚：未登记真实水位来源、未改 M016 验数状态、公共 API 形状、数据库、真实业务值或正式开关；`pDaySum` 仍返回 `watermark=null`。回滚只需撤销登记与出站校验代码，不需要数据迁移。
- 验证：新增水位登记、M016 查询及 V2 出站反例测试；类型检查、服务端构建、生产构建和代码格式检查通过。当前环境没有 Bun，完整 Bun 测试仍不能记为本机已运行。

## 2026-09-09 对齐核心经营总览合同与只读页面状态

- 根因修复：核心总览合同允许当前期和对比期非等长，但紧凑卡原模型把两条趋势压在同一组点位中，可能截断对比期独有日期；开发样板又维护了脱离正式合同的卡片数据结构。现改为两条独立序列，两边全部真实点均可悬停、聚焦和打开详情，同序位存在时展示双方值与差值，缺失不补 0。
- 页面模型：新增核心总览专属合同→卡片适配与只读状态层。浏览器只格式化服务端已计算结果，不重算聚合、比率、跨 PID 合计或水位；缺少指标中心名称/定义时失败关闭。真实 0、八类不可用、部分结果、普通刷新失败保留旧值、身份/权限失败清除旧结果、单卡固定周期子集重试和旧请求取消均有明确状态。
- 样板一致性：开发环境 9 卡 fixture 先通过 `coreOverviewQuerySuccessSchema`，再经同一适配层渲染；正常态和组合异常态视觉结构保持不变，正式 `/dashboards/public` 仍为待接入，样板仍不请求目录或业务查询。
- 页面模型切片边界：未修改公共合同、数据库、权限、导航状态、查询 executor、feature flag、M016 验数状态或真实业务值；链路、经营明细和导出仍只属于开发样板。可信水位服务端门禁的同期独立改动见上一条记录。
- 验证：紧凑卡与核心总览页面模型 Node 测试 12/12、核心总览合同测试 5/5、导航 3/3、主题 3/3、指标快照检查、类型检查、服务端构建、生产构建及代码格式检查通过；真实浏览器确认正常/组合异常态均为 9 卡、无运行错误和横向溢出，生产构建不含设计 fixture。构建仍只有既存 ECharts vendor chunk 体积警告；当前环境无 Bun，未把完整 Bun 套件记为通过。

## 2026-09-09 统一新 Product Shell 的 YiPinBI 品牌字标

- 用户确认：以提供的完整 YiPinBI Logo 替换新 Product Shell 左上角原有的内联图形标与独立 `YPBI` 文本。
- 资源处理：原始 PNG 的灰白棋盘格实际是无 Alpha 的 RGB 背景；已生成真透明背景、紧凑裁边并缩至适合多倍屏显示的项目资源，避免把棋盘格带入白色顶栏。
- 实现：新增 V2 共用 `BrandLogo`，同时覆盖产品顶栏和登录、认证不可用、首次改密页面；桌面和窄屏按比例显示完整字标，不再维护重复 SVG 与文字。
- 边界：登录文案、页面标题、指标“YPBI 接入”等产品术语及遗留经典版均未修改；没有接口、数据库、权限或数据变化。
- 验证：真实浏览器已确认桌面与窄屏 Logo 加载、旧标记消失、布局无挤压；类型、构建与代码检查见本次交付结果。

## 2026-09-09 固化核心经营总览首屏批量查询契约

- 产品与契约：新增 `core-overview/v1`，固定首屏 9 项指标的业务顺序、聚合语义和趋势形式；支持正式整体或动态多 PID、默认最近 7 个共同完整业务日与上一等长周期、明确日期范围及指标子集重试。正式整体不可由 PID 合计，多 PID 必须逐项返回。
- 数据正确性：每个范围逐卡返回可用、无记录、无值、未产出、未成熟、未准入、不支持或失败；真实 0 保持有效。当前期和对比期改为两条独立时间序列，非等长自定义对比不丢点；严格校验日期、单位 / 币种、比较算法、权威版本、映射、验数、来源和可信水位。
- 服务端：新增受登录、`bi:read`、PID 范围和当前数据准入保护的 `POST /api/bi/v2/queries/dashboards/core-overview`。读取型 POST 不要求 CSRF；部分卡失败仍为 200，畸形或失配响应失败关闭。`BI_V2_CORE_OVERVIEW_QUERY_ENABLED` 只接受明确布尔值且默认关闭，开关开启但缺真实执行器时应用拒绝启动。
- 前端：新增严格客户端，但正式 `/dashboards/public` 和开发设计样板均未调用；样板仍使用醒目标记且生产隔离的 fixture。未增加保存、编辑、导出、链接 Token、数据库或正式业务值。
- 验证：核心总览独立合同测试 5/5、`npm run typecheck` 和 `npm run build:server` 已通过；其余全量检查见本次最终交付。当前环境无 Bun，因此新增 Plugin / 客户端 Bun 用例不能记为已运行。
- 回滚：保持或恢复 `BI_V2_CORE_OVERVIEW_QUERY_ENABLED=false` 并重启即可关闭新路由；旧 M016 查询、数据库、正式页面及既有 API 不变。

## 2026-09-09 确认核心经营总览视觉基线并抽取紧凑指标卡

- 设计结论：用户确认核心经营总览样板的信息密度、6＋3 卡片布局、工具栏层级、组合状态与移动经营表可作为 V1 实现校准基线；它不是最终视觉稿，也不代表正式数据页已经开放。
- 组件边界：仅将有明确复用理由的紧凑指标卡、视图模型和样式抽入 `src/v2/features/dashboards/`；页头、筛选、业务链路、质量区和经营明细继续留在代表页，避免提前形成万能组件。
- 状态与语义：数据可用性、完整性和刷新状态分开；真实 0 保持有效；首次失败与刷新失败保留旧值互斥；验数、完整性和可信水位分别展示；无目标时涨跌只表达数学方向。M008/M102/M058 使用 0 基线迷你柱状图，其余首屏指标使用迷你折线，缺失点不补 0。
- 可用性：指标标题改为真实深链语义，对比详情和趋势点支持键盘操作，实例 ID 唯一；趋势提示保留实际日期、单位、差值和状态。
- 真实性边界：开发样板继续使用醒目标记且生产隔离的 fixture；正式 `/dashboards/public` 保持待接入。新增核心总览批量查询公共契约仍须先确认影响、兼容和回滚，本批未改数据库、正式 API、权限、业务数值或生产环境。
- 验证：紧凑指标卡模型测试 5/5、类型检查、前后端构建、生产包 fixture 检索和 `git diff --check` 通过；系统 Chrome 全量回归 V2 37/37＋经典版 2/2 共 39/39，最新卡片抽取另完成定向 1/1。当前环境没有 Bun，未把 Bun 套件记为通过；构建仍只有既存 ECharts vendor chunk 体积警告。

## 2026-09-09 实现 V1 核心经营总览候选样板并收口设计底座

- 代表页：在真实 Product Shell 内增加仅开发环境可达的 `/dashboards/public?design=core-overview` 候选样板；醒目标记固定演示数据，不进入产品导航，不请求正式指标目录或查询，不执行保存。正式 `/dashboards/public` 仍保持真实“待接入”。
- 页面结构：样板包含看板说明、收藏、复制当前视图链接与导出操作位置，业务范围 / 日期 / 对比筛选，6 项主结果、3 项效率与留存指标卡，内容消费 / 获客 / 支付三条阶段链路、客观数据状态，以及动态 PID 横向经营明细表。卡片独立显示当前值、合法对比、自身当前 / 对比趋势、数据完整性和可追溯入口；手机保留表格语义与横向浏览，不改造成另一套卡片数据结构。
- 状态与准确性：正常态之外，同页覆盖加载、真实 0、无记录、未成熟 Cohort、部分数据、刷新失败保留旧值和单卡失败；真实 0 仍显示有效 `-100%` 对比且趋势同步为 0，单卡异常不擦除其他成功结果，状态汇总按实际可展示结果计算。
- 交互与设计底座：修正 390px 筛选网格，手机低频操作收进键盘可达菜单；趋势点支持悬停 / 聚焦详情，看板说明与菜单支持 Esc 和焦点返回。`MenuSelect` 补齐上下循环、`Home / End`、前缀键入查找与选择后焦点恢复；通用按钮和状态样式复用共享基础组件。视觉 Token 收敛到 `tokens.css` 唯一权威源，TypeScript 仅保留 ECharts Canvas 必需投影并由测试防漂移。
- 生产隔离：候选样板由 `import.meta.env.DEV` 条件动态装配；生产构建产物检索不到样板组件、样板 CSS、开发评审标记或固定演示文案，未把 fixture 旁路带入正式产品。
- 验证：指标生成器 32/32、M016 离线验数 20/20、运行时发布准入 4/4、导航登记源 3/3、主题 Token 3/3、`validation:check`、`npm run typecheck`、`npm run build:server`、`npm run build` 与 `git diff --check` 通过；系统 Chrome 的 V2 Playwright 37/37、经典版迁移回归 2/2，共 39/39 通过。构建仅有既存 ECharts vendor chunk 超过 500 kB 的非阻断警告。
- 边界：该页面仍是待用户评审的实现候选，不是最终视觉稿，也不代表正式看板、筛选、复制链接、导出或卡片详情已经接入。评审通过前不批量扩展正式看板组件。当前环境没有 Bun，完整 `npm test` 未运行；未连接真实 PostgreSQL、真实上游、真实 Token，未验数或部署。

## 2026-09-09 固定 V1 代表页设计实施闸门

- 实施顺序：保留已完成的 Product Shell 作为产品架构、路由和导航底座；在批量建设正式看板前，先建设核心经营总览的开发环境独立设计样板。
- 真实性边界：样板只用明确标记的 fixture，不进入产品导航或正式语义路由，生产不可达，不调用正式业务查询、不执行保存、不伪装真实数据能力。
- 评审范围：覆盖 1280/1024/390 三档视口、正常态和组合异常态；一次确认整页密度、KPI 卡、工具栏、看板/卡片数据状态强度和移动端经营对比表，再固化 Token/共享组件并逐页接入真实数据。
- 文档边界：PRD 示意图只固定信息层级、内容范围和操作关系，不是最终视觉稿。本次只更新总 PRD、调整方案、索引与时间线，未修改代码、API、数据库、指标/事件口径或生产环境。

## 2026-09-09 完成稳定两级 Product Shell 与语义路由单一来源

- 产品架构：将已确认的看板中心、分析中心、数据中心及管理分组落实为 3 个工作空间一级入口、10 个二级页面和 1 个数据源维护页面；公共概览、我的概览、指标速览、我的收藏等名称与总 PRD 一致。
- 单一来源：页面名称、面包屑、一级 / 二级选中态、分区默认路由和保留页判断统一从 `src/v2/app/navigation.ts` 派生，删除路由组件内重复的保留路径数组。一级分区路径稳定跳转到其默认二级页。
- 真实状态：当前具备对应正式页面的仍只有指标中心和受保护的数据源维护；其余 9 个二级页只显示低权重“待接入”和目标说明，不渲染模拟卡片、虚构数量、无效创建 / 保存操作或旧页面拼接结果。
- 多端与回归：桌面持续显示完整二级架构，手机导航抽屉保持同一层级与名称；导航登记源 Node 测试 3/3、`npm run typecheck` 和 V2 系统 Chrome Playwright 35/35 通过，覆盖 11 个叶子路径唯一性、默认跳转、直接深链、返回 / 前进、权限可见性、390px 触控尺寸和无横向溢出。关键截图只作为本地结构验收证据，不是正式业务页视觉稿。
- 边界：本批未实现看板、完整分析、事件中心或个人保存，未改数据库、指标 / 事件口径、权限策略、真实数据、Token 或生产部署。

## 2026-09-09 完成可信数据水位公共契约兼容层

- 数据来源：所有正式业务值继续只允许来自已登记上游 API 或已登记服务端加工结果；指标 Markdown 只提供定义与准入依据，浏览器缓存、环境配置、返回行最大日期和“昨日”等推断均不得充当正式数据水位。当前 `pDaySum` 未提供经确认的完整日字段，因此 M016 source 继续明确返回 `watermark=null`。
- 公共契约：V2 指标查询响应的 `meta.watermark` 扩展为 `null` 或结构化对象，明确携带完整至业务日、`Asia/Shanghai`、PID、来源接口、来源类型和观测时间；来源类型只接受上游接口明确返回，或已登记的完成状态 API（上游或服务端加工）明确返回。
- 失败关闭：指标一旦处于 `passed`，缺失水位、对象结构错误、PID / 来源 / 日期 / 时间冲突或查询范围越过完整日，服务端和前端均拒绝整份正式结果；不得截短区间、保留旧业务值或把异常降级成 0。
- 兼容与回滚：旧上游 Adapter 可继续返回 `watermark=null`，无需修改数据库或持久化数据；真实水位接入顺序为先发布消费者与并集 Schema，再发布提供方 Adapter，回滚时恢复 `null` 并由正式查询返回 422，而不是恢复日期猜测。
- 验证：指标定义生成器 32/32、M016 离线验数工具 20/20、运行时发布准入 4/4、契约 smoke 2/2、`validation:check`、`npm run typecheck`、`npm run build:server`、`npm run build`、V2 Chrome Playwright 34/34 与 `git diff --check` 全部通过；构建只有既存 ECharts 大 chunk 非阻断提示。当前环境没有 Bun，完整 `npm test` 套件未运行，不声称全量发布校验通过。

## 2026-09-09 收口 M016 发布准入、会话作用域与正式水位门禁

- 会话隔离：`AuthProvider` 以账号、角色、权限与 PID 范围派生内部 scope signature，范围变化时换用不含 Token 的 auth epoch key 重挂载整个已认证 Product Shell 与路由子树；旧请求随卸载取消，旧查询结果、筛选草稿和弹层状态不再跨账号或权限范围复用。
- 正式结果门禁：M016 待验数维护查询继续允许明确返回 `watermark=null` 与告警；一旦验数状态为 `passed`，source 没有可信水位就由查询服务在构造响应前返回 `422 METRIC_NOT_READY`，不把近期候选行展示为正式结果，也不伪造水位。
- 运行时准入：生成并固定 `metric-release-attestation.json`，服务启动时重验指标快照、映射注册表、平台目录、结果态 evidence 和 comparison 产物的路径、哈希与一一对应，同时拒绝符号链接、路径逃逸及未来验证时间。该产物是仓库内一致性门禁，不是带外数字签名。
- 契约边界：本轮未修改公共 API、数据库、指标口径、验数注册状态或 PID 清单；当前 M016 仍为 `not_started`。现有 V2 契约只允许 `watermark=null`，真实水位必须以另一个经确认的公共契约批次接入；在此之前 M016 不能开放正式成功态。
- 验证：指标定义生成器 32/32、M016 离线验数工具 20/20、运行时发布准入 4/4、`validation:check`、`npm run typecheck`、`npm run build:server`、`npm run build`、V2 Chrome Playwright 33/33 和 `git diff --check` 全部通过；构建只有既存 ECharts 大 chunk 非阻断提示。当前环境没有 Bun，完整 `npm test` 套件未运行，不声称全量发布校验通过。

## 2026-09-08 完成 M016 可复核验数工具与首个样板交互收口

- 验数工具：新增只读离线计划、独立参考数据预检和候选/参考逐值比较；候选必须来自受控技术查询，参考只允许独立 SQL 或获批导出。自动检查全部一致仍只返回 `pending_review`，不联网、不修改注册表、不输出逐行业务值或凭据。
- 证据门禁：结果态注册表必须绑定仓库受控目录内的普通 JSON 证据，并精确匹配指标权威版本、映射版本、平台目录 revision、全部启用 PID、至少连续 7 个业务日和九项检查。缺失、路径逃逸、符号链接、版本/范围/状态不一致或待复核冒充通过均阻断快照生成与构建；M016 继续保持 `not_started`。
- 页面边界：无 PID 时改用服务端平台目录首项，不再特殊优先 PH；修改 PID/日期但未应用时显示草稿提示，URL、旧结果和请求仍保持上一次已应用条件，应用后才共同更新。
- 边界：未使用聊天中出现的 Token，未连接真实上游、未生成通过证据、未修改公共 API、数据库或正式注册状态，未部署。真实验数仍需受保护环境中的候选数据、独立参考结果及四项人工复核；正式默认近 7 日还需可靠数据水位。

## 2026-09-08 完成完整指标目录与 M016 准入样板收口

- 指标目录：从相邻权威《全站指标体系》与版本化映射验数注册表确定性生成并严格校验 92 个标准指标与 3 个周期派生指标；快照显式携带指标权威版本与接入登记版本，全部条目展示定义、源建设、YPBI 映射、当前版本验数及派生可分析状态。M016 是唯一候选映射样板，当前仍待真实验数，其余 94 项保持可见且明确不可用原因。
- 准入与契约：普通用户不能绕过页面直调未验数 M016；具备数据源维护权限的人员也只能对已配置、绑定当前指标权威版本且与真实查询 Adapter 契约完全一致的映射走受控技术查询。验数证据同时绑定 `authorityVersion` 与 `mappingVersion`；未配置、停用、过期、版本不匹配，或登记的来源接口 / 能力与执行器不一致，均在联网前失败关闭，旧兼容目录安全返回空而不拖垮完整服务。正式页面同时核对目录可用状态、查询结果 `passed` 和当前 `mappingVersion`，任一不一致均失败关闭；目录内容哈希由浏览器独立复算。查询响应为 V2 增量契约，旧 API、数据库和持久化数据未迁移。
- 平台与发布：平台、PID、显示顺序和双站路由收敛到版本化服务端目录，未知或停用 PID 在联网前拒绝；移除环境变量中的第二套 PID 白名单。`npm run build` 通过 `prebuild` 强制检查指标快照，`npm run verify:release` 固定包含生成器测试、类型检查、前后端构建及完整 Bun 测试。
- UI 与回归：指标中心支持动态分类、搜索、状态筛选、刷新和详情；手机分类以分组选择器保留一级 / 二级层级，详情在 1280×800 与 390×844 保持状态和底部操作可见、支持键盘滚动并把焦点恢复到实际触发入口，派生指标长状态不会溢出相邻单元格。指标生成器 Node 测试 10/10、语义路由 Node 测试 2/2、类型检查、前后端构建、V2 Chrome Playwright 30/30 与 `git diff --check` 通过；构建仅有 ECharts vendor chunk 超过 500 kB 的非阻断警告。
- 边界：当前环境没有 Bun，因此完整 Bun 测试未运行，统一发布命令不能在本机完整通过；未使用或保存真实 Token，未连接真实 PostgreSQL、未对 M016 做上游抽样验数、未验证生产代理 / 日志 / 配置，未部署、未提交。

## 2026-09-08 完成 V1 自有账号与公网访问本地候选实现

- 产品边界：普通互联网可直接打开 YPBI HTTPS 地址，但除登录页与必要健康检查外不匿名开放；同事无需先登录公司系统或 VPN。所有完成首次改密且具备 BI 访问权的账号都可查看服务端目录当次返回的全部有效 PID，PID 数量不写死；reader、analyst、maintainer 继续区分操作权限。
- 身份与账号：新增独立 PostgreSQL 账号 / 会话 migration、scrypt 密码、12 小时可撤销服务端会话、首次强制改密、连续失败锁定、角色权限和交互式 CLI 创建 / 重置 / 停用 / 启用 / 改角色。真实账号安全变化递增认证版本并撤销旧会话；幂等同值操作在事务锁定快照中判断，不误踢用户。scrypt 保持 128 MiB 推荐档位并增加 1 个执行、2 个排队的进程资源门，普通登录与改密共享代理全局限流，容量满载不误记密码失败。
- 公网与管理：生产候选统一为 Caddy 80/443 HTTPS 静态站点 → loopback Nginx API 白名单 / 限流 → loopback Fastify；生产构建不加载经典版，旧 API 不进公网白名单，遗留 workspace 写入再由应用返回 410。数据源维护要求 maintainer 普通身份、当前普通会话 CSRF、严格 Origin 和绑定账号 / 安全版本 / 客户端 IP 的独立维护密码二次会话。
- 稳定性与日志：公网身份池设置 2.5 秒语句、1 秒锁与 5 秒事务空闲上限，业务身份和公开认证端点的会话读取均有 3 秒请求期限，客户端中断或超时会取消底层 PostgreSQL query；migration 使用独立连接、最长 30 秒 try-lock 和更长但有限的语句 / 锁上限。Caddy 显式移除 query、Referer、Cookie、Authorization 和 CSRF 请求头日志字段；管理客户端仅在普通身份确实失效时返回全局登录。
- 人工凭据首接入：生产环境允许在未预置上游 Token 时以“数据源未配置”状态启动登录与维护入口；当前平台注册表明确两站归属，生产强制两站无凭据 HTTPS 地址、用户名、完整备用 PID 路由及维护二次密码，维护者可在受保护页面验证并保存首个 Token。无 Token 时禁止匿名访问上游，备用 PID 配置不完整时禁止回落主站。首次发布先安全开放维护入口，保存凭据且完整 readiness 通过后才宣布业务可用；服务器环境中的初始 Token 保持可选，运行时安全文件优先。
- 验证：`npm run typecheck`、`npm run build:server`、`npm run build`、语义路由 Node 测试 2/2、V2 Chrome Playwright 28/28 与 `git diff --check` 通过。当前环境没有 Bun，完整 Bun 测试未运行；真实 PostgreSQL timeout / migration、Caddy / Nginx / systemd、真实上游和公网黑盒仍是上线门槛。
- 边界：本批未部署、未连接真实身份数据库、未创建真实账号，也未使用、保存或提交用户在对话中提供的上游 Token。个人保存、看板编辑、导出和当前视图链接仍未进入本批写入实现。

## 2026-09-08 确认 V1 自有账号与统一 PID 范围

- 产品需求权威源确认当前无公司统一登录可复用，V1 使用 YPBI 自有独立账号，稳定区分个人对象 Owner 与操作者。
- 所有已登录且具备 BI 访问权的账号统一可查看服务端平台目录动态返回的全部 PID；不做逐人 PID 权限配置，也不硬编码 PID 数量。
- 上述统一数据范围不替代阅读者、分析者、维护者角色和对象归属校验；上游 Token、维护密码和共享账号不得作为普通用户身份。
- 现有平台调整方案同步记录自有账号、版本化迁移、全站 HTTPS 与旧匿名 API 收口仍是后续实施门槛。本次只确认产品边界，未修改公共 API、数据库或运行代码。

## 2026-09-08 收紧数据源维护可信代理与 HTTPS 生产门禁

- 服务端：production API 固定监听 `127.0.0.1:3000`，避免与代理 upstream 分叉；Fastify 只信任本机反向代理。全部 `/api/bi/admin/*` 响应统一禁止缓存，production 对缺失、HTTP、复合协议头及非可信来源伪造 HTTPS 返回 `426`，对缺失、无效或复合客户端 IP 头返回明确代理配置错误，退出接口也不能绕过。维护会话与连续失败锁定使用可信代理链解析出的真实客户端 IP。
- 部署契约：新增版本化 Caddy 目标配置，Caddy 在 443 终止 TLS、域名 HTTP 如启用只重定向 HTTPS，Nginx:5178 保留遗留 HTTP；两者分别直连本机 API/Web。Caddy 增加独立访问日志并禁止缓存 API 响应，Nginx 不再继承客户端自带 XFF。API systemd 启动命令强制 `NODE_ENV=production`；production 继续允许已确认的文件工作区，不强制引入 PostgreSQL。发布步骤先读取真实配置路径、备份、安装后复验并保留失败回滚，不再把校验仓库候选等同于配置已生效。
- 文档治理：为仓库根目录两份 2026-07 初始实施/评审文档增加明确历史标记，避免其旧模板中心、数据字典、一次上线和强制 PostgreSQL 等内容与当前两份权威需求文档竞争。
- 验证：`npm run typecheck`、`npm run build:server`、`npm run build` 和 V2 系统 Chrome 16/16 通过；另以当前 Fastify 运行时执行监听地址/端口拒绝、编码/未知管理路径、普通 404、缺协议头、HTTP、缺客户端 IP、可信 IPv4/IPv6 HTTPS、非可信来源伪造、会话真实 IP 绑定和退出门禁等 14 项检查，全部通过。线上只读 HEAD 核对显示 HTTPS 入口由 Caddy 响应、HTTP:5178 由 Nginx 响应；HTTP 管理 API 在普通和伪造 HTTPS 头两种情况下均返回 426。
- 边界：未部署、未修改数据库或持久化数据、未使用真实 Token。当前环境缺少 Bun，完整管理 API/代理测试仍待执行；正式发布前须对照 VPS 实际 Caddy/Nginx/systemd 配置，并使用可轮换测试凭证完成生产黑盒。API 重启会使现有内存维护会话失效，重新登录即可。

## 2026-09-08 完成 V2 数据源维护本地纵向切片

- 页面：`/admin/data-sources` 从占位页升级为受保护维护页面，复用既有管理 API；支持独立维护登录、站1/站2脱敏配置、测试当前连接、候选 Token 仅验证及二次确认后的验证并保存。配置状态和本次会话探针结果分开，探针通过不冒充全部 PID、接口或指标验收。
- 敏感状态：密码和候选 Token 只在当前组件内存存在，不写入本站浏览器存储；候选验证后保留以便直接继续保存，保存成功、发起退出、刷新、离开或会话失效后清空。管理响应使用闭合 Schema，`tokenHint` 还必须符合固定掩码并与配置状态一致；任何原文或畸形提示均拒绝渲染。页面不硬编码 PID 路由清单。
- 失败恢复：服务端明确拒绝更新时提示旧凭证保持不变；请求超时、断网或响应畸形时改为“保存结果未确认”，保留候选值并要求先测试当前连接或刷新核对。发起退出立即清空两站候选值；退出请求失败时继续停留在已登录维护页并提示服务端会话可能仍有效。维护能力中途关闭或会话失效同样清空敏感草稿。
- 服务端：两个站点的凭证更新改为完整串行执行，避免并发覆盖；每次写入使用独立临时文件、`0600` 和原子替换，成功或失败均清理临时文件。状态、测试和保存响应均设置 `no-store`，公开 API 和凭证文件格式未改变。
- 验证：`npm run typecheck`、`npm run build:server`、`npm run build` 与 `git diff --check` 通过；语义路由 Node 测试 2/2、系统 Chrome 的 V2 回归 15/15 通过，其中维护链路 10 条。管理 API、维护客户端和并发写入的 Bun 测试已新增，但当前环境没有 Bun，未实际执行。
- 边界：未使用、保存或部署任何真实 Token，未改数据库、普通用户身份、PID 权限、指标或埋点。真实站点凭证验证、动态 PID 状态、HTTPS / 可信代理和反代来源 IP 仍为生产开放门禁；本批未部署。

## 2026-09-08 确认 V1 上游 Token 人工维护边界

- 产品决定：上游暂不支持 Token 自动申请、续期或刷新；V1 采用授权维护者人工轮换，普通看板、分析和导出由 YPBI 服务端自动使用已生效凭据，不要求用户逐次输入。
- 产品规则：数据源维护归入导航底部“管理”；站1、站2分别展示真实状态、服务端配置用户名、动态 PID 范围、脱敏尾号、最近检测与 Token 更新时间，并明确区分“测试当前连接”“仅验证新 Token”和“验证并保存”。
- 安全边界：Token 与对应用户名必须来自同一上游登录会话；候选验证不得改写配置，保存必须服务端先验证并原子替换，失败保留旧值；完整 Token 不进入页面回显、浏览器存储、文档、普通日志或导出。
- 现状核对：服务端已经支持候选 Token 非保存验证，但经典版 UI 缺少入口；当前全局“数据连接正常”没有区分 BI 服务与逐站上游状态，纳入目标产品层重做范围。
- 文档调整：总 PRD 升级至 1.65，现有平台调整方案升级至 1.63；同步数据平台与部署说明。未修改运行代码、线上 Token、API 契约、指标体系或埋点权威源，未部署。

## 2026-09-08 Product Shell / V2 / M016 首批本地代码与浏览器回归完成

- 当前结果：代码已建立经典版与 Product Shell 的独立加载边界、目标语义路由、指标中心和 M016 单 PID 日趋势页；新增只读 `/api/bi/v2` 路由命名空间、指标 / 平台目录及 M016 查询契约，服务端运行入口未注入真实 IdentityProvider、默认失败关闭，并区分真实 0、无记录、空值、部分数据和未知水位。看板、事件、漏斗、保存和管理目标路径仍只显示未开放状态，不算功能完成；路由命名空间分离不等于生产安全隔离。
- 数据边界：M016 只使用 `pDaySum.loginUserCount` 候选映射，权威基线仍为 `v0.23-draft` 且待验数；没有新增指标公式、正式事件、跨 PID 大盘、新老拆分、数据库 Schema、migration 或目标对象写入。
- 生产门禁：正式身份源、角色与 PID 范围未接入前，V2 不向不可信网络开放；开放前须取得网关 / 功能开关隔离证据并通过身份与 PID 权限正反用例。遗留 `/api/bi/ready` 不检查 V2 IdentityProvider、目录或 PID 范围，不能证明 V2 已就绪。V2 查询日志已从代码中移除 PID 与原始错误对象，但仍须核验生产应用、框架和代理的实际日志输出；旧 workspace / `localStorage` 只作迁移期事实，不提供 V2 离线保存或跨会话草稿。
- 交互根因修复：系统 Chrome 回归发现 M016 分析页在浏览器返回触发 `popstate` 后、卸载前仍执行默认参数规范化，导致 `pid/start/end` 被写入指标目录 URL；现已要求异步 `replaceState` 前严格校验仍位于同一指标分析路径，并以“进入分析 → 刷新 → 返回目录”用例设防。
- 契约与兼容收口：V2 指标 / 平台目录、查询成功和错误响应均增加严格运行时校验，查询响应必须回显同一指标、PID、日期范围和粒度；根级遗留 handler 保持旧 `/api/bi/*` 的 `500 INTERNAL_ERROR` 契约，V2 统一错误处理仅封装在自身 plugin，并增加对应回归用例。SSR bundle inject 已对照核实旧坏 JSON 仍返回原 500，V2 同一坏请求返回安全 400。
- 文档收口：总 PRD 保持 1.64；现有平台调整方案升级至 1.62，需求入口、项目索引、UI 防回归及前后端 / 分析模块镜像同步，未新增功能文档。
- 验证状态：2026-09-08 已实际执行 `npm run typecheck`、`npm run build:server` 和 `npm run build`，均通过；构建仅报告 ECharts vendor chunk 超过 500 kB 的非阻断警告。语义路由 Node 测试 2/2、V2 系统 Chrome Playwright 5/5、经典版 Playwright 2/2 通过，关键截图已检查。当前环境没有 Bun，因此 V2 服务端与 Tooltip 单元 / 集成测试文件尚未执行；浏览器成功态使用受控 API fixture，真实身份与权限矩阵、真实上游抽样、生产日志输出、网关隔离和生产配置仍须实际执行并记录，部署状态为未部署。

## 2026-09-08 完成开工架构健康检查并固化长期工程规则

- 检查范围：覆盖已确认产品目标与首个闭环、前端 / 服务端模块、领域对象、数据与查询契约、上游 API、持久化、部署和测试；静态导入检查未发现循环依赖，确认无需整体重写，继续同仓分层迁移。
- 风险结论：P0 集中在普通 API 无身份 / 数据范围、匿名整包覆盖、缺失补 0 与伪漏斗、生产存储配置矛盾以及 root＋Vite preview＋HTTP 部署；P1/P2 覆盖超大模块、重复注册表、筛选失效、客户端拼比较、通用源查询、可观测性、代理 IP、测试和长期维护风险。
- 长期规则：项目根目录 `AGENTS.md` 增加按问题类型判定的权威源、模块化单体依赖方向、P0/P1/P2、公共契约 / Schema / 权限确认闸门、migration / revision / 幂等 / 事务、服务端权限、结构化日志、feature flag、分级验证和固定交付说明；同时修正“当前代码不能覆盖目标 PRD”和“最多 8 平台不是长期产品限制”。
- 文档调整：总 PRD 保持 1.63；现有平台调整方案升级至 1.60，集中承载架构健康检查、渐进治理和首批开发边界；需求入口与项目索引同步，不新增重复 `architecture.md` 或 ADR 目录。
- 明确未改：未修改前端、后端、公共 API、数据库 Schema、权限、安全策略、指标体系、埋点权威源或部署配置；高风险首批实现需按影响、兼容、迁移、回滚和验证方案确认后再执行。
- 验证状态：文档版本、权威边界、全文迁移方向及 Markdown 差异检查通过；本次为文档和长期规则变更，未改运行代码，未部署。

## 2026-09-08 确认官方看板来源归属

- 产品确认：V1 不增加官方 / 个人分析分类 Tab，底层只区分个人与维护范围；新建和普通另存默认归当前用户，官方看板只引用维护范围来源。
- 复制规则：维护者把个人来源加入官方看板时，系统先提示并原子创建独立维护副本，再建立组件引用；个人原件与维护副本后续互不联动，失败不留下组件或半成品。存量无归属来源保持只读，来源删除和复杂资产治理继续后置。
- 需求状态：SA-01 已移出待确认表，V1 产品架构与页面行为全部收口；剩余项目仅为最终分类、权威数据准入和身份权限源核对。
- 估算说明：调整方案中的 155～230 人日和 12～18 周是传统研发团队用于资源比较的等价工作量，不是 Codex 执行时长或交付承诺。
- 文档调整：总 PRD 升级至 1.63，现有平台调整方案升级至 1.59，需求入口与项目索引同步。未修改前端、后端、接口契约、指标体系、埋点权威源或部署代码。
- 部署状态：未部署。

## 2026-09-08 完成开工前产品与实施终审

- 产品终审：总 PRD 核心范围可按章节与数据准入分模块开发；来源分析删除、复杂资产分级和被引用删除治理不进入 V1。当前只剩官方看板来源的最小归属需要产品拍板，最终业务分类及 MD-01～MD-08 作为权威数据准入核对，不阻断通用框架。
- 实施结论：现有系统采用同仓分层迁移，保留 React/TypeScript/ECharts/Fastify/PostgreSQL、真实上游连接、已验数 Adapter 和查询保护；重做身份权限、领域对象、V2 查询契约、路由、正式页面、保存引用和服务端导出。旧 Workspace/Template/Card、全局工作区 JSON、Mock 与旧阶段“漏斗”只作短期只读迁移源，不长期双写。
- 安全门槛：普通用户身份、Owner/维护范围、PID 数据范围、revision、数据源查询准入、受控下钻、HTML/表格导出防注入和正式生产进程在页面批量建设前完成。
- 实施顺序：先完成事实冻结和身份/对象底座，再以“指标中心 → 指标分析 → 保存 → 个人看板 → 查看/刷新/导出/当前视图链接”验证首个闭环，随后接核心经营总览、事件分析、真实漏斗和其余正式看板。
- 文档调整：总 PRD 保持 1.62；现有平台调整方案升级至 1.58，需求入口与项目索引同步。未修改前端、后端、接口契约、指标体系、埋点权威源或部署代码。
- 验证状态：TypeScript 前后端检查与 Vite 生产构建通过；当前 shell 无 Bun，完整 Bun 测试待具备运行时后执行。
- 部署状态：未部署。

## 2026-09-08 确认视频首页一级分类使用与内容点击方案

- 模块：内容发现与互动看板、指标同步、数据能力映射和完整导出。
- 产品确认：指标权威基线同步至 `v0.23-draft`；现有六张首屏卡保持不变，第二层新增 M106～M108 视频首页一级分类 Tab 使用及 M109 一级分类下视频内容点击。V1 只展示一级分类，分类数量和名称由服务端动态返回。
- 展示交互：M106～M108 使用三张独立卡、可切换指标的完整一级分类横向柱状图和精确表；默认按实际 Tab 位置排列，可切换结果降序。M109 独立使用次数卡、日柱状图、一级分类比较和完整表；分类 Tab 点击与视频内容点击不相除、不拼漏斗或转化率。
- 数据边界：M106～M108 仍需真实 Tab 事件查询、身份、稳定分类键和验数；现有 `video.byCategory` 只可作为 M109 候选，且须补稳定分类 ID 并核对日期、点击语义和完整性。两区独立准入，未就绪时不显示空卡、假 0、Mock 或假 Sheet。
- 文档调整：总 PRD 升级至 1.61，现有平台调整方案升级至 1.56，需求入口和项目索引同步；未修改前端、后端、接口契约、指标体系、埋点权威源或部署代码。
- 部署状态：未部署。

## 2026-09-07 确认指标与维度准入规则

- 模块：产品全局规则、指标治理和需求协作。
- 产品确认：官方看板、指标速览及其他固定业务结果中的正式数值必须先登记在《全站指标体系》，再完成真实查询映射和验数；接口已有但权威源未登记的结果不得直接发布为正式指标。
- 口径边界：一级/二级分类、Tab、PID、客户端平台、新老用户、获客类型、渠道和位置均作为维度，不按每个具体取值复制指标；事件分析的三项通用统计和事件属性分组追溯正式事件及统计规则，不把临时组合全部升格为业务指标。
- 协作流程：发现缺失指标时先输出可复制的指标登记提示词，交由指标权威源确认后再由 YPBI 同步和接入，不自动修改权威源或跳过验数。
- 文档调整：总 PRD 升级至 1.60，需求入口和项目索引同步；未修改现有平台调整方案、前端、后端、接口契约、指标体系或埋点权威源。
- 部署状态：未部署。

## 2026-09-06 确认 V1 开放、角色与迁移规则

- 模块：官方看板准入、最小操作角色、存量迁移和需求治理。
- 产品确认：官方看板首次进入公共概览前须具备完整首屏核心、可用默认筛选、至少一个诊断 / 明细区和可追溯整板导出；V1 固定阅读者、分析者、维护者三档逐级操作能力，角色不替代对象与数据范围权限；未映射账号按阅读者处理。
- 迁移方案：系统模板迁为官方看板，个人模板迁为我的看板，专用页面按真实业务并入对应正式看板；资讯与问卷暂不迁，只迁移真实配置、稳定对象关系和已准入数据。
- 文档调整：三项从待确认清单移除；总 PRD 升级至 1.59，现有平台调整方案升级至 1.55，需求入口和项目索引同步。未修改前端、后端、接口契约、指标体系、埋点权威源或部署代码。
- 部署状态：未部署。

## 2026-09-06 当前视图链接与关键页面交互终审

- 模块：看板查看、看板编辑、指标分析、权限边界、现有平台差异和需求治理。
- 当前视图链接：补齐开发可直接实现的完整语义，包括最新已保存看板 / 来源、全部整板与分区查询状态、正式大盘与 PID 集合区分、状态优先级、不可读版本化令牌、安全白名单、生成前校验、登录与鉴权顺序、失效修复、看板更新提示、恢复默认、剪贴板降级及安全审计；链接不承担分享授权。
- 页面与交互：增加链接载入和失效修复示意，明确正式业务看板统一继承标准页头；看板编辑改为默认完整画布、选中后右侧属性面板，删除未定义的全局撤销 / 重做，只保留移除组件后的单次撤销；修正指标分析切换主指标不会使查询结果过期。
- 文档调整：总 PRD 升级至 1.58，现有平台调整方案升级至 1.54，需求入口与项目索引同步；未修改前端、后端、接口契约、指标体系、埋点权威源或部署代码。
- 验证范围：关键页面示意覆盖、动作入口与状态闭环、权限和防泄露边界、实现验收场景、版本引用及 Markdown 结构。
- 部署状态：未部署。

## 2026-09-06 复制当前视图链接纳入 V1

- 模块：看板中心、最小访问边界、现有平台差异和需求治理。
- 产品确认：看板查看态在看板级“更多”菜单提供“复制当前视图链接”；链接以稳定看板 ID 复现复制时已应用的具体日期、对比期、实际 PID 集合和安全整板筛选，快捷日期与“全部 PID”均固化为当次实际范围。
- 权限与状态：链接不携带查询结果、账号身份、凭据、未保存编辑或临时展示状态，也不授予看板或数据权限；打开时按访问者重新校验，失效或越权条件逐项提示且不静默删减。个人看板链接只有 Owner 可打开。
- 文档调整：总 PRD 升级至 1.57，现有平台调整方案升级至 1.53，需求入口和项目索引同步；主动分享授权及其他 V1.1 候选仍后置，未修改前端、后端、接口契约、指标体系、埋点权威源或部署代码。
- 验证范围：版本引用、功能清单、权限边界、页面动作、异常状态与模块级验收。
- 部署状态：未部署。

## 2026-09-06 确认其余五张正式业务看板

- 模块：播放与观看质量、内容发现与互动、运营玩法概览、产品体验与启动质量、功能使用概览、现有平台差异和需求治理。
- 产品确认：五张看板均升级为“已确认，可开发”，并补齐功能点、权威指标引用、默认展示、页面层级、筛选与联动、真实状态、完整导出、多端要求和模块级验收；功能使用主比较完整展示所有已准入页面/模块，多功能趋势最多选择 5 项。
- 数据边界：产品结构确认不替代生产准入。M032 默认分位、功能渗透率稳定指标 ID、M052 活跃分母、M080 启动终态覆盖及各项正式事件/查询/验数继续作为数据前置；禁止用旧字段、前端推算、空壳、假 0 或 Mock 补位。
- 现状差异：补齐五板对应的现有接口候选、缺失粒度、加工与状态要求；存量专项页面仍须逐页完成业务与指标映射，不能按代码页面直接迁移。
- 文档调整：总 PRD 升级至 1.56，现有平台调整方案升级至 1.52，需求入口和项目索引同步；未修改前端、后端、接口契约、指标体系、埋点权威源或部署代码。
- 验证结果：完成版本、章节状态、指标名称、功能点、导出、数据门槛、待确认项和 Markdown 格式一致性检查；五板无残留草稿状态。
- 部署状态：未部署。

## 2026-09-05 完成核心经营总览产品确认

- 产品状态：核心经营总览四层结构及功能细节已完整确认，章节改为“已确认，可开发”；数据能力是否可进入正式环境仍按指标、链路和区域分别通过权威映射、真实查询及验数，不用产品确认替代数据准入。
- 关键质量：右侧 V1 常驻目标为 M080 启动成功率和 M031 起播率；M032 首帧耗时待分位数查询、默认统计量、单位和验数明确后进入质量详情或后续版本，不以平均值占位。M036 已在首屏展示，不在右侧重复。
- 数据状态：明确实际数据范围、共同可靠水位、动态应覆盖 / 已覆盖 PID、当前与对比期完整性、各类不可用数量、本次查询时间及指标 × PID × 日期详情；查询时间不冒充数据截至时间，缺失不转 0，不生成综合质量分或无阈值经营结论。
- 经营明细：采用动态业务 PID × 首屏 9 项指标，一格同时承载当前值、比较结果和状态，默认按 M016 降序；确认冻结列、单元格详情、M020 成熟 Cohort、窄屏横向滚动和完整 XLSX 导出。大盘只在正式整体接口返回并验数通过后置顶，不由 YPBI 本地制造。
- 能力差异：现有 YPBI 无 M080/M031 正式查询映射，仅有部分请求级状态元数据和局部 PID 行；后续需补实际水位、完整性、单元格状态、统一周期比较、正式大盘及导出能力。未准入项不显示空壳、假 0 或 Mock。
- 文档调整：总 PRD 升级至 1.41，现有平台调整方案升级至 1.38，需求入口和项目索引同步；未修改前端、后端、接口契约、指标体系、埋点权威源或部署代码。
- 部署状态：未部署。

## 2026-09-04 确认核心经营总览三组关键链路

- 产品结构：关键业务链路按内容消费、获客转化、支付链路三组轻量切换；理想产品结构不因当前数据缺口删除，各组按真实接入和验数状态独立准入。
- 指标引用：内容链引用 M016/M026/M035 与 M081，M036 留在质量区；获客链引用 M001/M003/M008 与 M005/M006/M007，M099 仅作诊断；支付链引用 M084/M086/M090，并以 M070/M071/M072 定位环节问题。
- 支付事件：用户确认 `recharge_page_view`、`recharge_submit_click`、`order_create_request_result`、`payment_wait_timeout`、`payment_callback_process_result`、`recharge_credit_status_change` 六个埋点均已存在，不新增同义事件。现有 YPBI 仍需同步实际事件版本与状态、接入查询和链路加工并完成验数；旧三段汇总不得冒充新链路。
- 数据边界：埋点存在不等于指标已可查询；无稳定指标 ID 的支付等待超时结果不直接生成正式 KPI。获客三项转化保留权威源中的次数口径、主体/窗口差异等提示，不包装成真实用户漏斗。
- 文档调整：总 PRD 升级至 1.40，现有平台调整方案升级至 1.37，需求入口和项目索引同步；未修改前端、后端、接口契约、指标体系、埋点权威源或部署代码。
- 部署状态：未部署。

## 2026-09-04 补齐看板折线柱状与功能渗透率规则

- 模块：看板组件、通用图表、功能使用概览、现有平台差异。
- 竞品取舍：参考神策与 GrowingIO 的卡片信息层级、折线趋势、类别柱状、图例和悬停方式；不照搬任意双轴、卡头堆叠、虚线多义或对所有指标机械展示合计/均值。
- 视图切换：按来源与真实结果开放“趋势/对比/表格”或“漏斗/趋势/表格”；现有“变化”改为可核对的中性对比，不再输出模板化原因和好坏判断。
- 图表规则：折线缺失断开、当前期实线与对比期同色虚线；纵向柱用于时间或少量类别，横向柱完整承载 PID/功能等较多或长名称类别，柱状数值轴包含 0；悬停展示实际日期、精确值、比较基准和状态。
- 功能渗透率：登记视频详情页、个人中心、短视频等功能使用比较方向，复用权威模块通用渗透率模板和稳定功能维度，不拆成同义指标，不使用饼图或求和平均制造整体值；稳定指标引用、正式埋点、查询和验数未就绪前保持草稿。
- 文档调整：总 PRD 升级至 1.39，现有平台调整方案升级至 1.36，需求入口和项目索引同步；未修改前端、后端、接口契约、指标体系或部署代码。
- 部署状态：未部署。

## 2026-09-04 补齐核心经营总览竞品对照规则

- 模块：核心经营总览、看板编辑、通用数值显示、现有平台差异。
- 交互与结构：当前期/对比期趋势使用实线/虚线；卡片主体负责选择指标，标题及点位详情中的明确入口负责带条件进入指标分析；PID 结构完整保留并按值排序，占比只在可加总、分组互斥和整体分母一致时展示。
- 展示与语义：增加标题旁看板说明入口和统一数值格式；没有权威指标方向、目标或阈值时中性显示变化，不自动判定正常/异常。
- 布局规则：“6＋3”保留为默认业务分组，不锁成不可编辑整体；授权维护者可在独立编辑态按受控栅格逐卡调整，查看态不可直接修改布局。
- 示意与边界：总 PRD 内嵌结构示意同步补齐对比趋势、完整 PID 和动作分层；关键业务链路、质量区与经营明细的具体内容仍待下一轮确认。
- 文档调整：总 PRD 升级至 1.38，现有平台调整方案升级至 1.35，需求入口和项目索引同步；未修改前端、后端、接口契约或部署代码。
- 部署状态：未部署。

## 2026-09-04 确认核心经营总览趋势与结构布局

- 模块：看板中心、核心经营总览。
- 对比展示：数量与金额卡片正文显示变化率，悬停或详情补充对比基准值和绝对差值；比率指标显示百分点变化。
- 趋势与结构：首屏下方采用左侧共享主趋势、右侧能力受控结构；点击首屏指标同步切换。结构维度不支持时显示原因并清除上一指标结果。
- 后续层级：再下方固定为“关键业务链路＋质量与数据状态”，页面底部固定为经营明细表；具体内容、列和默认排序继续逐项确认。
- 文档调整：总 PRD 升级至 1.37，需求入口和项目索引同步；未修改前端、后端、接口契约或部署代码。
- 部署状态：未部署。

## 2026-09-04 回读确认项并补齐首屏实施细节

- 模块：需求治理、核心经营总览、通用图表、现有平台差异。
- 对话回写：将“每轮确认后主动回读对话并把实现相关细节写回总 PRD / 调整方案”固化为长期维护规则；进入下一议题前核对已确认、待确认和冲突项，不再依赖用户重复提醒。
- 首屏规格：补齐每张卡的固定信息、卡片与趋势的不同聚合语义、M102 小时单位、M058 币种边界、M020 Cohort 趋势横轴、PID 结构能力门槛及平板/手机换行规则；默认上一等长周期明确为核心总览特例。
- 通用展示：把“受控低基数维度完整展示，高基数维度只裁剪图表为 Top10，表格与导出保持完整”提升为指标分析和事件分析共用规则；明确展示形式属于“指标/统计项 × 使用场景”。
- 差异审计：补齐正式预览和模拟分析结果的 Mock 清理、广告排除范围、整板全屏、完整导出、动态 PID 前端硬编码及数据源维护现状；按 9 项首屏指标登记候选字段、真实路径、粒度、范围、验数状态和缺口类型。
- 文档调整：总 PRD 升级至 1.36，现有平台调整方案升级至 1.34，需求入口和项目索引同步；未修改前端、后端、接口契约或部署代码。
- 验证结果：已按产品、实现、测试和数据真实性视角交叉核对；确认主要模块无整块遗漏，仍待确认项继续保留在总 PRD 第 15 节。
- 部署状态：未部署。

## 2026-09-04 收口核心经营总览首屏日期与周期规则

- 模块：看板中心、核心经营总览。
- 默认日期：最近 7 个完整业务日，不并入尚未完整的今天；显示实际范围和共同数据截至时间。
- 周期语义：M016、M026 使用成熟日均，M008、M102、M058 使用区间累计，M059 使用区间去重，M081、M036 使用权威周期结果，M020 只汇总成熟注册 Cohort；卡片辅助文案明确聚合类型。
- 对比与联动：默认比较上一等长周期；比率变化使用百分点。共享趋势默认 M016 按日，结构默认动态完整 PID，点击卡片同步切换。
- 数据边界：周期结果必须由真实服务或加工层提供；禁止前端平均每日比率、累加日 UV、跨 PID 制造大盘或以缺失补 0。
- 文档调整：总 PRD 升级至 1.35，现有平台调整方案升级至 1.33；首屏产品规则收口，首屏下方内容和真实数据映射继续待确认。未修改任何系统代码。

## 2026-09-04 确认核心经营总览首屏默认展示

- 模块：看板中心、核心经营总览。
- 页面层级：桌面端首屏按 6 项主结果与 3 项效率/留存组织，不将九项无差别平铺。
- 默认形式：九项均使用数值卡；人数趋势默认折线，新增、观影总时长和充值金额默认按日柱状，三项比率默认折线且变化使用百分点。V1 卡片不嵌迷你图，点击切换共享主趋势。
- 留存边界：“次留”固定为 M020 注册用户D1留存率，卡片显示 Cohort 日期和成熟状态；M040 D1有效观影留存率留在内容消费专题。
- 待确认：卡片的周期展示语义和看板默认日期。
- 文档调整：总 PRD 升级至 1.34，现有平台调整方案升级至 1.32；未修改前端、后端、接口契约或部署代码。

## 2026-09-04 确认核心经营总览首屏指标

- 模块：看板中心、核心经营总览。
- 指标范围：首屏确认纳入 M016 日活跃用户数、M008 新增用户数、M026 观影用户数、M102 观影总时长、M059 付费用户数、M058 总充值金额、M081 活跃用户观影率、M036 有效观影率、M020 注册用户D1留存率。
- 信息层级：前 6 项作为主结果，观影率、有效观影率和注册用户D1留存率作为效率/留存层，不将 9 项无差别平铺。
- 数据边界：M102、M036 当前需平台加工，其余首屏指标仍待正式验数；未就绪指标不使用空壳、相邻指标或 Mock 补位。
- 待确认：各指标默认样式、多日周期展示语义和看板默认日期。
- 文档调整：总 PRD 升级至 1.33，现有平台调整方案升级至 1.31，指标核对基线同步为 `v0.20-draft`；未修改前端、后端、接口契约或部署代码。

## 2026-09-04 确认核心经营总览页面框架

- 模块：看板中心、核心经营总览、指标与看板目录。
- 产品定位：核心经营总览是公共概览置顶的官方跨业务看板，只承载核心指标和必要诊断，不展示全部指标，也不替代个人单日扫数的指标速览。
- 页面结构：确认按“核心结果 → 趋势与结构 → 关键链路与质量 → 经营明细”组织阅读；指标或组件继续进入真实来源分析或对应专题看板。
- 数据边界：目标默认读取正式服务提供的大盘整体结果并可切换 PID；未接入时显示暂不支持，不由 YPBI 本地跨 PID 去重、求和、加权或使用 Mock 补齐。
- 待确认：首批指标 ID/版本和默认日期仍未确定，核心经营总览保持草稿；业务分类待用户调整权威指标体系后统一核对，BI 指标中心不另建分类源。
- 文档调整：总 PRD 升级至 1.32，现有平台调整方案升级至 1.30；未修改前端、后端、接口契约或部署代码。

## 2026-09-03 确认指标速览单日查询与指标数据详情

- 模块：指标速览、指标数据详情、指标分析与看板联动。
- 日期规则：指标速览使用单日日期选择器；普通日指标以所选日 D 展示，并比较 D-1 和 D-7，详情趋势结束日同步锚定 D。
- 详情对象：点击指标名称或所选 PID 当前值打开轻量“指标数据详情”抽屉，不默认跳转卡片；桌面端保留速览上下文，手机端使用全屏抽屉。
- 展示内容：抽屉展示所选 PID 与正式大盘的当前、较昨日、较上周同日、近 7/30 日趋势及数据状态；V1 不展示当前份额、自动评价、原因总结或评论。
- 联动规则：详情分别提供进入指标分析、查看真实引用组件所在看板和查看指标定义；指标无看板引用时不得猜测卡片或强行跳转。
- 文档调整：总 PRD 升级至 1.31，现有平台调整方案升级至 1.29；未修改前端、后端、接口契约或部署代码。

## 2026-09-03 确认指标速览大盘与动态平台范围

- 模块：指标速览、平台公共维度、事件/漏斗平台展示和数据能力映射。
- 平台规则：目标产品不固定业务 PID 数量，平台目录、可查询范围和大盘覆盖范围均随正式服务动态返回；本次核对时的 20 个 PID 只保留为当前代码事实。
- 大盘规则：大盘读取正式接口已经计算好的整体结果，包含接口当日覆盖范围内的全部可用业务数据；YPBI 不遍历各 PID 去重、求和、加权或复用现有本地合并制造大盘。
- 展示规则：所选 PID 与大盘分别展示当前值、较昨日和较上周同日；V1 只展示客观数值、变化与数据状态，不提供相对大盘评价、原因分析、自然语言点评或运营建议。
- 能力边界：正式整体接口仍需定位并核验实际数据日、覆盖范围、完整/部分状态及逐指标结果；未接入或未验数时显示暂不支持，不能回退为静态 HTML 或本地聚合。
- 文档调整：总 PRD 升级至 1.30，现有平台调整方案升级至 1.28；关闭指标速览大盘的产品待确认项，未修改前端、后端、接口契约或部署代码。

## 2026-09-03 确认多份个人指标速览

- 模块：看板中心、指标速览、导出与数据能力映射。
- 产品定位：在看板中心新增固定“指标速览”入口，用一张密集表格集中查看自选权威指标；它不是普通看板、分析或新的报表中心。
- 对象规则：每位用户可创建多份独立速览并自定义名称；支持打开、重命名和二次确认删除，保存 Owner、默认 PID、观察项/顺序/分类及 `revision`，V1 不做共享、公开速览和自动分析结论。
- 查看规则：单次选择一个 PID 和一个基准日，展示当前值、较昨日、较上周同日、实际数据日及真实状态；指标观察项可带已验数固定维度，支持进入指标分析和定位真实看板组件。
- 真实性：外部每日分析 HTML 只作为信息结构参考，静态数值、自动结论、广告项和文件内产品清单不进入正式能力；大盘列待 QV-01 明确范围、去重和不可加指标口径后开放。
- 文档调整：总 PRD 升级至 1.29，现有平台调整方案升级至 1.27；未新增独立功能文档，未修改前端、后端、接口契约或部署代码。

## 2026-09-03 完成真实漏斗与统一保存规则收口

- 模块：真实漏斗分析、分析中心公共保存、数据能力映射。
- 竞品取舍：页面骨架采用 GrowingIO 易理解的“左侧配置、右侧大图、下方完整表格”；组链、首步日期、稳定排序和中位耗时采用更严谨的确定性规则；未照搬次数漏斗、多主体、用户列表和复杂模板。
- 漏斗规则：确认人数漏斗 2～10 步、有序非严格相邻、确定性代表序列、首步日期和成熟窗口、全局/步骤筛选、一个属性关联、一个分组、中位耗时、结果公式、周期对比及 20 个 PID 展示方式。
- 真实性：成熟进入才进入分母和流失；缺失稳定顺序的同时间戳序列排除并计数；多 PID 分别组链，部分成功不得生成伪完整总计；现有阶段汇总不能冒充真实漏斗。
- 统一保存：指标、事件和漏斗分析共用保存/另存入口，统一进入“已保存的分析”，保存来源类型并从列表返回对应分析编辑页；加入看板另行生成组件引用。
- 实现边界：分析资产只保存查询定义及最近成功运行的元数据/可失效缓存引用，不固化分析数值；事实唯一键只用于去重，同时间戳排序必须依赖单调事件序号或明确因果顺序；目标支持 20 个 PID 完整比较，现有最多 8 个独立系列属于待改造限制。
- 文档调整：总 PRD 升级至 1.28，真实漏斗章节标记为“已确认，可开发”；现有平台调整方案升级至 1.26；同步需求入口和项目索引，未新增独立功能文档。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-03 确认事件分析结果展示与分组值策略

- 模块：事件分析结果区、图表兼容、维度展示和数据能力映射。
- 竞品取舍：采用神策“图表下方保留聚合表”和多系列图例分页的优点，不采用双轴及会改变不可加指标语义的快速求和；不采用 GrowingIO 表格替换图表、异单位同轴和仅按展示集合计算整体的方式。
- 确认布局：结果固定按紧凑汇总条、大图表、完整聚合结果表排列；汇总值按完整时间范围准确重算，结果表不称为源数据或原始日志。
- 图表规则：按时间、分组和无分组结构选择默认图形；V1 支持 KPI、折线、横向/纵向柱状、饼图/环形图和表格，不同单位自动分图，气泡、累积和双轴后置。
- 维度规则：20 个业务平台等受控核心维度完整展示，趋势图使用图例分页和突出交互；渠道、内容 ID 等高基数开放维度超过 10 项时只裁剪图表为 Top10，表格和导出保持完整；两维组合超过 20 个系列时默认层级表格。
- 完整性：Top10 排序基于明确分析项的完整周期汇总；只有服务端可精确返回完整余量且统计可加总时才生成“其他”；接口截断必须标记，不能冒充完整合计或占比。
- 文档调整：总 PRD 升级至 1.17，现有平台调整方案升级至 1.16；未新增独立功能文档。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-03 确认事件分析数量、分组与筛选规则

- 模块：事件分析、数据能力映射。
- 竞品核对：神策支持多个指标、多个分组及两层 AND/OR 嵌套但未公开通用硬上限；GrowingIO 未公开指标及维度数量上限，其“最多 10 个”指维度值，全局过滤最多 5 条且按 AND。
- 确认规则：新建默认 1 个、最多 5 个分析项；默认不分组、最多 2 个非时间维度，第二维必须由查询服务返回真实交叉粒度；每个分析项最多 5 条局部条件。
- 条件语义：不同局部条件行按 AND，同一条件多值按 OR/`IN`；公共条件与各项局部条件按 AND 共同生效；复杂条件对比使用复制分析项完成，V1 不建设嵌套条件组。
- 真实性边界：多事件仅开放共同支持且语义一致的维度；查询服务无法真实应用的条件必须在提交前禁用，不能接收后静默忽略。
- 文档调整：总 PRD 升级至 1.16，现有平台调整方案升级至 1.15；未新增独立功能文档。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-03 确认事件分析统计方式与公式记录规则

- 模块：事件分析、数据能力映射、需求文档治理。
- 竞品核对：神策事件分析以总次数、触发用户数和人均次数为三项基础统计，天数频次由独立分布分析承接；GrowingIO 官方资料明确前三项及属性聚合，Demo 虽出现总天数和人均天数，但公开资料未给出可直接采用的精确公式。
- 确认规则：V1 基础统计为总次数、触发用户数、人均触发次数；属性聚合保留求和、均值、最大值、最小值及条件去重值数；触发人天数和人均触发天数按用户 × 业务日计算，仅在正式身份、统一业务日、查询能力和验数通过后开放。
- 口径治理：权威业务指标公式继续只由《全站指标体系》维护；YPBI 自身的通用分析统计方式必须在总 PRD 记录展示名称、定义、公式、去重粒度、时间归属、缺失身份和除零规则，AI 不得自行猜测。
- 数据边界：缺失身份的事件可进入总次数，但不进入用户类统计；人均触发次数使用有效身份事件事实作分子，禁止用全部次数除以部分可识别用户；天数能力不得由前端累加日用户数近似实现。
- 文档调整：总 PRD 升级至 1.15，现有平台调整方案升级至 1.14；未新增独立功能文档。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-03 完成指标分析需求收口

- 模块：指标分析、图表兼容。
- 确认规则：V1 提供 KPI、折线、普通横向/纵向柱状、堆叠柱状、饼图/环形图和表格；堆叠图仅用于可加总指标及互斥、非负、完整分组。
- 后续边界：高级图表根据未来已确认指标的语义、单位、可加总性、维度与真实数据结构扩展，不因竞品存在或前端可绘制而直接开放。
- 状态调整：指标分析的入口、指标数量、主指标、单位、分组、日期和图表规则均已确认，章节标记为“已确认，可开发”；当前数据能力仍为部分支持，需完成权威映射、接口核对和验数。
- 文档调整：总 PRD 升级至 1.14，现有平台调整方案升级至 1.13；未新增独立功能文档。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-03 确认全平台日期与对比规则

- 模块：全局日期、看板、指标分析、事件分析和漏斗分析。
- 确认规则：统一日期组件提供今天/昨天、周/月/年、过去 7/14/30/60/90/180 天和自定义；提供上一等长周期、上周/月/年同期及自定义对比，一次最多一个对比期。
- 保存与真实性：快捷日期保存动态方式，自定义保存固定日期；始终展示解析后的实际区间和数据截止时间，天数不同、能力不支持或数据未成熟时明确提示。
- 模型边界：漏斗日期仍表示第一步进入时间并受转化窗口和成熟样本约束；“上线至今”仅在存在可信上线日期时出现。
- 文档调整：总 PRD 升级至 1.13，现有平台调整方案升级至 1.12；同步关闭指标/事件日期对比待确认项，漏斗只剩分组数量待确认。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-03 确认指标分析分组维度

- 模块：指标分析、公共维度。
- 确认规则：新建分析默认不分组，V1 最多选择 2 个非时间分组，时间不占名额；第二维必须由查询服务返回真实交叉粒度，多指标只能使用共同支持的维度。
- 口径边界：客户端平台、新老用户和获客类型属于维度，“新增用户数”属于指标；新老用户和获客类型须由上游或加工层稳定产出并验数，YPBI 不自行推导。
- 文档调整：总 PRD 升级至 1.12，现有平台调整方案升级至 1.11；未新增独立功能文档。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-03 确认指标分析单位展示

- 模块：指标分析、多指标图表与聚合表。
- 确认规则：同单位指标可共用一张图和纵轴；不同单位按单位拆图，主指标所在图优先；表格并列保留全部指标并显示单位。
- V1 边界：不做双轴图和标准化指数图，不使用缩放造成不同单位可直接比较的错觉。
- 文档调整：总 PRD 升级至 1.11，现有平台调整方案升级至 1.10；未新增独立功能文档。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-03 确认指标分析主指标规则

- 模块：指标分析、多指标展示。
- 确认规则：第一个选择的指标自动成为主指标且可切换；主指标只影响默认标题、顶部核心数值、图表主要展示和默认排序，不改变公式、筛选、查询口径或数据优先级。
- 交互边界：调整指标顺序不自动改变主指标；删除主指标后由剩余列表第一项接替；结果完整时切换主指标不重新查询。
- 文档调整：总 PRD 升级至 1.10，现有平台调整方案升级至 1.9；未新增独立功能文档。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-03 确认指标分析选择数量

- 模块：指标分析。
- 确认规则：新建分析默认选择 1 个指标，必要时可增加相关指标，V1 最多暂定 5 个；多指标用于相同查询条件下并列观察，不创建组合公式或自动推断因果。
- 竞品边界：神策多维分析只作为多指标交互参考，不复制其指标创建、跨分析模型混合和表格中心能力。
- 现状差异：当前卡片允许最多 16 项本地指标，目标改为最多 5 个已接入、已验数且组合兼容的权威指标。
- 文档调整：总 PRD 升级至 1.9，现有平台调整方案升级至 1.8；未新增独立功能文档。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-03 确认指标分析定位

- 模块：分析中心、指标分析、指标来源看板组件。
- 确认规则：保留指标分析独立入口，定位为只读使用权威指标的轻量查询、对比和维度拆解能力；指标中心负责定义与能力状态，指标分析负责查询结果，事件分析和漏斗分析继续承接各自独立语义。
- V1 边界：不在 YPBI 创建或修改指标、公式和口径；具体指标数量、主指标、单位展示、分组、日期对比和图表范围继续逐项确认。
- 文档调整：总 PRD 升级至 1.8，现有平台调整方案升级至 1.7；未新增独立功能文档。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-03 完成总 PRD 与现状差异终审收口

- 模块：总产品需求、现有平台调整方案、需求入口、项目索引和 UI 防回归边界。
- 状态纠正：指标分析与真实漏斗只有入口定位/真实性原则已确认，详细功能继续待确认；来源分析删除入口在资产归属与引用删除策略确认前不开放。
- 数据事实：补记工作区匿名全局读写、无 Owner/并发保护、现有详情不是真实按卡片条件下钻、数据源检测状态未持久化、权威事件当前均为草稿，以及业务平台/客户端平台字段语义冲突。
- 规则补齐：增加最小身份与操作权限、`revision` 条件写入、页面级状态、配置变更后的旧结果禁用、导出格式/时效/权限边界、跨 PID 用户数、事件事实去重和漏斗共同水位规则。
- UI 边界：防回归文档只约束当前存在或总 PRD 已确认的模块；历史模块名不能授权新增、保留或恢复对应功能。
- 后续决策：已保存分析的资产分级和来源删除治理本轮暂不细化；存量未归属来源保持只读，暂不提供来源删除入口。
- 已确认交互：从看板组件进入来源分析时继承当前可支持的看板会话条件；临时条件不自动写回，不支持项必须显式提示。
- 文档版本：总 PRD 升级至 1.7，现有平台调整方案升级至 1.6；本轮未修改前端、后端、接口契约或部署代码。

## 2026-09-02 按聊天最终结论完成总 PRD 完整性复核

- 模块：总产品需求、现有平台调整方案、看板中心、分析引用、公共筛选和数据源维护。
- 纠正内容：公共看板 V1 为影响提示确认后直接保存，不建设草稿发布；看板组件标题直接进入来源分析，不再建设独立通用卡片详情页。
- 补齐内容：增加个人看板新建/复制/改名/删除生命周期、三层筛选及组合逻辑、分类迁移稳定性、看板设置与保存前校验、来源查询同步与另存隔离、共用分析交互、数据源维护、安全边界和模块级验收。
- 文档治理：总 PRD 升级至 1.4，调整方案升级至 1.3；待确认项改为一行一个决定；已确认的稳定引用与同步底座单独开放开发，资产治理继续待确认。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-02 明确总 PRD 的双重阅读目标

- 模块：产品需求治理与总 PRD 写作标准。
- 确认目标：总 PRD 既供产品经理和同事阅读评审，也供 AI 脱离聊天记录按已确认章节开发与验收。
- 文档调整：总 PRD 升级至 1.3，增加“快速理解 → 规则核对 → 开发执行”的分层阅读标准；明确事实与待确认分离、示意图与文字规则关系，以及不写无关实现细节的边界。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-02 回读完整讨论并补齐 AI 开发需求上下文

- 模块：总产品需求、现有平台调整方案、看板与来源分析、数据中心、导出参考。
- 需求/问题：总 PRD 合并时压缩了部分已确认的页面布局、组件动作、受控栅格、图表兼容和目录字段，后续 AI 可能需要依赖聊天记录自行猜测。
- 文档调整：总 PRD 与调整方案升级至 1.2；补齐看板查看/编辑/卡片详情线框、三类来源分析差异、组件操作边界、图表兼容矩阵、指标/事件中心字段、缺失分母规则和 AI 开发读取顺序；引用已生成的导出结构样例，并明确样例数据不代表正式能力。
- 状态处理：只补入对话中已经确认的规则；指标/事件/漏斗分析、已保存分析同步及删除策略等未决内容仍保持待确认。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-02 确认指标分析独立入口

- 模块：产品架构、分析中心和指标中心。
- 确认规则：分析中心二级入口包含指标分析、事件分析、漏斗分析和已保存的分析；指标中心负责指标目录、口径来源与能力状态，指标分析负责选择指标、维度和筛选并生成查询结果。
- 文档调整：总 PRD 与现有平台调整方案升级至 1.1，移除“指标分析入口位置”待确认项；未新增独立功能文档。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-02 收敛 YPBI 需求文档结构

- 模块：产品需求治理、需求索引和数据能力映射。
- 需求/问题：逐功能拆分 Markdown 与“单一总 PRD + 单一现状差异文档”的协作方式冲突，入口过多且同一规则分散重复。
- 文档调整：新建 `BI-产品需求文档.md`，统一收拢产品架构、看板、已保存分析、指标/事件/漏斗分析、数据中心、全局交互和导出；将原 `BI-数据能力映射.md` 合并进 `BI-现有平台调整方案.md`；`README.md` 只保留两份业务权威文档入口。
- 状态处理：总 PRD 使用章节级状态；仅产品架构、看板编辑、指标/事件中心架构和导出等用户已明确确认范围标记“已确认，可开发”，事件分析、漏斗分析、指标分析及来源同步规则继续待确认。
- 清理结果：原全局规则、数据能力映射和四份 `features/` 功能文件已被合并并取消独立权威入口，历史变更记录保留。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-02 确认 BI 看板编辑边界

- 模块：公共概览、我的概览、看板编辑、来源分析和多端规则。
- 确认规则：看板编辑使用“添加分析”，只管理引用、位置、尺寸、展示标题和兼容图表；指标、事件、漏斗步骤、筛选、分组、时间粒度及计算规则回到来源分析编辑。
- 保存规则：公共看板保存草稿后发布，个人看板直接保存；未保存离开需确认，复杂版本、审批与回滚后置。
- V1 收口：添加分析支持搜索、类型筛选和批量加入；编辑态可改看板名称、添加章节说明；快速布局、精细样式、另存为、订阅与预警后置。
- 多端边界：桌面端支持编辑，平板和手机仅支持查看、收藏、筛选、刷新、详情与导出。
- 代码与部署：仅更新需求文档，未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-02 确认 BI 导出 V1 结构与语义

- 模块：看板、组件和已保存分析导出；产品全局规则与现有平台调整方案。
- 确认规则：XLSX 按说明、关键结果、实际产生的聚合/业务明细和数据状态组织；整板导出增加看板总览、组件索引及各成功组件独立结果；无真实结果的 Sheet 不生成，单时间点结果不称为趋势。
- 口径表达：日 UV 跨日相加、Cohort 初始人数、可计算样本、百分点和合并区间使用准确名称；源结果与派生加工分开，聚合结果不得冒充原始明细。
- 数据真实性：导出继承实际查询条件并记录分析对象、统计方式、版本、时区、数据截至时间和失败范围；不使用 Mock、硬编码或推测数据补齐。
- 未决事项：50,000 行同步导出上限仍待确认。
- 代码与部署：仅更新需求文档，未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-01 完成 GrowingIO 实机走查并收敛 BI 核心需求

- 模块：产品架构、公共/我的概览、事件分析、真实漏斗、已保存分析与看板组件、指标中心及数据能力映射。
- 走查结论：GrowingIO 使用“元数据 → 分析 → 已保存单图 → 看板组件 → 看板”的对象链；看板组件标题进入来源分析，只读卡片菜单主要承接刷新，查询编辑和来源删除属于分析资产页。
- 需求调整：事件分析补齐总次数、总人数、总天数、人均次数、人均天数、预置/事件属性、日期对比和左配置右结果；漏斗补齐步骤属性、全步骤固定条件、跨步骤属性一致、成熟样本和结果视图。
- 新增需求：建立“已保存分析与看板组件”和“指标中心”；明确公共概览/我的概览二级结构、收藏、整板刷新、移除组件不删除来源，以及指标中心只读引用权威指标体系。
- 产品取舍：不照搬 GrowingIO 的用户属性、用户分群、多主体、工作台和订阅；这些能力在 YPBI 缺少真实底座或明确场景，继续后置。
- 数据差异：当前 YPBI 没有通用事件聚合、属性筛选、用户事件序列、跨步属性一致、成熟水位和真实对象引用能力；固定汇总与 Mock 不得冒充正式能力。
- 代码与部署：仅更新需求文档，未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-01 确认看板时间与筛选目标规则

- 模块：产品全局规则、现有平台差异、数据能力映射和需求文档写法。
- 确认规则：日期范围与业务平台为固定公共筛选；其他条件仅在全部卡片共同支持时出现；新老用户和获客类型作为完成平台加工、接口接入和逐指标验数后开放的候选公共维度；事件属性只进入事件分析、真实漏斗或事件专属卡片；用户属性和用户分群本阶段不显示。
- 文档规则：每份看板或独立功能需求必须有精简的功能点清单，并记录必要细节、页面结构与阅读顺序；截图和独立 Demo 仅作为视觉参考，文字规则是实现依据。
- 能力差异：多数接口支持日期但热搜等存在例外；当前查询链路未应用通用 `filters`；新老用户和获客类型目前只是部分独立汇总值，不是公共筛选参数。
- 代码与部署：未修改前端、后端、接口契约或部署代码，未部署。

## 2026-09-01 修正 BI 产品分类并补全事件与漏斗需求

- 模块：产品需求治理、目标信息架构、现有平台迁移、事件分析、真实漏斗和数据能力映射。
- 需求/问题：“专项报表”被错误地与核心经营、用户增长、内容等看板分组并列；三个专用实现页面被按技术实现方式直接判定为报表；指标治理与事件/漏斗建设被错误串行；全局规则中的功能摘要不足以作为完整 PRD。
- 文档修改：将产品形态、业务归属、使用范围和技术实现拆为正交维度；报表改为独立产品对象；逐项重分内容、渠道和专项页面；指标治理与行为分析改为并行工作流；新增 `features/事件分析.md` 和 `features/漏斗分析.md` 完整需求；同步需求索引、数据能力映射和项目索引。
- 核心规则：专用代码实现不决定页面属于看板还是报表；指标体系完善后仍须完成真实数据映射与验数；事件与漏斗可在共享真实性底座上并行设计，漏斗依赖事件数据基础而非事件分析页面先上线；正式结果不得使用 Mock，缺失与未成熟不得补 `0`。
- 验证结果：完成需求 ID、版本、状态、文档链接、业务分类、产品形态、并行依赖、现有接口与数据缺口的一致性检查；未运行代码测试，因为本次未修改运行代码。
- 部署状态：仅更新产品需求文档，未修改前端、后端、接口契约或部署代码，未部署 VPS。

## 2026-08-31 形成 BI 目标架构与现有平台调整方案

- 模块：产品需求治理、看板分类、分析中心、指标映射、筛选、数据状态和导出。
- 需求/问题：现有看板按接口与实现拆分，业务主题被称为分析模型，正式看板与模板、已保存分析与看板组件、普通看板与专用页面职责混合；事件分析和漏斗分析缺少真实数据能力边界。
- 文档修改：将 `BI-产品全局规则.md` 扩展为产品架构与全局规则；新增 `BI-现有平台调整方案.md`；数据能力映射补充指标分析、事件分析、真实漏斗、路径、导出、核心筛选维度和缺失值现状；需求索引登记两项待确认需求。
- 核心规则：指标体系作为指标分类、ID、版本、适用维度和口径唯一权威源；BI 按业务判断组织看板并逐项核对真实 API；`pid` 表示 20 个业务平台，`platform` 表示 Android、iOS、Web；正式展示、下钻和导出不得使用 Mock 或硬编码数据。
- 范围边界：广告不进入本阶段；平台级权限和复杂发布版本后置；路径分析登记为事件明细能力后的规划项。
- 部署状态：仅更新产品需求文档，未修改前端、后端、接口契约或部署代码，未部署 VPS。

## 2026-08-28 19:10 全平台视觉与交互完整收口

- 模块：品牌顶栏、分析中心、分析创建器、模板管理、数据字典、完整预览、字段说明、响应式 Shell 与 UI 自动回归。
- 需求/问题：品牌字标呈现为分离图形和文字；分析分类无提示裁切；分析模型等页面级筛选仍调用浏览器原生菜单；字段说明遮罩未覆盖导航；模板和创建器层层套框，状态像描边按钮；手机端旧版 overflow 规则导致 sticky 失效。
- 根因：前序修改停留在颜色和局部组件，未按业务控件、Portal 宿主、任务表面和根滚动容器建立全局影响面；现代样式对经典版结构属性重置不完整。
- 代码修改：重做连续 `YPBI` 字标；新增单选/多选业务菜单并移除 `main.tsx` 全部原生 select；桌面分类改为完整换行、手机保留滑动；Portal 弹层统一现代层级；模板右侧合并为单一编辑画布，创建器移除嵌套边框和假按钮状态；四层根容器清理旧 overflow，恢复手机顶栏和模板页头 sticky。
- 自动防回归：新增分类边界、业务区原生选择器、菜单视口对齐、Portal 层级、手机 sticky 和移动分类滚动断言；UI-006、UI-010 更新全局规则，新增 UI-016 至 UI-018。
- 兼容范围：未修改指标、接口路径、查询算法、真实/Mock 原则、平台范围、日期口径、模板数据契约和权限。

## 2026-08-28 18:48 全平台筛选、品牌与现代样式隔离重构

- 模块：全局筛选、产品 Shell、品牌标识、全平台表面 Token、工作区状态与 UI 回归。
- 需求/问题：日期筛选把分析周期和对比方式混为同一组选项，下方周期面板出现横向切割感；桌面筛选使用脱离触发器的居中 Modal；品牌仍是通用图标；经典版结构样式泄漏到新版 Toast 和弹层。
- 根因：日期 JSX 缺少语义分区且快捷周期立即应用；筛选只做视觉换肤，没有重新选择符合看板查询场景的交互容器；`styles.css` 先加载后，新版没有完整重置 `left/position/overflow` 等结构属性。
- 代码修改：桌面平台/日期筛选改为触发器锚定面板，移动端保留全屏 Sheet；日期拆成分析周期和对比生成方式两区，统一确认后刷新；新增原创 YPBI 字标和侧栏激活指示；提升全平台表面 Token；重置现代 Toast 定位；空/非 JSON 工作区响应转换为中文状态，远端不可用时停止自动保存重试。
- 兼容范围：未修改指标、接口路径、查询算法、平台范围、三种日期对比算法、模板契约和权限；经典版回退入口保留。
- 验证结果：应用内浏览器逐页检查六个工作区；1280px 日期/平台锚定、日期两组选中态、统一应用和无位移通过；390px 日期 Sheet 宽 390px、无横向溢出；前后端 TypeScript 与 Vite 构建通过；Playwright 用例已增加日期结构与几何回归。
- 部署状态：未部署 VPS。

## 2026-08-28 18:33 建立产品需求文档体系骨架

- 模块：产品需求治理、需求索引与数据能力映射。
- 需求/问题：YPBI 缺少与开发实现分离的产品需求目录，无法按单张看板和独立功能维护确认状态、指标引用及真实数据能力缺口。
- 代码修改：无；新增 `docs/requirements/`、需求索引、BI 产品全局规则骨架、BI 数据能力映射骨架，以及空的 `dashboards/`、`features/` 目录；同步增加项目索引入口。
- 验证结果：需求登记字段、八种状态、指标权威来源、API 支持状态分类、广告阶段边界和代码修改边界已写入；未登记未经确认的看板、指标、接口或业务规则。
- 部署状态：仅更新产品需求文档，未修改代码，未部署 VPS。

## 2026-08-28 17:27 建立 UI 已确认问题长期记录

- 模块：项目文档治理、UI 设计与交互回归。
- 需求/问题：已修复 UI 问题只存在于对话和时间线中，后续对话或迭代可能遗漏，导致用户重复指出同类问题。
- 代码修改：新增 `docs/UI-已确认问题与防回归规则.md`，以稳定 ID 记录 12 项已确认问题、根因、强制实现规则、验收方法和唯一实现入口；`AGENTS.md` 将该文件设为所有 UI 任务修改前必读、完成前必更新；索引同步增加阅读顺序和入口。
- 验证结果：已确认按钮反馈、覆盖层位移、Shell 跳动、原生选择器、重复错误、响应式溢出、缩略图溢出、页面表面、手机 Sheet 和主按钮层级均已登记，并与现有代码及 `qa-ui-shell.spec.js` 对应。
- 部署状态：仅更新项目规则与文档，未部署 VPS。

## 2026-08-28 17:24 全平台可点击反馈与覆盖层无位移修复

- 模块：设计 Token、全平台按钮与筛选、看板表面、模板抽屉、字段说明、完整预览和下钻。
- 需求/问题：部分可点击控件没有统一光标和悬停颜色；主操作与次级操作层级不稳定；页面背景和卡片表面仍偏线框；打开筛选时滚动条消失，主画布瞬间变宽 15px。
- 根因：交互状态分散在页面级样式，通用 `button` 没有统一语义基线；`.blue-action` 被更高优先级的操作区规则覆盖；各覆盖层分别直接修改 `body.overflow`，没有滚动条宽度补偿和嵌套锁计数。
- 代码修改：新增共享 `useBodyScrollLock`，同时锁定根节点和页面滚动、补偿实际释放宽度并支持嵌套；筛选、完整预览、字段说明、下钻、移动导航、卡片库和卡片编辑统一接入；新增全局可点击/禁用光标规则，统一主次按钮悬停状态；补充浅亮页面背景、静态表面阴影、轻量滚动条和缩略图徽标防溢出。
- 兼容范围：指标、接口、查询算法、多平台与双周期口径、模板配置、持久化和权限契约均未修改；经典版回退入口继续保留。
- 验证结果：应用内浏览器实测 1280/1024/390px 无横向溢出；桌面筛选、完整预览、下钻、字段说明、模板卡片库和卡片编辑抽屉打开前后主内容坐标与宽度一致；可见可用按钮点击光标漏项为 0；前后端 TypeScript 检查和 Vite 生产构建通过。
- 部署状态：未部署 VPS。

## 2026-08-28 14:29 核心看板阅读页首轮试点优化

- 模块：普通看板阅读页、模板切换、全局筛选、图表卡头、数据状态和下钻抽屉。
- 需求/问题：原生模板下拉与系统样式割裂；页面标题位于筛选工具区之后；卡片标题、平台、状态和多个操作挤在同一行；首次查询失败重复展示错误条和错误态；隐藏抽屉及 1280px 筛选区造成横向溢出。
- 根因：模板切换没有进入基础组件层；看板工具区装配在阅读页外；卡片状态由警告条和 `DataState` 两条路径同时渲染；关闭抽屉只做位移没有隔离布局边界；筛选最小宽度未覆盖 1280px 展开侧栏场景。
- 代码修改：新增分组 `MenuSelect`；普通看板改为标题先于工具区；卡片头拆分标题/元信息与分段视图/Icon 操作；首次失败、不支持和无数据统一由单一状态组件承接，保留旧结果时才显示刷新警告；空响应转换为稳定中文错误；关闭下钻抽屉增加 `visibility` 隔离；扩大紧凑筛选断点并在 1024px 使用单列工具区。
- 兼容范围：未修改指标、接口路径、查询算法、多平台规则、双周期口径、模板契约或权限；经典版回退入口继续保留。
- 验证结果：101 项 Bun 测试、前后端 TypeScript 检查和 Vite 生产构建通过；应用内浏览器实际验证 1280/1024/390px 无横向溢出，模板与工作区切换、移动筛选、导航开合和焦点返回正常。系统无头 Chrome 在启动阶段 `SIGABRT`，Playwright 未进入断言；回归用例已补充自定义模板选择与重复错误检查。
- 部署状态：未部署 VPS；本地未运行 BI API 时显示单一中文错误状态，不使用 Mock 数据。

## 2026-08-28 04:10 UI v1.5 稳定骨架与核心交互收敛

- 模块：全平台导航、看板筛选、卡片库、分析创建器、模板编辑器、表格和响应式工作区。
- 根因：侧栏入口使用普通 `<a>` 导致 React 应用整页重载；全局筛选在看板页额外占据一条 Shell 高度，造成不同工作区的侧栏顶部和内容起点变化；模板编辑器移除侧栏后又形成第二套页面骨架。
- 代码修改：工作区入口改为保留新标签能力的应用内导航并支持浏览器前进/后退；新增可持久化的 220/56px 侧栏收起；所有工作区统一使用同一顶栏、侧栏和内容栅格；筛选与模板选择合并为看板页内工具区；模板管理保留产品侧栏。
- 组件收敛：卡片库统一编辑/复制/删除按钮语义；分析创建器收敛为横向模型选择、条件区和右侧实时预览；漏斗步骤使用线性 Icon；表格拆分数据滚动区与固定分页，统一数字对齐和首列冻结；卡片、异常状态和编辑器间距回到唯一 Token 体系。
- 竞品取舍：吸收 Quick BI 的稳定 Shell、资源工具栏、卡片网格和画布/配置分工，以及神策的条件配置、查询反馈、结果卡片和保存分析流程；不引入 Quick BI 的装饰背景和重型配置，也不复制神策的双层导航、旧式高密度表单与样式割裂。
- 兼容范围：未修改指标、数据、接口、查询算法、模板契约或权限；继续保留 `?ui=classic` 项目级回退。
- 验证：Vite 生产构建通过；新增 Playwright 工作区回归，验证 SPA 切换不重载、侧栏和内容坐标稳定、筛选弹层 Esc、侧栏状态跨工作区保留，以及 1600/1024/390px 无横向溢出。
- 部署状态：未部署 VPS；本地未连接 BI API 时按既有规则展示真实加载/错误状态，不使用 Mock 冒充正式数据。

## 2026-08-28 02:45 UI v1.3 全平台统一改造

- 模块：全部前端工作区、全局筛选、响应式布局与覆盖层。
- 需求/问题：核心经营看板试点验证后，旧筛选与其他工作区仍受 `main.tsx`、旧样式层和桌面最小宽度影响，视觉、交互和移动端结构不一致；需要以唯一设计系统覆盖整个平台。
- 代码修改：v1.3 扩展到全部普通/专用看板、分析中心、分析创建器、模板管理、数据字典和数据源维护；重构全局筛选工具条与平台/日期弹层；新增 `features/filters`、`features/workspaces`、`features/overlays` 分层；统一表单、卡片、表格、状态、编辑器和覆盖层；移除源码中的试点命名并改为 `PlatformTopbar/PlatformSidebar` 全平台骨架。
- 响应式与稳定性：修复分析卡片缩略图数值溢出、分析创建器双重滚动、移动端漏斗 530px 最小宽度和模板编辑器强制双栏；保留 `VITE_UI_VERSION=classic`、`?ui=classic` 项目级回退，不改变指标、接口、计算、模板或权限契约。
- 验证结果：100 项 Bun 测试、前后端 TypeScript 检查通过；浏览器逐页验证 1440、1280、1024、390 四档视口无横向溢出，桌面筛选弹层、移动筛选 Sheet、导航 Drawer、Esc、焦点返回和背景锁定通过；经典版回退通过。
- 部署状态：未部署 VPS；本地未连接 BI API 时继续展示真实错误/空态，不使用 Mock 冒充正式数据。

## 2026-08-28 01:31 UI v1.3 核心经营看板试点

- 模块：看板与前端工作区、图表展示、响应式导航与筛选。
- 需求/问题：UI 改造作为第一阶段，但旧前端将页面、组件和样式集中在 `main.tsx`、`styles.css`，继续叠加会扩大样式覆盖和回归风险；先建立明确结构，再改核心经营看板。
- 代码修改：新增 `app/theme/components/ui/components/layout/features/dashboard` 分层；引入统一设计 Token 与 `lucide-react`；核心经营看板使用新顶栏、侧栏、页面头、阅读重点、卡片网格和可访问图表主题；移动端新增覆盖导航与单入口全屏筛选；补齐 `mobileOrder/mobileVisibility` 展示契约。
- 兼容与回滚：试点复用原指标、接口、查询、模板和权限契约；默认只作用于 `overview` 阅读页，项目级 `VITE_UI_VERSION=classic` 或 QA 参数 `?ui=classic` 可回到旧入口，不改变数据和配置。
- 验证结果：100 项 Bun 测试通过，前后端 TypeScript 检查通过，Vite 生产构建通过；浏览器实测 `1440×900`、`1280×800`、`1024×768`、`390×844` 无横向溢出，移动端焦点循环、Esc 关闭、焦点返回和背景锁定通过，经典版回退入口通过。
- 部署状态：未部署 VPS；本地未连接 BI API 时按既有规则显示真实查询失败，不使用 Mock 冒充数据。

## 2026-08-17 GitHub 仓库初始化

- 关联 GitHub 仓库 `FD-design/YPBI`，准备建立项目首次版本基线。
- 完善 `.gitignore`：排除环境密钥、运行时工作区、日志、部署压缩包、浏览器 QA 截图和测试输出。
- 保留源码、测试、接口契约、部署脚本和项目文档，避免后续发布覆盖 VPS 的 Token 与用户模板。
- 将 Playwright 小屏回归脚本归入独立 `test:e2e` 命令，避免被 Bun 单元测试运行器误收集。
- 升级兼容范围内的依赖并锁定四个漏洞传递依赖的安全版本，`bun audit` 从 9 个漏洞恢复为 0。
- 验证：暂存文件密钥扫描、依赖审计通过，96 项测试、类型检查和生产构建通过；部署状态：不涉及 VPS 发布。

## 2026-08-03 三种周期对比

- 顶部日期筛选新增自然月、等长周期、同期进度模式，并自动生成对比周期。
- 自然月允许 7 月 31 天与 6 月 30 天直接比较日均指标；累计指标明确保留实际周期累计值。
- 新增自然月、31 天等长周期和月中同期进度回归测试。

## 2026-08-03 日均与累计口径修复

- 修复真实 API 多日期查询将 DAU、观影人数、付费人数、端别日活和广告点击人数直接累加的问题，统一改为按自然日日均。
- 新增 8 个累计人天指标，并在经营总览补充“周期累计规模”默认卡片。
- 修正观影率、付费率、ARPU、ARPPU 的中文解释，明确当前按累计人天加权，而非周期用户去重。
- 新增日均、多平台同日汇总和累计人天回归测试。

## 2026-07-27 卡片编辑返回

- 修复从卡片库进入卡片编辑器后缺少返回入口的问题。
- 新增未保存修改退出确认，避免误丢当前配置。

## 2026-07-27 响应式主导航修复

- 修复小屏和中屏主导航文字拥挤：统一 `<a>` 导航响应式规则，中屏紧凑显示标题，手机端横向滚动。
- 进一步取消宽屏侧栏说明文字，覆盖高分辨率屏幕配合系统缩放后视觉区域过窄的场景。

## 2026-07-27 24小时实时走势

- 接入 `pRealDayLine` 的 288 个五分钟时点，新增登录、新增、观影用户三张双日期对比曲线。
- 24小时曲线最多对比 4 个平台，系统模板自动补入缺失卡片且不覆盖用户自建模板。
- 修正实时接口 UTC `sumDate` 转北京时间自然日，并修复部署排除规则误伤 `src/data/` 的问题。

## 2026-07-23 卡片指标上限修复

- 将卡片配置和分析查询协议的指标上限统一从 6 项提高到 16 项。
- 修复综合指标卡选择 10 项后实时预览请求被拒绝。
- 增加 16 项通过、17 项拒绝的协议回归测试。

## 2026-07-23 维护密码轮换

- 轮换数据源维护密码，仅在 VPS 保存 SHA-256 哈希。
- 验证登录、会话鉴权及错误密码拒绝均正常。

## 2026-07-23 系统卡片查询修复

- 修复“付费规模与效率”提示“查询配置不受支持”。
- 修复“内容消费汇总漏斗”因携带普通指标而被后端拒绝。
- 增加全部系统模板卡片的后端能力兼容性测试。

## 2026-07-23 模板持久化修复

- 定位 VPS 未配置 PostgreSQL，旧后端使用内存工作区，API 重启后模板清空。
- 新增磁盘工作区 Store，默认保存到 `data/workspace.json`，覆盖前保留 `.backup`。
- 前端新增模板与卡片本地备份，并按更新时间合并远端和本地配置。
- 新增 7 项持久化与合并测试；服务端 55 项、前端 32 项测试全部通过。

## 2026-07-23 获客转化与付费人数

- 将 `totalChargeUserCount` 作为“付费人数”开放到卡片工厂和经营模板。
- 新增获客转化模型，服务端关联 CNZZ 访问下载与 pDaySum 新增注册。
- 新增独立系统模板及 4 张真实数据卡片，旧工作区通过模板归一化自动补入。
- 修复无当前期记录被显示为 `-100%` 的问题，增加超过 100% 的口径不可比提示。
- 比例展示按数值大小动态保留 1 至 3 位小数。
- 修复 pDaySum 与 CNZZ 带时区日期直接截取导致的日期标签提前一天。
- 新增 Adapter 与能力校验测试；TypeScript、构建和 80 项业务测试通过。

## 2026-07-22

### 更新双数据源 Token 并恢复真实数据

- 仅更新 VPS 环境中的站1和站2 Token，不修改接口、业务代码和平台路由。
- 就绪状态由 `503 degraded` 恢复为 `200 ready`。
- 验证主数据源 `PH`、备用数据源 `TJD` 和20平台经营查询均成功；20平台经营返回0失败、0告警。
- 内容排行查询成功且无平台请求失败，当前7个平台存在排行记录，其余平台为上游暂无排行数据。

### 完善多平台数据可读性与卡片配置

- 模块：真实查询、系统模板、卡片创建/编辑、统计表和 ECharts Tooltip。
- 变化与下钻：卡片新增独立“查看明细”入口；“变化”仅保留周期分析，避免把分析结论误称为下钻。
- 数据状态：明确区分接口真实 `0`、平台暂无记录和能力不支持；标题区显示平台覆盖数量，占位 `0` 不再表述为真实业务值。
- 表格与模板：真实查询表头统一中文化；系统模板自动同步内置卡片新配置并保留用户宽度和平台范围；内容模板暂时移除长期为 0 的收入默认卡片。
- 配置体验：三个卡片配置入口统一增加指标分组、中文/字段 ID 搜索和已选数量。
- 性能与图表：完全相同请求共享 45 秒前端缓存；20 平台 Tooltip 按值排序、双列显示并脱离卡片裁切区域。
- 验证：74 项测试、TypeScript 类型检查和生产构建通过。
- 部署：已同步至 VPS，API、Web、Caddy 和 Nginx 均为 `active`，HTTPS、HTTP 与 `/api/bi/health` 返回 200。

### 修复全平台悬浮详情裁切和视频内容空数据

- ECharts Tooltip 统一挂载到页面 `body`，脱离卡片 `overflow:hidden`；增加视口内最大宽高和滚动，20 平台详情不再被卡片边界截断。
- 确认 `todayVideos/getMany` 当前只返回旧平台 PID，导致现有 20 平台过滤后为空；视频内容表现改用可返回 PH、TT、YK 等平台的 `videoWatchRanking`。
- 内容表现模型收紧为播放次数、点赞数、收藏数和收入；观看人数及人均时长因排行接口没有真实字段，不再在该模型展示 0。
- 旧内容卡片读取时通过能力归一化自动移除不受支持指标；人均观影时长仍在观看深度模型使用实时接口。
- 视频统计表只返回已选维度和指标，不再把 Adapter 内部兼容字段作为零值列透出。
- 74 项测试、TypeScript 类型检查、生产构建和线上真实内容查询通过。

### 补齐运营日报端别、来源、广告和新增付费口径

- 只读核对生产 `pDaySum/getAllByRole/pRealDayLine` 字段，确认遗漏主要来自 BI Adapter 未映射而非接口缺失。
- 新增 Android/IOS 活跃与新增、自然/内部导量新增、广告点击人数与人次及比例、新增付费金额/人数/付费率/ARPU/ARPPU、来源充值等 22 个可配置指标。
- 所有比例和客单价均先汇总原始分子分母再计算，不平均每日百分比。
- 经营总览增加终端结构、增长来源、广告触达和新增付费质量四张真实数据卡片；旧系统模板自动补卡且不覆盖用户模板。
- 修复收入 Adapter 字段名与指标注册表不一致的问题，将 `diamondChargeAmt` 统一归一化为 `revenue`。
- MAU、访问下载注册链路、支付宝/微信拉单拆分因缺少已确认历史日汇总字段继续标记待接入，不使用 Mock。
- 74 项测试、TypeScript 类型检查和生产构建通过。

### 优化周期涨跌视觉样式

- 实心圆箭头改为轻量 `↗/↘/→`，使用浅绿、浅红、浅灰状态底和细边框。
- 百分比保持主信息，对比期数值降级为灰色辅助文字，减少 KPI 和图表摘要的视觉拥挤。

### 修复周期涨跌箭头不可见

- 修复箭头圆形背景继承白色文字颜色，导致 `↑/↓/→` 显示成白圈的问题。
- 上涨、下降和持平分别使用绿色、红色和灰色实心圆背景，箭头保持白色并覆盖 KPI 与图表摘要。

### 修正字段说明的业务口径和指标级接口

- 修复技术公式被错误显示为“中文解释”的问题；中文解释改为独立维护的业务定义，不再回退到公式字符串。
- 指标接口从模型级接口集合改为指标级真实来源，避免付费率错误展示 `pRealDayLine/getAllByRole` 等无关接口。
- 26 个指标分别维护中文定义、业务公式、聚合算法、真实来源接口和口径限制；缺少接口的指标明确标记“暂无已确认接口”。
- 付费率明确为汇总 `totalChargeUserCount ÷ loginUserCount × 100%`，仅使用 `/api/admin/statistics/pDaySum`。
- 72 项测试、TypeScript 类型检查和生产构建通过。

### 重做问号字段说明交互

- 字段旁的 `?` 不再依赖浏览器原生 `title`；点击后打开统一中文说明弹窗。
- 弹窗展示中文解释、计算算法、具体公式、真实后台接口、数据粒度和能力边界。
- KPI、图表、明细表头、卡片编辑、模板编辑和分析创建中的指标/维度统一使用该说明交互。
- 配置项拆分为“选择字段”和独立 `?` 按钮，点击说明不会误选或取消字段。
- 72 项测试、TypeScript 类型检查和生产构建通过。

### 新增站1/站2 Token 安全维护页

- 一级导航新增“数据源维护”，提供站1和站2两个独立 Token 输入栏、当前连接测试、验证并保存和脱敏状态。
- 新增独立维护密码保护、同 IP 5 次失败锁定 15 分钟；维护密码不写入前端代码，Token 不写入浏览器存储且永不由接口回显。
- Token 保存前调用对应后台真实接口验证；失败保留旧值，成功后以 `0600` 私有文件原子持久化、清理缓存并立即生效，无需重启。
- 地址、用户名和 PID 路由仍由服务器环境变量维护，前端只允许更新 Token，缩小误操作范围。
- 新增运行时 Token 切换和脱敏状态回归测试；72 项测试及 TypeScript 类型检查通过。
- 新增 `https://187.77.129.207.nip.io` 管理入口，由 Caddy 使用 TLS-ALPN 自动签发和续签证书；HTTP 地址的维护接口强制返回 `426`，避免明文传输密码和 Token。

### 完整预览补齐口径、接口、算法与周期涨跌

- 所有卡片类型的标题栏固定展示实际平台范围和“完整预览”入口，不再只覆盖趋势类图表。
- 完整预览新增指标中文解释、维度解释、真实接口路径、聚合算法、公式、数据粒度和能力边界。
- KPI 与普通图表摘要统一展示当前周期相对对比周期的上涨、下降或持平箭头、变化百分比和对比期数值。
- 平台范围按钮可直接进入完整预览切换总体、单平台或 2–8 平台，并可应用到卡片后通过保存布局持久化。
- 71 项测试、TypeScript 类型检查和生产构建通过。

### 卡片独立平台范围与完整预览

- 卡片新增“跟随看板、全部平台、单平台、多平台对比”四种范围配置；独立范围随模板保存，最多对比 8 个平台。
- 卡片标题明确显示“跟随看板”或独立平台集合，避免顶部筛选与卡片实际查询范围混淆。
- 卡片新增“完整预览”，以全屏图表和明细查看数据，并可在预览内临时切换总体、单平台或多平台后重新查询真实接口。
- 导出使用每张卡片的有效平台范围；旧模板默认归一化为“跟随看板”，保持兼容。
- 新增卡片平台范围归一化回归测试；71 项测试、TypeScript 类型检查和生产构建通过。

### 修复平台范围错位、搜索聚合和字段解释

- 分类、渠道、专项三个专用系统模板不再固定读取 `filters.platforms[0]`；全部、单平台和 2–8 平台对比均使用顶部完整平台集合分别查询并聚合。
- 专用模板标题范围、导出文件和明细平台列同步当前选择，修复顶部“全部平台”但页面显示“按台姬店查询”的错位。
- 搜索模型选择平台维度时改用 `platform` 聚合，修复搜索总量有值但 20 个平台柱子全部为零。
- 支付等汇总事件步骤倒挂时停止计算转化率，表格显示“—”并说明独立事件计数不能证明用户级顺序。
- 指标卡、统计表头、卡片编辑和分析创建字段增加 `?` 口径提示，支持悬停与键盘聚焦查看来源、公式和解释。
- 新增平台搜索聚合和倒挂漏斗回归测试；70 项测试、TypeScript 类型检查和生产构建通过。

### 恢复双周期比较和完整留存档位

- 时间筛选新增“当前周期 + 对比周期”；快捷范围自动生成等长前置周期，自定义范围可分别按天选择，两段周期必须天数一致。
- 通用真实数据卡片并行查询两个周期；指标卡展示对比值和变化率，趋势图追加对比期系列，分析摘要显示两段明确日期。
- 生产接口原始返回确认包含 `afterFirstData1/3/7/30`；留存 Adapter、指标注册表、能力白名单和默认模板恢复 D1、D3、D7、D30。
- 各档留存按已达到观察期的 cohort 加权计算；未成熟的 D30 等字段保持空值并显示“—”，不按零处理。
- 留存注册日期从上游 UTC 时间转换为北京时间自然日，修复 Cohort 日期早一天的问题。
- Cohort 图横轴恢复 D1/D3/D7/D30，明细表同步展示四档留存；旧的系统留存模板自动迁移，不影响用户自建模板。
- 新增对比周期和多档留存回归测试；68 项测试、TypeScript 类型检查和生产构建通过。

### 按任务重构 BI 工作区与模板编辑模式

- 一级导航从五个入口收敛为“看板中心、分析中心、模板管理、数据字典”；卡片库与分析工作台合并为一个分析任务入口。
- 看板中心继续负责真实数据筛选、阅读和下钻；平台与日期筛选仅在看板状态出现，不再干扰卡片创建和模板编辑。
- 模板管理改为独立编辑模式，隐藏一级导航和业务筛选，画布使用完整页面宽度；顶部提供返回、预览、取消、保存、保存并发布和添加卡片。
- 模板属性区移除重复操作按钮；删除模板回归模板列表区域，卡片级工具仅在悬停、聚焦或选中时显示。
- 卡片库继续通过右侧按需抽屉打开，支持拖到指定插入位置；卡片排序、宽度调整和工作区持久化协议不变。
- 66 项测试、TypeScript 类型检查和生产构建通过。

### 重构模板编辑器的卡片添加交互

- 移除模板编辑器右侧常驻卡片栏，主编辑区恢复为“模板列表 + 全宽画布”，避免窄栏、双滚动和大块无效空白。
- 在页面标题和模板画布工具栏增加“添加卡片”，卡片库按需以右侧抽屉打开并可随时关闭。
- 抽屉内部独立滚动，保留图表类型筛选、结构预览、新建卡片、添加到顶部及拖到画布指定插入位置。
- 模板卡片原有拖动排序、宽度调整、编辑、移除、取消、保存和发布逻辑保持不变。
- 66 项测试、TypeScript 类型检查和生产构建通过。

### 修复全平台图表、顶部文字和变化结论

- 全部平台模式固定使用正式 20 平台集合；接口未返回记录的平台按 `0` 补齐并明确标记无数据，不生成 Mock 数值。
- 平台排行、趋势、散点、日期热力、Cohort、瀑布和贡献矩形图全部取消 Top N 数据截断；趋势使用滚动图例，排行超过 10 行时动态增加图表高度。
- Cohort 恢复为“平台 × 注册日期”矩阵，平台趋势按所选全部平台生成独立系列。
- 顶部顶栏和筛选控件改为自适应高度与明确行高，修复标题、平台和日期文字被裁切。
- 上游 `diamondChargeAmt` 等技术告警不再进入“关键变化/主要原因”，业务结论改为依据真实数据计算的最高平台、占比、区间变化和无数据平台数。
- 回归覆盖 20 平台排行、20 系列趋势、20 点散点、20 行热力/Cohort、20 项瀑布和贡献矩形；66 项测试、类型检查和生产构建通过，浏览器无横向溢出和控制台错误。

### 增加数据读取进度反馈

- 通用看板卡片新增顶部蓝色读取进度条，查询期间平滑推进至 92%，接口完成后补满并淡出；卡片状态同步显示百分比。
- 保留上次成功图表进行后台更新时同样显示进度，避免用户误判页面无响应。
- 渠道、分类、专项等页面的通用加载状态增加流动进度轨道，并兼容系统减少动态效果设置。
- 延迟接口回归确认桌面和 390px 移动端均显示 6 个卡片进度条，无控制台错误和横向溢出；64 项测试、类型检查与生产构建通过。

### 补齐真实 API 可计算图表

- 恢复 12 种系统图表类型的完整可配置入口；按模型能力开放热力图、瀑布图、贡献矩形图和汇总桑基图，不再由已有卡片数量决定图表是否可选。
- 真实结果转换新增平台 × 日期热力矩阵、维度贡献瀑布、平台/分类/内容贡献矩形，以及固定漏斗阶段汇总桑基。
- 桑基图明确标记为“汇总近似，非用户级路径”；页面 × 坑位热力图继续因接口缺少字段而关闭。
- 新图表作为卡片库预置资产提供，不强行加入默认看板，避免首屏增加重复的 20 平台查询；旧 localStorage 卡片会与新增系统资产合并。
- 服务端新增模型级指标、维度和漏斗事件白名单，查询与卡片校验协议要求显式 `modelId`，阻止模型字段错配产生假阳性结果。
- 编辑抽屉和模板配置统一使用真实 API 能力字段；查询请求保留 `category/content/keyword` 等真实维度；空服务端工作区不再覆盖浏览器迁移卡片。
- 热力图、瀑布图和贡献矩形图按卡片指标动态格式化，不再写死 CTR 或收入单位。
- 新增 10 项能力、查询协议与转换测试；全量 64 项测试、TypeScript 类型检查和生产构建通过。
- 生产发布：代码和构建产物已部署至 `/opt/config-driven-bi-demo`，保留 VPS `.env.local` 与双后台网络配置；API、Web、Nginx 均为 `active`。
- 线上验收：公网加载新资源 `index-C5xuTBs3.js`；`health` 为 `ok`、深度 `ready` 为 `ready`；模型字段错配在线返回 422；卡片库可见热力图、瀑布图、桑基图和贡献矩形图，浏览器无控制台错误和横向溢出。

### 重构模板预览与全局筛选

- 模板编辑器改为本地草稿：编辑、拖动、改名和移除不会立即写入工作区；保存或发布时才提交，取消修改恢复最近保存版本。
- 模板预览改为编辑器内独立状态，不再跳转看板中心；新增返回编辑、取消修改、保存模板和保存并发布操作。
- 平台筛选改为弹层内分模式选择并统一应用；支持搜索平台/PID、单平台和 2–8 平台对比，取消不触发查询。
- 时间筛选增加自定义起止日期，按自然日包含首尾两天；通用卡片、下钻和专用分析页统一使用同一日期转换。
- 55 项测试、类型检查、生产构建和依赖审计通过；桌面和 390px 移动端无横向溢出。
- 线上验证：平台弹层完整展示 20 个平台；取消平台选择不触发查询；自定义日期真实进入 API 请求体；模板草稿预览和取消修改未产生工作区 PUT。

### 完成 BI 全方位生产审计与收口

- 前端：经营总览、分析工作台、卡片库、模板编辑器和数据字典完成桌面/移动端回归；卡片库按类型折叠，缩短超长页面，并明确卡片与模板缩略图为示例数据结构预览。
- 图表：趋势日期改为正序；平台排行按指标降序，半宽卡片限制前 10；真实气泡矩阵保留全部平台，半宽常显 DAU 前 3、较宽卡片常显前 5，气泡按相对规模缩放，低值百分比轴保留有效精度。
- 口径：没有对比周期时 KPI 和下钻只标记当前筛选区间，不再显示伪“较上期”；诊断卡只使用真实数值字段判断平台高低，忽略缺失值。
- 下钻：普通图表节点按当前值和图表节点均值生成比较结论，不再把上游技术警告作为自动分析结论。
- 接口：逐项复验 30 个目录接口；A/B 落地页补齐真实业务参数，留存无当日数据归一化为空结果；有效参数下全部可调用。
- 稳定性：新增 `/api/bi/ready` 深度就绪检查；通用数据源入口限制分页和参数长度；上游业务参数错误统一映射为 422。
- 部署：Nginx 将 `/api/` 直接代理 Fastify，增加 10 req/s 限流、`no-store`、CSP、nosniff、SAMEORIGIN 和 Referrer Policy。
- 依赖：ECharts 升级到 6.1.0；`bun audit` 无漏洞。
- 验证：54 项测试、TypeScript 类型检查和生产构建通过；线上浏览器无控制台错误和失败请求。
- 待办：工作区写入认证、HTTPS、前端大文件拆分和正式 Playwright E2E 套件仍是上线前风险。

## 2026-07-21

### 双后台生产验收完成

- VPS `/etc/hosts` 仅针对两个上游域名固定 IPv4，解决 Bun `fetch` 仍走未授权 IPv6 的问题；保留修改前备份。
- BI API 回归确认 `PH` 使用主后台、`FBI/TJD` 使用第二后台，三者 `pDaySum` 均返回真实数据。
- `category.first`、`channel.byType`、`overview.byRole` 均通过生产调用；后两者分别返回 20 行和 2 行，原角色总览 HTTP 500 已消失。
- 接口目录状态更新为 30 个接口均可调用；分类接口在特定 `pid + type` 无匹配数据时保持真实空数组。

### 接入双后台凭证与平台路由

- 从两份浏览器 HAR 确认主后台账号为 `zhudan`，第二后台账号为 `danzhu`；修正此前误用 `zhudata` 导致的认证失败。
- 经 VPS 强制 IPv4 调用 `pDaySum`，完成 20 个平台在两个后台的覆盖矩阵，确认 11 个 PID 由第二后台提供数据。
- `UpstreamClient` 根据请求中的 `pid` 自动选择后台地址、Token 和匹配用户名；第二后台配置必须成组提供，凭证不进入代码和文档。
- 新增主/第二后台路由测试；全量 45 项测试、TypeScript 类型检查和生产构建通过。
- 发现 VPS 默认网络调用可能走 IPv6 `2a02:4780:5e:fe72::1` 并触发 IP 限制，生产调用继续优先 IPv4。

### 复查三个异常接口

- 反向核对：扫描线上后台发布的 Vue 分包，确认三个接口均为 GET，并还原页面实际参数。
- 分类：`getManyFirst` 必须传业务 `type`，不能作为无类型的通用一级分类接口；通用维度继续使用 `category.tree`。
- 渠道：`channelStatByType` 补齐原后台实际发送的 `pid`、`channel` 参数，以及 `CPC/CPA/CPT/CBD` 合作类型。
- 总览：`getAllByRole` 的真实参数与现有登记一致，在有效 Token 下仍返回上游 HTTP 500。
- 连接：识别 HTTP 200 内的业务码 `2002`，区分 Token 失效和 IP 白名单限制；API 进程优先 IPv4，避免命中未授权 IPv6。
- 当前阻断：VPS 保存的 Token 已失效；更新 Token 后复验分类和渠道接口。

## 2026-07-16

### 修复分析条件无法选择

- 模块：分析工作台、卡片实时预览。
- 问题：“查询筛选”被禁用；非漏斗模型的指标和维度实际修改统计表卡片，但右侧展示主卡片，造成点击后看不到变化。
- 修改：查询筛选改为可点选字段；图表、指标和维度统一作用于当前预览主卡片；选中项增加勾选、高亮和无障碍状态。
- 约束：指标和拆分维度至少保留一项；所有可选项仍由当前真实 API 能力表限定。

### 增加页面访问统计

- 模块：VPS Web 部署、Nginx、访问日志。
- 修改：公网 `5178` 改由 Nginx 接入，Vite preview 改为仅监听 `127.0.0.1:5179`；地址和 API 同源行为不变。
- 统计：记录真实访问 IP、时间、请求、状态码、来源页和浏览器；提供脚本汇总页面打开次数、去重 IP、今日数据和最后访问时间。
- 运维：访问日志每日轮转并保留 30 天；不占用或修改 VPS 上已有 Node 服务使用的 `80` 端口。
- 边界：原 Vite 服务未记录真实客户端 IP，历史访问人数无法补回，统计从本次上线后开始。

### 内容、渠道和专项分析收进系统模板

- 模块：工作区导航、看板中心、内容/渠道/专项分析页面。
- 问题：三个固定分析页作为一级导航存在，与“卡片组装模板”的产品主路径并行，容易被误认为另一套模板系统。
- 修改：左侧移除内容分析、渠道分析和专项分析；在看板中心新增“分类内容分析、渠道经营分析、专项运营分析”三个系统模板入口。
- 分组：模板选择器明确区分“系统模板”和“我的模板”；当前共 8 个系统模板，自建模板继续来自模板编辑器。
- 数据：三个专用模板保留原 `/api/bi/content/*`、`/api/bi/channels/*`、`/api/bi/special/*` 真实接口和原有交互，不更改上游接口。
- 验证：系统模板三项均可切换渲染；左侧只保留看板中心、分析工作台、卡片库、模板编辑器和数据字典；横向溢出为 0，控制台错误为 0。

### 分析工作台移除双侧栏

- 模块：分析工作台、分析模型选择器、响应式布局。
- 问题：主导航与“API 分析模型”纵向侧栏并排，形成重复层级；状态标签拥挤，配置和图表区域被压缩。
- 修改：移除第二条纵向侧栏；9 个分析模型改为内容顶部横向选择条；状态收敛为圆点和短标签；当前模型说明只显示一次。
- 布局：桌面使用“176px 主导航 + 自适应工作区”，1280px 以下主导航收窄为 72px，移动端模型条横向滚动。
- 验证：TypeScript 和生产构建通过；1440×1000 回归中工作台宽度为 1234px、横向溢出为 0、控制台错误为 0。

### 看板与模板卡片支持拖动调整宽度

- 模块：看板中心、模板编辑器、工作区持久化。
- 需求：查看模板和配置模板时，可以按住卡片左右拖动放大缩小，并保存调整后的布局。
- 修改：新增卡片右侧宽度手柄；按 12 列网格吸附为半宽、2/3 宽、整行；看板中心使用待保存布局，模板编辑器同步预览；两处均增加“保存布局”。
- 交互：窗口级 Pointer Event 保证鼠标移出卡片后仍可继续调整；支持左右方向键；移动端禁用手柄并保持整行卡片。
- 持久化：尺寸继续写入 `DashboardCardConfig.size`，复用现有 `/api/bi/workspace` 保存链路，不增加数据库结构或新接口。
- 验证：40 项测试、TypeScript 和生产构建通过；自动化验证拖动缩小、保存、刷新恢复、模板编辑器调整；横向溢出为 0，控制台错误为 0。

### 统一筛选、查询可观测性与看板视觉收敛

- 模块：全局筛选、看板中心、内容/渠道/专项分析、卡片库、分析工作台。
- 问题：页面重复筛选和重复元信息过多；查询慢或失败时缺少全局反馈；看板二级侧栏长期占用图表空间。
- 修改：新增全局平台/日期筛选和保存范围；独立接口继承主平台；顶部查询健康状态；失败保留上次成功结果和卡片重试；卡片复制与 20 步撤销；五类看板阅读重点。
- 视觉：看板中心改为单导航栏和全宽内容区；模板切换收敛为一行选择器；卡片数据范围收进状态提示；移动 KPI 改为 2×2。
- 验证：40 项测试通过；TypeScript 与生产构建通过；完成 1440×1000 和 390×844 截图回归，无首屏横向溢出。
- 边界：3 个后台 500 接口、指标口径复核、登录/HTTPS 等上线能力按用户要求暂缓。

### 建立项目文档治理

- 模块：全项目。
- 需求：功能、模块细节和修改历史必须落盘；后续修改前先读索引。
- 修改：新增项目 `AGENTS.md`、总索引、前端、分析、后端、数据平台、部署文档和本时间线。
- 验证：索引覆盖当前 8 个工作区、9 个分析模型、BI 路由、30 个接口、20 个平台和部署信息。
- 部署：已同步到 VPS 项目目录，线上与本地使用同一套索引。

### 优化真实查询加载性能

- 模块：上游请求客户端、分析看板。
- 问题：全部平台时多张卡片重复请求同一 PID，异常情况下加载可累计 47–50 秒。
- 修改：相同 GET 请求并发合并；成功结果内存缓存 60 秒；不修改原后台接口和数据。
- 验证：40 项测试通过；经营总览冷加载约 7.5 秒、缓存内约 2 秒、无失败请求。
- 部署：已部署 VPS。

### 完成多平台查询链路

- 模块：总览、视频、热搜、留存、使用深度、漏斗。
- 问题：部分模型界面支持多平台，但后端实际只查询 `platformIds[0]`。
- 修改：按 PID 受控并发；支持全部 20 平台和最多 8 平台对比；对比图生成独立平台系列；漏斗跨平台求和后重算转化率。
- 验证：PH + TT 总览返回 14 行、留存返回 10 行、漏斗返回 2 平台汇总；无配置不可用。
- 部署：已部署 VPS。

## 2026-07-15

### 精简分析工作台

- 模块：分析工作台。
- 问题：三列重复展示接口、能力边界、保存位置和两张预览，信息密度过高。
- 修改：模型列表精简；接口和限制折叠；右侧只保留卡片设置和一张主预览；保存按钮合并为一个。
- 验证：无横向溢出、无失败请求、预览正常。
- 部署：已部署 VPS。

### 修复全站卡片不出数据

- 模块：全部前端工作区、API 能力校验、真实查询。
- 问题：默认模板、旧配置、前端能力表和后端 Validator 不一致，出现“配置不可用”和 422。
- 修改：统一可用指标和维度；旧配置归一化；修复视频日期筛选与拆分维度混用；空图不初始化 ECharts。
- 验证：40 项测试、类型检查和构建通过；全站浏览器回归无致命错误。
- 部署：已部署 VPS。

### 接入真实后台 API 与工作区持久化

- 模块：后端 API、Adapter、PostgreSQL、前端真实数据状态。
- 修改：登记 30 个去重接口；实现 27 个可调用接口的 Adapter；新增分析查询、下钻、渠道、内容、专项和元数据路由；工作区保存至 PostgreSQL。
- 边界：3 个后台 500 接口暂未用于核心运行链路。
- 验证：Adapter 单元测试、API 校验和浏览器回归通过。
- 部署：VPS 使用 systemd 运行 API 和 Web。

### 确认正式平台与 PID

- 模块：平台注册表。
- 修改：保留 HX 表中的 20 个平台；移除未出现在 HX 表的 9 个旧平台；确认 Pornhub=`PH`、PH·Prem=`BZMH`。
- 验证：注册表测试确保 20 个平台且 PID 唯一。
- 部署：已应用于真实查询。
## 2026-07-23

### 修复小屏卡片库滚动区域

- 问题：模板管理右侧卡片库仅设置 `overflow:auto`，没有占据抽屉剩余高度，小屏或页面缩放后列表滚动区域被顶部内容挤压。
- 修改：抽屉使用动态视口高度；卡片列表使用 `flex:1` 独立滚动；增加稳定滚动条、触控纵向滚动和底部安全区。
- 响应式：1280px以下使用覆盖式宽抽屉；高度低于760px时隐藏示例预览，优先保证筛选、拖动和添加操作。
- 验证：TypeScript 类型检查和生产构建通过；1024×700 线上回归确认标题、关闭按钮和列表可见，列表可从顶部滚动到底部。

### 数据源配置页增加安全登录

- HTTP 看板的“数据源维护”入口自动跳转 HTTPS 配置页，并通过 `?workspace=dataSources` 直接打开配置模块。
- 维护密码不再存入 `sessionStorage`，也不再作为每个请求的自定义请求头发送。
- 新增30分钟 HttpOnly 安全会话、退出接口、IP绑定、5次错误锁定15分钟和密码哈希支持。
- 站1、站2 Token 输入、真实连接验证、失败不覆盖和即时生效逻辑保持不变。
- 验证：77项测试、TypeScript 类型检查和生产构建通过。
- 生产验证：未登录返回401；登录、读取两个站点状态和退出均返回200；退出后再次读取返回401。
# 2026-07-24

- 为前端查询缓存和后端上游缓存增加过期清理及 200/500 条硬上限，避免长期组合查询导致内存增长。
- 主工作区 Tab 改为新标签页打开，并为全部工作区保留 URL。
- 强化全局平台和双周期筛选器的层级与小屏展示。
- 新增 4 个老用户广告指标、5 个老用户充值指标及两张卡片资产。
- 修正用户来源分组混入充值指标的问题。
- “预支付”中文展示统一改为“拉起支付”，内部事件 ID 保持 `pre_pay`。
- 改善完整预览、模板预览、图表和表格的小屏适配。
