# 项目变更时间线

## 2026-08-26 NewAV 完整 BI 工作台

- 不再维护独立的浅层 NewAV 看板，复用原 BI 已验证的工作台、卡片工厂、模板管理、完整预览、下钻、双周期对比、宽度调整和 12 类图表。
- 新增 NewAV 指标注册与 `/api/bi/analytics/query` 兼容层，把渠道、内容、广告、会员和订单接口归一化到成熟分析引擎。
- 提供 NewAV 经营诊断、渠道增长与效率、内容与播放质量、广告素材表现、留存与会员五套默认模板。
- NewAV 工作区、浏览器存储和数据源均独立命名；升级到 `newav-workspace-v4.json` 和 `newav-bi-*-v2`，同时隔离服务端与浏览器早期测试资产。
- 修正终端访问结构误包含广告曝光的问题，并在后端加入并发请求合并，避免多张卡片同时读取同一周期时重复请求上游。
- 清理机械复用残留的旧平台 Mock 数据、默认平台名称和 PID 限制文案，保证 NewAV 产品空间源码与运行数据完全隔离。
- 验证：12 项测试、类型检查、生产构建、1440/1024 截图和无头浏览器 DOM 检查通过；五套模板存在、旧平台无残留、首屏 5 张卡片均为真实数据且无超时。
- 新增独立 VPS 部署定义：`/opt/newav-bi`、`newav-bi-api.service`、API 端口 `3200` 和独立 HTTPS 域名，不修改原 BI 服务。
- 已部署至 `https://newav-bi.187.77.129.207.nip.io`；线上健康检查、真实数据渲染、五套模板、图表、完整预览和下钻入口通过，无旧平台残留，原 BI 服务保持运行。
- 修复线上批量查询失败：生产上游超时由误配的 15 秒恢复为 30 秒；渠道和广告查询不再额外请求周期总览，减少重复请求与上游压力。冷启动回归产生 13 个 API 请求，全部返回 200，页面 5 张卡片全部更新且无错误。

> 时间倒序排列，只追加新记录，不删除历史。时间均为 Asia/Shanghai。

## 2026-08-25 NewAV 七日数据分析报告

- 基于 `2026-08-19` 至 `2026-08-25` 的 12 个真实统计接口生成独立 HTML 数据分析报告。
- 报告覆盖管理摘要、增长趋势、访问到付费链路、渠道贡献与效率、播放质量、设备结构、广告、收入会员、数据质量和行动建议。
- 明确区分全站经营与渠道归因口径，识别渠道新增覆盖率 63.4%、播放错误率 26.9% 和头部渠道集中等关键问题。
- 报告文件：`D:\CodexArtifacts\newav-analysis-report\newav-data-analysis-2026-08-19_2026-08-25.html`；数据快照和可重复生成脚本保存在同目录。

## 2026-08-25 NewAV 真实数据接入

- 将测试 Bearer Token 保存到 Git 忽略的 `new-platform/.env.local`，重启独立 API 后生效，凭证未进入源码和提交历史。
- 逐一验证 12 个统计数据集，全部返回成功；明细型接口和汇总型接口均能通过统一 Adapter 处理。
- 使用 Codex 内置浏览器确认经营总览已显示真实指标、趋势和 67 条渠道日明细，不再显示“等待配置”。
- 当前测试凭证有效期至 2026-09-01 22:15:40（UTC+8），过期后需替换环境变量并重启 API。

## 2026-08-25 独立产品 BI 基础应用

- 在 `feature/new-platform` 分支新增完全独立的 `new-platform/` 应用，未复用原 20 平台的 PID、双 Token 路由和工作区资产。
- 完成 NewAV 产品注册、12 个统计数据集目录、Bearer 客户端、Adapter、统一查询 API、错误映射和 45 秒有界缓存。
- 新增看板中心、卡片库、模板中心、数据源四个工作区，以及经营、渠道、收入续费和广告四套默认模板。
- 新增渠道自然日归一化、日均、累计人天和加权注册转化，缺失字段保持空值，不伪造为零。
- ECharts 改为按需注册折线图和柱状图，前端图表产物由约 1.13 MB 降至约 517 KB。
- 验证：9 项测试、类型检查、生产构建、依赖审计和 Codex 内置浏览器主路径检查通过；尚未配置 NewAV 生产 Token，未部署 VPS。

## 2026-08-25 NewAV API 盘点

- 在 `feature/new-platform` 分支使用 Codex 内置浏览器核对 NewAV 后台，不与原平台浏览器会话和接口配置混用。
- 从已登录页面和前端脚本确认 `/api/v1` 前缀下 12 个统计接口的方法与参数。
- 页面验证渠道每日数据可按渠道和自然日返回 13 项指标；补充渠道质量 17 项指标、中文解释和算法。
- 新增 `docs/new-platform/newav0-api-inventory.md`，明确已验证范围与待复验的鉴权、原始响应、金额单位和时区。
- 验证：文档与页面、前端请求定义交叉核对；代码和 VPS 均未修改，未执行部署。

## 2026-08-17 GitHub 仓库初始化

- 关联 GitHub 仓库 `FD-design/YPBI`，准备建立项目首次版本基线。
- 完善 `.gitignore`：排除环境密钥、运行时工作区、日志、部署压缩包、浏览器 QA 截图和测试输出。
- 保留源码、测试、接口契约、部署脚本和项目文档，避免后续发布覆盖 VPS 的 Token 与用户模板。
- 将 Playwright 小屏回归脚本归入独立 `test:e2e` 命令，避免被 Bun 单元测试运行器误收集。
- 升级兼容范围内的依赖并锁定四个漏洞传递依赖的安全版本，`bun audit` 从 9 个漏洞恢复为 0。
- 验证：暂存文件密钥扫描、依赖审计通过，96 项测试、类型检查和生产构建通过；部署状态：不涉及 VPS 发布。

## 2026-08-03 三种周期对比

- 顶部日期筛选新增自然月、等长周期、同期进度模式，并自动生成对比周期。
- 自然月允许 7 月 31 天与 6 月 30 天直接比较日均指标；累计指标明确保留实际周期累计值。
- 新增自然月、31 天等长周期和月中同期进度回归测试。

## 2026-08-03 日均与累计口径修复

- 修复真实 API 多日期查询将 DAU、观影人数、付费人数、端别日活和广告点击人数直接累加的问题，统一改为按自然日日均。
- 新增 8 个累计人天指标，并在经营总览补充“周期累计规模”默认卡片。
- 修正观影率、付费率、ARPU、ARPPU 的中文解释，明确当前按累计人天加权，而非周期用户去重。
- 新增日均、多平台同日汇总和累计人天回归测试。

## 2026-07-27 卡片编辑返回

- 修复从卡片库进入卡片编辑器后缺少返回入口的问题。
- 新增未保存修改退出确认，避免误丢当前配置。

## 2026-07-27 响应式主导航修复

- 修复小屏和中屏主导航文字拥挤：统一 `<a>` 导航响应式规则，中屏紧凑显示标题，手机端横向滚动。
- 进一步取消宽屏侧栏说明文字，覆盖高分辨率屏幕配合系统缩放后视觉区域过窄的场景。

## 2026-07-27 24小时实时走势

- 接入 `pRealDayLine` 的 288 个五分钟时点，新增登录、新增、观影用户三张双日期对比曲线。
- 24小时曲线最多对比 4 个平台，系统模板自动补入缺失卡片且不覆盖用户自建模板。
- 修正实时接口 UTC `sumDate` 转北京时间自然日，并修复部署排除规则误伤 `src/data/` 的问题。

## 2026-07-23 卡片指标上限修复

- 将卡片配置和分析查询协议的指标上限统一从 6 项提高到 16 项。
- 修复综合指标卡选择 10 项后实时预览请求被拒绝。
- 增加 16 项通过、17 项拒绝的协议回归测试。

## 2026-07-23 维护密码轮换

- 轮换数据源维护密码，仅在 VPS 保存 SHA-256 哈希。
- 验证登录、会话鉴权及错误密码拒绝均正常。

## 2026-07-23 系统卡片查询修复

- 修复“付费规模与效率”提示“查询配置不受支持”。
- 修复“内容消费汇总漏斗”因携带普通指标而被后端拒绝。
- 增加全部系统模板卡片的后端能力兼容性测试。

## 2026-07-23 模板持久化修复

- 定位 VPS 未配置 PostgreSQL，旧后端使用内存工作区，API 重启后模板清空。
- 新增磁盘工作区 Store，默认保存到 `data/workspace.json`，覆盖前保留 `.backup`。
- 前端新增模板与卡片本地备份，并按更新时间合并远端和本地配置。
- 新增 7 项持久化与合并测试；服务端 55 项、前端 32 项测试全部通过。

## 2026-07-23 获客转化与付费人数

- 将 `totalChargeUserCount` 作为“付费人数”开放到卡片工厂和经营模板。
- 新增获客转化模型，服务端关联 CNZZ 访问下载与 pDaySum 新增注册。
- 新增独立系统模板及 4 张真实数据卡片，旧工作区通过模板归一化自动补入。
- 修复无当前期记录被显示为 `-100%` 的问题，增加超过 100% 的口径不可比提示。
- 比例展示按数值大小动态保留 1 至 3 位小数。
- 修复 pDaySum 与 CNZZ 带时区日期直接截取导致的日期标签提前一天。
- 新增 Adapter 与能力校验测试；TypeScript、构建和 80 项业务测试通过。

## 2026-07-22

### 更新双数据源 Token 并恢复真实数据

- 仅更新 VPS 环境中的站1和站2 Token，不修改接口、业务代码和平台路由。
- 就绪状态由 `503 degraded` 恢复为 `200 ready`。
- 验证主数据源 `PH`、备用数据源 `TJD` 和20平台经营查询均成功；20平台经营返回0失败、0告警。
- 内容排行查询成功且无平台请求失败，当前7个平台存在排行记录，其余平台为上游暂无排行数据。

### 完善多平台数据可读性与卡片配置

- 模块：真实查询、系统模板、卡片创建/编辑、统计表和 ECharts Tooltip。
- 变化与下钻：卡片新增独立“查看明细”入口；“变化”仅保留周期分析，避免把分析结论误称为下钻。
- 数据状态：明确区分接口真实 `0`、平台暂无记录和能力不支持；标题区显示平台覆盖数量，占位 `0` 不再表述为真实业务值。
- 表格与模板：真实查询表头统一中文化；系统模板自动同步内置卡片新配置并保留用户宽度和平台范围；内容模板暂时移除长期为 0 的收入默认卡片。
- 配置体验：三个卡片配置入口统一增加指标分组、中文/字段 ID 搜索和已选数量。
- 性能与图表：完全相同请求共享 45 秒前端缓存；20 平台 Tooltip 按值排序、双列显示并脱离卡片裁切区域。
- 验证：74 项测试、TypeScript 类型检查和生产构建通过。
- 部署：已同步至 VPS，API、Web、Caddy 和 Nginx 均为 `active`，HTTPS、HTTP 与 `/api/bi/health` 返回 200。

### 修复全平台悬浮详情裁切和视频内容空数据

- ECharts Tooltip 统一挂载到页面 `body`，脱离卡片 `overflow:hidden`；增加视口内最大宽高和滚动，20 平台详情不再被卡片边界截断。
- 确认 `todayVideos/getMany` 当前只返回旧平台 PID，导致现有 20 平台过滤后为空；视频内容表现改用可返回 PH、TT、YK 等平台的 `videoWatchRanking`。
- 内容表现模型收紧为播放次数、点赞数、收藏数和收入；观看人数及人均时长因排行接口没有真实字段，不再在该模型展示 0。
- 旧内容卡片读取时通过能力归一化自动移除不受支持指标；人均观影时长仍在观看深度模型使用实时接口。
- 视频统计表只返回已选维度和指标，不再把 Adapter 内部兼容字段作为零值列透出。
- 74 项测试、TypeScript 类型检查、生产构建和线上真实内容查询通过。

### 补齐运营日报端别、来源、广告和新增付费口径

- 只读核对生产 `pDaySum/getAllByRole/pRealDayLine` 字段，确认遗漏主要来自 BI Adapter 未映射而非接口缺失。
- 新增 Android/IOS 活跃与新增、自然/内部导量新增、广告点击人数与人次及比例、新增付费金额/人数/付费率/ARPU/ARPPU、来源充值等 22 个可配置指标。
- 所有比例和客单价均先汇总原始分子分母再计算，不平均每日百分比。
- 经营总览增加终端结构、增长来源、广告触达和新增付费质量四张真实数据卡片；旧系统模板自动补卡且不覆盖用户模板。
- 修复收入 Adapter 字段名与指标注册表不一致的问题，将 `diamondChargeAmt` 统一归一化为 `revenue`。
- MAU、访问下载注册链路、支付宝/微信拉单拆分因缺少已确认历史日汇总字段继续标记待接入，不使用 Mock。
- 74 项测试、TypeScript 类型检查和生产构建通过。

### 优化周期涨跌视觉样式

- 实心圆箭头改为轻量 `↗/↘/→`，使用浅绿、浅红、浅灰状态底和细边框。
- 百分比保持主信息，对比期数值降级为灰色辅助文字，减少 KPI 和图表摘要的视觉拥挤。

### 修复周期涨跌箭头不可见

- 修复箭头圆形背景继承白色文字颜色，导致 `↑/↓/→` 显示成白圈的问题。
- 上涨、下降和持平分别使用绿色、红色和灰色实心圆背景，箭头保持白色并覆盖 KPI 与图表摘要。

### 修正字段说明的业务口径和指标级接口

- 修复技术公式被错误显示为“中文解释”的问题；中文解释改为独立维护的业务定义，不再回退到公式字符串。
- 指标接口从模型级接口集合改为指标级真实来源，避免付费率错误展示 `pRealDayLine/getAllByRole` 等无关接口。
- 26 个指标分别维护中文定义、业务公式、聚合算法、真实来源接口和口径限制；缺少接口的指标明确标记“暂无已确认接口”。
- 付费率明确为汇总 `totalChargeUserCount ÷ loginUserCount × 100%`，仅使用 `/api/admin/statistics/pDaySum`。
- 72 项测试、TypeScript 类型检查和生产构建通过。

### 重做问号字段说明交互

- 字段旁的 `?` 不再依赖浏览器原生 `title`；点击后打开统一中文说明弹窗。
- 弹窗展示中文解释、计算算法、具体公式、真实后台接口、数据粒度和能力边界。
- KPI、图表、明细表头、卡片编辑、模板编辑和分析创建中的指标/维度统一使用该说明交互。
- 配置项拆分为“选择字段”和独立 `?` 按钮，点击说明不会误选或取消字段。
- 72 项测试、TypeScript 类型检查和生产构建通过。

### 新增站1/站2 Token 安全维护页

- 一级导航新增“数据源维护”，提供站1和站2两个独立 Token 输入栏、当前连接测试、验证并保存和脱敏状态。
- 新增独立维护密码保护、同 IP 5 次失败锁定 15 分钟；维护密码不写入前端代码，Token 不写入浏览器存储且永不由接口回显。
- Token 保存前调用对应后台真实接口验证；失败保留旧值，成功后以 `0600` 私有文件原子持久化、清理缓存并立即生效，无需重启。
- 地址、用户名和 PID 路由仍由服务器环境变量维护，前端只允许更新 Token，缩小误操作范围。
- 新增运行时 Token 切换和脱敏状态回归测试；72 项测试及 TypeScript 类型检查通过。
- 新增 `https://187.77.129.207.nip.io` 管理入口，由 Caddy 使用 TLS-ALPN 自动签发和续签证书；HTTP 地址的维护接口强制返回 `426`，避免明文传输密码和 Token。

### 完整预览补齐口径、接口、算法与周期涨跌

- 所有卡片类型的标题栏固定展示实际平台范围和“完整预览”入口，不再只覆盖趋势类图表。
- 完整预览新增指标中文解释、维度解释、真实接口路径、聚合算法、公式、数据粒度和能力边界。
- KPI 与普通图表摘要统一展示当前周期相对对比周期的上涨、下降或持平箭头、变化百分比和对比期数值。
- 平台范围按钮可直接进入完整预览切换总体、单平台或 2–8 平台，并可应用到卡片后通过保存布局持久化。
- 71 项测试、TypeScript 类型检查和生产构建通过。

### 卡片独立平台范围与完整预览

- 卡片新增“跟随看板、全部平台、单平台、多平台对比”四种范围配置；独立范围随模板保存，最多对比 8 个平台。
- 卡片标题明确显示“跟随看板”或独立平台集合，避免顶部筛选与卡片实际查询范围混淆。
- 卡片新增“完整预览”，以全屏图表和明细查看数据，并可在预览内临时切换总体、单平台或多平台后重新查询真实接口。
- 导出使用每张卡片的有效平台范围；旧模板默认归一化为“跟随看板”，保持兼容。
- 新增卡片平台范围归一化回归测试；71 项测试、TypeScript 类型检查和生产构建通过。

### 修复平台范围错位、搜索聚合和字段解释

- 分类、渠道、专项三个专用系统模板不再固定读取 `filters.platforms[0]`；全部、单平台和 2–8 平台对比均使用顶部完整平台集合分别查询并聚合。
- 专用模板标题范围、导出文件和明细平台列同步当前选择，修复顶部“全部平台”但页面显示“按台姬店查询”的错位。
- 搜索模型选择平台维度时改用 `platform` 聚合，修复搜索总量有值但 20 个平台柱子全部为零。
- 支付等汇总事件步骤倒挂时停止计算转化率，表格显示“—”并说明独立事件计数不能证明用户级顺序。
- 指标卡、统计表头、卡片编辑和分析创建字段增加 `?` 口径提示，支持悬停与键盘聚焦查看来源、公式和解释。
- 新增平台搜索聚合和倒挂漏斗回归测试；70 项测试、TypeScript 类型检查和生产构建通过。

### 恢复双周期比较和完整留存档位

- 时间筛选新增“当前周期 + 对比周期”；快捷范围自动生成等长前置周期，自定义范围可分别按天选择，两段周期必须天数一致。
- 通用真实数据卡片并行查询两个周期；指标卡展示对比值和变化率，趋势图追加对比期系列，分析摘要显示两段明确日期。
- 生产接口原始返回确认包含 `afterFirstData1/3/7/30`；留存 Adapter、指标注册表、能力白名单和默认模板恢复 D1、D3、D7、D30。
- 各档留存按已达到观察期的 cohort 加权计算；未成熟的 D30 等字段保持空值并显示“—”，不按零处理。
- 留存注册日期从上游 UTC 时间转换为北京时间自然日，修复 Cohort 日期早一天的问题。
- Cohort 图横轴恢复 D1/D3/D7/D30，明细表同步展示四档留存；旧的系统留存模板自动迁移，不影响用户自建模板。
- 新增对比周期和多档留存回归测试；68 项测试、TypeScript 类型检查和生产构建通过。

### 按任务重构 BI 工作区与模板编辑模式

- 一级导航从五个入口收敛为“看板中心、分析中心、模板管理、数据字典”；卡片库与分析工作台合并为一个分析任务入口。
- 看板中心继续负责真实数据筛选、阅读和下钻；平台与日期筛选仅在看板状态出现，不再干扰卡片创建和模板编辑。
- 模板管理改为独立编辑模式，隐藏一级导航和业务筛选，画布使用完整页面宽度；顶部提供返回、预览、取消、保存、保存并发布和添加卡片。
- 模板属性区移除重复操作按钮；删除模板回归模板列表区域，卡片级工具仅在悬停、聚焦或选中时显示。
- 卡片库继续通过右侧按需抽屉打开，支持拖到指定插入位置；卡片排序、宽度调整和工作区持久化协议不变。
- 66 项测试、TypeScript 类型检查和生产构建通过。

### 重构模板编辑器的卡片添加交互

- 移除模板编辑器右侧常驻卡片栏，主编辑区恢复为“模板列表 + 全宽画布”，避免窄栏、双滚动和大块无效空白。
- 在页面标题和模板画布工具栏增加“添加卡片”，卡片库按需以右侧抽屉打开并可随时关闭。
- 抽屉内部独立滚动，保留图表类型筛选、结构预览、新建卡片、添加到顶部及拖到画布指定插入位置。
- 模板卡片原有拖动排序、宽度调整、编辑、移除、取消、保存和发布逻辑保持不变。
- 66 项测试、TypeScript 类型检查和生产构建通过。

### 修复全平台图表、顶部文字和变化结论

- 全部平台模式固定使用正式 20 平台集合；接口未返回记录的平台按 `0` 补齐并明确标记无数据，不生成 Mock 数值。
- 平台排行、趋势、散点、日期热力、Cohort、瀑布和贡献矩形图全部取消 Top N 数据截断；趋势使用滚动图例，排行超过 10 行时动态增加图表高度。
- Cohort 恢复为“平台 × 注册日期”矩阵，平台趋势按所选全部平台生成独立系列。
- 顶部顶栏和筛选控件改为自适应高度与明确行高，修复标题、平台和日期文字被裁切。
- 上游 `diamondChargeAmt` 等技术告警不再进入“关键变化/主要原因”，业务结论改为依据真实数据计算的最高平台、占比、区间变化和无数据平台数。
- 回归覆盖 20 平台排行、20 系列趋势、20 点散点、20 行热力/Cohort、20 项瀑布和贡献矩形；66 项测试、类型检查和生产构建通过，浏览器无横向溢出和控制台错误。

### 增加数据读取进度反馈

- 通用看板卡片新增顶部蓝色读取进度条，查询期间平滑推进至 92%，接口完成后补满并淡出；卡片状态同步显示百分比。
- 保留上次成功图表进行后台更新时同样显示进度，避免用户误判页面无响应。
- 渠道、分类、专项等页面的通用加载状态增加流动进度轨道，并兼容系统减少动态效果设置。
- 延迟接口回归确认桌面和 390px 移动端均显示 6 个卡片进度条，无控制台错误和横向溢出；64 项测试、类型检查与生产构建通过。

### 补齐真实 API 可计算图表

- 恢复 12 种系统图表类型的完整可配置入口；按模型能力开放热力图、瀑布图、贡献矩形图和汇总桑基图，不再由已有卡片数量决定图表是否可选。
- 真实结果转换新增平台 × 日期热力矩阵、维度贡献瀑布、平台/分类/内容贡献矩形，以及固定漏斗阶段汇总桑基。
- 桑基图明确标记为“汇总近似，非用户级路径”；页面 × 坑位热力图继续因接口缺少字段而关闭。
- 新图表作为卡片库预置资产提供，不强行加入默认看板，避免首屏增加重复的 20 平台查询；旧 localStorage 卡片会与新增系统资产合并。
- 服务端新增模型级指标、维度和漏斗事件白名单，查询与卡片校验协议要求显式 `modelId`，阻止模型字段错配产生假阳性结果。
- 编辑抽屉和模板配置统一使用真实 API 能力字段；查询请求保留 `category/content/keyword` 等真实维度；空服务端工作区不再覆盖浏览器迁移卡片。
- 热力图、瀑布图和贡献矩形图按卡片指标动态格式化，不再写死 CTR 或收入单位。
- 新增 10 项能力、查询协议与转换测试；全量 64 项测试、TypeScript 类型检查和生产构建通过。
- 生产发布：代码和构建产物已部署至 `/opt/config-driven-bi-demo`，保留 VPS `.env.local` 与双后台网络配置；API、Web、Nginx 均为 `active`。
- 线上验收：公网加载新资源 `index-C5xuTBs3.js`；`health` 为 `ok`、深度 `ready` 为 `ready`；模型字段错配在线返回 422；卡片库可见热力图、瀑布图、桑基图和贡献矩形图，浏览器无控制台错误和横向溢出。

### 重构模板预览与全局筛选

- 模板编辑器改为本地草稿：编辑、拖动、改名和移除不会立即写入工作区；保存或发布时才提交，取消修改恢复最近保存版本。
- 模板预览改为编辑器内独立状态，不再跳转看板中心；新增返回编辑、取消修改、保存模板和保存并发布操作。
- 平台筛选改为弹层内分模式选择并统一应用；支持搜索平台/PID、单平台和 2–8 平台对比，取消不触发查询。
- 时间筛选增加自定义起止日期，按自然日包含首尾两天；通用卡片、下钻和专用分析页统一使用同一日期转换。
- 55 项测试、类型检查、生产构建和依赖审计通过；桌面和 390px 移动端无横向溢出。
- 线上验证：平台弹层完整展示 20 个平台；取消平台选择不触发查询；自定义日期真实进入 API 请求体；模板草稿预览和取消修改未产生工作区 PUT。

### 完成 BI 全方位生产审计与收口

- 前端：经营总览、分析工作台、卡片库、模板编辑器和数据字典完成桌面/移动端回归；卡片库按类型折叠，缩短超长页面，并明确卡片与模板缩略图为示例数据结构预览。
- 图表：趋势日期改为正序；平台排行按指标降序，半宽卡片限制前 10；真实气泡矩阵保留全部平台，半宽常显 DAU 前 3、较宽卡片常显前 5，气泡按相对规模缩放，低值百分比轴保留有效精度。
- 口径：没有对比周期时 KPI 和下钻只标记当前筛选区间，不再显示伪“较上期”；诊断卡只使用真实数值字段判断平台高低，忽略缺失值。
- 下钻：普通图表节点按当前值和图表节点均值生成比较结论，不再把上游技术警告作为自动分析结论。
- 接口：逐项复验 30 个目录接口；A/B 落地页补齐真实业务参数，留存无当日数据归一化为空结果；有效参数下全部可调用。
- 稳定性：新增 `/api/bi/ready` 深度就绪检查；通用数据源入口限制分页和参数长度；上游业务参数错误统一映射为 422。
- 部署：Nginx 将 `/api/` 直接代理 Fastify，增加 10 req/s 限流、`no-store`、CSP、nosniff、SAMEORIGIN 和 Referrer Policy。
- 依赖：ECharts 升级到 6.1.0；`bun audit` 无漏洞。
- 验证：54 项测试、TypeScript 类型检查和生产构建通过；线上浏览器无控制台错误和失败请求。
- 待办：工作区写入认证、HTTPS、前端大文件拆分和正式 Playwright E2E 套件仍是上线前风险。

## 2026-07-21

### 双后台生产验收完成

- VPS `/etc/hosts` 仅针对两个上游域名固定 IPv4，解决 Bun `fetch` 仍走未授权 IPv6 的问题；保留修改前备份。
- BI API 回归确认 `PH` 使用主后台、`FBI/TJD` 使用第二后台，三者 `pDaySum` 均返回真实数据。
- `category.first`、`channel.byType`、`overview.byRole` 均通过生产调用；后两者分别返回 20 行和 2 行，原角色总览 HTTP 500 已消失。
- 接口目录状态更新为 30 个接口均可调用；分类接口在特定 `pid + type` 无匹配数据时保持真实空数组。

### 接入双后台凭证与平台路由

- 从两份浏览器 HAR 确认主后台账号为 `zhudan`，第二后台账号为 `danzhu`；修正此前误用 `zhudata` 导致的认证失败。
- 经 VPS 强制 IPv4 调用 `pDaySum`，完成 20 个平台在两个后台的覆盖矩阵，确认 11 个 PID 由第二后台提供数据。
- `UpstreamClient` 根据请求中的 `pid` 自动选择后台地址、Token 和匹配用户名；第二后台配置必须成组提供，凭证不进入代码和文档。
- 新增主/第二后台路由测试；全量 45 项测试、TypeScript 类型检查和生产构建通过。
- 发现 VPS 默认网络调用可能走 IPv6 `2a02:4780:5e:fe72::1` 并触发 IP 限制，生产调用继续优先 IPv4。

### 复查三个异常接口

- 反向核对：扫描线上后台发布的 Vue 分包，确认三个接口均为 GET，并还原页面实际参数。
- 分类：`getManyFirst` 必须传业务 `type`，不能作为无类型的通用一级分类接口；通用维度继续使用 `category.tree`。
- 渠道：`channelStatByType` 补齐原后台实际发送的 `pid`、`channel` 参数，以及 `CPC/CPA/CPT/CBD` 合作类型。
- 总览：`getAllByRole` 的真实参数与现有登记一致，在有效 Token 下仍返回上游 HTTP 500。
- 连接：识别 HTTP 200 内的业务码 `2002`，区分 Token 失效和 IP 白名单限制；API 进程优先 IPv4，避免命中未授权 IPv6。
- 当前阻断：VPS 保存的 Token 已失效；更新 Token 后复验分类和渠道接口。

## 2026-07-16

### 修复分析条件无法选择

- 模块：分析工作台、卡片实时预览。
- 问题：“查询筛选”被禁用；非漏斗模型的指标和维度实际修改统计表卡片，但右侧展示主卡片，造成点击后看不到变化。
- 修改：查询筛选改为可点选字段；图表、指标和维度统一作用于当前预览主卡片；选中项增加勾选、高亮和无障碍状态。
- 约束：指标和拆分维度至少保留一项；所有可选项仍由当前真实 API 能力表限定。

### 增加页面访问统计

- 模块：VPS Web 部署、Nginx、访问日志。
- 修改：公网 `5178` 改由 Nginx 接入，Vite preview 改为仅监听 `127.0.0.1:5179`；地址和 API 同源行为不变。
- 统计：记录真实访问 IP、时间、请求、状态码、来源页和浏览器；提供脚本汇总页面打开次数、去重 IP、今日数据和最后访问时间。
- 运维：访问日志每日轮转并保留 30 天；不占用或修改 VPS 上已有 Node 服务使用的 `80` 端口。
- 边界：原 Vite 服务未记录真实客户端 IP，历史访问人数无法补回，统计从本次上线后开始。

### 内容、渠道和专项分析收进系统模板

- 模块：工作区导航、看板中心、内容/渠道/专项分析页面。
- 问题：三个固定分析页作为一级导航存在，与“卡片组装模板”的产品主路径并行，容易被误认为另一套模板系统。
- 修改：左侧移除内容分析、渠道分析和专项分析；在看板中心新增“分类内容分析、渠道经营分析、专项运营分析”三个系统模板入口。
- 分组：模板选择器明确区分“系统模板”和“我的模板”；当前共 8 个系统模板，自建模板继续来自模板编辑器。
- 数据：三个专用模板保留原 `/api/bi/content/*`、`/api/bi/channels/*`、`/api/bi/special/*` 真实接口和原有交互，不更改上游接口。
- 验证：系统模板三项均可切换渲染；左侧只保留看板中心、分析工作台、卡片库、模板编辑器和数据字典；横向溢出为 0，控制台错误为 0。

### 分析工作台移除双侧栏

- 模块：分析工作台、分析模型选择器、响应式布局。
- 问题：主导航与“API 分析模型”纵向侧栏并排，形成重复层级；状态标签拥挤，配置和图表区域被压缩。
- 修改：移除第二条纵向侧栏；9 个分析模型改为内容顶部横向选择条；状态收敛为圆点和短标签；当前模型说明只显示一次。
- 布局：桌面使用“176px 主导航 + 自适应工作区”，1280px 以下主导航收窄为 72px，移动端模型条横向滚动。
- 验证：TypeScript 和生产构建通过；1440×1000 回归中工作台宽度为 1234px、横向溢出为 0、控制台错误为 0。

### 看板与模板卡片支持拖动调整宽度

- 模块：看板中心、模板编辑器、工作区持久化。
- 需求：查看模板和配置模板时，可以按住卡片左右拖动放大缩小，并保存调整后的布局。
- 修改：新增卡片右侧宽度手柄；按 12 列网格吸附为半宽、2/3 宽、整行；看板中心使用待保存布局，模板编辑器同步预览；两处均增加“保存布局”。
- 交互：窗口级 Pointer Event 保证鼠标移出卡片后仍可继续调整；支持左右方向键；移动端禁用手柄并保持整行卡片。
- 持久化：尺寸继续写入 `DashboardCardConfig.size`，复用现有 `/api/bi/workspace` 保存链路，不增加数据库结构或新接口。
- 验证：40 项测试、TypeScript 和生产构建通过；自动化验证拖动缩小、保存、刷新恢复、模板编辑器调整；横向溢出为 0，控制台错误为 0。

### 统一筛选、查询可观测性与看板视觉收敛

- 模块：全局筛选、看板中心、内容/渠道/专项分析、卡片库、分析工作台。
- 问题：页面重复筛选和重复元信息过多；查询慢或失败时缺少全局反馈；看板二级侧栏长期占用图表空间。
- 修改：新增全局平台/日期筛选和保存范围；独立接口继承主平台；顶部查询健康状态；失败保留上次成功结果和卡片重试；卡片复制与 20 步撤销；五类看板阅读重点。
- 视觉：看板中心改为单导航栏和全宽内容区；模板切换收敛为一行选择器；卡片数据范围收进状态提示；移动 KPI 改为 2×2。
- 验证：40 项测试通过；TypeScript 与生产构建通过；完成 1440×1000 和 390×844 截图回归，无首屏横向溢出。
- 边界：3 个后台 500 接口、指标口径复核、登录/HTTPS 等上线能力按用户要求暂缓。

### 建立项目文档治理

- 模块：全项目。
- 需求：功能、模块细节和修改历史必须落盘；后续修改前先读索引。
- 修改：新增项目 `AGENTS.md`、总索引、前端、分析、后端、数据平台、部署文档和本时间线。
- 验证：索引覆盖当前 8 个工作区、9 个分析模型、BI 路由、30 个接口、20 个平台和部署信息。
- 部署：已同步到 VPS 项目目录，线上与本地使用同一套索引。

### 优化真实查询加载性能

- 模块：上游请求客户端、分析看板。
- 问题：全部平台时多张卡片重复请求同一 PID，异常情况下加载可累计 47–50 秒。
- 修改：相同 GET 请求并发合并；成功结果内存缓存 60 秒；不修改原后台接口和数据。
- 验证：40 项测试通过；经营总览冷加载约 7.5 秒、缓存内约 2 秒、无失败请求。
- 部署：已部署 VPS。

### 完成多平台查询链路

- 模块：总览、视频、热搜、留存、使用深度、漏斗。
- 问题：部分模型界面支持多平台，但后端实际只查询 `platformIds[0]`。
- 修改：按 PID 受控并发；支持全部 20 平台和最多 8 平台对比；对比图生成独立平台系列；漏斗跨平台求和后重算转化率。
- 验证：PH + TT 总览返回 14 行、留存返回 10 行、漏斗返回 2 平台汇总；无配置不可用。
- 部署：已部署 VPS。

## 2026-07-15

### 精简分析工作台

- 模块：分析工作台。
- 问题：三列重复展示接口、能力边界、保存位置和两张预览，信息密度过高。
- 修改：模型列表精简；接口和限制折叠；右侧只保留卡片设置和一张主预览；保存按钮合并为一个。
- 验证：无横向溢出、无失败请求、预览正常。
- 部署：已部署 VPS。

### 修复全站卡片不出数据

- 模块：全部前端工作区、API 能力校验、真实查询。
- 问题：默认模板、旧配置、前端能力表和后端 Validator 不一致，出现“配置不可用”和 422。
- 修改：统一可用指标和维度；旧配置归一化；修复视频日期筛选与拆分维度混用；空图不初始化 ECharts。
- 验证：40 项测试、类型检查和构建通过；全站浏览器回归无致命错误。
- 部署：已部署 VPS。

### 接入真实后台 API 与工作区持久化

- 模块：后端 API、Adapter、PostgreSQL、前端真实数据状态。
- 修改：登记 30 个去重接口；实现 27 个可调用接口的 Adapter；新增分析查询、下钻、渠道、内容、专项和元数据路由；工作区保存至 PostgreSQL。
- 边界：3 个后台 500 接口暂未用于核心运行链路。
- 验证：Adapter 单元测试、API 校验和浏览器回归通过。
- 部署：VPS 使用 systemd 运行 API 和 Web。

### 确认正式平台与 PID

- 模块：平台注册表。
- 修改：保留 HX 表中的 20 个平台；移除未出现在 HX 表的 9 个旧平台；确认 Pornhub=`PH`、PH·Prem=`BZMH`。
- 验证：注册表测试确保 20 个平台且 PID 唯一。
- 部署：已应用于真实查询。
## 2026-07-23

### 修复小屏卡片库滚动区域

- 问题：模板管理右侧卡片库仅设置 `overflow:auto`，没有占据抽屉剩余高度，小屏或页面缩放后列表滚动区域被顶部内容挤压。
- 修改：抽屉使用动态视口高度；卡片列表使用 `flex:1` 独立滚动；增加稳定滚动条、触控纵向滚动和底部安全区。
- 响应式：1280px以下使用覆盖式宽抽屉；高度低于760px时隐藏示例预览，优先保证筛选、拖动和添加操作。
- 验证：TypeScript 类型检查和生产构建通过；1024×700 线上回归确认标题、关闭按钮和列表可见，列表可从顶部滚动到底部。

### 数据源配置页增加安全登录

- HTTP 看板的“数据源维护”入口自动跳转 HTTPS 配置页，并通过 `?workspace=dataSources` 直接打开配置模块。
- 维护密码不再存入 `sessionStorage`，也不再作为每个请求的自定义请求头发送。
- 新增30分钟 HttpOnly 安全会话、退出接口、IP绑定、5次错误锁定15分钟和密码哈希支持。
- 站1、站2 Token 输入、真实连接验证、失败不覆盖和即时生效逻辑保持不变。
- 验证：77项测试、TypeScript 类型检查和生产构建通过。
- 生产验证：未登录返回401；登录、读取两个站点状态和退出均返回200；退出后再次读取返回401。
# 2026-07-24

- 为前端查询缓存和后端上游缓存增加过期清理及 200/500 条硬上限，避免长期组合查询导致内存增长。
- 主工作区 Tab 改为新标签页打开，并为全部工作区保留 URL。
- 强化全局平台和双周期筛选器的层级与小屏展示。
- 新增 4 个老用户广告指标、5 个老用户充值指标及两张卡片资产。
- 修正用户来源分组混入充值指标的问题。
- “预支付”中文展示统一改为“拉起支付”，内部事件 ID 保持 `pre_pay`。
- 改善完整预览、模板预览、图表和表格的小屏适配。
